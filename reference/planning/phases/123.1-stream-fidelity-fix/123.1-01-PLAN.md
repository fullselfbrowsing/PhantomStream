---
phase: 123.1-stream-fidelity-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [content/dom-stream.js]
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "captureComputedStyles runs on ALL visible elements, not just those with style/className attrs"
    - "STYLE_PROPS includes flex-wrap, flex-grow, flex-shrink, flex-basis, order, grid-column, grid-row, white-space, text-overflow, float, top, left, right, bottom"
    - "Inline style tags from document.head are collected and included in the snapshot alongside external stylesheet URLs"
  artifacts:
    - path: "content/dom-stream.js"
      provides: "Comprehensive computed style capture and inline style tag collection"
      contains: "flex-wrap"
---

<objective>
Fix DOM clone rendering broken layouts on complex sites (Google, YouTube). Three root causes:
1. captureComputedStyles skips elements without style/className (line 120 guard)
2. STYLE_PROPS missing critical flex/grid/position properties
3. Inline style tags not captured (only link[rel=stylesheet] collected)
</objective>

<tasks>

<task id="1" name="Fix computed style capture and add missing CSS properties">
<read_first>
- content/dom-stream.js (lines 22-142: STYLE_PROPS, STYLE_PROP_CSS, STYLE_DEFAULTS, captureComputedStyles)
</read_first>

<action>
Three changes in `content/dom-stream.js`:

**1. Remove the skip guard at line 120.**
Replace:
```javascript
  function captureComputedStyles(original, clone) {
    // Heuristic: only capture computed styles if element has style attr or classes
    if (!original.getAttribute('style') && !original.className) return;
```
With:
```javascript
  function captureComputedStyles(original, clone) {
```
This ensures ALL visible elements get computed styles, not just those with explicit style/class. The payload grows but fidelity is critical.

**2. Expand STYLE_PROPS and STYLE_PROP_CSS arrays.** Add these properties after the existing entries:

STYLE_PROPS additions:
```
'flexWrap', 'flexGrow', 'flexShrink', 'flexBasis', 'order',
'gridColumn', 'gridRow', 'gridGap', 'gridAutoFlow',
'whiteSpace', 'textOverflow', 'textDecoration',
'float', 'clear',
'top', 'left', 'right', 'bottom', 'inset',
'minWidth', 'maxHeight',
'letterSpacing', 'wordSpacing', 'verticalAlign',
'backgroundImage', 'backgroundSize', 'backgroundPosition',
'borderTop', 'borderBottom', 'borderLeft', 'borderRight',
'overflowX', 'overflowY', 'cursor', 'pointerEvents',
'objectFit', 'objectPosition'
```

STYLE_PROP_CSS additions (same order, kebab-case):
```
'flex-wrap', 'flex-grow', 'flex-shrink', 'flex-basis', 'order',
'grid-column', 'grid-row', 'grid-gap', 'grid-auto-flow',
'white-space', 'text-overflow', 'text-decoration',
'float', 'clear',
'top', 'left', 'right', 'bottom', 'inset',
'min-width', 'max-height',
'letter-spacing', 'word-spacing', 'vertical-align',
'background-image', 'background-size', 'background-position',
'border-top', 'border-bottom', 'border-left', 'border-right',
'overflow-x', 'overflow-y', 'cursor', 'pointer-events',
'object-fit', 'object-position'
```

**3. Collect inline style tags.** In `serializeDOM()`, after the external stylesheet collection (lines 287-294), add:

```javascript
    // Collect inline <style> tags from document.head
    var inlineStyles = [];
    var styleTags = document.querySelectorAll('head style');
    for (var st = 0; st < styleTags.length; st++) {
      var cssText = styleTags[st].textContent;
      if (cssText && cssText.length < 500000) { // Skip huge injected CSS (>500KB)
        inlineStyles.push(cssText);
      }
    }
```

Then in the return object (line 321+), add `inlineStyles: inlineStyles`.

**4. In `showcase/js/dashboard.js` `handleDOMSnapshot()`**, inject inline styles into the iframe HTML. After the `stylesheetLinks` variable (line 1661-1663), add:

```javascript
      var inlineStyleTags = (payload.inlineStyles || []).map(function(css) {
        return '<style>' + css + '</style>';
      }).join('\n');
```

Then in the fullHTML string, add `inlineStyleTags` after `stylesheetLinks`:
```javascript
      var fullHTML = '<!DOCTYPE html><html><head><meta charset="UTF-8">' +
        '<meta name="viewport" content="width=' + (payload.viewportWidth || 1920) + '">' +
        stylesheetLinks +
        inlineStyleTags +
        '<style>body { margin: 0; overflow: hidden; } ...' +
```
</action>

<acceptance_criteria>
- content/dom-stream.js captureComputedStyles does NOT have the `!original.getAttribute('style') && !original.className` guard
- content/dom-stream.js STYLE_PROPS array contains 'flexWrap', 'whiteSpace', 'top', 'left', 'right', 'bottom'
- content/dom-stream.js serializeDOM return object contains 'inlineStyles' key
- content/dom-stream.js has `document.querySelectorAll('head style')` collection loop
- showcase/js/dashboard.js handleDOMSnapshot builds inlineStyleTags from payload.inlineStyles
- showcase/js/dashboard.js fullHTML includes inlineStyleTags between stylesheetLinks and the body style tag
</acceptance_criteria>
</task>

</tasks>
