---
phase: 04-relay-ws-transport-two-tab-demo
plan: 03
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - src/renderer/index.js
  - src/renderer/README.md
  - tests/renderer-health-events.test.js
  - tests/renderer-viewer.test.js
autonomous: true
requirements: [VIEW-02]
must_haves:
  truths:
    - "Viewer hosts can subscribe with `viewer.on('state', handler)` and receive `connecting`, `live`, `stale`, and `disconnected` lifecycle states"
    - "Viewer hosts can subscribe with `viewer.on('health', handler)` and receive counters/timestamps/transport telemetry with no mirrored page content"
    - "The viewer handle keeps existing `detach`, `destroy`, and `registerOverlay` behavior while adding `on`"
    - "Transport close/reconnect signals make relay shutdown observable as stale and then disconnected"
  artifacts:
    - path: "src/renderer/index.js"
      provides: "Viewer event emitter, public lifecycle state, safe health snapshots, optional transport.onStatus integration"
      contains: "on: on"
    - path: "tests/renderer-health-events.test.js"
      provides: "VIEW-02 lifecycle, unsubscribe, transport status, and health privacy coverage"
    - path: "tests/renderer-viewer.test.js"
      provides: "Intentional handle-shape update for the new on() method"
    - path: "src/renderer/README.md"
      provides: "Documented viewer.on('state'|'health') contract and event payload fields"
  key_links:
    - from: "src/renderer/index.js"
      to: "transport.onStatus"
      via: "optional status subscription"
      pattern: "onStatus"
    - from: "src/renderer/index.js"
      to: "src/transport/websocket.js"
      via: "shared status object conventions from plan 04-02"
      pattern: "transport"
---

<objective>
Extend `createViewer` with the host-facing event surface required by VIEW-02 while preserving the existing viewer handle methods. State events use exactly the roadmap names and health events expose counters/timestamps only.

Purpose: hosts and the two-tab demo need to observe stream lifecycle and relay health without the viewer library imposing UI chrome.
Output: renderer event API, event tests, README contract, full suite green.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-relay-ws-transport-two-tab-demo/04-CONTEXT.md
@.planning/phases/04-relay-ws-transport-two-tab-demo/04-RESEARCH.md
@.planning/phases/04-relay-ws-transport-two-tab-demo/04-PATTERNS.md
@.planning/phases/04-relay-ws-transport-two-tab-demo/04-VALIDATION.md
@src/renderer/index.js
@src/renderer/README.md
@tests/renderer-viewer.test.js

<interfaces>
Existing handle:
```javascript
{
  detach: () => void,
  destroy: () => void,
  registerOverlay: (kind, renderFn) => void
}
```

New handle:
```javascript
viewer.on('state', function (event) {
  // event: { state: 'connecting'|'live'|'stale'|'disconnected', reason, ts }
});
viewer.on('health', function (health) {
  // counters and timestamps only; no mirrored payload data
});
// returns unsubscribe function
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Viewer state subscriptions and transport status mapping</name>
  <files>src/renderer/index.js, tests/renderer-health-events.test.js, tests/renderer-viewer.test.js</files>
  <read_first>
    - .planning/phases/04-relay-ws-transport-two-tab-demo/04-CONTEXT.md decisions D-19 through D-22 and D-24
    - .planning/phases/04-relay-ws-transport-two-tab-demo/04-RESEARCH.md "Pitfall 4" and "Architectural Responsibility Map"
    - .planning/phases/04-relay-ws-transport-two-tab-demo/04-PATTERNS.md sections for src/renderer/index.js, tests/renderer-health-events.test.js, and tests/renderer-viewer.test.js
    - src/renderer/index.js in full, especially ViewerTransport typedef, handle typedef, viewerState, requestResync, handleSnapshot, handleMutations, dispatch, detach, destroy
    - tests/renderer-viewer.test.js exact handle-shape assertions
  </read_first>
  <behavior>
    - Test: the viewer handle has `detach`, `destroy`, `registerOverlay`, and `on`; existing methods remain functions.
    - Test: `viewer.on('state', handler)` immediately reports `connecting` and returns an unsubscribe function.
    - Test: accepted snapshot/frame transitions state to `live`.
    - Test: `requestResync` or repeated stale mutation misses transitions state to `stale` while retaining the last frame.
    - Test: transport status `closed` transitions to `stale` immediately and then `disconnected` after the configured short disconnect delay.
    - Test: unsubscribe stops later state delivery; invalid event names throw `viewer-event-unsupported` at call time.
  </behavior>
  <action>
    Write tests/renderer-health-events.test.js first using the local JSDOM pattern from tests/renderer-viewer.test.js and a recording transport that supports `onMessage`, `emitMessage`, `onStatus`, and `emitStatus`. Update tests/renderer-viewer.test.js intentionally so the exact handle shape includes `on`. In src/renderer/index.js, add public lifecycle state separate from the existing internal `waiting|streaming` gate. Initial public state is `connecting`; `on('state', handler)` immediately delivers the current state object and returns unsubscribe. Emit `live` when the first accepted snapshot/frame is handled. Emit `stale` when `requestResync` latches or when transport status reports `closed`, `reconnecting`, or `error`. Emit `disconnected` after a short stale window when the transport remains closed; use an option such as `disconnectDelayMs` for tests while the default stays short enough for the demo checkpoint. Subscribe to optional `transport.onStatus(handler)` if present and clean it up in detach/destroy. Unknown STREAM types remain forward-compatible, but handle `STREAM.STATE` payloads by mapping valid state strings into the public event surface. The viewer library must not add badges, banners, or visible product chrome.
  </action>
  <verify>
    <automated>node --test tests/renderer-health-events.test.js tests/renderer-viewer.test.js -x</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/renderer-health-events.test.js tests/renderer-viewer.test.js -x` exits 0.
    - `grep -c "on: on" src/renderer/index.js` returns 1.
    - `grep -c "connecting\\|live\\|stale\\|disconnected" tests/renderer-health-events.test.js` returns at least 4.
    - Existing detach/destroy/registerOverlay tests still pass without weakening sandbox assertions.
  </acceptance_criteria>
  <done>Viewer hosts can subscribe to lifecycle state events and observe relay closure through stale and disconnected transitions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Health telemetry counters, privacy assertions, and renderer docs</name>
  <files>src/renderer/index.js, src/renderer/README.md, tests/renderer-health-events.test.js</files>
  <read_first>
    - .planning/phases/04-relay-ws-transport-two-tab-demo/04-CONTEXT.md decisions D-20, D-23, D-24, D-28
    - .planning/phases/04-relay-ws-transport-two-tab-demo/04-VALIDATION.md rows T-04-06 and T-04-07
    - .planning/phases/04-relay-ws-transport-two-tab-demo/04-PATTERNS.md "Viewer Event Health Snapshot" example
    - src/renderer/index.js counters, sanitizeCounters, safeSend, dispatch, and handleSnapshot/handleMutations
    - src/renderer/README.md existing viewer contract section
  </read_first>
  <behavior>
    - Test: `viewer.on('health', handler)` immediately delivers a health object and returns unsubscribe.
    - Test: health updates after snapshot, mutation, scroll, overlay, dialog, send, stale miss, apply failure, sanitizer counter movement, and transport status events.
    - Test: health includes `lastFrameAt`, `lastSnapshotAt`, `lastMutationAt`, `receivedByType`, `sentByType`, `staleMisses`, `applyFailures`, `resyncPending`, sanitizer counters, transport drops/errors when available.
    - Test: JSON-stringified health output does not include mirrored `html`, text node contents, payload objects, page `url`, or page `title`.
    - Test: health unsubscribe stops later delivery.
  </behavior>
  <action>
    Add `on('health', handler)` to the same `on` method. Track safe counters in src/renderer/index.js: increment `receivedByType[type]` in dispatch before handling accepted messages, update `lastFrameAt` for any decoded frame, `lastSnapshotAt` for snapshots, and `lastMutationAt` for mutations. Increment `sentByType[type]` inside safeSend when a CONTROL message is attempted. Include existing `counters.staleMisses`, `counters.applyFailures`, `resyncPending`, and a shallow copy of sanitizeCounters in health snapshots. Merge optional transport health/status fields from plan 04-02 such as bufferedAmount, drops, errors, readyState, and lastCloseAt. Emit health after state changes, frame handling, safeSend, and transport status events. Never include raw payloads, snapshot html, title, url, text contents, or DOM nodes in health objects. Update src/renderer/README.md with the exact `viewer.on('state'|'health')` API, unsubscribe semantics, event names, and a field list that explicitly says health contains counters/timestamps only.
  </action>
  <verify>
    <automated>node --test tests/renderer-health-events.test.js tests/renderer-viewer.test.js -x && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/renderer-health-events.test.js tests/renderer-viewer.test.js -x` exits 0.
    - `npm test` exits 0.
    - `grep -c "viewer.on('state'\\|viewer.on('health'" src/renderer/README.md` returns at least 2.
    - Health privacy tests fail if `html`, `text`, `payload`, `url`, or `title` appears as a key in a health object.
  </acceptance_criteria>
  <done>Viewer health telemetry is host-observable, documented, and privacy-bounded to counters/timestamps/transport diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transport -> renderer | Decoded stream frames and transport status enter renderer state logic |
| renderer -> host app | Viewer emits lifecycle and health events to untrusted host callbacks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Availability / Observability | `viewer.on('state')` | mitigate | Add state subscription with immediate current state, unsubscribe, exact state names, and transport close mapping to stale/disconnected. |
| T-04-07 | Information Disclosure | `viewer.on('health')` | mitigate | Emit only counters/timestamps/transport diagnostics; tests stringify health objects and assert absence of mirrored content keys. |
| T-04-04 | Denial of Service / Reliability | renderer dispatch error containment | transfer | Decode errors are handled by plan 04-02 transport; renderer keeps existing try/catch dispatch containment and records apply failures in health. |
</threat_model>

<verification>
- `node --test tests/renderer-health-events.test.js tests/renderer-viewer.test.js -x`
- `npm test`
</verification>

<success_criteria>
- VIEW-02 is implemented and documented.
- Existing viewer handle methods and sandbox/security behavior remain intact.
- Demo UI can subscribe to state/health without viewer-owned chrome.
</success_criteria>

<output>
After completion, create `.planning/phases/04-relay-ws-transport-two-tab-demo/04-03-SUMMARY.md`
</output>
