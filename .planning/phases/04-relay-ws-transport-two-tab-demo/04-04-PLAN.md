---
phase: 04-relay-ws-transport-two-tab-demo
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - package.json
  - package-lock.json
  - bin/phantom-stream.js
  - examples/two-tab-demo/server.js
  - examples/two-tab-demo/source.html
  - examples/two-tab-demo/viewer.html
  - examples/two-tab-demo/demo.css
  - examples/two-tab-demo/source.js
  - examples/two-tab-demo/viewer.js
  - tests/demo-cli.test.js
autonomous: false
requirements: [PKG-01, VIEW-02]
must_haves:
  truths:
    - "`node bin/phantom-stream.js demo` starts a 127.0.0.1-only demo server plus bundled relay and prints source/viewer URLs with the generated room key"
    - "Opening the viewer tab first and the source tab second produces a live mirror through the relay"
    - "The source tab mutates a small page with Add row, Remove row, Edit text, Show dialog, and Auto-mutate controls"
    - "The viewer tab displays the mirror plus compact lifecycle and health status from `viewer.on('state'|'health')`"
    - "Stopping the demo process makes the viewer transition live -> stale -> disconnected while retaining the last frame"
  artifacts:
    - path: "bin/phantom-stream.js"
      provides: "Package binary with `demo` subcommand and deterministic URL output"
    - path: "examples/two-tab-demo/server.js"
      provides: "Local-only HTTP/static server plus relay backend startup and shutdown helper"
      exports: ["startDemoServer"]
    - path: "examples/two-tab-demo/source.html"
      provides: "Source tab shell"
    - path: "examples/two-tab-demo/viewer.html"
      provides: "Viewer tab shell"
    - path: "examples/two-tab-demo/source.js"
      provides: "Capture-side demo wiring through createWebSocketTransport"
    - path: "examples/two-tab-demo/viewer.js"
      provides: "Viewer-side demo wiring through createViewer and viewer.on events"
    - path: "tests/demo-cli.test.js"
      provides: "PKG-01 CLI/server/static safety coverage"
  key_links:
    - from: "bin/phantom-stream.js"
      to: "examples/two-tab-demo/server.js"
      via: "imports startDemoServer for demo subcommand"
      pattern: "startDemoServer"
    - from: "examples/two-tab-demo/source.js"
      to: "src/capture/index.js"
      via: "createCapture with WebSocket transport and skipElement recursion guard"
      pattern: "createCapture"
    - from: "examples/two-tab-demo/viewer.js"
      to: "src/renderer/index.js"
      via: "createViewer and viewer.on('state'|'health')"
      pattern: "viewer\\.on"
---

<objective>
Ship the first networked PhantomStream proof: a local CLI demo that prints source and viewer URLs, serves two browser tabs on 127.0.0.1, connects both through the bundled relay, and proves lifecycle/health behavior with a browser kill-relay checkpoint.

Purpose: PKG-01 demonstrates the capture -> relay -> render pipeline end-to-end without adding later adapter or publishing scope.
Output: package binary, demo server/pages, demo tests, full suite green, browser checkpoint recorded.
</objective>

<scope_rationale>
Plan 04-04 intentionally touches 10 files because the CLI, local server, source page, viewer page, shared CSS, browser wiring, and demo tests form one cohesive end-to-end demo surface. Splitting those assets would create artificial cross-plan dependencies and make the browser checkpoint harder to reason about; the plan controls churn through three focused tasks and a final manual checkpoint.
</scope_rationale>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-relay-ws-transport-two-tab-demo/04-CONTEXT.md
@.planning/phases/04-relay-ws-transport-two-tab-demo/04-RESEARCH.md
@.planning/phases/04-relay-ws-transport-two-tab-demo/04-PATTERNS.md
@.planning/phases/04-relay-ws-transport-two-tab-demo/04-VALIDATION.md
@.planning/phases/04-relay-ws-transport-two-tab-demo/04-UI-SPEC.md
@examples/serve.js
@examples/loopback-mirror.html
@examples/loopback-transport.js
@src/capture/index.js
@src/renderer/index.js
@src/transport/websocket.js
@src/relay/index.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLI demo command, local server, room URLs, and static safety tests</name>
  <files>package.json, package-lock.json, bin/phantom-stream.js, examples/two-tab-demo/server.js, tests/demo-cli.test.js</files>
  <read_first>
    - .planning/phases/04-relay-ws-transport-two-tab-demo/04-CONTEXT.md decisions D-03, D-05, D-06, D-13, D-25, D-26, D-28
    - .planning/phases/04-relay-ws-transport-two-tab-demo/04-UI-SPEC.md "Terminal Output" and "Shared Page Shell"
    - .planning/phases/04-relay-ws-transport-two-tab-demo/04-PATTERNS.md sections for bin/phantom-stream.js, examples/two-tab-demo/server.js, package.json, package-lock.json, and tests/demo-cli.test.js
    - examples/serve.js
    - package.json
    - src/relay/index.js
  </read_first>
  <behavior>
    - Test: `startDemoServer({ host: '127.0.0.1', port: 0 })` binds only 127.0.0.1 and returns actual sourceUrl, viewerUrl, roomKey, roomKeyPrefix, server, and close handles.
    - Test: source and viewer URLs include the same generated room key and use `127.0.0.1`.
    - Test: CLI output includes exactly the required UI-SPEC lines: `PhantomStream demo running on 127.0.0.1`, `Source tab:`, `Viewer tab:`, and `Room:`.
    - Test: static server serves `.html`, `.js`, and `.css` with strict MIME types and rejects malformed encodings, traversal, and directory listings.
    - Test: startup/shutdown leaves no listener open.
  </behavior>
  <action>
    Write tests/demo-cli.test.js first. Implement examples/two-tab-demo/server.js as an exported `startDemoServer(options)` helper that creates one Node HTTP server, attaches `createRelay()` and `createWebSocketRelayBackend({ server, relay, path: '/ws' })`, serves the repo root with the same decode-before-resolve root-prefix guard as examples/serve.js, and binds only `127.0.0.1`. Use default port `8643` with next-free-port fallback; tests pass port `0` for deterministic OS assignment. Generate room keys with Node `crypto.randomBytes(16).toString('hex')`; source URL path is `/examples/two-tab-demo/source.html?room=<room>&ws=<encoded ws url>` and viewer URL path is `/examples/two-tab-demo/viewer.html?room=<room>&ws=<encoded ws url>`. Implement bin/phantom-stream.js as an executable ESM bin with a tiny manual parser for `demo`, `--port <number>`, and `--no-open` as an accepted deterministic flag; default behavior prints URLs and does not auto-open a browser. Add `"bin": { "phantom-stream": "./bin/phantom-stream.js" }` and a `"demo": "node bin/phantom-stream.js demo"` script to package.json, then sync package-lock root metadata with `npm install --package-lock-only`. Do not add a CLI framework, public admin endpoint, remote control, Playwright/CDP adapter, extension adapter, bookmarklet adapter, npm publish steps, FSB swap-in, CSSOM work, or node identity work.
  </action>
  <verify>
    <automated>node --test tests/demo-cli.test.js -x</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/demo-cli.test.js -x` exits 0.
    - `node bin/phantom-stream.js demo --help` or invalid usage prints a concise usage line without starting a server.
    - `grep -c "127.0.0.1" bin/phantom-stream.js examples/two-tab-demo/server.js tests/demo-cli.test.js` returns at least 3.
    - `grep -c "PhantomStream demo running on 127.0.0.1\\|Source tab:\\|Viewer tab:\\|Room:" tests/demo-cli.test.js` returns at least 4.
    - package.json contains `"phantom-stream": "./bin/phantom-stream.js"`.
  </acceptance_criteria>
  <done>The package binary and local-only demo server start deterministically, print the required URLs, serve static assets safely, and shut down cleanly under test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Source/viewer pages, WebSocket wiring, UI contract, and demo smoke tests</name>
  <files>examples/two-tab-demo/source.html, examples/two-tab-demo/viewer.html, examples/two-tab-demo/demo.css, examples/two-tab-demo/source.js, examples/two-tab-demo/viewer.js, tests/demo-cli.test.js</files>
  <read_first>
    - .planning/phases/04-relay-ws-transport-two-tab-demo/04-CONTEXT.md decisions D-04, D-07, D-20 through D-24, D-29
    - .planning/phases/04-relay-ws-transport-two-tab-demo/04-UI-SPEC.md all sections, especially Source Tab, Viewer Tab, Lifecycle & Health Interaction Contract, and Copywriting Contract
    - .planning/phases/04-relay-ws-transport-two-tab-demo/04-PATTERNS.md sections for source.html, viewer.html, demo.css, source.js, and viewer.js
    - examples/loopback-mirror.html
    - src/capture/index.js
    - src/renderer/index.js
    - src/transport/websocket.js
    - src/protocol/messages.js
  </read_first>
  <behavior>
    - Test/static check: source page title is `PhantomStream — Source Tab`, viewer page title is `PhantomStream — Viewer Tab`, and both link `demo.css`.
    - Test/static check: source controls appear in order: `Add row`, `Remove row`, `Edit text`, `Show dialog`, `Auto-mutate`.
    - Test/static check: viewer page contains status fields for lifecycle, room prefix, last frame, relay transport state, frames, snapshots, mutations, misses, apply failures, drops, and errors.
    - Test/static check: CSS includes stage `aspect-ratio: 16 / 10`, `min-height: 280px`, `height: min(72vh, 720px)`, small-screen `min-height: 220px`, and the UI-SPEC colors.
    - Test/static check: source.js imports `createCapture` and `createWebSocketTransport`, starts capture on WebSocket open and on CONTROL.START, and uses skipElement for `data-phantomstream-ui`.
    - Test/static check: viewer.js imports `createViewer` and `createWebSocketTransport`, subscribes to `viewer.on('state')` and `viewer.on('health')`, and sends CONTROL.START on open.
  </behavior>
  <action>
    Build the demo as the usable first screen, not a landing page. source.html owns the source tab shell with title/subtitle/capturing badge/status strip/pane/controls/row list/log. viewer.html owns the viewer shell with lifecycle badge/status strip/health row/mirror stage/empty state/log. demo.css implements the UI-SPEC tokens exactly: system font, mono metrics, `#0f1117` background, `#1e1e2e` panes, `#f59e0b` primary CTA/focus, status dot colors, 36px minimum interactive height, stage dimensions, and polite status accessibility. source.js reads `room` and `ws` query params, creates `createWebSocketTransport({ url, role: 'source' })`, creates `createCapture({ transport, skipElement })`, seeds at least three rows, implements all five controls, starts auto-mutate by default at about 1000ms, starts capture when transport opens, and handles CONTROL.START/STOP/PAUSE/RESUME. viewer.js reads query params, creates `createWebSocketTransport({ url, role: 'viewer' })`, creates `createViewer({ container, transport })`, subscribes to `viewer.on('state')` and `viewer.on('health')` to update compact chrome, subscribes to transport status for relay state, sends CONTROL.START when the socket opens, and keeps the last frame visible for stale/disconnected states. Extend tests/demo-cli.test.js with static/HTTP checks for the files and required strings. Do not render mirrored HTML manually in the demo; all mirrored content must go through createViewer.
  </action>
  <verify>
    <automated>node --test tests/demo-cli.test.js -x && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/demo-cli.test.js -x` exits 0.
    - `npm test` exits 0.
    - `grep -c "viewer.on('state'\\|viewer.on('health'" examples/two-tab-demo/viewer.js` returns at least 2.
    - `grep -c "createCapture\\|createWebSocketTransport" examples/two-tab-demo/source.js` returns at least 2.
    - `grep -c "aspect-ratio: 16 / 10\\|min-height: 280px\\|height: min(72vh, 720px)" examples/two-tab-demo/demo.css` returns 3.
  </acceptance_criteria>
  <done>The two-tab demo pages are wired through the WebSocket transport and viewer events, satisfy the UI contract, and pass automated smoke/static checks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser two-tab live mirror and kill-relay checkpoint</name>
  <files>(none - browser verification checkpoint)</files>
  <read_first>
    - .planning/phases/04-relay-ws-transport-two-tab-demo/04-VALIDATION.md manual verifications and T-04-09 row
    - .planning/phases/04-relay-ws-transport-two-tab-demo/04-UI-SPEC.md Lifecycle & Health Interaction Contract
    - tests/demo-cli.test.js latest automated gate output
  </read_first>
  <action>
    After Task 2 is green, run `npm test`, start `node bin/phantom-stream.js demo --no-open`, and present the printed URLs to the human verifier. Pause for the browser verification steps below. Record the outcome in the plan summary after approval or fix any reported issue and repeat the checkpoint.
  </action>
  <what-built>
    A local-only networked PhantomStream demo: source tab captures, viewer tab mirrors through the bundled relay, and viewer status is driven by `viewer.on('state'|'health')`.
  </what-built>
  <how-to-verify>
    1. Run `node bin/phantom-stream.js demo --no-open`.
    2. Confirm terminal output includes `PhantomStream demo running on 127.0.0.1`, `Source tab:`, `Viewer tab:`, and `Room:`.
    3. Open the printed viewer URL first, then the printed source URL.
    4. Confirm the viewer moves from `Connecting` to `Live` after the source tab starts.
    5. In the source tab, click `Add row`, `Remove row`, and `Edit text`; each visible change appears in the viewer mirror.
    6. Click `Show dialog`; the source native alert appears and the viewer/demo status remains usable after dismissal.
    7. Confirm the viewer health row updates frames, snapshots, mutations, misses, apply failures, drops, and errors without showing mirrored page text.
    8. Stop the demo process in the terminal. Confirm the viewer keeps the last rendered frame visible and the lifecycle status reports `Live` -> `Stale` -> `Disconnected`.
  </how-to-verify>
  <resume-signal>Type "approved" if all eight checks pass, or describe the first failing step with browser console output if available.</resume-signal>
  <verify>
    <automated>npm test</automated>
    <manual>Run `node bin/phantom-stream.js demo --no-open`, open the printed viewer/source URLs, mutate source, then stop the relay and observe live -> stale -> disconnected.</manual>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 before the checkpoint is presented.
    - Human confirms live mutation mirroring in a real browser.
    - Human confirms relay shutdown yields `Live` -> `Stale` -> `Disconnected`.
    - The summary records browser, URLs shape, and checkpoint result.
  </acceptance_criteria>
  <done>User approved the real-browser two-tab demo and kill-relay lifecycle behavior, or reported issues were fixed and re-verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal user -> CLI | CLI parses local flags and starts local services |
| browser -> local demo server | Browser requests static assets and upgrades WebSocket connections |
| source tab -> viewer tab | Mirrored page content travels through relay and renderer sandbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Information Disclosure | `examples/two-tab-demo/server.js` | mitigate | Bind only `127.0.0.1`; reuse decode-before-resolve root-prefix guard; no directory listings; tests cover malformed encoding/traversal. |
| T-04-09 | Spoofing | `bin/phantom-stream.js`, `server.js`, demo URLs | mitigate | Generate a random room key with `crypto.randomBytes(16)`, require it in URLs and WebSocket upgrades, and print only room prefix in compact UI. |
| T-04-06 | Availability / Observability | browser checkpoint and viewer page | mitigate | Viewer page subscribes to state events; checkpoint proves live -> stale -> disconnected on relay shutdown. |
| T-04-07 | Information Disclosure | viewer health UI | mitigate | Health row displays counters/timestamps only; tests and checkpoint verify no mirrored page text appears in telemetry. |
</threat_model>

<verification>
- `node --test tests/demo-cli.test.js -x`
- `npm test`
- Browser checkpoint: source/viewer URLs, live mutation mirror, relay kill lifecycle transition
</verification>

<success_criteria>
- PKG-01 works end-to-end locally through the bundled relay.
- VIEW-02 is observable in a real browser through demo lifecycle and health UI.
- Demo services bind to 127.0.0.1, print deterministic URLs, and use generated room keys.
</success_criteria>

<output>
After completion, create `.planning/phases/04-relay-ws-transport-two-tab-demo/04-04-SUMMARY.md`
</output>
