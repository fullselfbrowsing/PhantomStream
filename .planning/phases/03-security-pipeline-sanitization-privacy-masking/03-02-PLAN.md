---
phase: 03-security-pipeline-sanitization-privacy-masking
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/sanitize.js
  - src/renderer/diff.js
  - src/renderer/snapshot.js
  - src/renderer/index.js
  - tests/security-sanitize-render.test.js
  - tests/renderer-diff.test.js
  - tests/renderer-snapshot.test.js
  - tests/renderer-purity.test.js
autonomous: true
requirements: [SEC-02]
must_haves:
  truths:
    - "Every reconstructed-DOM insertion runs through the named render chokepoint sanitizeFragment — DOM-fragment based, never string-scrub-then-innerHTML"
    - "Add-op html parses in template context (tr/td/col/option no longer drop) and is scrubbed before importNode into the mirror"
    - "Attr ops are scrubbed render-side: on*/srcdoc ops dropped, dangerous URL values neutralized, style values CSS-scrubbed (defense-in-depth behind the capture chokepoint)"
    - "buildSnapshotHtml output carries the exact adopted CSP meta (style-src http: https: 'unsafe-inline' per the CONTEXT adjustment clause, documented rationale) as the first element after <head>, and inlineStyles are CSS-scrubbed at assembly"
    - "The post-parse snapshot-body scrub is behaviorally proven: a hostile STREAM.SNAPSHOT fed directly to the viewer, glued via cd.open/write/close and a re-fired load event, yields a mirror body with zero on* attributes"
    - "Render-side sanitization counters live on a per-session lifecycle (reset only in destroy, never in handleSnapshot)"
    - "Benign content renders identically — existing renderer/loopback tests stay green after deliberate re-pins"
  artifacts:
    - path: "src/renderer/sanitize.js"
      provides: "sanitizeFragment fragment walker + sanitizeAttrValue + scrubCssText (render-side chokepoint module)"
      exports: ["sanitizeFragment", "sanitizeAttrValue", "scrubCssText"]
      min_lines: 80
    - path: "src/renderer/diff.js"
      provides: "Template-context ADD parse + sanitizeFragment + attr-op scrub"
      contains: "createElement('template')"
    - path: "src/renderer/snapshot.js"
      provides: "CSP meta injection + inlineStyles CSS scrub (string passes only — module stays DOM-free)"
      contains: "Content-Security-Policy"
    - path: "tests/security-sanitize-render.test.js"
      provides: "Render-side mXSS corpus run against sanitizeFragment + sanitizeAttrValue"
      min_lines: 150
  key_links:
    - from: "src/renderer/diff.js"
      to: "src/renderer/sanitize.js"
      via: "sanitizeFragment on template.content before importNode; sanitizeAttrValue before setAttribute"
      pattern: "import \\{[^}]*sanitizeFragment[^}]*\\} from '\\./sanitize\\.js'"
    - from: "src/renderer/index.js"
      to: "src/renderer/sanitize.js"
      via: "post-parse scrub of the loaded mirror document (snapshot-path defense-in-depth)"
      pattern: "sanitizeFragment"
    - from: "src/renderer/snapshot.js"
      to: "src/renderer/sanitize.js"
      via: "scrubCssText on inlineStyles entries (pure string import — DOM-free contract intact)"
      pattern: "scrubCssText"
---

<objective>
Land the render-side sanitization chokepoint (SEC-02): a new `src/renderer/sanitize.js` module exporting `sanitizeFragment` (TreeWalker over parsed DOM, never string-scrub) plus `sanitizeAttrValue` and `scrubCssText`; upgrade the diff ADD branch from div-context to template-context parsing (taking the queued Phase-3+ item — jsdom-29-verified in 03-RESEARCH.md); inject the adopted CSP meta into `buildSnapshotHtml` (CONTEXT baseline adjusted via its documented-rationale clause: style-src gains http: https: so the external stylesheet links the capture deliberately emits keep loading — script-blocking untouched); scrub inline CSS at srcdoc assembly; add a post-parse scrub of the loaded mirror document, proven behaviorally by a hostile-snapshot test. Two pinned diff tests and two pinned snapshot parity tests flip deliberately.

Purpose: defense-in-depth behind the capture chokepoint — the render side must independently neutralize hostile content across the serialize-reparse boundary (mXSS class), with CSP meta and the existing sandbox assertion as further backstops (success criterion 2).
Output: render chokepoint module + wired diff/snapshot/viewer + render-side mXSS suite, full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-security-pipeline-sanitization-privacy-masking/03-CONTEXT.md
@.planning/phases/03-security-pipeline-sanitization-privacy-masking/03-RESEARCH.md
@.planning/phases/03-security-pipeline-sanitization-privacy-masking/03-PATTERNS.md
@src/protocol/messages.js
</context>

<interfaces>
From src/renderer/diff.js (existing, do not break):

```javascript
export function applyMutations(doc, mutations, counters, hooks): void
// hooks = { logger, requestResync }  — this plan adds OPTIONAL hooks.sanitizeCounters
```

From src/renderer/snapshot.js (existing, signature stays):

```javascript
export function buildSnapshotHtml(payload): string   // pure, DOM-free (module contract, header lines 1-8)
```

Contracts this plan DEFINES (consumed by plan 03-05's purity scan):

```javascript
// src/renderer/sanitize.js — named exports, no module-level side effects.
// DOM access only through nodes passed in (walker via root.ownerDocument).
export function sanitizeFragment(root, counters, logger): void
//   Walks root (DocumentFragment | Element) + descendants. Drops script/
//   noscript/object/embed subtrees (Pitfall 9: noscript content IS DOM in
//   no-allow-scripts sandboxes). Strips on* attrs (namespace-aware),
//   removes srcdoc, neutralizes dangerous URL schemes (href/src/action/
//   formaction/poster/data/xlink:href + srcset candidates), scrubs style
//   attrs via scrubCssText. Mutates in place — never serializes back.
export function sanitizeAttrValue(name, value): { drop: boolean, value: string }
export function scrubCssText(css): string
//   Same regex policy as the capture side (03-RESEARCH.md A4) — duplicated
//   by design (zero shared-module coupling), comment cross-references
//   src/capture/index.js.
// counters = { strippedHandlers, blockedUrls, droppedSubtrees, cssScrubs } — mutated in place
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/renderer/sanitize.js chokepoint module + render-side mXSS corpus</name>
  <files>src/renderer/sanitize.js, tests/security-sanitize-render.test.js, tests/renderer-purity.test.js</files>
  <read_first>
    - .planning/phases/03-security-pipeline-sanitization-privacy-masking/03-RESEARCH.md sections "Pattern 2" (template parse, verified), "Pattern 4" (fragment walker), "Anti-Patterns" (never string-scrub, never srcdoc-assert in jsdom), Pitfalls 2, 4, 9, "Don't Hand-Roll" (scheme + CSS regex recipes), "Code Examples" (mXSS rows)
    - src/capture/index.js lines 596-615 (TreeWalker SHOW_ELEMENT collect-then-mutate pattern — the structural cousin) — read this range only
    - src/renderer/snapshot.js lines 40-46 (escapeAttribute — the pure value-helper shape for sanitizeAttrValue/scrubCssText) and lines 1-8 (module-contract comment style)
    - tests/renderer-diff.test.js lines 24-83 (setupEnv/recordingHooks/freshCounters/nidEl helpers — the doc-based test recipe to duplicate locally)
    - tests/renderer-purity.test.js lines 39-56 (required-module pin to extend) and 57-90 (forbidden-pattern scan that auto-picks-up the new file)
  </read_first>
  <behavior>
    - Test: sanitizeFragment over a template-parsed fragment containing button onclick, a href=javascript:, iframe srcdoc, object, embed, noscript-wrapped img onerror — afterwards the fragment has no on* attrs anywhere, no srcdoc attr, no object/embed/noscript/script elements (noscript dropped EXPLICITLY — Pitfall 9)
    - Test: the namespace-confusion math/mglyph payload parsed via template + sanitizeFragment yields a fragment with no onerror attribute on any element (attributes enumerated on every element regardless of namespace)
    - Test: xlink:href="javascript:..." inside an svg subtree is neutralized; srcset with a javascript: candidate is neutralized per-candidate
    - Test: style="width:expression(alert(1))" is scrubbed; benign style values pass through unchanged
    - Test: a benign fragment (nested divs, classes, http(s) hrefs, relative srcs, text) is byte-identical before/after (fidelity pin — serialize via a detached container's innerHTML for comparison only inside the test)
    - Test: sanitizeAttrValue('onclick', ...) returns drop:true; ('srcdoc', ...) drop:true; ('href', 'javascript:alert(1)') returns value ''; ('href', 'https://x.test') unchanged; ('style', hostile) returns scrubbed value
    - Test: scrubCssText neutralizes url(javascript:), expression(), -moz-binding, non-http @import, and the literal "&lt;/style" sequence; relative url() and url(https://...) and url(data:image/png;base64,...) pass unchanged
    - Test: counters increment per category and the logger received a '[Renderer]'-prefixed warn when strips occurred; zero warns on a benign fragment
    - Test: NO test in this file ever assigns iframe.srcdoc and asserts contentDocument (Pitfall 2 — jsdom never parses srcdoc; fragments are built directly via doc.createElement('template'))
  </behavior>
  <action>
    Create src/renderer/sanitize.js per the interfaces block, cross-runtime style (var, || defaulting, named exports, JSDoc, .js import extensions). sanitizeFragment: create a TreeWalker (root.ownerDocument.createTreeWalker, NodeFilter.SHOW_ELEMENT) or equivalent collect-first iteration — collect all elements into an array BEFORE mutating (the capture serializer's collect-then-mutate precedent at src/capture/index.js:596-615); for each element: tag in {script, noscript, object, embed} -> remove the element (parentNode.removeChild, count droppedSubtrees) and skip its subtree; otherwise enumerate el.attributes into a names array first, then for each: lowercased name starting with 'on' -> removeAttribute (count strippedHandlers); 'srcdoc' -> removeAttribute; name in {href, src, action, formaction, poster, data} or the xlink:href namespaced attr -> if hasDangerousScheme(value) set '' (count blockedUrls); 'srcset' -> neutralize dangerous candidates; 'style' -> setAttribute(scrubCssText(value)) counting cssScrubs when changed. hasDangerousScheme: same control-char-stripping prefix test as the capture side (javascript:, vbscript:, data:text/html; data:image/* allowed) — keep it module-internal here. scrubCssText: same policy as capture (url() with explicit non-http(s)/non-data:image scheme -> contents replaced with about:blank; NO-scheme relative url() allowed; strip expression() and -moz-binding; neutralize non-http(s) @import; rewrite "&lt;/style" to "&lt;&#92;/style") with a comment cross-referencing src/capture/index.js and 03-RESEARCH.md A4 (deliberate duplication, no shared module). sanitizeAttrValue(name, value): the attr-op-shaped dispatch returning { drop, value } per the behavior rows. Emit ONE aggregated logger.warn('[Renderer] sanitization strips', {...}) per sanitizeFragment call when any counter moved (counted + logged, never silent). All functions null-tolerant, exceptions contained to identity (absolutifyUrl discipline). Extend tests/renderer-purity.test.js: add 'sanitize.js' to the required-module pin array at line 48 (the forbidden-pattern scans pick the new file up automatically). Write tests/security-sanitize-render.test.js FIRST per the behavior list, duplicating the renderer-diff.test.js env helpers locally; build fragments exclusively via doc.createElement('template') + tpl.innerHTML inside the test.
  </action>
  <verify>
    <automated>node --test tests/security-sanitize-render.test.js tests/renderer-purity.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/security-sanitize-render.test.js` exits 0 with at least 10 passing tests
    - `grep -c "export function" src/renderer/sanitize.js` returns 3 (sanitizeFragment, sanitizeAttrValue, scrubCssText)
    - `grep -c "srcdoc" tests/security-sanitize-render.test.js` returns 0 except in attribute-name string fixtures: specifically `grep -c "iframe.srcdoc\s*=" tests/security-sanitize-render.test.js` returns 0 (Pitfall 2)
    - `grep -c "'sanitize.js'" tests/renderer-purity.test.js` returns 1 (required-module pin extended)
    - `grep -v '^\s*//' src/renderer/sanitize.js | grep -c "innerHTML"` returns 0 (the walker never serializes or re-parses — DOM-fragment-based only)
    - `node --test tests/renderer-purity.test.js` exits 0 (new module obeys the forbidden-pattern rules)
  </acceptance_criteria>
  <done>The render chokepoint module exists with the full mXSS corpus green against it; purity scan covers the new file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: diff.js template-context parse + chokepoint integration + deliberate re-pins</name>
  <files>src/renderer/diff.js, tests/renderer-diff.test.js, tests/security-sanitize-render.test.js</files>
  <read_first>
    - src/renderer/diff.js in full (188 lines) — ADD branch at 94-126 (the div-context parse being replaced, WR-02 warn+count discipline), ATTR branch at 136-147 (the setAttribute to interpose), TEXT branch at 149-157 (textContent — no parse path, no scrub), hooks defaulting at 62-67, per-op try/catch at 159-175
    - .planning/phases/03-security-pipeline-sanitization-privacy-masking/03-RESEARCH.md "Pattern 2" (template parse + importNode example, jsdom-29-verified) and Pitfall 5
    - tests/renderer-diff.test.js lines 85-160 (the two pins that flip: the temp-div add test at ~85 and the WR-02 tr-drop test at ~133 — re-pin, never delete)
  </read_first>
  <behavior>
    - Test (re-pin of line ~85): 'add' op inserts the html's first element via TEMPLATE-context parsing + importNode — same assertions as before (present, correct tag, under parentNid, appendChild when no beforeNid)
    - Test (FLIP of line ~133): an 'add' op whose html is a bare &lt;tr&gt; row now INSERTS successfully under a table-shaped parent (template context preserves it — jsdom-29-verified); zero stale misses
    - Test (kept WR-02 path): an 'add' whose html parses to no element at all (empty/whitespace-only string) still warns with the real cause and counts through recordStaleMiss — the warn message no longer says "div context"
    - Test: an 'add' whose html carries onclick and javascript: href inserts a node with neither (sanitizeFragment ran on template.content before importNode)
    - Test: an 'attr' op with attr 'onclick' is DROPPED — target unchanged, no setAttribute, sanitize counter incremented, no stale miss
    - Test: an 'attr' op with attr 'href' val 'javascript:alert(1)' sets href to ''; benign attr ops apply unchanged
    - Test: an 'attr' op with attr 'srcdoc' is dropped
    - Test: 'text' ops still apply via textContent unchanged (no parse path)
  </behavior>
  <action>
    Replace the ADD branch's div-context parse (diff.js:94-126) with the 03-RESEARCH.md Pattern 2 shape: doc.createElement('template'), tpl.innerHTML = m.html (this becomes the renderer's ONLY innerHTML sink for wire content — note it in a comment; plan 03-05's purity scan pins it), sanitizeFragment(tpl.content, sanitizeCounters, logger) imported from './sanitize.js', take tpl.content.firstElementChild, on null keep the WR-02 discipline (logger.warn with the updated real cause — "html parsed to no element" — plus recordStaleMiss) and otherwise doc.importNode(newNode, true) then insertBefore (importNode is REQUIRED — cross-doc adoption from the template parser doc; keep the null-beforeNid appendChild parity). Interpose the ATTR branch: call sanitizeAttrValue(m.attr, m.val) before the existing apply — drop:true -> count + break (keep m.val === null removeAttribute path BEFORE the scrub since null is a removal, not a value); else setAttribute with the returned value. Leave the TEXT branch untouched and add a one-line comment stating WHY (textContent assignment has no HTML parse path). Plumb counters: accept OPTIONAL hooks.sanitizeCounters (default a local object via the hooks.logger defaulting shape at lines 62-67) so the public 4-arg applyMutations signature is unchanged; keep all sanitize calls INSIDE the per-op try (a throwing scrub is contained per-op, lines 159-175). Update the file-top provenance comment (the division-of-responsibility block) to name the template upgrade and the chokepoint. Re-pin tests/renderer-diff.test.js deliberately: update the line-85 test name/assertions to template semantics; FLIP the line-133 WR-02 test from drop-to-miss into successful tr insertion; add the empty-html warn+count case so the WR-02 path stays covered — never delete assertions wholesale (execution note). Add the chokepoint-integration rows (hostile add/attr ops) to tests/security-sanitize-render.test.js.
  </action>
  <verify>
    <automated>node --test tests/renderer-diff.test.js tests/security-sanitize-render.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/renderer-diff.test.js` exits 0 — the tr/td case asserts successful insertion (zero stale misses), the empty-html case still warns and counts
    - `grep -c "createElement('template')" src/renderer/diff.js` returns 1 and `grep -c "createElement('div')" src/renderer/diff.js` returns 0
    - `grep -c "importNode" src/renderer/diff.js` returns >= 1
    - `grep -c "sanitizeFragment\|sanitizeAttrValue" src/renderer/diff.js` returns >= 2 (both insertion points covered)
    - `node --test tests/renderer-loopback.test.js` exits 0 (e2e loopback unaffected by the parse upgrade)
  </acceptance_criteria>
  <done>Add ops parse in template context and are fragment-scrubbed before import; attr ops are scrubbed; the two pinned tests are deliberately re-pinned; loopback e2e still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: snapshot.js CSP meta + CSS scrub, index.js counter lifecycle + post-parse scrub, snapshot re-pins</name>
  <files>src/renderer/snapshot.js, src/renderer/index.js, tests/renderer-snapshot.test.js, tests/security-sanitize-render.test.js</files>
  <read_first>
    - src/renderer/snapshot.js in full (109 lines) — module-contract header lines 1-8 (DOM-FREE: string passes only may land here), WR-03 inventory comment at 18-28 (update entries 1 and 2), buildSnapshotHtml assembly at 89-109 (head open + inlineStyleTags join)
    - src/renderer/index.js lines 176-186 (sandbox assertion — DO NOT TOUCH, cite in comments), 221-230 (closure state block where sanitizeCounters declares), 337-346 (per-snapshot counter reset — sanitize counters must NOT join it, Pitfall 3 — and the iframe.srcdoc write), 361-366 (applyMutations call site for hooks.sanitizeCounters), 502-514 (destroy — the ONLY sanitize-counter reset site)
    - tests/renderer-loopback.test.js lines 180-195 (jsdom load-semantics ledger note: the iframe load event fires exactly ONCE, for about:blank) and 248-264 (glueMirror — the cd.open/write/close recipe the post-parse-scrub test reuses)
    - tests/renderer-viewer.test.js lines 46-166 (setupEnv / createRecordingTransport / recordingLogger / snapshotPayload / waitForStreaming — the stub-transport viewer recipe the new behavioral test duplicates locally)
    - tests/renderer-snapshot.test.js lines 28-44 (minimalPayload helper + charset-meta test shape) and lines 95-120 (the two parity pins: inlineStyles-raw at ~104 FLIPS, payload.html-raw at ~113 is re-rationalized)
    - .planning/phases/03-security-pipeline-sanitization-privacy-masking/03-RESEARCH.md "CSP meta injection" code example + Pitfall 8 (meta-unsupported directives) + Pitfall 3 (counter lifecycle)
  </read_first>
  <behavior>
    - Test: buildSnapshotHtml output contains the exact adopted CSP meta — content="default-src 'none'; img-src http: https: data:; style-src http: https: 'unsafe-inline'; font-src http: https: data:" — positioned as the FIRST element after the &lt;head&gt; open, before the charset meta
    - Test (FLIP of ~104): an inlineStyles entry containing a literal &lt;/style&gt; no longer passes through raw — the emitted style tag contains no raw "&lt;/style" sequence inside the CSS body, and url(javascript:) within it is scrubbed; benign CSS passes byte-identical
    - Test (re-pin of ~113): payload.html is still inserted raw between the body tags AT THE STRING LAYER — assertion kept, comment updated: string-level scrubbing is the mXSS anti-pattern; protection for this insertion point is the capture chokepoint upstream + the post-parse scrub + CSP + sandbox
    - Test: the CSP meta contains none of frame-ancestors / sandbox / report-uri (meta-unsupported directives, Pitfall 8)
    - Test (post-parse scrub, behavioral — lives in tests/security-sanitize-render.test.js): a STREAM.SNAPSHOT whose payload.html carries hostile content (button onclick plus a javascript: href — simulating a bypassed capture chokepoint) is fed DIRECTLY to the viewer through the recorded onMessage handler; the test then glues the srcdoc via the loopback recipe (cd.open(); cd.write(iframe.getAttribute('srcdoc')); cd.close();) and dispatches a synthetic 'load' event on the iframe (jsdom fires load exactly ONCE, for about:blank — the re-fire exercises the creation-time listener against the populated document); afterwards contentDocument.body contains zero on* attributes and no javascript: href, and the recording logger received a '[Renderer] sanitization strips' warn
  </behavior>
  <action>
    In src/renderer/snapshot.js (string passes ONLY — the module stays DOM-free per its header contract; the fragment walker may not land here): define the CSP meta as a module constant inserted immediately after the &lt;head&gt; open in buildSnapshotHtml so it precedes any parser-initiated fetch, using the ADOPTED policy: default-src 'none'; img-src http: https: data:; style-src http: https: 'unsafe-inline'; font-src http: https: data: — this adjusts the CONTEXT-locked baseline (style-src 'unsafe-inline') by invoking the decision's own adjustment clause ("adjust only with documented rationale if mirror fidelity requires, never weaker than script-blocking"): the capture deliberately emits external stylesheet links (stylesheets[] collection, src/capture/index.js:718-723) and 'unsafe-inline' alone would block every link-rel-stylesheet load in the mirror, breaking real-world fidelity from Phase 4 on; script-blocking is untouched (default-src 'none' still governs scripts; no script-src is introduced); put this rationale in a comment on the constant (cross-referencing docs/SECURITY.md, where plan 03-05 documents the adjustment) and follow the 03-RESEARCH.md code-example shape for assembly; route each inlineStyles entry through scrubCssText imported from './sanitize.js' (pure string function — importing it does not violate the DOM-free contract since this file's own content stays DOM-free); update the WR-03 inventory comment entries 1 and 2 (entry 1: inlineStyles now CSS-scrubbed; entry 2: payload.html intentionally raw at the string layer with the defense chain named) and the file-top "known gap" paragraph. In src/renderer/index.js: declare a sanitizeCounters closure object ({ strippedHandlers: 0, blockedUrls: 0, droppedSubtrees: 0, cssScrubs: 0 }) in the closure state block with a comment documenting the PER-SESSION lifecycle decision (Pitfall 3: per-snapshot for miss counters, per-session for sanitization — these measure different things); do NOT reset it in handleSnapshot (lines 339-341); reset it ONLY in destroy(); pass it to applyMutations via hooks.sanitizeCounters at the line-361 call site; add a post-parse defense-in-depth scrub: register an iframe 'load' listener at creation that, when contentDocument has a body, runs sanitizeFragment(contentDocument.body, sanitizeCounters, logger) — this is the snapshot-path render-side coverage (the srcdoc string cannot be fragment-scrubbed before the browser parses it; scrubbing the PARSED document avoids the serialize-reparse anti-pattern; in jsdom the listener is inert because srcdoc never parses — note this in the comment, the loopback/browser path exercises it). Update tests/renderer-snapshot.test.js per the behavior list: add the CSP meta tests (copy the charset-meta test shape), flip the ~104 pin, re-rationalize the ~113 pin. Add the post-parse-scrub behavioral test to tests/security-sanitize-render.test.js: duplicate the renderer-viewer.test.js stub-transport recipe locally (setupEnv, createRecordingTransport, recordingLogger, snapshotPayload), create the viewer, invoke the recorded onMessage handler directly with (STREAM.SNAPSHOT, a snapshotPayload whose html carries button-onclick + javascript:-href content), glue the written srcdoc via the loopback glueMirror recipe (cd.open(); cd.write(iframe.getAttribute('srcdoc')); cd.close();), then dispatch a synthetic 'load' Event on the iframe with a comment explaining WHY (jsdom fires iframe load exactly once — for about:blank, before any srcdoc write — so the deliberate re-fire is what exercises the creation-time load listener against the populated document, which reads contentDocument fresh per call); assert zero on* attributes and no javascript: href anywhere under contentDocument.body, and that the recording logger received a '[Renderer] sanitization strips' warn.
  </action>
  <verify>
    <automated>node --test tests/renderer-snapshot.test.js tests/renderer-viewer.test.js tests/security-sanitize-render.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/renderer-snapshot.test.js` exits 0; the CSP test pins the exact adopted policy string with default-src 'none' first and style-src http: https: 'unsafe-inline'
    - The post-parse-scrub behavioral row passes in tests/security-sanitize-render.test.js: hostile snapshot -> glue -> re-fired load -> scrubbed mirror body (the 'load' listener is exercised, not inert)
    - `grep -c "Content-Security-Policy" src/renderer/snapshot.js` returns >= 1 and `grep -c "frame-ancestors\|report-uri" src/renderer/snapshot.js` returns 0
    - `grep -v '^\s*//' src/renderer/snapshot.js | grep -cE "\bdocument\.|\bwindow\." ` returns 0 (module still DOM-free)
    - `grep -c "sanitizeCounters" src/renderer/index.js` returns >= 3 (declaration, applyMutations hooks, destroy reset) and the handleSnapshot reset block (counters.staleMisses/applyFailures) does NOT mention sanitizeCounters
    - `npm test` exits 0 — full suite green (renderer, loopback, oracle, capture all unaffected on benign content)
  </acceptance_criteria>
  <done>Adopted CSP meta backstop (with documented style-src rationale) and CSS scrub land at srcdoc assembly; sanitize counters live per-session; the parsed mirror document gets a post-parse scrub proven behaviorally via the glue + re-fired-load recipe; both snapshot parity pins deliberately re-pinned; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wire -> renderer | Payloads are sanitized upstream (03-01) but the render side must NOT trust that — defense-in-depth is the bar (success criterion 2) |
| srcdoc string -> browser parser | The serialize-reparse boundary where mXSS mutations occur — the reason the chokepoint is DOM-fragment based |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | serialize-reparse mXSS across template parsing | mitigate | sanitizeFragment operates on the PARSED fragment and importNode moves it without re-serialization; string-scrub-then-innerHTML is structurally absent (purity-scanned in 03-05) |
| T-03-08 | EoP | attr ops applying on*/srcdoc/javascript: via setAttribute (Pitfall 5, render side) | mitigate | sanitizeAttrValue interposes before every setAttribute in the ATTR branch |
| T-03-09 | EoP | snapshot body html parsed raw from the srcdoc string | mitigate (layered) | Cannot fragment-scrub a string without re-parse (the anti-pattern); layers: capture chokepoint upstream + post-parse sanitizeFragment on load + CSP meta (script-src absent under default-src 'none') + allow-same-origin-only sandbox. Residual: a window between parse and load-scrub in real browsers — accepted, scripts cannot execute under sandbox+CSP |
| T-03-10 | EoP | CSS url(javascript:)/expression in inlineStyles + </style> breakout | mitigate | scrubCssText at assembly; CSP blocks script/plugin fetches from broken-out markup (the adjusted style-src http: https: permits stylesheet loads by design — the scrub layers remain the primary control for CSS) |
| T-03-11 | Repudiation | counter-reset bug hiding a sustained strip rate (Pitfall 3) | mitigate | per-session lifecycle, reset only in destroy(), documented at declaration |
| T-03-12 | Tampering | noscript content parsed as DOM in no-allow-scripts sandboxes (Pitfall 9) | mitigate | sanitizeFragment drops noscript subtrees explicitly |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase |
</threat_model>

<verification>
- `node --test tests/security-sanitize-render.test.js tests/renderer-diff.test.js tests/renderer-snapshot.test.js tests/renderer-purity.test.js tests/renderer-viewer.test.js` green
- `npm test` green — loopback e2e and oracle unaffected on benign content
- No test anywhere in the new file asserts mirror content via iframe.srcdoc + contentDocument (Pitfall 2)
</verification>

<success_criteria>
- One named render-side chokepoint (sanitizeFragment) covers both reconstructed-DOM insertion points: add-op fragments and (post-parse) the snapshot document; attr ops scrubbed via sanitizeAttrValue (success criterion 2)
- The adopted CSP meta (CONTEXT baseline + documented style-src http: https: adjustment) is in every srcdoc document; sandbox assertion untouched and still pinned by renderer-viewer tests
- Template parsing upgrade taken: tr/td/col/option add ops now render (queued WR-02 fix), flipped pins deliberate
- mXSS corpus green render-side; benign fidelity pinned
</success_criteria>

<output>
Create `.planning/phases/03-security-pipeline-sanitization-privacy-masking/03-02-SUMMARY.md` when done.
</output>
