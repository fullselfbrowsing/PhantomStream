---
phase: 03-security-pipeline-sanitization-privacy-masking
plan: 05
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified:
  - tests/security-chokepoint-purity.test.js
  - docs/SECURITY.md
  - README.md
  - src/capture/README.md
  - src/renderer/README.md
autonomous: false
requirements: [SEC-01, SEC-02, SEC-03]
must_haves:
  truths:
    - "A static-scan purity test fails CI if any serialization path bypasses sanitizeForWire or any renderer module references allow-scripts or grows an unsanctioned innerHTML sink"
    - "The embed security contract is documented in docs/SECURITY.md: sandbox token contract, exact CSP policy (+ meta-unsupported directives note), both chokepoints, masking guarantees, host must-nevers, residual risks"
    - "README.md points to docs/SECURITY.md; module READMEs retire the queued Phase-3 items and record the E3 capture extension"
    - "npm test is fully green (Phase 2's 130 tests + all Phase 3 additions) and the loopback demo renders benign content with no visible fidelity change (human-verified)"
  artifacts:
    - path: "tests/security-chokepoint-purity.test.js"
      provides: "Static scans: capture chokepoint coverage, renderer allow-scripts/innerHTML rules, SECURITY.md existence guard"
      min_lines: 80
    - path: "docs/SECURITY.md"
      provides: "Embed security contract (CONTEXT locked location)"
      min_lines: 80
  key_links:
    - from: "tests/security-chokepoint-purity.test.js"
      to: "src/capture/index.js"
      via: "stripComments scan counting safeSend vs sanitizeForWire references (Pattern 1)"
      pattern: "sanitizeForWire"
    - from: "README.md"
      to: "docs/SECURITY.md"
      via: "pointer link in the docs section"
      pattern: "docs/SECURITY\\.md"
---

<objective>
Close the phase: land the chokepoint-purity static scan (the durable defense against future bypass drift — 03-RESEARCH.md Pitfall 1), write the embed security contract in `docs/SECURITY.md` (CONTEXT locked location), update the README pointers and module READMEs, then gate the phase on the full suite plus a human verification that the loopback demo dogfoods the entire security pipeline with no visible fidelity change on benign content.

Purpose: success criterion 3 (documented embed contract + fail-loud enforcement) and the phase-wide "always-on, demo dogfoods it" CONTEXT decision.
Output: purity test, SECURITY.md, README updates, full-suite gate, human-verified demo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-security-pipeline-sanitization-privacy-masking/03-CONTEXT.md
@.planning/phases/03-security-pipeline-sanitization-privacy-masking/03-RESEARCH.md
@.planning/phases/03-security-pipeline-sanitization-privacy-masking/03-PATTERNS.md
@.planning/phases/03-security-pipeline-sanitization-privacy-masking/03-01-SUMMARY.md
@.planning/phases/03-security-pipeline-sanitization-privacy-masking/03-02-SUMMARY.md
@.planning/phases/03-security-pipeline-sanitization-privacy-masking/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Chokepoint-purity static-scan test</name>
  <files>tests/security-chokepoint-purity.test.js</files>
  <read_first>
    - tests/capture-purity.test.js in full (stripComments helper at 25-32, per-file scan loop, explanatory assert messages)
    - tests/renderer-purity.test.js in full (vacuous-pass guard at 39-55, multi-pattern forbidden scan at 57-90 — the module-list scan that now includes sanitize.js)
    - src/capture/index.js — ONLY the serialization-path inventory comment block above sanitizeForWire (landed by 03-01 Task 2) and the sanitizeForWire call sites (grep, do not re-read the file): `grep -n "sanitizeForWire(\|safeSend(" src/capture/index.js`
    - .planning/phases/03-security-pipeline-sanitization-privacy-masking/03-RESEARCH.md "Pattern 1" (the coverage-counting rule and its rationale)
  </read_first>
  <behavior>
    - Test: capture coverage — after stripComments, src/capture/index.js contains exactly one `function sanitizeForWire` definition, and the count of sanitizeForWire( call references is >= the number of distinct dispatch kinds in use (pin the floor to the actual call-site count at time of writing, with an explanatory message naming the five serialization paths)
    - Test: capture inventory ground truth — the serialization-path inventory comment marker exists in the RAW (uncomment-stripped) source, so a future path addition without an inventory update is visible in review
    - Test: renderer forbidden tokens — after stripComments, no file in src/renderer/*.js contains the string allow-scripts ANYWHERE except the assertion/comment context in index.js: enforce zero matches of `allow-scripts` in stripped sources of all renderer modules (the sandbox attribute is set to allow-same-origin only; the assertion compares against allow-same-origin, not allow-scripts)
    - Test: renderer innerHTML allowlist — innerHTML assignment sinks in stripped src/renderer sources exist ONLY at the sanctioned sites: the template-parse in diff.js and the static ICON_SVG writes in overlays.js; any new innerHTML sink fails with a message pointing at sanitizeFragment
    - Test: render chokepoint wiring — diff.js references sanitizeFragment and sanitizeAttrValue; index.js references sanitizeFragment (post-parse scrub); snapshot.js references scrubCssText and Content-Security-Policy
    - Test: docs guard — docs/SECURITY.md exists and contains the headings/markers for sandbox contract, CSP policy, masking guarantees, and host must-nevers (vacuous-pass guard shape from renderer-purity 39-55; this test is written RED first and goes green when Task 2 lands)
  </behavior>
  <action>
    Create tests/security-chokepoint-purity.test.js following the capture-purity/renderer-purity recipe: stripComments helper copied verbatim (comments stripped so provenance notes stay legal; string literals NOT stripped), readFileSync over src/capture/index.js and every .js in src/renderer/. Implement the behavior rows. Grep-gate hygiene: every count runs against the comment-STRIPPED source except the inventory-comment presence check (which targets the raw source by design — state this in a comment); never use a bare zero-match gate on unfiltered file content. For the innerHTML allowlist use a per-file expected-count map (diff.js: 1, overlays.js: the current static-icon count, all others: 0) with assert messages explaining that new wire-content sinks must route through sanitizeFragment. For the SECURITY.md guard, assert file existence AND at least four required marker strings (e.g. 'allow-same-origin', the exact CSP policy substring "default-src 'none'", 'maskTextSelector', a must-never phrase) so an empty stub cannot pass. The test will be RED on the SECURITY.md rows until Task 2 — commit order within this plan: write test, see the docs rows fail, land Task 2, see green (RED-GREEN discipline).
  </action>
  <verify>
    <automated>node --test tests/security-chokepoint-purity.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The test file exists with at least 6 tests; all non-docs rows pass immediately (chokepoints landed in waves 1-2)
    - The capture-coverage test names its floor and fails if a sanitizeForWire call site is removed: verified by the assert message content
    - `grep -c "stripComments" tests/security-chokepoint-purity.test.js` returns >= 2 (helper + uses)
    - The docs-guard rows fail RED before Task 2 and pass after it
  </acceptance_criteria>
  <done>Bypass drift (new serialization path, new innerHTML sink, allow-scripts regression, gutted SECURITY.md) fails CI with explanatory messages.</done>
</task>

<task type="auto">
  <name>Task 2: docs/SECURITY.md + README pointer + module README updates</name>
  <files>docs/SECURITY.md, README.md, src/capture/README.md, src/renderer/README.md</files>
  <read_first>
    - docs/ARCHITECTURE.md lines 1-30 (style analog: H1, framing paragraph with file references, numbered H2 sections)
    - README.md line 40 (the docs/ARCHITECTURE.md pointer style) and the docs/ tree listing around line 73
    - src/capture/README.md entry E2 at ~line 102 (extension-entry format) and the "Behavioral changes queued" section at ~line 121
    - src/renderer/README.md lines 242-286 (the four "queued for Phase 3+" items to retire) and the Environment section's sandbox-contract paragraph (cite, keep)
    - .planning/phases/03-security-pipeline-sanitization-privacy-masking/03-RESEARCH.md "Security Domain" (threat-pattern table to adapt), Pitfall 8 (meta-unsupported CSP directives), Open Question 4 (object/embed: dropped)
    - .planning/phases/03-security-pipeline-sanitization-privacy-masking/03-CONTEXT.md decisions (the locked CSP policy string, the docs/SECURITY.md location, always-on no-opt-out)
  </read_first>
  <action>
    Write docs/SECURITY.md in the ARCHITECTURE.md style with these sections: (1) Threat model — adapt the 03-RESEARCH Security Domain table (inline handlers, javascript: URLs, nested srcdoc, object/embed, namespace-confusion mXSS, CSS vectors, password/PII leakage, chokepoint-bypass drift), each row naming its mitigation layer(s); (2) Defense-in-depth pipeline — the five layers in order: capture sanitizeForWire (src/capture/index.js), wire, render sanitizeFragment/sanitizeAttrValue (src/renderer/sanitize.js) + post-parse scrub, srcdoc CSP meta (quote the EXACT locked policy and note that frame-ancestors/sandbox/report-uri are silently ignored via meta — the iframe sandbox attribute is the analogous control, Pitfall 8), iframe sandbox token contract (allow-same-origin ONLY, never allow-scripts; the createViewer startup assertion throws viewer-sandbox-invalid — cite src/renderer/index.js); (3) Sanitization policy — blocklist (fidelity-first, allowlist rejected per CONTEXT), what is stripped/neutralized/dropped (on*, dangerous schemes incl. obfuscated, srcdoc, object/embed dropped per the discretion call, noscript, CSS scrub incl. relative-url allowance rationale), always-on with NO opt-out config on either side; (4) Masking guarantees — capture-side only, password always masked (non-configurable), rrweb-compatible vocabulary and semantics (asterisk per non-whitespace char, rr_width/rr_height placeholders), fail-closed containment, the select/option display-text residual, factory-throw on invalid selectors; (5) Host must-nevers — never add sandbox tokens (especially allow-scripts), never render wire payloads outside createViewer, never re-serialize the mirror document into innerHTML elsewhere, never treat overlay/dialog text as HTML; (6) Residual risks — snapshot html is string-raw at assembly by design (string scrubbing is the mXSS anti-pattern) with the layered protections named; novel CSS/markup vectors backstopped by CSP + sandbox; counters/logs as the observability signal (strips are counted and logged, never silent). Update README.md: add the docs/SECURITY.md pointer in the line-40 style and the docs tree listing. Update src/capture/README.md: add extension entry E3 (capture-side sanitization + masking; deliberate divergence ledgered as D7 — cite tests/differential/divergence-ledger.js) in the E2 format and refresh the "Behavioral changes queued" section. Update src/renderer/README.md: retire the four "queued for Phase 3+" items at 242-286 (raw inline-style insertion -> CSS-scrubbed + CSP; on* survival -> both chokepoints landed; div-context parsing -> template-context landed; embed contract -> docs/SECURITY.md), KEEPING the pre-onload mutation-drop note and the Phase-7 querySelector note; update the Environment sandbox paragraph's "documented in Phase 3" sentence to point at docs/SECURITY.md.
  </action>
  <verify>
    <automated>node --test tests/security-chokepoint-purity.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/security-chokepoint-purity.test.js` exits 0 — including the SECURITY.md content guards (RED in Task 1, GREEN now)
    - `grep -c "docs/SECURITY.md" README.md` returns >= 1
    - `grep -c "allow-scripts" docs/SECURITY.md` returns >= 1 (the must-never is explicit) and `grep -c "default-src 'none'" docs/SECURITY.md` returns >= 1 (exact policy quoted)
    - `grep -c "E3" src/capture/README.md` returns >= 1 and `grep -c "queued for Phase 3+" src/renderer/README.md` returns 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>The embed security contract is documented at the locked location with pointers from the README and both module READMEs; the purity guard enforces its substance.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase gate — full suite + loopback demo dogfood verification</name>
  <files>(none — human verification; everything automatable was automated in Tasks 1-2 and waves 1-3)</files>
  <read_first>
    - .planning/phases/03-security-pipeline-sanitization-privacy-masking/03-VALIDATION.md "Manual-Only Verifications" (the demo dogfood spot-check row this checkpoint discharges)
    - examples/loopback-mirror.html (the page being verified — know what benign content looks like before judging fidelity)
  </read_first>
  <action>Run the automated gate (npm test — must exit 0 before pausing), then present the verification steps below to the user and pause. This checkpoint covers only what jsdom/CI cannot prove: real-browser srcdoc parsing under the new CSP meta, visual fidelity of sanitized output, and live hostile-injection neutralization (02-06 precedent).</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with 0 failures before the checkpoint is presented
    - User responds "approved" after steps 2-5, or every reported issue is resolved and the checkpoint re-presented
  </acceptance_criteria>
  <what-built>The complete Phase 3 security pipeline: capture chokepoint (sanitizeForWire) on all five serialization paths, render chokepoint (sanitizeFragment + sanitizeAttrValue + post-parse scrub), template-context add-op parsing, srcdoc CSP meta, privacy masking (blockSelector/maskTextSelector/maskInputs/password-always), the D7 oracle ledger entry, the purity static scan, and docs/SECURITY.md. The suite (npm test) is fully green — Phase 2's 130 tests plus all Phase 3 additions — and the loopback demo now dogfoods the always-on pipeline.</what-built>
  <how-to-verify>
    1. Run: npm test — confirm 0 failures (the automated gate; the executor ran this, re-run to witness it)
    2. Run: npm run example:loopback and open the printed URL in a real Chromium browser
    3. Verify the mirror renders the demo page with NO visible fidelity change versus pre-Phase-3 behavior: layout, styles, images, and live mutation tracking all look identical on this benign content (CONTEXT: "the demo runs the full security pipeline by default with no visible fidelity change")
    4. Open DevTools on the demo page and run in the page console:
       document.body.insertAdjacentHTML('beforeend', '&lt;button onclick="alert(1)" style="position:fixed;bottom:8px;right:8px"&gt;hostile&lt;/button&gt;&lt;a href="javascript:alert(2)"&gt;bad link&lt;/a&gt;')
       — confirm the button and link APPEAR in the mirror (fidelity) but inspecting the mirror iframe shows no onclick attribute and an empty/neutralized href (sanitization), and clicking them inside the mirror does nothing
    5. Inspect the mirror iframe element: sandbox attribute is exactly "allow-same-origin"; its document head contains the Content-Security-Policy meta
  </how-to-verify>
  <resume-signal>Type "approved" if the demo is visually identical on benign content and hostile injections arrive neutralized; otherwise describe what differs</resume-signal>
  <done>Full suite green and the human confirms: benign fidelity unchanged, hostile injections neutralized in the mirror, sandbox token exactly allow-same-origin, CSP meta present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future contributors -> security pipeline | The biggest long-term threat is drift: a new serialization path, a new innerHTML sink, or a weakened sandbox landing without review |
| host integrators -> embed surface | Hosts can misconfigure embedding in ways the library cannot prevent — the contract document is the mitigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-22 | Tampering | future serialization path bypassing sanitizeForWire (Pitfall 1) | mitigate | static-scan coverage counting + inventory-comment ground truth; fails CI |
| T-03-23 | EoP | allow-scripts or a new innerHTML sink creeping into src/renderer | mitigate | forbidden-token scan + per-file innerHTML allowlist |
| T-03-24 | EoP | host weakening the embed (adding sandbox tokens, rendering wire HTML outside the viewer) | transfer | docs/SECURITY.md host must-nevers — host responsibility, explicitly documented (ASVS V4 partial) |
| T-03-25 | Repudiation | SECURITY.md rotting into an empty stub | mitigate | purity test asserts required content markers, not just existence |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase |
</threat_model>

<verification>
- `node --test tests/security-chokepoint-purity.test.js` green
- `npm test` green — the full phase gate
- Human checkpoint: loopback demo fidelity + live hostile-injection neutralization + sandbox/CSP inspection
</verification>

<success_criteria>
- Success criterion 3 fully closed: sandboxed no-allow-scripts rendering asserted at startup (pre-existing, now purity-scanned), embed security contract documented
- Chokepoint bypass, sink drift, and contract rot all fail CI
- The demo dogfoods the always-on pipeline with no visible fidelity change on benign content (human-verified)
</success_criteria>

<output>
Create `.planning/phases/03-security-pipeline-sanitization-privacy-masking/03-05-SUMMARY.md` when done.
</output>
