---
phase: 03-security-pipeline-sanitization-privacy-masking
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-03"]
files_modified:
  - tests/differential/fixtures/sanitize-corpus.html
  - tests/differential/scenarios/sanitize-divergence.js
  - tests/differential/divergence-ledger.js
  - tests/differential/oracle.test.js
autonomous: true
requirements: [SEC-01, SEC-03]
must_haves:
  truths:
    - "The capture-side sanitization + always-on password mask divergence from the reference is a DECLARED, scenario-pinned ledger entry (D7) — not an excuse hole"
    - "The fixture + scenario actually exhibit the divergence: the reference wire CARRIES hostile content and the password plaintext, the extracted wire LACKS them (belt-and-braces direction checks)"
    - "D7 is load-bearing: the same stream pair with an empty ledger throws UNDECLARED DIVERGENCE"
    - "D7 stays scoped: every other scenario still compares with zero ledger consultations; stale-entry detection passes (D7 matches every run)"
    - "Ledger entry + fixture + scenario land TOGETHER (execution note: stale-entry detection at oracle.test.js:395-407 fails CI otherwise)"
  artifacts:
    - path: "tests/differential/fixtures/sanitize-corpus.html"
      provides: "Frozen hostile-content fixture: on* attrs, javascript: URLs, object/embed, srcdoc iframe, hostile CSS, password input, id-addressed mutation targets"
    - path: "tests/differential/scenarios/sanitize-divergence.js"
      provides: "Scenario driving post-snapshot hostile mutations so the attr-op divergence is exercised, not just the snapshot path"
      exports: ["name", "run"]
    - path: "tests/differential/divergence-ledger.js"
      provides: "D7-capture-sanitization mismatch entry with scenario-guard-first predicate (D6 discipline)"
      contains: "D7"
    - path: "tests/differential/oracle.test.js"
      provides: "MATRIX entry + per-scenario D7 branch + zero-consultation exclusion + load-bearing empty-ledger test"
  key_links:
    - from: "tests/differential/oracle.test.js MATRIX"
      to: "sanitize-corpus.html + sanitize-divergence scenario"
      via: "matrix pair with config {}"
      pattern: "sanitize-corpus\\.html"
    - from: "divergence-ledger.js D7 appliesTo"
      to: "scenarioName === 'sanitize-divergence'"
      via: "load-bearing scenario guard FIRST (D1/D6 discipline)"
      pattern: "sanitize-divergence"
---

<objective>
Make the deliberate capture-side divergence honest in the differential oracle: the reference passes raw hostile content where the extracted core strips it (SEC-01) and masks password values (SEC-03 always-on). Land the three pieces TOGETHER — fixture, scenario, ledger entry — plus the oracle.test.js wiring (MATRIX, per-scenario assertion branch, zero-consultation exclusion at lines ~300-308, load-bearing empty-ledger test), exactly the D6 discipline.

Purpose: oracle discipline is a CONTEXT locked decision ("Ledger it like D6 — tightly scoped, scenario-pinned, load-bearing"); without the corpus fixture the new entry never matches and stale-entry detection fails CI (03-RESEARCH.md Pitfall 7).
Output: oracle green with D7 declared, exhibited, scoped, and load-bearing; full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-security-pipeline-sanitization-privacy-masking/03-CONTEXT.md
@.planning/phases/03-security-pipeline-sanitization-privacy-masking/03-RESEARCH.md
@.planning/phases/03-security-pipeline-sanitization-privacy-masking/03-PATTERNS.md
@tests/differential/divergence-ledger.js
@tests/differential/scenarios/text-childlist.js
</context>

<interfaces>
Scenario module contract (from tests/differential/scenarios/text-childlist.js — copy exactly):

```javascript
export const name = 'sanitize-divergence';
export async function run(side, settle) { /* side = { window, document }; drive mutations; await settle(side.window) */ }
```

Ledger entry contract (from divergence-ledger.js D6, lines 147-195):

```javascript
{ id, kind: 'mismatch', description, rationale, affectedMessages, affectedScenarios,
  appliesTo(refMsg, extMsg, scenarioName) }  // scenario guard FIRST, exact-shape predicate
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: sanitize-corpus.html fixture + sanitize-divergence.js scenario</name>
  <files>tests/differential/fixtures/sanitize-corpus.html, tests/differential/scenarios/sanitize-divergence.js</files>
  <read_first>
    - tests/differential/fixtures/basic.html in full (the fixture shape: full document, id-addressed elements, head inline style, a form with an input)
    - tests/differential/scenarios/text-childlist.js in full (29 lines — the module contract and header-comment discipline: explain why THIS scenario keeps the ledger entry real)
    - tests/differential/harness.js lines 50-110 (buildFixtureDom: fixtures load with runScripts 'outside-only' by default — inline handlers in markup are inert in the harness, safe to include)
    - .planning/phases/03-security-pipeline-sanitization-privacy-masking/03-RESEARCH.md "Code Examples" mXSS fixture rows and Pitfall 5 (the attr-op row must be a post-snapshot mutation, not markup)
  </read_first>
  <action>
    Create tests/differential/fixtures/sanitize-corpus.html modeled on basic.html (full document, lang/charset/title, head inline style, id-addressed body elements) with BOTH benign structure (headings, paragraphs, a list — so the streams stay structurally comparable) and the hostile rows the divergence needs: a button with onclick="alert(1)", an anchor with href="javascript:alert(1)", an iframe with a srcdoc attribute, an object element with data="javascript:alert(1)" and an embed element, an svg anchor with xlink:href="javascript:alert(1)", a div with style="background:url(javascript:alert(1));width:expression(alert(1))", a head style tag containing url(javascript:...) plus a non-http @import, an input with type="password" value="hunter2" (the SEC-03 always-on mask divergence), and two id-addressed mutation targets (id="tgt", id="tgt2") with benign initial state. Create tests/differential/scenarios/sanitize-divergence.js with export const name = 'sanitize-divergence' and an async run(side, settle) that drives the POST-SNAPSHOT divergence rows: document.getElementById('tgt').setAttribute('onclick', 'alert(2)') (reference emits the attr op, extracted drops it — Pitfall 5), document.getElementById('tgt').setAttribute('href', 'javascript:alert(2)') (reference raw value, extracted neutralizes to ''), and one BENIGN mutation (e.g. tgt2.setAttribute('class', 'after')) proving the comparator still aligns non-divergent ops; await settle(side.window) once after the writes (single rAF batch keeps the op-batch shapes deterministic on both sides). Header comment per the text-childlist discipline: this scenario is what keeps ledger entry D7 real and pinned — without it the entry goes stale and CI fails.
  </action>
  <verify>
    <automated>node --test tests/differential/oracle.test.js && test -f tests/differential/fixtures/sanitize-corpus.html && test -f tests/differential/scenarios/sanitize-divergence.js</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; the fixture contains all hostile rows: `grep -c "onclick=\|javascript:\|srcdoc=\|<object\|<embed\|xlink:href\|expression(\|type=\"password\"" tests/differential/fixtures/sanitize-corpus.html` returns >= 8
    - The scenario exports name 'sanitize-divergence' and an async run driving at least one on*-attr mutation and one javascript:-URL mutation post-snapshot
    - The fixture has id="tgt" and id="tgt2" targets the scenario addresses
    - oracle.test.js still passes at this point (the fixture/scenario are not yet in the MATRIX — Task 2 wires them; running the suite proves nothing broke)
  </acceptance_criteria>
  <done>The load-bearing fixture and scenario exist with both snapshot-shape and attr-op-shape divergence rows plus benign comparability anchors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D7 ledger entry + oracle.test.js wiring (MATRIX, scenario branch, exclusion, load-bearing test)</name>
  <files>tests/differential/divergence-ledger.js, tests/differential/oracle.test.js</files>
  <read_first>
    - tests/differential/divergence-ledger.js D6 entry in full (lines 147-195 — the discipline to copy: scenario guard FIRST with the load-bearing comment, exact-shape predicate, description citing reference behavior, rationale citing the phase decision)
    - tests/differential/oracle.test.js: scenario import block (27-34), MATRIX (43-55), the flipped-loop per-scenario branches (268-308 — the text-childlist branch with belt-and-braces direction checks AND the else-branch zero-consultation assertion whose message currently reads "no ledger consultation outside pause-resume/text-childlist"), the load-bearing empty-ledger tests (~340-371), stale-entry detection (395-407 — runs LAST, consumes matchedMismatchIds)
    - .planning/phases/03-security-pipeline-sanitization-privacy-masking/03-PATTERNS.md sections "tests/differential/divergence-ledger.js" and "tests/differential/oracle.test.js" (the four touch points and the predicate-shape note: SAME-INDEX content mismatches, not D6's trailing-message shape)
  </read_first>
  <behavior>
    - Test (matrix run): the sanitize-divergence pair compares green WITH the ledger and matched.has('D7-capture-sanitization') is asserted in a dedicated per-scenario branch
    - Test (direction, belt-and-braces): the REFERENCE stream really carries the hostile content (its SNAPSHOT html matches /onclick|javascript:|<object|srcdoc|hunter2/i and its MUTATIONS ops include an on*-named attr op) while the EXTRACTED stream lacks all of them and carries the href attr op with val ''
    - Test (scoping): every other scenario still hits the zero-consultation else-branch — its exclusion list now reads pause-resume/text-childlist/sanitize-divergence
    - Test (load-bearing): sanitize-divergence with an EMPTY ledger throws UNDECLARED DIVERGENCE (mirrors the D6 test at ~360-371)
    - Test (stale-entry): the final stale-entry test passes — D7 matched at least one real divergence during the run
  </behavior>
  <action>
    Add the D7 entry to DIVERGENCES in divergence-ledger.js: id 'D7-capture-sanitization', kind 'mismatch', affectedMessages [STREAM.SNAPSHOT, STREAM.MUTATIONS], affectedScenarios ['sanitize-divergence']; description states the reference serializes raw on*/javascript:/srcdoc/object/embed content and raw password values where the extracted core's sanitizeForWire chokepoint strips/neutralizes/masks (cite src/capture/index.js sanitizeForWire and the CONTEXT "always-on, no opt-out" decision in the rationale, like D6 cites its phase decision); appliesTo(refMsg, extMsg, scenarioName) with the scenario guard FIRST and its load-bearing comment (a sanitization divergence surfacing in any OTHER scenario must hard-fail as UNDECLARED DIVERGENCE), then the exact shape — unlike D6's extracted-only-trailing-message shape, D7 is a SAME-INDEX content mismatch: both messages defined AND same type AND either (type SNAPSHOT and the ref payload html matches the hostile marker regex /on\w+\s*=|javascript:|<object|<embed|srcdoc=|expression\(/i or contains the password plaintext while the ext html does not) or (type MUTATIONS and the ref batch contains an attr op whose attr matches /^on/i or whose val has a dangerous scheme, while the ext batch lacks that op or carries it neutralized). In oracle.test.js make the four touch-point edits: (1) import * as sanitizeDivergence and add the MATRIX entry { fixture: 'sanitize-corpus.html', scenario: sanitizeDivergence, config: {} } (the Mode-1 ref-vs-ref loop picks it up automatically and compares clean — both sides raw); (2) add the per-scenario branch (else-if on entry.scenario.name === 'sanitize-divergence') asserting matched.has('D7-capture-sanitization'), matched.size === 1, plus the belt-and-braces direction checks from the behavior list (filter refStream/extStream SNAPSHOT html and MUTATIONS ops both ways); (3) extend the else-branch zero-consultation exclusion and its assertion message to include 'sanitize-divergence' (execution note: lines ~300-308); (4) add the load-bearing empty-ledger test mirroring the D6 one at ~360-371 ('sanitize-divergence with an EMPTY ledger throws UNDECLARED DIVERGENCE — D7 is load-bearing, not decorative') placed BEFORE the final stale-entry test (node:test preserves in-file definition order; the stale-entry test must stay last). No change to stale-entry detection itself — it enforces D7 automatically once the entry exists.
  </action>
  <verify>
    <automated>node --test tests/differential/oracle.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/differential/oracle.test.js` exits 0 — including the stale-entry test (D7 matched) and the new empty-ledger throw test
    - `grep -c "D7-capture-sanitization" tests/differential/divergence-ledger.js tests/differential/oracle.test.js` returns >= 1 in EACH file
    - The D7 appliesTo predicate's first executable check is the scenarioName guard: `grep -A 3 "appliesTo" tests/differential/divergence-ledger.js | grep -c "sanitize-divergence"` returns >= 1
    - The zero-consultation assertion message names all three excluded scenarios
    - `npm test` exits 0 — the full suite including every pre-existing oracle scenario stays green
  </acceptance_criteria>
  <done>D7 is declared, exhibited by the corpus scenario, direction-verified both ways, scoped to its scenario, load-bearing against an empty ledger, and passing stale-entry detection; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference behavior -> extracted behavior | The oracle is the integrity mechanism proving the ONLY behavior differences are the declared security strips/masks — an undeclared divergence is either a regression or an unaudited security gap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-19 | Repudiation | sanitization divergence hidden by an over-broad ledger predicate ("any mismatch in this scenario") | mitigate | exact-shape predicate (hostile-marker direction checks inside appliesTo) + belt-and-braces stream assertions in the scenario branch |
| T-03-20 | Tampering | a FUTURE undeclared capture-behavior change excused by D7 | mitigate | scenario guard FIRST (D1/D6 discipline) + zero-consultation assertion for all other scenarios + load-bearing empty-ledger test |
| T-03-21 | Repudiation | stale D7 entry silently rotting after a fixture edit | mitigate | existing stale-entry detection (oracle.test.js:395-407) fails CI when D7 stops matching — the reason the triple lands together |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase |
</threat_model>

<verification>
- `node --test tests/differential/oracle.test.js` green: matrix pair, ref-vs-ref mode, D7 branch, empty-ledger throw, stale-entry detection
- `npm test` green — full suite
- Direction checks prove the divergence is the security fix (extracted lacks hostile content), never a reference-behavior accident
</verification>

<success_criteria>
- The CONTEXT oracle-discipline decision is satisfied: sanitization + always-on password masking are ledgered like D6 — tightly scoped, scenario-pinned, load-bearing
- The mXSS corpus has a frozen differential fixture exercising snapshot-shape AND attr-op-shape divergence
- Stale-entry detection and zero-consultation discipline both enforce D7's honesty permanently in CI
</success_criteria>

<output>
Create `.planning/phases/03-security-pipeline-sanitization-privacy-masking/03-04-SUMMARY.md` when done.
</output>
