---
phase: 03-security-pipeline-sanitization-privacy-masking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/capture/index.js
  - tests/security-sanitize-capture.test.js
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - "No on* event-handler attribute ever appears in any wire payload (snapshot html, add-op html, attr ops) emitted by the extracted capture"
    - "javascript:/vbscript:/data:text/html URL values are neutralized in every URL-carrying attribute on every serialization path, including whitespace-obfuscated forms"
    - "srcdoc attributes are dropped, <object>/<embed> subtrees are dropped, and <script>/<noscript> stripping is preserved — all through the one named chokepoint sanitizeForWire"
    - "Inline CSS (head styles, style attribute values, attr-op style values) is value-scrubbed: dangerous url() schemes, expression(), -moz-binding, non-http(s) @import, </style breakout"
    - "Benign content passes through byte-identical — the existing 130-test suite and differential oracle stay green"
    - "The live observed page is never mutated by sanitization — strips happen only on detached clones / wire values"
    - "Every strip increments a named counter and surfaces through the logger — never silent"
  artifacts:
    - path: "src/capture/index.js"
      provides: "sanitizeForWire chokepoint (element/subtree/attr/text/css dispatch) + hasDangerousScheme + scrubCssText helpers + sanitization counters + five-path serialization inventory comment"
      contains: "function sanitizeForWire"
    - path: "tests/security-sanitize-capture.test.js"
      provides: "Capture-side mXSS/injection corpus (>= 12 vectors) covering snapshot, add-op, and attr-op shapes plus benign-fidelity pins"
      min_lines: 200
  key_links:
    - from: "src/capture/index.js processMutationBatch attr branch"
      to: "sanitizeForWire"
      via: "attr-op values routed through the chokepoint before diffs.push"
      pattern: "sanitizeForWire\\('attr'"
    - from: "src/capture/index.js processAddedNode"
      to: "sanitizeForWire"
      via: "wire clone scrubbed before outerHTML serialization"
      pattern: "sanitizeForWire\\('subtree'"
    - from: "src/capture/index.js serializeDOM"
      to: "sanitizeForWire"
      via: "per-element clone scrub inside the pairs walk + head inline-style css scrub"
      pattern: "sanitizeForWire\\('element'|sanitizeForWire\\('css'"
---

<objective>
Land the capture-side sanitization chokepoint (SEC-01): one named inner function `sanitizeForWire` inside `createCapture` through which all five serialization paths emit — snapshot clone walk, `processAddedNode` add-op subtrees, the attr-op branch, the characterData text branch, and the E2 text-childlist branch. Strips `on*` handler attributes, dangerous URL schemes (`javascript:`/`vbscript:`/`data:text/html`), `srcdoc` attributes; drops `<object>`/`<embed>` subtrees; scrubs CSS values. Blocklist policy, fidelity-first (CONTEXT locked decision: allowlist explicitly rejected). The `text` dispatch shape is created here as an identity hook and the attr-op `value` attribute passes through with a marked seam comment; plan 03-03 fills both with masking.

Purpose: capture is the only side that touches host content; this chokepoint is the primary line guaranteeing hostile markup never reaches the wire (success criterion 1).
Output: `sanitizeForWire` + helpers in src/capture/index.js, the capture-side mXSS test corpus, full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-security-pipeline-sanitization-privacy-masking/03-CONTEXT.md
@.planning/phases/03-security-pipeline-sanitization-privacy-masking/03-RESEARCH.md
@.planning/phases/03-security-pipeline-sanitization-privacy-masking/03-PATTERNS.md
@src/protocol/messages.js
</context>

<interfaces>
Contracts this plan DEFINES (consumed by plans 03-03 and 03-05):

```javascript
// Inner function of createCapture (closure access to cfg, logger, counters).
// Single name, multiple shapes via dispatch tag (03-RESEARCH.md Pattern 3):
//   sanitizeForWire('element',  { orig, clone })          -> { drop?: true }   // snapshot pairs walk
//   sanitizeForWire('subtree',  { root, liveRoot })       -> void (mutates root) // processAddedNode wire clone
//   sanitizeForWire('attr',     { name, value, target })  -> { drop?: true, value?: string }
//   sanitizeForWire('text',     { text, owner })          -> { text }          // IDENTITY here; 03-03 fills masking
//   sanitizeForWire('css',      { css })                  -> { css }
//
// Pure helpers (module-scope, absolutifyUrl shape: string in, string out,
// guarded, exception-to-identity):
//   hasDangerousScheme(value)  // true for javascript:/vbscript:/data:text/html,
//                              // after stripping chars <= 0x20 from the prefix
//   scrubCssText(css)          // url() scheme scrub + expression() + -moz-binding
//                              // + non-http(s) @import + </style breakout
//
// Closure counters (declared this plan; masked* incremented by plan 03-03):
//   sanitizeCounters = { strippedHandlers, blockedUrlSchemes, blockedSubtrees,
//                        cssScrubs, maskedTextNodes, maskedInputs }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sanitizeForWire chokepoint + snapshot-path coverage (clone walk, processAddedNode, head styles)</name>
  <files>src/capture/index.js, tests/security-sanitize-capture.test.js</files>
  <read_first>
    - src/capture/index.js in full ONCE (1333 lines) — extract in one pass: header comment lines 25-35 (stale Phase-3 deferral notes to update), URL_ATTRS at line 56, safeSkipElement at 286-293 (containment shape), absolutifyUrl at 482-491 (pure-helper shape + the javascript:-passthrough comment to update), serializeDOM pairs walk at 595-735 (script/noscript toRemove at ~626, safeSkipElement/skipElementWithAncestors at 636-646, canvas swap at 666-682, URL absolutify at 684-707, head styles collection at 728-735), processAddedNode at 846-873 (NOTE: it mutates the LIVE node — nid + absolutify — reference parity), staleFlushCount counter pattern at 344/1039/1095, beginStreamSession (counter-reset site)
    - .planning/phases/03-security-pipeline-sanitization-privacy-masking/03-RESEARCH.md sections "Pattern 3" (chokepoint shape), "Don't Hand-Roll" (scheme + CSS regex recipes), "Code Examples" (mXSS fixture rows), Pitfalls 4, 5, 9
    - .planning/phases/03-security-pipeline-sanitization-privacy-masking/03-PATTERNS.md section "src/capture/index.js" (serialization-path table with line anchors, counter pattern, inventory-comment discipline)
    - tests/capture-skip.test.js lines 30-150 (env recipe to duplicate locally: AUDITED_GLOBALS, setupEnv, settle, createLoopbackTransport, silentLogger, try/finally teardown; wire assertions filtering STREAM.SNAPSHOT)
  </read_first>
  <behavior>
    - Test: snapshot of a body containing &lt;button onclick="alert(1)"&gt; emits SNAPSHOT html with no /onclick/i match; the LIVE document's button still has its onclick attribute after start() (sanitization never mutates the observed page)
    - Test: href="javascript:alert(1)" and the whitespace-obfuscated form "jav&#92;tascript:alert(1)" are both neutralized to "" in SNAPSHOT html; a benign href="https://x.test/a" passes through unchanged after absolutification
    - Test: vbscript: and data:text/html URL values are neutralized; data:image/png values pass through
    - Test: the namespace-confusion payload (form/math/mtext/mglyph/style + img onerror, 03-RESEARCH.md Code Examples) emits no /onerror/i on the wire
    - Test: &lt;iframe srcdoc="..."&gt; emits with the srcdoc attribute absent; &lt;object&gt; and &lt;embed&gt; subtrees are absent from SNAPSHOT html entirely
    - Test: formaction="javascript:..." on a button and xlink:href="javascript:..." inside svg are neutralized
    - Test: inline style="background:url(javascript:alert(1))" and style="width:expression(alert(1))" and style="-moz-binding:url(evil.xml#x)" are scrubbed in SNAPSHOT html; a benign computed-style attribute value passes through byte-identical (fidelity pin)
    - Test: a head &lt;style&gt; containing url(javascript:...), a non-http @import, and a literal &lt;/style&gt; breakout emits in payload.inlineStyles scrubbed (no /url\(\s*javascript/i, no raw "&lt;/style" sequence); a benign head style with relative url(/img/x.png) and url(https://...) passes through unchanged
    - Test: a post-snapshot appendChild of a hostile subtree (div containing a child with onclick and a javascript: href) emits an add op whose html carries neither; the LIVE added node retains its onclick attribute
    - Test: when strips occur, the recording logger received at least one '[DOM Stream]' warn whose payload object includes nonzero counter values; a fully benign snapshot logs no sanitization warn
  </behavior>
  <action>
    In src/capture/index.js, add the chokepoint per the interfaces block. Module-scope pure helpers (absolutifyUrl shape, per CONTEXT blocklist decision): hasDangerousScheme(value) strips all chars with code &lt;= 0x20 from the value before case-insensitively testing the prefix against javascript:, vbscript:, and data:text/html (data: with any non-image MIME of text/html form; data:image/* is allowed — keep the check centralized, one place to add schemes); scrubCssText(css) applies the targeted value scrub: url() whose explicit scheme is not http/https/data:image* gets its contents replaced with about:blank (NO-scheme url() values — relative paths, fragments — are ALLOWED: they resolve against the document base, which preserves fidelity per the CONTEXT fidelity-first decision; document this interpretation in a comment), strips expression(...) occurrences, strips -moz-binding declarations, neutralizes @import whose target is not http(s), and neutralizes the &lt;/style breakout by rewriting the sequence "&lt;/style" case-insensitively to "&lt;&#92;/style" (CSS string-escape preserving evaluated string values). Inside createCapture, declare the sanitizeCounters closure object with all six keys (strippedHandlers, blockedUrlSchemes, blockedSubtrees, cssScrubs, maskedTextNodes, maskedInputs — the masked* pair is incremented by plan 03-03; say so in the declaration comment), reset in beginStreamSession() with a comment documenting the per-session lifecycle. Define function sanitizeForWire(kind, payload) dispatching on kind: 'element' scrubs one clone element in place (enumerate clone.attributes collecting names FIRST then removing — live NamedNodeMap; remove any attr whose lowercased name starts with 'on' via name.indexOf('on') === 0, the serializeShellAttributes precedent; remove srcdoc; for URL_ATTRS entries plus formaction plus xlink:href (getAttributeNS per the line-696 precedent) set value to '' when hasDangerousScheme; for srcset, scheme-check each candidate URL and neutralize offending candidates; scrub the style attribute value through scrubCssText) and returns { drop: true } for object/embed tag names; 'subtree' walks payload.root plus root.querySelectorAll('*') applying the element scrub to each and REMOVING object/noscript/script/embed descendants from the detached clone; 'css' returns { css: scrubCssText(payload.css) }; 'text' is an identity hook returning { text: payload.text } with a comment naming it the SEC-03 masking seam (plan 03-03) and the Phase 8 CAPT-05 seam. Thread the snapshot path: in the serializeDOM pairs walk, extend the script/noscript toRemove check to also catch object/embed via a sanitizeForWire('element', ...) call whose { drop: true } return pushes cl onto toRemove and increments blockedSubtrees (decision logic lives INSIDE the chokepoint, the loop only routes); call the element scrub after absolutifyUrl/captureComputedStyles so the scheme check runs on final wire values. Route each head inline style through sanitizeForWire('css', ...) at the line-728-735 collection site. In processAddedNode, PRESERVE the live-node nid stamping and absolutification exactly as-is (reference parity; the observed page must keep its event handlers — stripping the live node would change page behavior), then build the wire HTML from el.cloneNode(true): call sanitizeForWire('subtree', { root: wireClone, liveRoot: el }) and return wireClone.outerHTML — never el.outerHTML directly. Counting + logging: every strip increments its counter; emit ONE structured logger.warn('[DOM Stream] sanitization strips', {...counter snapshot...}) per serializeDOM call and per flushMutations call when any counter moved during that pass (aggregate, not per-strip spam — counted + logged, never silent, per CONTEXT). Update the stale header comment at lines 30-35 ("javascript: URLs pass through absolutifyUrl (Phase 3 SEC-01 introduces...)") and the absolutifyUrl JSDoc note to state the chokepoint now exists and where. Write tests/security-sanitize-capture.test.js FIRST per the behavior list, duplicating the capture-skip.test.js env recipe locally (deliberate parallel-safe duplication, stated in that file's header); use a recording logger (not silentLogger) for the counter-warn assertions.
  </action>
  <verify>
    <automated>node --test tests/security-sanitize-capture.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/security-sanitize-capture.test.js` exits 0 with at least 10 passing tests
    - `grep -c "function sanitizeForWire" src/capture/index.js` returns 1 (one named chokepoint, not several)
    - `grep -c "sanitizeForWire('element'\|sanitizeForWire('subtree'\|sanitizeForWire('css'" src/capture/index.js` returns >= 3
    - `grep -v '^\s*//' src/capture/index.js | grep -c "el.outerHTML"` returns 0 (processAddedNode serializes the scrubbed wire clone, never the live node)
    - hasDangerousScheme strips control/whitespace chars before matching: the test corpus includes a tab-obfuscated javascript: vector and it passes
    - `npm test` exits 0 — the existing 130-test suite and differential oracle stay green (existing fixtures are sanitization-quiet; verified in 03-RESEARCH.md Pitfall 7)
  </acceptance_criteria>
  <done>Snapshot and add-op serialization emit only sanitized markup through the named chokepoint; live page untouched; benign fidelity pinned; full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Differ-path coverage (attr-op, text branches) + five-path inventory comment</name>
  <files>src/capture/index.js, tests/security-sanitize-capture.test.js</files>
  <read_first>
    - src/capture/index.js processMutationBatch lines 880-1020 (attr-op branch at 987-1005 with the absolutifyUrl/absolutifySrcset threading to mirror; characterData at 1006-1016; E2 text-childlist emission at ~974-986; the existing `if (!targetNid) continue` guards)
    - src/capture/index.js dialog relay at 440-470 and overlay payload at 1240-1250 (side channels — text-only payloads rendered via textContent in the viewer, overlays.js T-02-04; these are documented in the inventory comment, not routed through the markup scrub)
    - src/renderer/snapshot.js lines 18-28 (the WR-03 wire-value insertion-point inventory comment — the discipline to mirror on the capture side)
    - .planning/phases/03-security-pipeline-sanitization-privacy-masking/03-RESEARCH.md Pitfall 5 (attr-op bypass) and Pitfall 1 (chokepoint-bypass drift)
  </read_first>
  <behavior>
    - Test: post-snapshot el.setAttribute('onclick', 'alert(1)') emits NO attr op on the wire (the op is dropped, not value-neutralized) and strippedHandlers increments (Pitfall 5)
    - Test: post-snapshot el.setAttribute('srcdoc', ...) on an iframe emits no attr op
    - Test: post-snapshot el.setAttribute('href', 'javascript:alert(1)') emits an attr op whose val is '' (attr existence preserved for mirror parity, value neutralized)
    - Test: post-snapshot el.setAttribute('style', 'width:expression(alert(1))') emits an attr op whose val has no /expression\(/i match (A2: style attr mutations flow through the attr-op branch — explicit fixture row)
    - Test: a benign attr mutation (class change) emits an attr op with the exact unmodified value (fidelity pin)
    - Test: a characterData edit and a textContent= (E2 childlist) edit both emit text ops with UNCHANGED text (identity pin — plan 03-03 flips this for masked elements)
    - Test: a hostile added subtree arriving via the differ (appendChild) emits add-op html with no on* and no javascript: (routes through Task 1's subtree scrub via processAddedNode)
  </behavior>
  <action>
    In processMutationBatch, route the attr-op branch through sanitizeForWire('attr', { name: m.attributeName, value: attrVal, target: m.target }) AFTER the existing absolutifyUrl/absolutifySrcset threading (scheme check runs on final wire values): a lowercased name starting with 'on' returns { drop: true } (continue — no diffs.push) and increments strippedHandlers; name 'srcdoc' returns { drop: true }; URL_ATTRS/formaction values failing hasDangerousScheme return { value: '' } and increment blockedUrlSchemes; srcset values get per-candidate scheme neutralization; name 'style' returns { value: scrubCssText(value) } incrementing cssScrubs when changed; the 'value' attribute passes through unchanged with a comment marking it the plan 03-03 maskInputs seam; everything else passes through unchanged. Route the characterData branch and the E2 text-childlist emission through sanitizeForWire('text', { text: ..., owner: ... }) — owner is m.target.parentElement for characterData and m.target for E2 — identity transform in this plan (the masking hook). Add the serialization-path inventory comment block immediately above sanitizeForWire mirroring the WR-03 discipline (renderer snapshot.js:18-28): enumerate (1) serializeDOM clone walk, (2) processAddedNode add-op subtrees, (3) attr-op branch, (4) characterData text branch, (5) E2 text-childlist branch — each annotated with its sanitizeForWire dispatch kind — plus a note that the dialog (lines ~449-468) and overlay (~1249) side channels carry text/metadata only and are rendered via textContent in the viewer (overlays.js T-02-04), so they bypass the markup scrub by design; this comment is the ground truth the plan 03-05 purity static-scan audits (Pitfall 1). Extend tests/security-sanitize-capture.test.js with the behavior rows above (mutation tests follow the capture-skip.test.js mutate -&gt; settle -&gt; filter STREAM.MUTATIONS recipe).
  </action>
  <verify>
    <automated>node --test tests/security-sanitize-capture.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/security-sanitize-capture.test.js` exits 0 with at least 16 passing tests total (Task 1 rows + these)
    - `grep -c "sanitizeForWire('attr'" src/capture/index.js` returns 1 and `grep -c "sanitizeForWire('text'" src/capture/index.js` returns 2 (characterData + E2)
    - The inventory comment exists: `grep -c "serialization-path inventory\|Serialization-path inventory" src/capture/index.js` returns >= 1
    - A wire-level scan in the test proves no on*-named attr op ever reaches transport.sent for the hostile mutation rows
    - `npm test` exits 0 — full suite green including the differential oracle (sanitization-quiet fixtures)
  </acceptance_criteria>
  <done>All five serialization paths route through sanitizeForWire; attr-op injection is closed; text branches expose the masking seam; the inventory comment documents the chokepoint's ground truth; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| observed page -> capture serializer | All snapshot/diff content originates from an attacker-influenced page |
| capture -> wire | Everything emitted here is later parsed in the viewer; this plan is the primary neutralization layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering/EoP | inline on* handlers in snapshot/add-op/attr-op | mitigate | sanitizeForWire strips on* on all five paths; attr ops dropped entirely (Pitfall 5); residual: render chokepoint (03-02) + sandbox no-allow-scripts |
| T-03-02 | EoP | javascript:/vbscript:/data:text/html URLs incl. whitespace obfuscation | mitigate | hasDangerousScheme with control-char stripping, applied post-absolutification on URL_ATTRS + formaction + xlink:href + srcset candidates; residual: CSP default-src 'none' |
| T-03-03 | Tampering | namespace-confusion mXSS (math/mglyph, svg) | mitigate | attribute enumeration on EVERY element in the walk regardless of namespace (Pitfall 4); residual: render-side DOM-fragment scrub (03-02) closes the serialize-reparse class |
| T-03-04 | EoP | nested iframe srcdoc / object / embed | mitigate | srcdoc attr dropped, object/embed subtrees dropped at the chokepoint (CONTEXT decision) |
| T-03-05 | EoP | CSS expression()/-moz-binding/url(javascript:)/@import, </style> breakout | mitigate | scrubCssText on head styles + style attrs + style attr-ops; residual risk (novel CSS vectors): CSP + sandbox backstops, accepted |
| T-03-06 | Tampering | sanitizer mutating the LIVE observed page (behavior change of the host app) | mitigate | processAddedNode scrubs a detached clone only; snapshot scrubs the clone walk only; pinned by live-page-untouched tests |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase (03-RESEARCH.md Package Legitimacy Audit: nothing to gate) |
</threat_model>

<verification>
- `node --test tests/security-sanitize-capture.test.js` green (>= 16 tests)
- `npm test` green — 130 pre-existing tests + oracle unaffected (fixtures sanitization-quiet)
- Wire-stream scans (transport.sent) prove absence of on*/javascript:/srcdoc/object across SNAPSHOT and MUTATIONS payloads for every hostile vector
- Live-document assertions prove the observed page keeps its original attributes
</verification>

<success_criteria>
- One named capture-side chokepoint covers all five serialization paths (success criterion 1), verified by the mXSS fixture suite
- Benign fidelity preserved: existing suite + oracle green, benign vectors byte-identical
- Strips counted + logged, never silent (CONTEXT observability decision)
- The text-hook and attr-op 'value' masking seams exist for plan 03-03
</success_criteria>

<output>
Create `.planning/phases/03-security-pipeline-sanitization-privacy-masking/03-01-SUMMARY.md` when done.
</output>
