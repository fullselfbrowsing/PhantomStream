---
phase: 15-media-security-masking-threat-model-docs
plan: 03
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - tests/security-media.test.js
autonomous: true
requirements: [MSEC-04]

must_haves:
  truths:
    - "An explicit named media-security test asserts src/renderer/media-player.js contains zero allow-scripts outside comments (the sandbox-token-over-media-code invariant, backing the object-URL threat model — independent of the purity test, which Plan 04 owns)"
    - "dependencies is exactly { ws: '8.21.0' } and peerDependencies['hls.js'] stays a version-range string with peerDependenciesMeta.optional===true — re-asserted as a Phase-15 gate (no new deps from masking)"
    - "A named 'media-sync security' traceability assertion confirms a late cross-session STREAM.MEDIA payload (streamSessionId:'STALE', snapshotId:999) is rejected by isCurrentStream with no driver call (cites the shipped behavior at renderer-media.test.js:411)"
    - "The parent-realm object URL is revoked on destroy/destroyAll (cites the shipped media-player revoke coverage) — the docs threat-model claim is test-backed"
    - "The full suite + differential oracle stay green at 48/48 after the Plan-01/02 changes land (no regression, masking off-by-default byte-identical)"
  artifacts:
    - path: "tests/security-media.test.js"
      provides: "NEW media-security traceability suite: media-path allow-scripts assertion, deps byte-unchanged gate, late-cross-session reject named case, object-URL revoke cite"
      contains: "media-player.js"
  key_links:
    - from: "tests/security-media.test.js (allow-scripts media-path)"
      to: "src/renderer/media-player.js"
      via: "readFileSync + stripComments + assert zero /allow-scripts/ matches (sandbox-token invariant over media code)"
      pattern: "allow-scripts"
    - from: "tests/security-media.test.js (deps gate)"
      to: "package.json dependencies/peerDependencies"
      via: "assert.deepEqual dependencies === { ws: '8.21.0' }; peerDependenciesMeta['hls.js'].optional === true"
      pattern: "peerDependenciesMeta"
---

## Phase Goal

**As a** host embedding the PhantomStream viewer to mirror attacker-influenced pages, **I want to** redact, strip, or block the asset/media URLs that leave my page (signed-CDN tokens, sensitive media) and minimize what the viewer-side fetch leaks, **so that** I can mirror real media by reference without exposing CDN credentials, private media timelines, or the mirrored page URL to third-party origins — and never weaken the sandbox.

> Derived from 15-CONTEXT `<decisions>` (the host masking vocabulary) + REQUIREMENTS MSEC-03/MSEC-04; the ROADMAP `**Goal**` line is outcome-shaped, so this user story is grounded in those locked artifacts, not invented. The ROADMAP goal verbatim: "Close the milestone by completing the security contract that was threaded through Phases 12-14 — full asset/media URL masking + referrerpolicy=no-referrer, a threat-review of the parent-realm object-URL blast radius, media-specific security tests, and the SECURITY/ARCHITECTURE documentation updates."
>
> **This plan's slice:** the media-security invariants that back the threat model — the host gets test-proven guarantees: no `allow-scripts` on media code, no new dependency, late cross-session media rejected, object URL revoked on destroy.

<objective>
Pin the milestone's media-security invariants one last time as a dedicated, named traceability suite (MSEC-04,
criteria 3-4) — the assertions that *back* the object-URL threat model and the no-new-deps guarantee, kept
independent of `tests/security-chokepoint-purity.test.js` (which Plan 04 edits for the doc markers). Specifically:
(1) `src/renderer/media-player.js` carries no `allow-scripts` outside comments (the sandbox-token-over-media-code
invariant — the static scan already covers it via the renderer glob; this adds an explicit *named* media-path
assertion for traceability, Pitfall 6 says do NOT edit the glob); (2) `dependencies`/`peerDependencies` are
byte-unchanged after the masking work (no new dep); (3) a named "media-sync security" case confirms a late
cross-session `STREAM.MEDIA` is rejected by `isCurrentStream` (cites the shipped test); (4) the parent-realm object
URL is revoked on destroy/destroyAll (cites the shipped media-player coverage). Then gate the full suite + oracle.

Purpose: The `docs/SECURITY.md` object-URL threat subsection (Plan 04) must be backed by green tests, and the
"zero new deps" milestone promise must hold after masking shipped. This plan makes those claims test-true and
runs the phase's regression gate.

Output: `tests/security-media.test.js` (new, named media-security traceability suite). No production code change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-media-security-masking-threat-model-docs/15-CONTEXT.md
@.planning/phases/15-media-security-masking-threat-model-docs/15-RESEARCH.md
@.planning/phases/15-media-security-masking-threat-model-docs/15-VALIDATION.md

<interfaces>
<!-- All four assertions CITE already-shipped behavior/files. This plan adds the named traceability suite only. -->

allow-scripts static-scan pattern to REUSE — tests/security-chokepoint-purity.test.js:42-54, :110-120
  rendererModules() globs every src/renderer/*.js (INCLUDES media-player.js — verified, Pitfall 6: do NOT edit the glob).
  stripComments() then countMatches(stripped, /allow-scripts/g) === 0.
  Plan 03 reads src/renderer/media-player.js directly and asserts the same, as a NAMED media-path case.

deps-shape guard to CITE/re-assert — tests/package-publish.test.js:56-89
  assert.deepEqual(pkg.dependencies, { ws: '8.21.0' }); typeof pkg.peerDependencies['hls.js'] === 'string';
  pkg.peerDependenciesMeta['hls.js'].optional === true; hls.js absent from dependencies/devDependencies.
  Plan 03 re-asserts this as a Phase-15 gate (the masking work must add no dep).

Late cross-session reject (CITE — already green) — tests/renderer-media.test.js:411-421
  test('handleMedia rejects a payload with mismatched stream identity (no driver call)') with
  { streamSessionId: 'STALE', snapshotId: 999 } -> rec.plays === 0. isCurrentStream — src/protocol/messages.js:338.
  Plan 03 adds a named "media-sync security" assertion exercising the same staleness path (do not duplicate the whole
  fixture if a direct isCurrentStream unit assertion is cleaner — assert isCurrentStream(staleMsg, active) === false).

Object-URL revoke-on-destroy (CITE — already green) — tests/renderer-media-player.test.js (revokeObjectURL/destroyAll)
  The Phase-14 player revokes minted object URLs on destroy/destroyAll. Plan 03 cites/links this as the test backing
  threat-model row #3; add an explicit revoke assertion ONLY if the existing coverage does not already pin it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Named media-security traceability suite — allow-scripts media-path + deps gate + late-cross-session + object-URL revoke</name>
  <files>tests/security-media.test.js</files>
  <read_first>
    - tests/security-chokepoint-purity.test.js:42-54, :110-120 (the rendererModules glob + stripComments + allow-scripts scan to reuse the pattern; Pitfall 6 — do NOT edit this file or its glob)
    - tests/package-publish.test.js:56-89 (the deps/peerDeps shape guard to re-assert)
    - tests/renderer-media.test.js:411-421 (the shipped late-cross-session reject to cite/exercise)
    - tests/renderer-media-player.test.js (the shipped object-URL revoke-on-destroy coverage to cite)
    - src/protocol/messages.js:338 (isCurrentStream — the staleness guard)
    - 15-RESEARCH.md "Parent-Realm Object-URL Threat Model" (rows 1-5; the verification-tasks list at the end),
      Pitfall 6 (allow-scripts scan already covers media-player.js — verify, don't re-add a glob)
  </read_first>
  <action>
    Create tests/security-media.test.js as a named media-security traceability suite (node:test + node:assert/strict; ESM import for messages.js/package.json as the repo does), with four named tests. (1) "media-player.js carries no allow-scripts (sandbox-token invariant over media code)": readFileSync src/renderer/media-player.js, strip comments using the same stripComments approach the purity test uses (inline a minimal comment-stripper or import the shared helper if one is exported), and assert zero /allow-scripts/g matches — an explicit media-path assertion (the purity glob already covers it, Pitfall 6; do NOT edit the purity test or its glob). (2) "no new dependency from media masking (deps byte-unchanged)": read package.json and assert.deepEqual(pkg.dependencies, { ws: '8.21.0' }), assert typeof pkg.peerDependencies['hls.js'] === 'string', assert pkg.peerDependenciesMeta['hls.js'].optional === true, and assert hls.js absent from devDependencies — re-asserting the package-publish guard as a Phase-15 gate. (3) "media-sync security: a late cross-session STREAM.MEDIA is rejected by isCurrentStream (no driver)": exercise the staleness path — assert isCurrentStream({ streamSessionId: 'STALE', snapshotId: 999 }, { streamSessionId: <active>, snapshotId: <active> }) === false (a direct unit assertion against messages.js), citing the integration-level proof at renderer-media.test.js:411 in a comment. (4) "parent-realm object URL is revoked on destroy/destroyAll": cite the shipped renderer-media-player.test.js revoke coverage in a comment, and add an explicit revoke assertion ONLY if that coverage does not already pin URL.revokeObjectURL on destroy — otherwise keep this as a documented cite (no duplicate). No fenced code blocks in this action.
  </action>
  <verify>
    <automated>node --test tests/security-media.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/security-media.test.js` passes with all four named tests green.
    - The allow-scripts media-path assertion reads src/renderer/media-player.js and asserts zero matches outside comments; it does NOT edit tests/security-chokepoint-purity.test.js or the rendererModules glob.
    - The deps gate asserts dependencies === { ws: '8.21.0' } and peerDependenciesMeta['hls.js'].optional === true.
    - The late-cross-session assertion proves isCurrentStream rejects the stale identity (=== false).
  </acceptance_criteria>
  <done>The named media-security traceability suite is green: media-path allow-scripts invariant, deps byte-unchanged, late-cross-session reject, and object-URL revoke are all test-backed for the Plan-04 threat-model docs.</done>
</task>

<task type="auto">
  <name>Task 2: Phase regression gate — full suite + differential oracle stay green at 48/48 after Plan 01/02</name>
  <files>tests/security-media.test.js</files>
  <read_first>
    - 15-VALIDATION.md "Sampling Rate" + "Validation Sign-Off" (full suite per wave; oracle 48/48; deps byte-unchanged)
    - tests/differential/oracle.test.js:1-30 (the ledger-coverage contract — no new entry for off-by-default masking)
  </read_first>
  <action>
    Run the full suite and the differential oracle as the Phase-15 Wave-2 regression gate now that the Plan-01 masking and Plan-02 referrer changes have landed: `npm test` must be green (>=665 baseline + the new Phase-15 tests), and `node --test tests/differential/oracle.test.js` must stay at 48/48 with NO new divergence-ledger entry (masking is off-by-default -> byte-identical wire). Also run `node --test tests/package-publish.test.js` to confirm the published deps shape is byte-unchanged. If the oracle count moved or a ledger entry appeared, STOP and flag it as a byte-identity regression introduced by Plan 01 (Pitfall 1 — a URL.toString() normalization) for that plan to fix; do not patch the ledger here. This task adds no new assertions beyond running the gate (the named suite is Task 1); record the gate result in the SUMMARY.
  </action>
  <verify>
    <automated>node --test tests/differential/oracle.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` full suite green (>=665 + new Phase-15 tests).
    - `node --test tests/differential/oracle.test.js` green at 48/48; `git diff --stat tests/differential/divergence-ledger.js` shows no change.
    - `node --test tests/package-publish.test.js` green (dependencies/peerDependencies byte-unchanged).
  </acceptance_criteria>
  <done>The full suite + differential oracle (48/48, no new ledger entry) + the deps-shape guard are all green after Plan 01/02 — the phase regression gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mirrored content -> host (parent realm) | The sandbox (`allow-same-origin`, never `allow-scripts`) is the boundary; this plan pins that no `allow-scripts` enters the media-player code that runs the parent-realm object URL. |
| prior-session media frame -> active viewer | Stream identity (`isCurrentStream`) rejects late cross-session `STREAM.MEDIA`; this plan names that as a security traceability case. |
| supply chain -> published package | `dependencies`/`peerDependencies` define what a consumer installs; this plan re-asserts they are byte-unchanged after masking. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | Elevation of Privilege (V14) | Sandbox weakened to `allow-scripts` on media code (would let mirrored content read/exfiltrate the parent-realm object URL) | mitigate | Named assertion: `src/renderer/media-player.js` has zero `allow-scripts` outside comments (the purity glob already covers it; this is the explicit media-path traceability case — Pitfall 6, glob unchanged). Backs object-URL threat-model rows 1-2. |
| T-15-11 | Information Disclosure / DoS (V3) | A late cross-session `STREAM.MEDIA` drives the wrong/active element (timeline confusion / replay) | mitigate | `isCurrentStream` rejects mismatched stream identity with no driver call — named "media-sync security" case; cites the shipped integration test at renderer-media.test.js:411. |
| T-15-12 | Information Disclosure / DoS (V12) | A leaked/orphaned parent-realm `blob:` object URL persists across sessions | mitigate | Revoked on destroy/destroyAll (`URL.revokeObjectURL`); destroyAll runs before any new-identity snapshot swap (Phase-14 decision). Test-backed (cite renderer-media-player.test.js); backs threat-model row 3. |
| T-15-13 | Tampering (supply chain) | The masking work silently adds a runtime dependency | mitigate | Re-assert `dependencies === { ws: '8.21.0' }` and `peerDependenciesMeta['hls.js'].optional === true`; masking is pure platform `URL`/`URLSearchParams`. |
| T-15-SC | Tampering | npm/pip/cargo installs in this plan | mitigate | This plan installs NOTHING — it is tests-only over already-present `node:test`/`jsdom`. It actively *asserts* zero new deps. No `[ASSUMED]`/`[SUS]` package. |
</threat_model>

<verification>
- `node --test tests/security-media.test.js` green (4 named traceability tests).
- `npm test` full suite green; `node --test tests/differential/oracle.test.js` at 48/48, no new ledger entry.
- `node --test tests/package-publish.test.js` green (deps byte-unchanged).
</verification>

<success_criteria>
- A named media-security suite backs the object-URL threat model: media-path allow-scripts invariant, late-cross-session reject, object-URL revoke.
- `dependencies`/`peerDependencies` are byte-unchanged (no new dep from masking).
- The full suite + differential oracle stay green at 48/48 with no new ledger entry (off-by-default byte-identity holds end-to-end).
- No production code or purity-test edit in this plan (Plan 04 owns the purity-test marker additions).
</success_criteria>

<output>
Create `.planning/phases/15-media-security-masking-threat-model-docs/15-03-SUMMARY.md` when done
</output>
