---
phase: 01-capture-core-extraction-differential-oracle
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - tests/differential/harness.js
  - tests/differential/normalize.js
  - tests/differential/divergence-ledger.js
  - tests/differential/oracle.test.js
autonomous: true
requirements: [CAPT-04, CAPT-01]
tags: [differential-testing, equivalence, divergence-ledger]

must_haves:
  truths:
    - "The oracle proves reference-vs-EXTRACTED structural op-stream equivalence on the full fixture x scenario matrix"
    - "The reference-vs-reference self-test remains a permanent harness sanity check and runs before the flip tests"
    - "Every intentional divergence is a declared ledger entry; any undeclared divergence fails the suite"
    - "Every mismatch-kind ledger entry matched at least one real divergence (stale-entry detection) — D1 matches only in the pause-resume scenario"
    - "The extracted side emits through a loopback transport — the Transport seam proven end-to-end (phase success criterion 2)"
  artifacts:
    - path: "tests/differential/harness.js"
      provides: "createExtractedSide: second JSDOM + tracked/restored globals + loopback transport + createCapture"
      contains: "createCapture"
    - path: "tests/differential/divergence-ledger.js"
      provides: "Declared divergences D1 (mismatch) + D2/D3/D4/D5 (documented-mapping)"
      contains: "D1-resume-no-resnapshot"
    - path: "tests/differential/oracle.test.js"
      provides: "Dual-mode oracle: ref-vs-ref self-test + ref-vs-extracted matrix + stale-entry detection"
  key_links:
    - from: "tests/differential/harness.js"
      to: "src/capture/index.js"
      via: "ESM import of createCapture for the extracted side"
      pattern: "from '\\.\\./\\.\\./src/capture/index\\.js'"
    - from: "tests/differential/oracle.test.js"
      to: "tests/differential/divergence-ledger.js"
      via: "ledger consulted on every mismatch + final stale-entry assertion"
      pattern: "DIVERGENCES"
---

<objective>
Flip side B of the differential oracle from a second reference instance to the extracted `src/capture/` core, and get reference-vs-extracted GREEN on the full fixture x scenario matrix with every intentional divergence declared in the machine-readable ledger (D-02, D-03). The reference-vs-reference mode stays as a permanent harness self-test. This is the phase's exit bar: parity proven, divergences accounted.

Purpose: this turns "we extracted it" into "we proved it's the same thing" — phase success criterion 1's second half and the wire-compatibility guarantee FSB's swap-in (Phase 11) depends on.
Output: extracted-side harness loader, finalized divergence ledger (D1 mismatch entry + D2/D3/D4/D5 documented mappings), dual-mode oracle green.
</objective>

## Phase Goal

**As a** host developer embedding PhantomStream, **I want to** run the capture core in any injection context through an injected Transport, **so that** I get a capture stream provably equivalent to the shipped FSB reference.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-capture-core-extraction-differential-oracle/01-CONTEXT.md
@.planning/phases/01-capture-core-extraction-differential-oracle/01-RESEARCH.md
@tests/differential/harness.js
@tests/differential/normalize.js
@tests/differential/divergence-ledger.js
@tests/differential/oracle.test.js
@src/capture/index.js
</context>

<interfaces>
From src/capture/index.js (created in Plan 01-03):
```javascript
export function createCapture({ transport, logger, overlayProvider, skipElement });
// -> { start(), stop(), pause(), resume() }
// transport: { send(type, payload), flush?() } — send fire-and-forget
// emits STREAM.READY once at factory creation; STREAM.SNAPSHOT on start()
```

Audited ambient-global set the extracted core dereferences (RESEARCH Pattern 2):
window, document, Node, NodeFilter, MutationObserver, requestAnimationFrame, cancelAnimationFrame, CustomEvent, ShadowRoot, location, getComputedStyle, URL.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extracted-side harness loader + first flipped pair green</name>
  <files>tests/differential/harness.js, tests/differential/normalize.js, tests/differential/oracle.test.js</files>
  <read_first>
    - tests/differential/harness.js (shared config factory, createReferenceSide surface — the extracted side must return the IDENTICAL surface)
    - tests/differential/normalize.js (normalizeReference + canonicalizeIdentity + compareStreams)
    - src/capture/index.js (createCapture signature, READY emission timing, ambient globals it dereferences)
    - .planning/phases/01-capture-core-extraction-differential-oracle/01-RESEARCH.md (Pattern 2: globals from a second JSDOM, tracked + restored; Pitfalls 6, 8, 10)
  </read_first>
  <behavior>
    - Test: reference-vs-EXTRACTED on basic.html x basic-mutations passes compareStreams with zero ledger consultations (no divergence in plain mutation scenarios)
    - Existing ref-vs-ref tests remain green (harness self-test intact)
    - Globals set for the extracted side are restored after each run (no cross-test pollution; protocol tests in the same suite stay green)
  </behavior>
  <action>
    **harness.js — createExtractedSide(fixtureHtml, config):** build the second JSDOM through the SAME shared config factory used by createReferenceSide (identical url 'https://fixture.test/page', pretendToBeVisual, runScripts, virtualConsole, patchRects — Pitfall 6: any drift is a phantom divergence). Then: record the prior values of the audited global set (window, document, Node, NodeFilter, MutationObserver, requestAnimationFrame, cancelAnimationFrame, CustomEvent, ShadowRoot, location, getComputedStyle, URL) on globalThis, assign each from the new instance's window, import createCapture from '../../src/capture/index.js', construct a loopback transport (a `sent` array + send(type, payload) pushing { type, payload }; NO flush property — exercises the optional-flush default, phase success criterion 2), call createCapture({ transport: loopback }), and return the same surface shape as the reference side: { dom, window, document, sent, start, stop, pause, resume, close }, where close() calls capture.stop(), restores every recorded global in a finally, and dom.window.close()s. Because scenarios run to FULL completion on one side before the other side is even constructed (Pitfall 10), the globals window is safe — but restoration must be unconditional (finally) so a thrown scenario cannot poison later tests (Pitfall 8).

    **normalize.js — normalizeExtracted(msgs):** maps the loopback's { type, payload } records into the same canonical { type, payload } shape normalizeReference produces (types already protocol STREAM values; payload passthrough; READY payload normalizes to {} on both sides). Reuse canonicalizeIdentity unchanged — ordinal SESSION_n/SNAPSHOT_n placeholders preserve the same-vs-fresh structure that D1 depends on. While in normalize.js, verify normalizeReference maps ONLY keys actually present on the source message and fix it if Plan 01-01 shipped a fixed-field mapping: the reference's stop-path final MUTATIONS flush carries NO staleFlushCount, and emitting `staleFlushCount: undefined` from a fixed 4-field table would manufacture a missing-vs-undefined phantom divergence under assert.deepStrictEqual.

    **oracle.test.js:** add ONE flipped test now: 'reference and extracted captures emit equivalent streams for basic-mutations on basic.html' — reference side A run to completion, then extracted side B, normalize both (normalizeReference / normalizeExtracted), canonicalize, compareStreams with the ledger. Keep all existing ref-vs-ref tests unchanged and ordered FIRST in the file (self-test before flip tests — if the harness itself drifts, ref-vs-ref fails first and points at harness error, not extraction error).
  </action>
  <verify>
    <automated>node --test tests/differential/oracle.test.js && npm test</automated>
  </verify>
  <done>
    First ref-vs-extracted pair green; all prior tests green; suite exits cleanly; globals provably restored (protocol tests later in the same npm test run unaffected).
  </done>
  <acceptance_criteria>
    - `node --test tests/differential/oracle.test.js` exits 0 with the new flipped test passing
    - harness.js imports createCapture from ../../src/capture/index.js (grep)
    - The extracted side's loopback transport has NO flush property (grep harness.js: the loopback object literal contains `send` but not `flush`)
    - Global restoration uses try/finally (grep harness.js for `finally`)
    - normalize.js's normalizeReference copies only present keys — no `staleFlushCount: undefined` can be emitted for the stop-path flush (grep normalize.js: no unconditional staleFlushCount field assignment)
    - `npm test` exits 0 — protocol tests green after oracle tests ran (no globalThis pollution)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full-matrix flip + divergence ledger finalization + stale-entry detection</name>
  <files>tests/differential/oracle.test.js, tests/differential/divergence-ledger.js</files>
  <read_first>
    - tests/differential/divergence-ledger.js (DivergenceEntry schema, kind field, ledgerCovers)
    - tests/differential/oracle.test.js (matrix table + compareStreams usage)
    - tests/differential/scenarios/pause-resume.js (the scenario that carries D1)
    - reference/extension/dom-stream.js (lines 1061–1085: the resume body the extracted core intentionally does not copy — note broadcastOverlayState(true) at line 1078, where force=true bypasses the 500 ms throttle guard at line 937, plus the domStreamRequestOverlay control case at lines 1082–1085 that the extracted core drops)
    - .planning/phases/01-capture-core-extraction-differential-oracle/01-RESEARCH.md (Pattern 6 ledger schema; Pitfall 2)
  </read_first>
  <behavior>
    - Test (matrix): every fixture x scenario pair runs ref-vs-extracted and passes — divergences only where the ledger declares them
    - Test (D1 scope): D1 matches occur ONLY in the pause-resume scenario; running pause-resume WITHOUT the ledger throws UNDECLARED DIVERGENCE (proves D1 is load-bearing, not decorative)
    - Test (stale detection): after the full matrix, every kind:'mismatch' ledger entry has matched at least once — a final test asserts the matched-id set covers all mismatch entries
  </behavior>
  <action>
    **divergence-ledger.js — finalize entries:**
    - D1 (kind 'mismatch'): id 'D1-resume-no-resnapshot'; description: reference resume() (ref 1061–1080) mints a new session (beginStreamSession), re-snapshots (serializeDOM → SNAPSHOT send), and force-broadcasts overlay state (broadcastOverlayState(true) at ref 1078 — force=true bypasses the 500 ms throttle at ref 937, so the post-resume OVERLAY message ALWAYS emits); extracted resume() re-arms observers and continues the same streamSessionId/snapshotId with NO snapshot and NO overlay broadcast (USER OVERRIDE, 01-CONTEXT.md). affectedMessages: [STREAM.SNAPSHOT, STREAM.OVERLAY, STREAM.MUTATIONS]; affectedScenarios: ['pause-resume']. appliesTo(refMsg, extMsg, scenarioName): require scenarioName === 'pause-resume' AND permit the three mismatch shapes the two extra ref-side post-resume messages (SNAPSHOT + OVERLAY) produce: (a) ref-only/trailing length mismatch — refMsg defined while extMsg === undefined (the ref stream is longer once the extra messages shift indices); (b) shifted-index TYPE mismatch — refMsg.type !== extMsg.type (e.g. ref's post-resume SNAPSHOT or OVERLAY aligned against the extracted side's post-resume MUTATIONS); (c) identity-placeholder mismatch — same type but the reference payload carries SESSION_2/SNAPSHOT_2 where the extracted carries SESSION_1/SNAPSHOT_1 (the fresh-vs-continued structure ordinal canonicalization deliberately preserves — exactly the signal D-02 requires). The scenarioName guard is load-bearing: these same mismatch shapes must still hard-fail as UNDECLARED DIVERGENCE in every other scenario.
    - D2 (kind 'documented-mapping'): envelope shape — reference wraps as { action, ...fields }, extracted emits transport.send(type, payload); absorbed structurally by normalize.js's fixed mapping table; documented for the human-readable derivation (D-03: docs derive from the ledger).
    - D3 (kind 'documented-mapping'): ready timing — reference pings domStreamReady at script-injection time, extracted emits STREAM.READY at factory creation; in harness ordering both precede start() so streams align, but the host-visible timing contract differs.
    - D4 (kind 'documented-mapping'): pingDomStream readiness probe dropped from the core (direct function calls make it moot; the MV3 adapter reintroduces it host-side in Phase 6 per ADPT-01).
    - D5 (kind 'documented-mapping'): domStreamRequestOverlay control path dropped from the core (ref 1082–1085 — background-triggered forced broadcastOverlayState(true), used by the MV3 watchdog alarm to demand an overlay rebroadcast). The factory surface is exactly { start, stop, pause, resume } per D-05, so no host-facing on-demand overlay rebroadcast exists in this version; the MV3 adapter reintroduces it host-side in Phase 6 (ADPT-01), mirroring D4. Produces no runtime mismatch by construction — no fixture scenario sends the control message, so neither side ever emits the on-demand rebroadcast.
    Document in the module JSDoc that kind:'documented-mapping' entries are exempt from stale-entry detection (they never produce runtime mismatches by construction) while kind:'mismatch' entries MUST match — and that human-readable divergence docs derive from this module, never the reverse (D-03).

    **oracle.test.js — full flip + meta-tests:**
    - Extend the flipped mode across the ENTIRE Plan 01-02 matrix: basic x [basic-mutations, mutation-burst, structural-ops, scroll, pause-resume], heavy-realistic x [snapshot-only, structural-ops], truncation-overflow x snapshot-only (patchRects), canvas x snapshot-only, dialog x dialog (dangerously). One flat test() per pair, full-sentence names. compareStreams returns the Set of matched ledger ids — accumulate into a module-level Set across tests.
    - Keep the truncation guard (truncated === true + equal missingDescendants on BOTH sides) and dialog guard (>= 1 dialog message on BOTH sides) in flipped mode.
    - D1 scope test: run pause-resume ref-vs-extracted with an EMPTY ledger and assert compareStreams throws /UNDECLARED DIVERGENCE/ — proves the divergence is real and D1 is the thing permitting it.
    - Final test (runs last in file — node:test preserves in-file order): 'every declared mismatch divergence matched at least one real divergence' — assert the accumulated matched-id set contains the id of every kind:'mismatch' DIVERGENCES entry (stale-entry detection per D-03/RESEARCH Pattern 6).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    Full suite green: ref-vs-ref self-tests + ref-vs-extracted on all matrix pairs + D1 scope test + stale-entry detection. The phase's equivalence claim is now machine-checked end-to-end.
  </done>
  <acceptance_criteria>
    - `npm test` exits 0; oracle.test.js reports >= 22 passing tests (>= 10 ref-vs-ref + >= 10 flipped + D1 scope + stale detection)
    - divergence-ledger.js contains exactly one kind:'mismatch' entry and its id is 'D1-resume-no-resnapshot' (grep)
    - D1's affectedMessages includes STREAM.OVERLAY alongside STREAM.SNAPSHOT and STREAM.MUTATIONS (grep divergence-ledger.js for OVERLAY within the D1 entry)
    - D1's appliesTo guards on scenarioName === 'pause-resume' (grep: the predicate references the scenario name)
    - divergence-ledger.js contains D2/D3/D4/D5 entries with kind 'documented-mapping' (grep for four 'documented-mapping' occurrences minimum)
    - The D1 scope test passes: pause-resume with an empty ledger throws UNDECLARED DIVERGENCE (grep oracle.test.js for the empty-ledger assertion)
    - The stale-entry test passes and runs last (grep: final test() in file references mismatch coverage)
    - `git diff --exit-code -- reference/extension/dom-stream.js` exits 0 — the reference is the frozen spec and must remain byte-identical. (Fixing extraction bugs the oracle exposes in src/capture/index.js IS permitted and expected; re-run `node --test tests/capture-purity.test.js` after any such fix)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture HTML → both capture implementations | same as prior plans; dialog fixture runs 'dangerously' |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Elevation of privilege | dialog fixture under runScripts 'dangerously' | mitigate | Unchanged from Plan 01-02: confined to the single frozen dialog fixture |
| T-01-03 | Tampering | sanitization gap present identically in BOTH streams | accept | The oracle proves the gap is preserved (parity), not fixed — by design (D-11). Phase 3 SEC-01 owns the fix; ledger/README record it |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
- `npm test` green: dual-mode oracle (self-test + flipped matrix), D1 scope test, stale-entry detection
- reference/extension/dom-stream.js byte-identical to its pre-phase state (git diff empty)
- Loopback transport (no flush) proves the seam — phase success criterion 2's "proven with a loopback transport"
</verification>

<success_criteria>
- Differential harness reports structural op-stream equivalence reference-vs-extracted on all frozen fixtures, and records every intentional divergence in the machine-readable ledger (phase success criterion 1, complete)
- The only mismatch-kind divergence is D1 (resume semantics — USER OVERRIDE), scoped to the pause-resume scenario and covering the reference's post-resume SNAPSHOT + forced OVERLAY emissions plus the index shifts they cause
- Every FSB-decoupling drop is ledgered: D4 (pingDomStream) and D5 (domStreamRequestOverlay) document the removed host-control paths
- CAPT-04 fully satisfied; CAPT-01's "emits through any injected Transport, proven with a loopback transport" satisfied
</success_criteria>

<output>
Create `.planning/phases/01-capture-core-extraction-differential-oracle/01-04-SUMMARY.md` when done.
</output>
