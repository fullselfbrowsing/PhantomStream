---
phase: 01-capture-core-extraction-differential-oracle
plan: 05
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - tests/capture-lifecycle.test.js
  - tests/capture-defenses.test.js
  - tests/capture-watchdog.test.js
  - src/capture/README.md
autonomous: true
requirements: [CAPT-02, CAPT-03]
tags: [capture-core, lifecycle, reliability-defenses, watchdog]

must_haves:
  truths:
    - "stop() then start() mints a fresh session: new streamSessionId AND new snapshotId (fresh-session semantics matching the reference)"
    - "resume() continues the SAME streamSessionId/snapshotId and emits NO snapshot; mutations during pause are missed by design"
    - "A transport whose send throws routes the error to the injected logger and never breaks the capture path"
    - "Each reliability defense has a dedicated green test: rAF-batched diffs, watchdog force-flush (with staleFlushCount), identity stamping on every message type, budgeted truncation with single-pass layout reads"
    - "The watchdog test completes in seconds via the fake-Date/real-setTimeout recipe (no 5-second waits, no mock.timers)"
  artifacts:
    - path: "tests/capture-lifecycle.test.js"
      provides: "CAPT-02 lifecycle semantics + Transport seam proof via loopback"
    - path: "tests/capture-defenses.test.js"
      provides: "rAF batching, identity stamping, truncation single-pass defense tests"
    - path: "tests/capture-watchdog.test.js"
      provides: "Watchdog force-flush defense test (own file — Date-faking process isolation)"
      contains: "staleFlushCount"
    - path: "src/capture/README.md"
      provides: "Factory docs, Transport contract, pause/resume host contract, deferred-split note"
      contains: "resume"
  key_links:
    - from: "tests/capture-lifecycle.test.js"
      to: "src/capture/index.js"
      via: "ESM import of createCapture"
      pattern: "from '\\.\\./src/capture/index\\.js'"
    - from: "tests/capture-watchdog.test.js"
      to: "src/protocol/constants.js"
      via: "MUTATION_STALE_THRESHOLD_MS / WATCHDOG_TICK_MS imports drive the fake-clock advance"
      pattern: "MUTATION_STALE_THRESHOLD_MS"
---

<objective>
Lock the extracted core's lifecycle semantics (CAPT-02) and reliability defenses (CAPT-03) with dedicated tests — one per defense per locked decision D-15 — plus rewrite src/capture/README.md to document the shipped factory contract (D-05/D-06/D-07/D-08).

Purpose: the oracle (Plan 01-04) proves parity on fixtures; these tests pin the BEHAVIORAL CONTRACTS so future refactors (the deferred module split, Phase 7 identity rework) cannot silently drop a defense or change lifecycle semantics.
Output: three test files (lifecycle, defenses, watchdog) + rewritten capture README.

Parallel-safety: this plan shares ZERO files with Plan 01-04 (same wave) — its tests must not import from tests/differential/.
</objective>

## Phase Goal

**As a** host developer embedding PhantomStream, **I want to** run the capture core in any injection context through an injected Transport, **so that** I get a capture stream provably equivalent to the shipped FSB reference.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-capture-core-extraction-differential-oracle/01-CONTEXT.md
@.planning/phases/01-capture-core-extraction-differential-oracle/01-RESEARCH.md
@.planning/phases/01-capture-core-extraction-differential-oracle/01-PATTERNS.md
@src/capture/index.js
@src/capture/README.md
@src/protocol/constants.js
@src/protocol/messages.js
@tests/protocol.test.js
</context>

<interfaces>
From src/capture/index.js (Plan 01-03):
```javascript
export function createCapture({ transport, logger, overlayProvider, skipElement });
// -> { start(), stop(), pause(), resume() }
// READY emitted once at factory creation; SNAPSHOT on each start()
```

From src/protocol/constants.js: MUTATION_STALE_THRESHOLD_MS = 5000, WATCHDOG_TICK_MS = 500, SNAPSHOT_BUDGET_BYTES = 838860, TRUNCATION_VIEWPORT_MULTIPLIER = 3.
From src/protocol/messages.js: STREAM type strings for asserting message types; session id format is `stream_<ts36>_<rand>` (regex `^stream_[a-z0-9]+_[a-z0-9]+$`).

jsdom test recipes (verified in 01-RESEARCH.md): Pattern 2 (globals from a JSDOM instance, tracked + restored in finally), Pattern 3 (settle cadence: setTimeout 0 → rAF → setTimeout 20), Pattern 4 (fake Date + suppressed rAF + REAL setTimeout for the watchdog), Pattern 5 (getBoundingClientRect prototype patch reading data-test-top).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lifecycle semantics tests (CAPT-02) + transport-error containment</name>
  <files>tests/capture-lifecycle.test.js</files>
  <read_first>
    - src/capture/index.js (factory surface, lifecycle implementations, READY/SNAPSHOT emission points)
    - tests/protocol.test.js (test style: flat test(), inline fakes, full-sentence names)
    - .planning/phases/01-capture-core-extraction-differential-oracle/01-RESEARCH.md (Pattern 2 globals recipe, Pattern 3 settle cadence, Pitfalls 3/8, "Loopback transport" example)
  </read_first>
  <behavior>
    - Test 1: "factory emits ready and start emits a snapshot stamped with fresh identity" — after createCapture + start: first sent message type is STREAM.READY, a STREAM.SNAPSHOT follows with streamSessionId matching /^stream_[a-z0-9]+_[a-z0-9]+$/ and a numeric snapshotId > 0
    - Test 2: "stop then start mints a new stream session and snapshot identity" — second session's streamSessionId !== first's AND snapshotId differs (fresh-session semantics, phase success criterion 3)
    - Test 3: "pause suspends emission and resume continues the same session without re-snapshot" — after pause: a DOM mutation + settle produces ZERO new messages; after resume: NO snapshot message is emitted, and a post-resume mutation + settle produces a STREAM.MUTATIONS message carrying the ORIGINAL streamSessionId and snapshotId (D-06 USER OVERRIDE pinned by test)
    - Test 4: "transport errors route to the injected logger and never throw into the capture path" — a transport whose send always throws + an injected logger that records error calls: createCapture/start/mutate/settle/stop all complete without throwing, and the logger recorded at least one error
    - Test 5: "a transport without flush works through the no-op default" — loopback with only send defined; full start/mutate/stop cycle completes without throwing
  </behavior>
  <action>
    Create tests/capture-lifecycle.test.js: jsdom-hosted node:test file. A shared per-test setup helper builds a JSDOM ('https://fixture.test/page', pretendToBeVisual: true, fresh VirtualConsole), records prior globalThis values for the audited global set (window, document, Node, NodeFilter, MutationObserver, requestAnimationFrame, cancelAnimationFrame, CustomEvent, ShadowRoot, location, getComputedStyle, URL), assigns them from the instance window, and returns a teardown that restores every global, stops the capture, and closes the window — every test body wraps in try/finally(teardown) (Pitfalls 3 and 8). Inline a small HTML body (no fixture file needed — a few divs suffice). Loopback transport per the RESEARCH example: a sent array plus send(type, payload) pushing { type, payload }, with flush deliberately omitted. Use the settle cadence helper (setTimeout 0 → rAF → setTimeout 20) — duplicate it locally rather than importing from tests/differential/harness.js to keep this plan parallel-safe with Plan 01-04 (zero shared files).

    Implement the five behavior tests as flat test() calls with full-sentence names. No describe blocks, no mocking framework, node:assert/strict throughout. Import STREAM from ../src/protocol/messages.js for type assertions (never restate ext: strings).
  </action>
  <verify>
    <automated>node --test tests/capture-lifecycle.test.js</automated>
  </verify>
  <done>
    Five lifecycle tests green; CAPT-02 fresh-session and D-06 resume-continuation semantics pinned; transport seam error-containment proven.
  </done>
  <acceptance_criteria>
    - `node --test tests/capture-lifecycle.test.js` exits 0 with >= 5 passing tests
    - Test 3 asserts BOTH: no SNAPSHOT message after resume AND post-resume mutations carry the pre-pause streamSessionId/snapshotId (grep the file for both assertions)
    - The file does NOT import from tests/differential/ (grep `differential` in the file → zero matches; parallel-safety with Plan 01-04)
    - Every test restores globals in finally (grep for `finally`)
    - The test process exits cleanly within 30s (no watchdog timer leak — teardown stops captures)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Defense tests — rAF batching, identity stamping, budgeted truncation with single-pass reads</name>
  <files>tests/capture-defenses.test.js</files>
  <read_first>
    - src/capture/index.js (flushMutations, serializeDOM truncation passes, side-channel identity stamping)
    - tests/capture-lifecycle.test.js (reuse the setup/teardown + settle helper shapes from Task 1)
    - src/protocol/constants.js (SNAPSHOT_BUDGET_BYTES, TRUNCATION_VIEWPORT_MULTIPLIER)
    - .planning/phases/01-capture-core-extraction-differential-oracle/01-RESEARCH.md (Pattern 5 rect patch; Pitfall 7 UTF-16 budget)
  </read_first>
  <behavior>
    - Test 1: "mutations within one frame batch into a single rAF flush" — >= 10 synchronous mutations with no settle between them, then one settle → EXACTLY one STREAM.MUTATIONS message containing all the resulting ops (rAF-batched diffs defense)
    - Test 2: "every emitted message type carries stream session and snapshot identity" — drive snapshot (start), mutations (mutate + settle), scroll (dispatch window scroll event + settle), overlay (emitted via start's overlay broadcast) and assert each captured payload carries the session's streamSessionId AND snapshotId (identity-stamping defense; dialog identity is covered by the oracle's dialog scenario in Plan 01-04)
    - Test 3: "oversized pages truncate whole subtrees within budget using one layout read per element" — a programmatically built oversized DOM (> SNAPSHOT_BUDGET_BYTES serialized, ASCII content, below-fold subtrees stamped data-test-top > 2304): snapshot payload has truncated === true and missingDescendants > 0, AND a counting getBoundingClientRect patch proves NO element's rect was read more than once during serialization (single-pass defense, measured)
  </behavior>
  <action>
    Create tests/capture-defenses.test.js reusing Task 1's setup/teardown/settle helper shapes (duplicated locally — same parallel-safety rule: no imports from tests/differential/). For Test 3: before start(), patch the instance's Element.prototype.getBoundingClientRect with a wrapper that (a) returns the Pattern-5 fake rect derived from data-test-top (top from the attribute, left 0, width 100, height 50, right 100, bottom top+50), and (b) increments a per-element call count in a Map keyed by the element; after serialization assert the maximum count across all elements === 1 (one layout read per element — the single-pass defense as a measured invariant, not a source grep). Build the oversized DOM in-test by appending repeated-paragraph sections (pure ASCII — the budget compares UTF-16 code-unit length, Pitfall 7) with data-test-top values of 3000/6000/9000 so pass-1 has below-fold candidates past the jsdom cutoff (innerHeight 768 x TRUNCATION_VIEWPORT_MULTIPLIER 3 = 2304). Import SNAPSHOT_BUDGET_BYTES from ../src/protocol/constants.js to size the content (budget + ~50% margin) rather than hardcoding 838860.

    Flat test() calls, full-sentence names, node:assert/strict, try/finally teardown everywhere.
  </action>
  <verify>
    <automated>node --test tests/capture-defenses.test.js</automated>
  </verify>
  <done>
    Three defense tests green: rAF batching, identity stamping across message types, budgeted truncation with measured single-pass layout reads.
  </done>
  <acceptance_criteria>
    - `node --test tests/capture-defenses.test.js` exits 0 with >= 3 passing tests
    - Test 1 asserts exactly ONE mutations message after the burst (grep for an equality assertion on the mutations-message count)
    - Test 2 asserts identity on >= 4 distinct STREAM types (grep for SNAPSHOT, MUTATIONS, SCROLL, OVERLAY)
    - Test 3 asserts truncated === true, missingDescendants > 0, and a max-rect-reads-per-element === 1 invariant (grep for the count assertion)
    - SNAPSHOT_BUDGET_BYTES imported from src/protocol/constants.js, not hardcoded (grep: no literal 838860 in the file)
    - No imports from tests/differential/ (parallel-safety)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Watchdog force-flush test (own file) + capture README rewrite</name>
  <files>tests/capture-watchdog.test.js, src/capture/README.md</files>
  <read_first>
    - src/capture/index.js (watchdog tick implementation: WATCHDOG_TICK_MS setTimeout chain, MUTATION_STALE_THRESHOLD_MS check, staleFlushCount increment-before-flush)
    - src/capture/README.md (current "extraction pending" content to replace)
    - .planning/phases/01-capture-core-extraction-differential-oracle/01-RESEARCH.md (Pattern 4 — fake Date, REAL setTimeout, suppressed rAF; Pitfall 8 — Date-faking tests isolated per file; Assumption A1 — never mock.timers)
    - .planning/phases/01-capture-core-extraction-differential-oracle/01-CONTEXT.md (D-05/D-06/D-07/D-08 exact contract wording for the README)
  </read_first>
  <behavior>
    - Test: "watchdog force-flushes stuck mutations and reports the stale rescue" — with rAF suppressed (batch flush never fires) and a fake Date: start capture, mutate, advance fakeNow by MUTATION_STALE_THRESHOLD_MS + 1000, wait ~2.5 x WATCHDOG_TICK_MS REAL time, assert a STREAM.MUTATIONS message arrived with staleFlushCount === 1 carrying the stuck ops
    - The whole test file completes in well under 5 seconds (the fake-Date recipe works; no real 5-second stale wait)
  </behavior>
  <action>
    **tests/capture-watchdog.test.js** — its OWN file (node --test runs each file in its own process, so the Date fake cannot poison other suites — Pitfall 8). Recipe per verified Pattern 4: build the jsdom + globals setup as in Task 1, then BEFORE createCapture: swap globalThis.Date for a FakeDate (constructor delegates to the real Date; static now() returns a mutable fakeNow initialized to a fixed value such as 1000000) and set globalThis.requestAnimationFrame to a function returning 0 with cancelAnimationFrame a no-op (suppresses the rAF flush so pendingMutations stay stuck). Leave setTimeout REAL — never node:test mock.timers (RESEARCH A1: jsdom timer interception unverified; the fake-Date recipe sidesteps it entirely). Import MUTATION_STALE_THRESHOLD_MS and WATCHDOG_TICK_MS from ../src/protocol/constants.js to compute the fakeNow advance (threshold + 1000) and the real wait (~2.5 x tick). Restore Date and all globals in finally; stop the capture; close the window.

    **src/capture/README.md** — replace the "extraction pending" document with the shipped contract:
    - Factory: createCapture({ transport, logger, overlayProvider, skipElement }) -> { start, stop, pause, resume } — parameter docs matching the JSDoc typedefs in index.js.
    - Transport contract (D-07): send(type, payload) required, fire-and-forget (mirrors chrome.runtime.sendMessage semantics); flush optional with a no-op default; transport errors are routed to the logger and never thrown into the capture path.
    - Lifecycle host contract (D-06, state precisely): stop() then start() begins a fresh session (new streamSessionId + new snapshotId); pause() suspends observers/flushing but keeps the session alive; resume() re-arms observers and continues the SAME streamSessionId/snapshotId WITHOUT re-snapshotting — mutations occurring while paused are missed by design; guidance: "pause when the page is quiescent, or stop()/start() to force a fresh snapshot." No refresh() method in this version (deliberate — the factory surface is exactly the four methods per D-05).
    - Module layout: single-file extraction per the Phase 1 user override (D-10); the serializer/differ/side-channels/session split is deferred to a later phase — parity with the reference was the Phase 1 exit bar. Link tests/differential/divergence-ledger.js as the machine-readable record of intentional divergences (including D1 resume semantics); human-readable divergence docs derive from the ledger, never the reverse (D-03).
    - Retain the existing "Behavioral changes queued for the standalone version" list — post-snapshot computed styles (Phase 8), full-path on*/javascript: sanitization (Phase 3 — accepted threat T-01-03 until then), CSSOM capture mode (Phase 9).
  </action>
  <verify>
    <automated>node --test tests/capture-watchdog.test.js && node -e "const s=require('fs').readFileSync('src/capture/README.md','utf8'); const need=['createCapture','transport','resume','divergence-ledger','flush']; const missing=need.filter(t=>!s.includes(t)); if(missing.length){console.error('README missing: '+missing.join(', '));process.exit(1)} if(s.includes('extraction pending')){console.error('stale extraction-pending header');process.exit(1)}" && npm test</automated>
  </verify>
  <done>
    Watchdog defense test green in seconds; README documents the shipped factory contract, lifecycle host contract, deferred split, and ledger pointer; full suite green.
  </done>
  <acceptance_criteria>
    - `node --test tests/capture-watchdog.test.js` exits 0 and completes in < 5 seconds wall time
    - The test asserts staleFlushCount === 1 on the force-flushed mutations message (grep `staleFlushCount`)
    - The file imports MUTATION_STALE_THRESHOLD_MS and WATCHDOG_TICK_MS from src/protocol/constants.js (grep; no hardcoded 5000/500 stale-threshold literals)
    - grep tests/capture-watchdog.test.js for `mock.timers` → zero matches; `setTimeout` is never reassigned in the file
    - src/capture/README.md contains: createCapture signature, transport flush default, the resume-does-not-re-snapshot contract, the deferred-split note, and a tests/differential/divergence-ledger.js reference; the string "extraction pending" is gone
    - `npm test` exits 0 — entire phase suite green
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → createCapture config | host-supplied transport/logger callables run in the capture path |
| test process globals | fake Date/rAF installed on globalThis during the watchdog test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Denial of service | throwing transport.send in capture path | mitigate | Pinned by lifecycle Test 4: errors route to logger, capture continues (D-07 enforced by test, not just convention) |
| T-01-05 | Tampering | fake Date leaking across test files | mitigate | Date-faking confined to tests/capture-watchdog.test.js (per-file process isolation under node --test) + finally restoration; real setTimeout untouched |
| T-01-03 | Tampering | sanitization gap documented in README behavioral-changes list | accept | Parity-only (D-11); Phase 3 SEC-01 owns the fix; README records the queue |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
- `node --test tests/capture-lifecycle.test.js tests/capture-defenses.test.js tests/capture-watchdog.test.js` all green
- `npm test` green (full phase suite: protocol + purity + oracle + lifecycle + defenses + watchdog)
- Watchdog file wall time < 5s (fake-Date recipe proven)
- README contract matches D-05/D-06/D-07/D-08 wording
</verification>

<success_criteria>
- CAPT-02 satisfied: start/stop/pause/resume with fresh-session semantics pinned by tests (phase success criterion 3)
- CAPT-03 satisfied: all four reliability defenses enforced by dedicated tests — rAF-batched diffs, self-watchdog force-flush with staleFlushCount, session/snapshot identity stamping, budgeted whole-subtree truncation with measured single-pass layout reads (phase success criterion 4)
- D-15 satisfied: one dedicated test per defense, node:test + node:assert/strict style
</success_criteria>

<output>
Create `.planning/phases/01-capture-core-extraction-differential-oracle/01-05-SUMMARY.md` when done.
</output>
