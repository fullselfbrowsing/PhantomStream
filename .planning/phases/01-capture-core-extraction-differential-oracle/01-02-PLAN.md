---
phase: 01-capture-core-extraction-differential-oracle
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - tests/differential/fixtures/heavy-realistic.html
  - tests/differential/fixtures/truncation-overflow.html
  - tests/differential/fixtures/canvas.html
  - tests/differential/fixtures/dialog.html
  - tests/differential/fixtures/generate-truncation-overflow.js
  - tests/differential/scenarios/mutation-burst.js
  - tests/differential/scenarios/structural-ops.js
  - tests/differential/scenarios/scroll.js
  - tests/differential/scenarios/dialog.js
  - tests/differential/scenarios/pause-resume.js
  - tests/differential/scenarios/snapshot-only.js
  - tests/differential/harness.js
  - tests/differential/oracle.test.js
autonomous: true
requirements: [CAPT-04]
tags: [differential-testing, fixtures, jsdom]

must_haves:
  truths:
    - "The oracle is green reference-vs-reference across the full fixture x scenario matrix (every reliability defense exercised)"
    - "Truncation actually triggers: both sides report truncated === true with identical missingDescendants on the overflow fixture"
    - "The dialog channel is actually exercised: at least one dialog message recorded on both sides (not identical-but-empty)"
    - "Regenerating truncation-overflow.html is byte-identical (deterministic generator; fixture frozen)"
  artifacts:
    - path: "tests/differential/fixtures/truncation-overflow.html"
      provides: "Frozen fixture exceeding SNAPSHOT_BUDGET_BYTES with data-test-top stamped subtrees"
      min_lines: 100
    - path: "tests/differential/fixtures/generate-truncation-overflow.js"
      provides: "Deterministic committed generator (provenance for the frozen fixture)"
    - path: "tests/differential/fixtures/heavy-realistic.html"
      provides: "Heavy realistic page (deep nesting, table, form, srcset, svg xlink, style cascade, script/noscript)"
    - path: "tests/differential/fixtures/canvas.html"
      provides: "Canvas fixture locking the degenerate toDataURL-null path"
    - path: "tests/differential/fixtures/dialog.html"
      provides: "Dialog-interceptor fixture (the only one run with runScripts dangerously)"
    - path: "tests/differential/scenarios/pause-resume.js"
      provides: "Lifecycle scenario that will carry divergence D1 after the oracle flip"
  key_links:
    - from: "tests/differential/oracle.test.js"
      to: "tests/differential/fixtures/"
      via: "matrix table of fixture x scenario pairs"
      pattern: "truncation-overflow"
    - from: "tests/differential/harness.js"
      to: "Element.prototype.getBoundingClientRect"
      via: "patchRects config applied identically to every side before capture starts"
      pattern: "getBoundingClientRect"
    - from: "tests/differential/fixtures/generate-truncation-overflow.js"
      to: "src/protocol/constants.js"
      via: "SNAPSHOT_BUDGET_BYTES import sizes the overflow margin"
      pattern: "SNAPSHOT_BUDGET_BYTES"
---

<objective>
Complete the frozen fixture corpus and scripted scenario set so the oracle exercises every reliability defense (truncation budget overflow, mutation bursts, add/rm/attr/text ops, scroll, dialogs, pause/resume) plus one heavy realistic page — all green reference-vs-reference. Implements locked decision D-04 (fixtures crafted per defense, checked in, no live sites) and finishes the "oracle green on frozen fixtures BEFORE extraction" gate (D-09).

Purpose: the matrix defines what "provably equivalent" means for the extraction in Plan 01-03/01-04. A defense without a fixture is a defense the oracle cannot protect.
Output: 4 new frozen fixtures + deterministic generator, 6 scenario modules, harness config extensions (rect patching, per-fixture runScripts), oracle matrix green ref-vs-ref.
</objective>

## Phase Goal

**As a** host developer embedding PhantomStream, **I want to** run the capture core in any injection context through an injected Transport, **so that** I get a capture stream provably equivalent to the shipped FSB reference.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-capture-core-extraction-differential-oracle/01-CONTEXT.md
@.planning/phases/01-capture-core-extraction-differential-oracle/01-RESEARCH.md
@.planning/phases/01-capture-core-extraction-differential-oracle/01-PATTERNS.md
@tests/differential/harness.js
@tests/differential/oracle.test.js
@reference/extension/dom-stream.js
@src/protocol/constants.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frozen fixtures + deterministic truncation generator</name>
  <files>tests/differential/fixtures/heavy-realistic.html, tests/differential/fixtures/truncation-overflow.html, tests/differential/fixtures/canvas.html, tests/differential/fixtures/dialog.html, tests/differential/fixtures/generate-truncation-overflow.js</files>
  <read_first>
    - reference/extension/dom-stream.js (serializer lines 266–609: what gets stripped, absolutified, style-collected; truncation logic around line 549)
    - src/protocol/constants.js (SNAPSHOT_BUDGET_BYTES = 838860, TRUNCATION_VIEWPORT_MULTIPLIER = 3)
    - tests/differential/fixtures/basic.html (existing fixture style)
    - .planning/phases/01-capture-core-extraction-differential-oracle/01-RESEARCH.md (Pitfalls 4, 5, 7; Pattern 5 rect values; jsdom viewport defaults innerWidth=1024/innerHeight=768)
  </read_first>
  <action>
    Author four frozen fixtures (D-04 — checked in, no live sites, script-free except dialog.html which is also script-free in markup; the interceptor script is injected by the capture code itself and only executes under runScripts 'dangerously').

    **heavy-realistic.html**: a realistic page at the LOW end of the 50–150 KB range — target ~50 KB. Reach size through REPETITIVE markup (the >= 20-row table, repeated list items / paragraph blocks with varied attributes), not hand-crafted unique content; the serializer paths care about structural variety, not prose volume. Must exercise every serializer path identically on both sides — nesting at least 10 levels deep; a table with >= 20 rows; a form with text input, select, textarea, checkbox; anchors and images with RELATIVE href/src URLs (absolutification against the harness base URL); an img with `srcset` (multiple candidates — exercises absolutifySrcset); an inline `<svg>` containing a `<use>` with `xlink:href` (exercises getAttributeNS handling); `data-*` attributes (dataset camelCase mapping); a `<style>` block with element, class, and descendant selectors plus one `!important` rule (jsdom 29 cascade resolution feeds collectComputedStyleText); one `<script>` element and one `<noscript>` element in the body (the serializer must strip both — identical removal on both sides is the assertion); a `<canvas>`-free body (canvas gets its own fixture).

    **generate-truncation-overflow.js**: a deterministic Node ESM script (zero randomness, zero Date dependence) that writes truncation-overflow.html next to itself. Import SNAPSHOT_BUDGET_BYTES from `../../../src/protocol/constants.js` and size content so the serialized body innerHTML length comfortably exceeds it — target 1,200,000–1,500,000 ASCII characters (the budget compares `html.length`, UTF-16 code units, NOT bytes — Pitfall 7 — so use pure-ASCII repetition for predictable sizing). Structure: a small above-fold header subtree with `data-test-top="0"`, then several (4–8) large sibling sections stamped `data-test-top` values of 3000, 6000, 9000, ... — all beyond the jsdom truncation cutoff `innerHeight 768 x TRUNCATION_VIEWPORT_MULTIPLIER 3 = 2304` — each filled with repeated paragraph blocks so pass-1 (drop below-fold subtrees) has real candidates under the Pattern-5 rect patch. Print the generated file's character count on completion.

    Run the generator and commit truncation-overflow.html. The fixture file is the frozen artifact; the generator is committed provenance. Prove determinism by running the generator TWICE and comparing sha-256 hashes of the output between runs — do NOT rely on `git diff --exit-code` for this check (it is vacuous while the freshly generated file is still untracked).

    **canvas.html**: a small page containing a `<canvas>` element with a few sibling text elements. Locks the degenerate jsdom path: `toDataURL()` returns null without throwing (Pitfall 4) — both sides serialize the same bogus result; note in a fixture HTML comment that real-browser canvas behavior is re-verified in later phases.

    **dialog.html**: a small page with a heading and a button (markup script-free). The harness will run this fixture with `runScripts: 'dangerously'` so the capture's injected dialog-interceptor `<script>` executes (Pitfall 5 — under outside-only it silently never installs).
  </action>
  <verify>
    <automated>node tests/differential/fixtures/generate-truncation-overflow.js && H1=$(shasum -a 256 tests/differential/fixtures/truncation-overflow.html | cut -d' ' -f1) && node tests/differential/fixtures/generate-truncation-overflow.js && H2=$(shasum -a 256 tests/differential/fixtures/truncation-overflow.html | cut -d' ' -f1) && test "$H1" = "$H2" && node -e "const fs=require('fs'); const t=fs.readFileSync('tests/differential/fixtures/truncation-overflow.html','utf8'); if(t.length < 1200000) {console.error('too small: '+t.length); process.exit(1)}; if(!/data-test-top=\"3000\"/.test(t)) {console.error('missing data-test-top stamps'); process.exit(1)}; const h=fs.readFileSync('tests/differential/fixtures/heavy-realistic.html','utf8'); for (const probe of ['srcset','xlink:href','<script','<noscript','!important','data-']) { if(!h.includes(probe)) {console.error('heavy-realistic missing '+probe); process.exit(1)} }; fs.accessSync('tests/differential/fixtures/canvas.html'); fs.accessSync('tests/differential/fixtures/dialog.html')"</automated>
  </verify>
  <done>
    Four fixture files committed; truncation fixture >= 1.2M ASCII chars with data-test-top stamps; running the generator twice produces identical sha-256 hashes (deterministic + frozen); heavy-realistic contains srcset, xlink, script/noscript, !important, data-* probes.
  </done>
  <acceptance_criteria>
    - Running `node tests/differential/fixtures/generate-truncation-overflow.js` twice in a row produces byte-identical truncation-overflow.html (sha-256 hashes equal between consecutive runs — determinism proven independent of git tracking state)
    - truncation-overflow.html character length >= 1,200,000, pure ASCII (no chars > U+007F)
    - truncation-overflow.html contains data-test-top values 0 and at least three values > 2304
    - heavy-realistic.html contains all of: `srcset`, `xlink:href`, `<script`, `<noscript`, `!important`, `data-` attributes, a `<table>` with >= 20 `<tr>`, a `<form>`
    - canvas.html contains `<canvas`; dialog.html contains no `<script` in markup
    - generate-truncation-overflow.js imports SNAPSHOT_BUDGET_BYTES from src/protocol/constants.js (grep)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scenario modules + harness config + ref-vs-ref matrix green</name>
  <files>tests/differential/scenarios/mutation-burst.js, tests/differential/scenarios/structural-ops.js, tests/differential/scenarios/scroll.js, tests/differential/scenarios/dialog.js, tests/differential/scenarios/pause-resume.js, tests/differential/scenarios/snapshot-only.js, tests/differential/harness.js, tests/differential/oracle.test.js</files>
  <read_first>
    - tests/differential/harness.js (current factory + settle + runScenario — extend, do not rewrite)
    - tests/differential/oracle.test.js (current ref-vs-ref structure)
    - tests/differential/scenarios/basic-mutations.js (scenario module contract: name + run(side, settle))
    - reference/extension/dom-stream.js (scroll tracker 884–924; dialog interceptor 163–254; pause/resume handlers 1053–1080; mutation batch processing 620–874)
    - .planning/phases/01-capture-core-extraction-differential-oracle/01-RESEARCH.md (Pattern 5 rect patch; Pitfalls 5, 7, 10)
  </read_first>
  <behavior>
    - Test (matrix): every fixture x scenario pair in the table below passes ref-vs-ref with zero divergences
    - Test (truncation guard): on truncation-overflow x snapshot-only, BOTH sides' snapshot payloads have truncated === true and equal missingDescendants > 0 — explicit assertion, not just deep-equal (guards against silent non-trigger)
    - Test (dialog guard): on dialog x dialog scenario, BOTH sides record >= 1 dialog message (guards against identical-but-empty false confidence)
  </behavior>
  <action>
    **Harness extension** (tests/differential/harness.js): extend the side-factory config to `{ runScripts: 'outside-only' | 'dangerously', patchRects: boolean }`. When `patchRects` is true, BEFORE the capture code loads/starts, patch that instance's `Element.prototype.getBoundingClientRect` (via `dom.window.Element.prototype`) to return `{ top: Number(this.getAttribute('data-test-top')) || 0, left: 0, width: 100, height: 50, right: 100, bottom: top + 50, x: 0, y: top }` — Pattern 5, verified recipe. The patch must be applied through the SAME shared config factory for every side (Pitfall 6: any cross-instance drift is a phantom divergence).

    **Scenario modules** — same contract as basic-mutations (`export const name`, `export async function run(side, settle)`; pure functions of the side handle; no module state; await `settle(side.window)` at each cadence point):
    - `snapshot-only.js`: no mutations — snapshot + initial side-channel messages only. Used for truncation-overflow, canvas, and heavy-realistic pairs.
    - `mutation-burst.js`: >= 50 synchronous DOM mutations (mixed appendChild/setAttribute/textContent) with NO settle between them, then one settle — all ops must batch into rAF-cadence flushes identically on both sides.
    - `structural-ops.js`: one group per DIFF_OP type with settles between groups — add a multi-element subtree (op 'add'), remove an existing node (op 'rm'), setAttribute including a relative-URL attribute like href (op 'attr' + absolutification), and a character-data change on a text node (op 'text', addressed via parent nid + attributeOldValue config).
    - `scroll.js`: dispatch `new side.window.Event('scroll')` on window, settle; then a real >= 250 ms wait (beyond SCROLL_THROTTLE_MS = 200) and dispatch again, settle — exercises the throttle identically on both sides (jsdom scroll offsets are 0; values are degenerate-but-identical).
    - `dialog.js`: call `side.window.alert('oracle-alert')`, settle, then `side.window.confirm('oracle-confirm')`, settle — jsdom implements these as real functions, the injected interceptor monkey-patches them and relays open/dismiss CustomEvents (only meaningful under runScripts 'dangerously').
    - `pause-resume.js`: mutate, settle; `side.pause()`; mutate (missed-by-design), settle; `side.resume()`, settle; mutate, settle. Reference-vs-reference this is symmetric and green (both re-snapshot on resume). After the Plan 01-04 flip, this scenario carries divergence D1.

    **Oracle matrix** (tests/differential/oracle.test.js): define a MATRIX table of `{ fixture, scenario, config }` pairs — basic x [basic-mutations, mutation-burst, structural-ops, scroll, pause-resume]; heavy-realistic x [snapshot-only, structural-ops]; truncation-overflow x snapshot-only with `{ patchRects: true }`; canvas x snapshot-only; dialog x dialog with `{ runScripts: 'dangerously' }`. One flat `test()` per pair with a full-sentence name (e.g. "reference and extracted captures emit identical streams for mutation-burst on basic.html" — keep "reference-vs-reference" wording for now; Plan 01-04 generalizes). Run the FULL scenario to completion on side A before constructing side B (Pitfall 10). Add the truncation guard and dialog guard assertions described in behavior. try/finally close() on every side.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    Full suite green: 10 matrix pairs + guards + Plan 01-01 tests + protocol tests. Truncation provably triggers; dialog channel provably carries messages; suite exits cleanly in < 60s.
  </done>
  <acceptance_criteria>
    - `npm test` exits 0; oracle.test.js reports >= 10 passing tests (one per matrix pair minimum)
    - oracle.test.js asserts `truncated === true` and `missingDescendants` equality for the truncation pair (grep `truncated` and `missingDescendants` in oracle.test.js)
    - oracle.test.js asserts >= 1 dialog message on both sides for the dialog pair (grep for a dialog-count assertion)
    - Only the dialog pair passes `runScripts: 'dangerously'` (grep: exactly one `dangerously` occurrence in oracle.test.js)
    - harness.js contains the data-test-top rect patch (grep `data-test-top` in harness.js)
    - Suite completes and the node process exits without hanging (no leaked watchdog timers)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture HTML → jsdom test environment | dialog.html runs with runScripts 'dangerously' — page-realm script execution inside the test process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Elevation of privilege | dialog fixture under runScripts 'dangerously' | mitigate | 'dangerously' confined to the single checked-in, markup-script-free dialog.html (the only executing script is the capture's own interceptor); all other fixtures run 'outside-only'; fixtures frozen in repo — no external content |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan; lockfile from Plan 01-01 governs CI |
| T-01-03 | Tampering | sanitization gap in serialized fixture output | accept | Parity-only phase (D-11); fixtures are trusted repo content; Phase 3 SEC-01 owns the fix |
</threat_model>

<verification>
- `npm test` green: protocol + oracle matrix (>= 10 pairs) + guards
- Regenerating truncation-overflow.html twice yields identical sha-256 hashes
- Truncation and dialog defenses provably exercised (explicit guards, not just deep-equal)
</verification>

<success_criteria>
- Oracle green reference-vs-reference across the complete frozen fixture x scenario matrix — every defense from phase success criterion 4 has a corresponding oracle pair, and the D-09 gate ("oracle exists and is green before the first serializer refactor") is fully satisfied
</success_criteria>

<output>
Create `.planning/phases/01-capture-core-extraction-differential-oracle/01-02-SUMMARY.md` when done.
</output>
