---
phase: 01-capture-core-extraction-differential-oracle
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - tests/capture-purity.test.js
  - src/capture/index.js
  - package.json
autonomous: true
requirements: [CAPT-01, CAPT-02]
tags: [capture-core, extraction, transport-seam, purity]

must_haves:
  truths:
    - "createCapture is importable from the @fullselfbrowsing/phantom-stream/capture subpath and from src/capture/index.js"
    - "The capture core contains zero chrome.* and zero window.FSB references, enforced by a static-scan test that runs in CI"
    - "Importing src/capture/index.js in bare Node (no DOM) succeeds — no DOM access at module top level"
    - "The full existing suite (protocol + oracle ref-vs-ref matrix) stays green — extraction lands without touching the reference or the harness"
  artifacts:
    - path: "src/capture/index.js"
      provides: "Single-file capture core: createCapture({transport, logger, overlayProvider, skipElement}) -> {start, stop, pause, resume}"
      exports: ["createCapture"]
      min_lines: 600
    - path: "tests/capture-purity.test.js"
      provides: "Static-scan purity gate (grep-equivalent, portable, runs in CI)"
    - path: "package.json"
      provides: "./capture subpath export"
      contains: "./src/capture/index.js"
  key_links:
    - from: "src/capture/index.js"
      to: "src/protocol/constants.js"
      via: "imported numeric constants (never redefined)"
      pattern: "from '\\.\\./protocol/constants\\.js'"
    - from: "src/capture/index.js"
      to: "src/protocol/messages.js"
      via: "STREAM, NID_ATTR, createStreamSessionId imports"
      pattern: "from '\\.\\./protocol/messages\\.js'"
    - from: "package.json"
      to: "src/capture/index.js"
      via: "exports map"
      pattern: "\"\\./capture\""
---

<objective>
Extract `reference/extension/dom-stream.js` (1,117 lines — the frozen spec) into a single-file `src/capture/index.js` behind the injected Transport seam, with the purity gate written first (red → green). Implements locked decisions D-05 (factory shape), D-06 (resume does NOT re-snapshot — USER OVERRIDE), D-07 (Transport contract), D-08 (options surface), D-10 (single-file extraction — USER OVERRIDE; the 5-module split is deliberately deferred beyond Phase 1), D-11 (parity-only — no behavioral changes), D-12 (side channels extracted behind seams), D-13 (purity static-scan).

Purpose: this is the SDK seam FSB plugs back into and the framework everyone else imports. Parity, not improvement, is the bar — every "fix" temptation (sanitization, computed styles on added nodes, WeakMap identity) is a later phase.
Output: `src/capture/index.js` with createCapture, purity test green, `./capture` package export. Equivalence proof lands in Plan 01-04 (oracle flip); lifecycle/defense tests in Plan 01-05.
</objective>

## Phase Goal

**As a** host developer embedding PhantomStream, **I want to** run the capture core in any injection context through an injected Transport, **so that** I get a capture stream provably equivalent to the shipped FSB reference.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-capture-core-extraction-differential-oracle/01-CONTEXT.md
@.planning/phases/01-capture-core-extraction-differential-oracle/01-PATTERNS.md
@.planning/phases/01-capture-core-extraction-differential-oracle/01-RESEARCH.md
@reference/extension/dom-stream.js
@src/protocol/constants.js
@src/protocol/messages.js
@src/protocol/envelope.js
@src/capture/README.md
</context>

<interfaces>
From src/protocol/constants.js (import — never redefine):
```javascript
export const RELAY_PER_MESSAGE_LIMIT_BYTES = 1048576; // 1 MiB
export const SNAPSHOT_BUDGET_BYTES; // Math.floor(limit * 0.8) = 838860 — replaces reference line 549's inline computation
export const TRUNCATION_VIEWPORT_MULTIPLIER = 3;
export const SCROLL_THROTTLE_MS = 200;
export const OVERLAY_THROTTLE_MS = 500;
export const MUTATION_STALE_THRESHOLD_MS = 5000;
export const WATCHDOG_TICK_MS = 500;
export const INLINE_STYLE_MAX_BYTES = 500000;
```

From src/protocol/messages.js (import):
```javascript
export const STREAM = { SNAPSHOT, MUTATIONS, SCROLL, OVERLAY, DIALOG, READY, ... };
export const NID_ATTR = 'data-fsb-nid';
export function createStreamSessionId(nowMs, rand); // 'stream_' + nowMs.toString(36) + '_' + rand
```

Style analog for injected dependencies + JSDoc typedefs: src/protocol/envelope.js (LZCodec injected-codec pattern, `var` + `||` defaulting for cross-runtime files).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Purity gate first (red)</name>
  <files>tests/capture-purity.test.js</files>
  <read_first>
    - reference/tests/dom-stream-perf.test.js (lines 32–44: source-text invariant scan strategy)
    - tests/protocol.test.js (node:test style: flat test(), node:assert/strict, full-sentence names)
    - .planning/phases/01-capture-core-extraction-differential-oracle/01-RESEARCH.md ("Purity static-scan test" code example + the FSB-coupling seam inventory)
  </read_first>
  <behavior>
    - Test 1: "capture core exists" — readdirSync('src/capture') filtered to .js files yields at least one entry (RED now: src/capture/ contains only README.md)
    - Test 2: "capture core contains zero chrome.* and window.FSB references" — for every src/capture/*.js file, after stripping // line comments and /* */ block comments, the source matches neither /\bchrome\s*\./ nor /window\.FSB|\bFSB\b/
  </behavior>
  <action>
    Create tests/capture-purity.test.js per the RESEARCH.md example, refined: read every `.js` file under src/capture/ with node:fs readdirSync/readFileSync; strip line AND block comments before applying the FSB regex (so provenance comments like "extracted from FSB dom-stream.js" stay legal); assert zero matches for /\bchrome\s*\./ and /window\.FSB|\bFSB\b/ with per-file failure messages. Include the file-count >= 1 assertion so the gate cannot pass vacuously before extraction exists. Note: the wire attribute value 'data-fsb-nid' must come from the NID_ATTR import in the capture core, so the literal never needs to appear in capture source and the FSB regex stays strict (string literals are NOT comment-stripped — `data-fsb-` inside a string would trip \bFSB\b only if uppercase; the regexes target `chrome.` API calls and the FSB namespace, lowercase 'fsb-dialog' event-name literals are fine).

    This is the portable grep-equivalent CI enforcement for phase success criterion 2 (D-13). It must FAIL right now for the right reason (no .js files in src/capture yet).
  </action>
  <verify>
    <automated>node --test tests/capture-purity.test.js; test $? -ne 0 && node --test tests/capture-purity.test.js 2>&1 | grep -qi "capture"</automated>
  </verify>
  <done>
    Purity test exists and is RED with a failure message indicating src/capture has no modules yet (red for the right reason — goes green in Task 2).
  </done>
  <acceptance_criteria>
    - `node --test tests/capture-purity.test.js` exits NON-zero before Task 2 lands
    - The failing assertion message names src/capture (not an unrelated crash like ENOENT on the directory — handle the directory-exists case; src/capture/ exists with README.md)
    - Test file uses node:test + node:assert/strict, flat test() calls, no describe blocks
    - Comment stripping happens BEFORE the FSB regex (grep test source for a replace that removes comments)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Single-file extraction of the capture core behind the Transport seam</name>
  <files>src/capture/index.js, package.json</files>
  <read_first>
    - reference/extension/dom-stream.js (FULL FILE — this is the spec; the extraction ports it section by section)
    - .planning/phases/01-capture-core-extraction-differential-oracle/01-PATTERNS.md (section "src/capture/index.js" — the complete seam map with reference line numbers; read before writing any code)
    - .planning/phases/01-capture-core-extraction-differential-oracle/01-RESEARCH.md (Reference Source Map table + "Complete FSB-coupling seam inventory")
    - src/protocol/envelope.js (JSDoc typedef + injected-dependency + var/|| style to replicate)
    - src/protocol/constants.js and src/protocol/messages.js (exact import names)
    - package.json (exports map shape)
  </read_first>
  <action>
    Create `src/capture/index.js` — a single ESM file (D-10: the serializer/differ/side-channels/session 5-module split from src/capture/README.md is deliberately DEFERRED beyond Phase 1; parity is the exit bar). Port the reference behavior VERBATIM (D-11) with exactly these transformations and nothing else:

    **Factory + state.** Named export `createCapture(config)` returning `{ start, stop, pause, resume }` (D-05 — exactly these four methods; no refresh() this phase: hosts use stop()/start() for a fresh snapshot, documented in Plan 01-05's README task). Reference module-level vars (lines 13–34: streaming, mutationObserver, batchTimer, pendingMutations, nextNodeId, scrollHandler, lastScrollSend, dialogRelayActive, lastOverlayBroadcast, streamSessionId, currentSnapshotId, lastDrainTs, staleFlushCount, watchdogTimer) become per-factory closure state so two captures can coexist in the oracle process. NO DOM access at module top level — all window/document dereferences happen inside createCapture or the functions it builds (ambient globals, exactly like the reference; injection contexts and the test harness both provide them).

    **Config seams (D-07, D-08).** `config.transport` required: `{ send(type, payload), flush?() }`; validate `typeof transport.send === 'function'` at factory time and throw a plain Error with a lowercase-hyphenated message ('transport-send-required') — factory-time validation is allowed to throw; the capture path after start is not. `flush` defaults to a no-op via typeof guard. Every send is wrapped try/catch routing errors to the logger — transport errors NEVER propagate into the capture path (locked D-07). `config.logger` defaults to a console-backed `{ info, warn, error }`; `config.overlayProvider` defaults to null; `config.skipElement` defaults to a predicate returning false. Define `@typedef {Object} Transport` and `@typedef {Object} CaptureOptions` JSDoc blocks following the LZCodec pattern in envelope.js. Use `var` + `||` defaulting inline (cross-runtime convention per CLAUDE.md), named exports only, explicit `.js` import extensions.

    **Imports (never redefine).** From `../protocol/constants.js`: RELAY_PER_MESSAGE_LIMIT_BYTES, SNAPSHOT_BUDGET_BYTES (replaces the reference's inline `Math.floor(... * 0.8)` at line 549), TRUNCATION_VIEWPORT_MULTIPLIER, SCROLL_THROTTLE_MS, OVERLAY_THROTTLE_MS, MUTATION_STALE_THRESHOLD_MS, WATCHDOG_TICK_MS, INLINE_STYLE_MAX_BYTES. From `../protocol/messages.js`: STREAM, NID_ATTR (replaces ~14 'data-fsb-nid' literals), createStreamSessionId — called as createStreamSessionId(Date.now(), Math.random().toString(36).slice(2, 8)) inside beginStreamSession, preserving reference line 124's wire format byte-for-byte. The URL_ATTRS, STYLE_DEFAULTS (24 entries), CURATED_PROPS (~85 entries), SHELL_PROPS prop lists are NOT in protocol — copy them verbatim into the capture module with their reference comments.

    **Transport seam (8 sites).** Each chrome.runtime.sendMessage call becomes transport.send with the protocol type: dialog open (ref 221) and dialog dismiss (ref 240) → STREAM.DIALOG with payload { dialog: ... } preserving attachStreamMetadata; mutation flush (ref 750) → STREAM.MUTATIONS with { mutations, streamSessionId, snapshotId, staleFlushCount }; stop-path final flush (ref 858–864) → STREAM.MUTATIONS WITHOUT staleFlushCount — PARITY TRAP: preserve this asymmetry exactly; scroll (ref 897) → STREAM.SCROLL; overlay (ref 987) → STREAM.OVERLAY; snapshot on start (ref 1029) → STREAM.SNAPSHOT. Drop the `typeof rateLimitedWarn`/`redactForLog` guarded fallbacks entirely (typeof-guarded loose globals; logger replaces them; wire-invisible).

    **Lifecycle (D-06).** start(): re-injection guard (if streaming, stop observers + scroll tracker first, ref 1017–1043) → beginStreamSession() → serializeDOM() → send STREAM.SNAPSHOT → startMutationStream() → startScrollTracker() → streaming = true → broadcastOverlayState(true). stop(): stop both trackers (final flush included), streaming = false (ref 1045–1051). pause(): stop observers + scroll, KEEP streaming = true (ref 1053–1059, comment "paused state, not stopped"). resume(): re-arm observers + scroll tracker ONLY — do NOT call beginStreamSession, do NOT call serializeDOM, do NOT send any snapshot; the same streamSessionId/currentSnapshotId continue (USER OVERRIDE D-06; the reference's 1061–1080 body re-snapshots — explicitly do not copy it; this is divergence-ledger entry D1, declared in Plan 01-04). Mutations occurring while paused are missed by design (host contract, documented in Plan 01-05). pingDomStream readiness probe (ref 1007–1015): dropped — hosts call functions directly (ledger D4). domStreamReady ping at script load (ref 1109): becomes transport.send(STREAM.READY, {}) once at factory-creation time (transport exists at factory time; closest analog to the reference's load-moment ping; any residual timing difference is ledger D3).

    **Defense mechanics — copy verbatim (CAPT-03).** MutationObserver config { childList, attributes, characterData, subtree, attributeOldValue: all true } on document.body; observer callback pushes into pendingMutations and re-arms batchTimer via cancelAnimationFrame/requestAnimationFrame(flushMutations) (ref 784–800). Watchdog: self-re-arming setTimeout CHAIN (NOT setInterval) at WATCHDOG_TICK_MS; fires force-flush when pendingMutations.length > 0 && Date.now() - lastDrainTs > MUTATION_STALE_THRESHOLD_MS; increments staleFlushCount BEFORE the forced flush; whole tick body in try/catch (ref 801–828, "watchdog must not crash"). Serializer (ref 266–609): clone document.body, parallel TreeWalker walk stamping NID_ATTR on BOTH live element and clone, strip script/noscript, single-pass live-DOM read building the rect Map (one getBoundingClientRect per element — the single-pass defense), 2-pass budgeted truncation against SNAPSHOT_BUDGET_BYTES (keep the chars-as-bytes `html.length` comparison — inherited quirk, preserve for parity per Pitfall 7), URL/xlink/srcset absolutification, canvas-to-img dataURL path, iframe live-src + pointer-events handling, stylesheet + inline-style collection with INLINE_STYLE_MAX_BYTES skip, serializeShellAttributes (which already strips on* attrs on html/body shells ONLY — do NOT extend stripping to other paths; that is Phase 3 SEC-01, see threat T-01-03).

    **skipElement seam.** isFsbOverlay (ref 266–276) becomes options.skipElement at all three call sites: serializer clone-side check (ref 416–424 — pass the clone element to skipElement), mutation added-node filter (ref 661–666), mutation target filter (ref 674). Default predicate returns false → nothing skipped, identical to the reference on a page with no FSB overlay (oracle-invisible on fixtures).

    **overlayProvider seam.** broadcastOverlayState (ref 934–999): replace FSB.actionGlowOverlay/FSB.highlightManager/FSB.overlayState reads with options.overlayProvider (a function returning { glow, progress } or null); no provider → send { glow: null, progress: null, ...identity } preserving FSB-absent reference wire shape (RESEARCH assumption A3 — the oracle verifies). Keep OVERLAY_THROTTLE_MS throttling.

    **Dialog interceptor.** Ref 163–254 extracted as-is: page-realm script injection monkey-patching alert/confirm/prompt relaying via CustomEvents. Keep 'fsb-dialog' / 'fsb-dialog-dismiss' event names and the 'fsb-dialog-interceptor' element id BYTE-IDENTICAL (wire-adjacent; renaming would force a ledger entry — don't). Preserve the reference's install/teardown call points relative to start/stop exactly.

    **Logger.** Keep the exact '[DOM Stream] ...' log strings, routed through options.logger (aids trace-diffing against reference runs).

    **package.json**: add `"./capture": "./src/capture/index.js"` to the exports map, mirroring the existing "./protocol" entry shape.

    Scroll tracker (ref 884–924): passive listener, SCROLL_THROTTLE_MS timestamp throttle, sends scrollX/scrollY + identity — extract verbatim behind the transport seam (D-12).
  </action>
  <verify>
    <automated>node --test tests/capture-purity.test.js && node -e "import('./src/capture/index.js').then(m => process.exit(typeof m.createCapture === 'function' ? 0 : 1))" && node -e "const p=require('./package.json'); process.exit(p.exports['./capture'] === './src/capture/index.js' ? 0 : 1)" && npm test</automated>
  </verify>
  <done>
    Purity gate green (zero chrome./FSB in src/capture); createCapture importable in bare Node with no DOM side effects; ./capture subpath exported; full suite (protocol + oracle ref-vs-ref matrix) still green — the reference and harness are untouched.
  </done>
  <acceptance_criteria>
    - `node --test tests/capture-purity.test.js` exits 0 (Task 1's red test now green)
    - `node -e "import('./src/capture/index.js')"` succeeds in bare Node — no window/document access at module top level
    - createCapture is a named export and a function
    - package.json exports contains `"./capture": "./src/capture/index.js"` alongside "./protocol"
    - grep src/capture/index.js: imports `from '../protocol/constants.js'` and `from '../protocol/messages.js'` both present
    - grep src/capture/index.js after comment-strip: zero matches for `\bchrome\s*\.` and zero for `window\.FSB`
    - grep src/capture/index.js: zero occurrences of the literal `data-fsb-nid` (NID_ATTR import used instead); `fsb-dialog` and `fsb-dialog-interceptor` literals PRESENT (wire-adjacent, preserved)
    - grep src/capture/index.js: `setInterval` zero matches (watchdog is a setTimeout chain)
    - `npm test` exits 0 — reference/extension/dom-stream.js and tests/differential/ are byte-unchanged (git diff on those paths empty)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| observed page DOM → capture serializer | attacker-influenced HTML enters the capture output (snapshot html, add-op subtrees, attr values) |
| host → createCapture config | transport/logger/overlayProvider/skipElement are host-supplied callables executed in the page realm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | serializer output (on* attrs stripped only on html/body shells; javascript: URLs pass through absolutifyUrl) | accept | Intentionally preserved — parity-only locked decision D-11; the differential oracle would flag any unilateral "fix" as a divergence. Phase 3 (SEC-01) introduces the named capture-side sanitization chokepoint across ALL serialization paths. Recorded in src/capture/README.md behavioral-changes list (Plan 01-05) |
| T-01-04 | Denial of service | host-supplied transport.send throwing into the capture path | mitigate | Every send wrapped try/catch → logger; transport errors never propagate (D-07); watchdog tick body try/catch (reference defense preserved) |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan; src/capture stays dependency-free (imports only ../protocol) |
</threat_model>

<verification>
- Purity static-scan green and running under `npm test` (CI inherits it — phase success criterion 2's grep enforcement)
- Bare-Node import smoke proves zero import-time DOM coupling
- Full suite green with reference + harness untouched (git diff empty on reference/ and tests/differential/)
</verification>

<success_criteria>
- src/capture/index.js exists as the single-file extraction (D-10) with all four seams (transport, logger, overlayProvider, skipElement) and locked lifecycle semantics (D-05/D-06/D-07/D-08)
- Zero chrome.runtime / window.FSB references, statically enforced (CAPT-01 enforcement half)
- All reliability defense mechanics ported verbatim (verification lands in Plans 01-04/01-05)
</success_criteria>

<output>
Create `.planning/phases/01-capture-core-extraction-differential-oracle/01-03-SUMMARY.md` when done.
</output>
