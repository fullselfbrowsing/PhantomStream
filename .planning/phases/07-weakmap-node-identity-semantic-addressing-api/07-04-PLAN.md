---
phase: 07-weakmap-node-identity-semantic-addressing-api
plan: 04
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - src/adapters/playwright-inject.js
  - src/capture/README.md
  - src/renderer/README.md
  - docs/ARCHITECTURE.md
  - tests/node-identity-static.test.js
  - tests/renderer-loopback.test.js
  - tests/adapter-exports.test.js
  - tests/playwright-adapter.test.js
  - tests/extension-adapter.test.js
  - tests/bookmarklet-adapter.test.js
autonomous: true
requirements: [CAPT-07, VIEW-03]
must_haves:
  truths:
    - "D-23: tests prove the observed page is not mutated by identity tracking"
    - "D-24: existing NID_ATTR-based assertions migrate to sidecar/API assertions"
    - "D-25: moved live elements keep their nid and remain renderer-addressable after ops apply"
    - "D-26: regression gate fails if identity querySelector hot paths return"
    - "D-27: adapter and demo-relevant test surfaces run after migration"
    - "Phase 7 success criterion: grep gate enforces no nid-attribute identity strings in src/capture or src/renderer"
  artifacts:
    - path: "tests/node-identity-static.test.js"
      provides: "Source-level identity mutation/querySelector regression gate"
    - path: "src/adapters/playwright-inject.js"
      provides: "Updated checked-in classic inject artifact carrying sidecar identity behavior"
    - path: "src/capture/README.md"
      provides: "Documented capture identity and semantic getNodeId contract"
    - path: "src/renderer/README.md"
      provides: "Documented renderer index and semantic resolve/highlight contract"
---

<objective>
Finish the identity migration across injected artifacts, docs, migrated tests, and full-suite verification so every adapter consumes the same Phase 7 contract.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-weakmap-node-identity-semantic-addressing-api/07-CONTEXT.md
@.planning/phases/07-weakmap-node-identity-semantic-addressing-api/07-RESEARCH.md
@.planning/phases/07-weakmap-node-identity-semantic-addressing-api/07-PATTERNS.md
@src/adapters/playwright-inject.js
@src/capture/README.md
@src/renderer/README.md
@docs/ARCHITECTURE.md
@tests/renderer-loopback.test.js
@tests/adapter-exports.test.js
@tests/playwright-adapter.test.js
@tests/extension-adapter.test.js
@tests/bookmarklet-adapter.test.js
@tests/node-identity-static.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate remaining identity-dependent tests and static gates</name>
  <files>tests/node-identity-static.test.js, tests/renderer-loopback.test.js, tests/capture-skip.test.js, tests/security-mask.test.js, tests/renderer-remote-control.test.js</files>
  <read_first>
    - `tests/node-identity-static.test.js`
    - `tests/renderer-loopback.test.js`
    - `tests/capture-skip.test.js`
    - `tests/security-mask.test.js`
    - `tests/renderer-remote-control.test.js`
    - `.planning/phases/07-weakmap-node-identity-semantic-addressing-api/07-VALIDATION.md`
  </read_first>
  <action>
    Replace remaining test assertions that use `NID_ATTR` or `data-fsb-nid` as proof of framework identity with sidecar/API assertions. Block placeholders should be verified by finding their placeholder element and confirming its corresponding nid is present in `payload.nodeIds`, not by reading a `data-fsb-nid` attribute. Loopback and remote-control-adjacent tests should build snapshots/add ops with `nodeIds`. Extend `tests/node-identity-static.test.js` so it reads `src/capture/index.js`, `src/renderer/index.js`, `src/renderer/diff.js`, and `src/renderer/overlays.js`, and asserts the exact strings `data-fsb-nid`, `NID_ATTR`, and `querySelector('[' + NID_ATTR` are absent from those runtime files.
  </action>
  <verify>
    <automated>node --test tests/node-identity-static.test.js tests/renderer-loopback.test.js tests/capture-skip.test.js tests/security-mask.test.js tests/renderer-remote-control.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "data-fsb-nid" tests/node-identity-static.test.js` returns a match.
    - `grep -n "NID_ATTR" tests/node-identity-static.test.js` returns a match.
    - `node --test tests/node-identity-static.test.js tests/renderer-loopback.test.js tests/capture-skip.test.js tests/security-mask.test.js tests/renderer-remote-control.test.js` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update checked-in inject artifact for identity sidecars</name>
  <files>src/adapters/playwright-inject.js, tests/adapter-exports.test.js, tests/playwright-adapter.test.js, tests/extension-adapter.test.js, tests/bookmarklet-adapter.test.js</files>
  <read_first>
    - `src/adapters/playwright-inject.js`
    - `src/adapters/browser-inject.js`
    - `src/capture/index.js`
    - `tests/adapter-exports.test.js`
    - `tests/playwright-adapter.test.js`
    - `tests/extension-adapter.test.js`
    - `tests/bookmarklet-adapter.test.js`
  </read_first>
  <action>
    Update `src/adapters/playwright-inject.js` so the checked-in classic script capture artifact contains the same WeakMap identity mirror and `nodeIds` sidecar behavior as `src/capture/index.js`, while still containing no ESM `import ` or `export ` syntax. Keep bridge globals required by adapter tests: `window.__phantomStreamBridge`, `window.__phantomStreamStart`, and `window.__phantomStreamStop`. If `src/adapters/browser-inject.js` derives source from this artifact, no API change is needed; otherwise update it so extension/bookmarklet helpers receive the current artifact.
  </action>
  <verify>
    <automated>node --test tests/adapter-exports.test.js tests/playwright-adapter.test.js tests/extension-adapter.test.js tests/bookmarklet-adapter.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "nodeIds" src/adapters/playwright-inject.js` returns a match.
    - `grep -n "WeakMap" src/adapters/playwright-inject.js` returns a match.
    - `grep -n "import " src/adapters/playwright-inject.js` returns no matches.
    - `grep -n "export " src/adapters/playwright-inject.js` returns no matches.
    - `node --test tests/adapter-exports.test.js tests/playwright-adapter.test.js tests/extension-adapter.test.js tests/bookmarklet-adapter.test.js` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update documentation for the new identity model</name>
  <files>src/capture/README.md, src/renderer/README.md, docs/ARCHITECTURE.md</files>
  <read_first>
    - `src/capture/README.md`
    - `src/renderer/README.md`
    - `docs/ARCHITECTURE.md`
    - `.planning/phases/07-weakmap-node-identity-semantic-addressing-api/07-CONTEXT.md`
  </read_first>
  <action>
    Update `src/capture/README.md` to document that capture uses an internal WeakMap identity mirror, emits `nodeIds` sidecars on snapshots/add ops, and exposes `getNodeId(element) -> string|null`. Update `src/renderer/README.md` to document the internal `Map<nid, Node>` renderer index, the absence of per-op identity querySelector, and viewer handle methods `resolveNode`, `highlightNode`, and `clearHighlight`. Update `docs/ARCHITECTURE.md` section 2.1 and known limitations section so `data-fsb-nid` stamping is described as the former reference design and WeakMap sidecar identity as the standalone framework design after Phase 7. Keep shadow DOM, iframe, CSSOM, and subtree fetch limitations deferred.
  </action>
  <verify>
    <automated>grep -n "nodeIds" src/capture/README.md src/renderer/README.md docs/ARCHITECTURE.md && grep -n "getNodeId" src/capture/README.md && grep -n "resolveNode" src/renderer/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "WeakMap" src/capture/README.md docs/ARCHITECTURE.md` returns matches in both files.
    - `grep -n "nodeIds" src/capture/README.md src/renderer/README.md docs/ARCHITECTURE.md` returns matches in all three files.
    - `grep -n "resolveNode" src/renderer/README.md` returns a match.
    - `grep -n "highlightNode" src/renderer/README.md` returns a match.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Run full Phase 7 regression gate</name>
  <files>tests/node-identity-static.test.js, package.json</files>
  <read_first>
    - `package.json`
    - `tests/node-identity-static.test.js`
    - `.planning/phases/07-weakmap-node-identity-semantic-addressing-api/07-VALIDATION.md`
  </read_first>
  <action>
    Run the final focused identity/adapters command and then `npm test`. If failures are due to outdated tests still expecting identity attributes, update those tests to use `nodeIds`, `getNodeId`, `resolveNode`, or sidecar-aware fixture helpers. Do not weaken security/masking assertions, sandbox assertions, relay rawness tests, or remote-control authorization tests. Leave `tests/node-identity-static.test.js` as the final source-level guard.
  </action>
  <verify>
    <automated>node --test tests/capture-identity.test.js tests/renderer-diff.test.js tests/renderer-overlays.test.js tests/semantic-addressing.test.js tests/node-identity-static.test.js tests/renderer-loopback.test.js tests/adapter-exports.test.js tests/playwright-adapter.test.js tests/extension-adapter.test.js tests/bookmarklet-adapter.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - The focused command in `<verify>` exits 0.
    - `npm test` exits 0.
    - `tests/node-identity-static.test.js` remains present and referenced by the final focused command.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source docs -> future implementers | Docs become the public contract for identity behavior. |
| Inject artifact -> adapter consumers | Classic script must match ESM capture behavior without imports. |
| Full suite -> release confidence | Existing security, adapter, and renderer guarantees must survive identity migration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Tampering | checked-in inject artifact | mitigate | Adapter tests assert bridge hooks, no imports/exports, and sidecar identity behavior. |
| T-07-11 | Information Disclosure | docs/API | mitigate | Docs specify geometry/identity-only API output by default. |
| T-07-12 | Denial of Service | stale test contract | mitigate | Full `npm test` and static gates catch partial migration or querySelector regression. |
</threat_model>

<verification>
```bash
node --test tests/capture-identity.test.js tests/renderer-diff.test.js tests/renderer-overlays.test.js tests/semantic-addressing.test.js tests/node-identity-static.test.js tests/renderer-loopback.test.js tests/adapter-exports.test.js tests/playwright-adapter.test.js tests/extension-adapter.test.js tests/bookmarklet-adapter.test.js
npm test
```
</verification>

<success_criteria>
- Runtime capture/renderer source no longer depends on framework-owned identity attributes.
- Browser inject artifact carries the new sidecar identity behavior.
- Public docs describe WeakMap identity, nodeIds sidecars, and semantic addressing APIs.
- Adapter and loopback tests pass with the new contract.
- Full `npm test` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/07-weakmap-node-identity-semantic-addressing-api/07-04-SUMMARY.md`.
</output>
