---
phase: 07-weakmap-node-identity-semantic-addressing-api
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/renderer/diff.js
  - src/renderer/index.js
  - src/renderer/overlays.js
  - tests/renderer-diff.test.js
  - tests/renderer-overlays.test.js
  - tests/node-identity-static.test.js
autonomous: true
requirements: [VIEW-03, CAPT-07]
must_haves:
  truths:
    - "D-17: renderer diff application uses an internal Map<nid, Node> index"
    - "D-18: renderer index rebuilds on accepted snapshots"
    - "D-19: add/remove ops update the renderer index incrementally"
    - "D-20: overlay anchor resolution uses the same renderer index"
    - "D-21: sanitization happens before indexing imported nodes"
  artifacts:
    - path: "src/renderer/index.js"
      provides: "Renderer identity index lifecycle and indexed overlay resolver"
    - path: "src/renderer/diff.js"
      provides: "Diff applier that resolves nids via injected identity hooks"
    - path: "tests/node-identity-static.test.js"
      provides: "Regression gate against identity querySelector hot paths"
---

<objective>
Move renderer nid resolution from DOM attribute selectors to an internal incremental index used by diff application and overlay anchoring.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-weakmap-node-identity-semantic-addressing-api/07-CONTEXT.md
@.planning/phases/07-weakmap-node-identity-semantic-addressing-api/07-RESEARCH.md
@.planning/phases/07-weakmap-node-identity-semantic-addressing-api/07-PATTERNS.md
@src/renderer/index.js
@src/renderer/diff.js
@src/renderer/overlays.js
@tests/renderer-diff.test.js
@tests/renderer-overlays.test.js
@tests/renderer-viewer.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED renderer index and static hot-path tests</name>
  <files>tests/renderer-diff.test.js, tests/renderer-overlays.test.js, tests/node-identity-static.test.js</files>
  <read_first>
    - `tests/renderer-diff.test.js`
    - `tests/renderer-overlays.test.js`
    - `src/renderer/diff.js`
    - `src/renderer/index.js`
    - `.planning/phases/07-weakmap-node-identity-semantic-addressing-api/07-VALIDATION.md`
  </read_first>
  <action>
    Update renderer tests to construct snapshots/add ops with `nodeIds` sidecars and no framework identity attributes in `html`. Add assertions that mutations still apply by nid through the sidecar-built index: add inserts under `parentNid`, attr sets the target attribute, text sets target text, remove deletes target and makes later resolution stale. Update overlay tests so a payload `{ glow: { nid: '2' } }` resolves through the viewer identity index rather than a mirrored DOM attribute. Create `tests/node-identity-static.test.js` that reads `src/renderer/diff.js`, `src/renderer/index.js`, and `src/renderer/overlays.js` and asserts none contains the exact substring `querySelector('[' + NID_ATTR` and none contains the exact string `[data-fsb-nid`.
  </action>
  <verify>
    <automated>set +e; node --test tests/renderer-diff.test.js tests/renderer-overlays.test.js tests/node-identity-static.test.js; code=$?; test "$code" -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `tests/node-identity-static.test.js` contains `querySelector('[' + NID_ATTR`.
    - `tests/node-identity-static.test.js` contains `[data-fsb-nid`.
    - `tests/renderer-diff.test.js` contains `nodeIds`.
    - The RED verify command exits 0 only because the focused renderer tests fail before implementation.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement renderer identity index lifecycle</name>
  <files>src/renderer/index.js, src/renderer/diff.js, tests/renderer-diff.test.js, tests/renderer-overlays.test.js</files>
  <read_first>
    - `src/renderer/index.js`
    - `src/renderer/diff.js`
    - `src/renderer/overlays.js`
    - `tests/renderer-diff.test.js`
    - `tests/renderer-overlays.test.js`
  </read_first>
  <action>
    In `src/renderer/index.js`, add closure-local `nidToNode = new Map()` and `nodeToNid = new WeakMap()` plus helpers `resetIdentityIndex(doc, nodeIds)`, `resolveIndexedNode(nid)`, `indexSubtree(root, nodeIds)`, and `removeIndexedSubtree(root)`. `resetIdentityIndex` walks `doc.body` element descendants in document order and pairs them with `payload.nodeIds`; missing or extra ids should log `[Renderer] identity sidecar mismatch` and continue with the ids that can be paired. In `handleSnapshot`, after `iframe.srcdoc = buildSnapshotHtml(p)` and once `contentDocument.body` is available, call `resetIdentityIndex(iframe.contentDocument, p.nodeIds || [])`. Pass identity hooks into `applyMutations`: `{ resolve, indexSubtree, removeSubtree }`. Replace `resolveNidRect` querySelector logic with `resolveIndexedNode(nid)`. Clear both maps in `destroy()`.
  </action>
  <verify>
    <automated>node --test tests/renderer-diff.test.js tests/renderer-overlays.test.js tests/renderer-viewer.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "nidToNode" src/renderer/index.js` returns a match.
    - `grep -n "resetIdentityIndex" src/renderer/index.js` returns a match.
    - `grep -n "resolveIndexedNode" src/renderer/index.js` returns a match.
    - `node --test tests/renderer-diff.test.js tests/renderer-overlays.test.js tests/renderer-viewer.test.js` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Refactor diff applier to indexed resolution</name>
  <files>src/renderer/diff.js, tests/renderer-diff.test.js, tests/node-identity-static.test.js</files>
  <read_first>
    - `src/renderer/diff.js`
    - `src/renderer/index.js`
    - `tests/renderer-diff.test.js`
    - `tests/node-identity-static.test.js`
  </read_first>
  <action>
    In `src/renderer/diff.js`, remove the local `selectByNid` querySelector implementation. Read `hooks.identity` and derive `resolve(nid)`, `indexSubtree(root, nodeIds)`, and `removeSubtree(root)` functions with no-op fallbacks. For add ops, resolve `m.parentNid` and `m.beforeNid` through `resolve`, sanitize the template content, import the first element, insert it, then call `indexSubtree(imported, m.nodeIds || [])`. For remove ops, resolve `m.nid`, call `removeSubtree(el)` before `parentNode.removeChild(el)`, and preserve existing stale-miss and apply-failure thresholds. Attr and text ops resolve through the same `resolve` function. Do not call querySelector for nid resolution in this file.
  </action>
  <verify>
    <automated>node --test tests/renderer-diff.test.js tests/node-identity-static.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "hooks.identity" src/renderer/diff.js` returns a match.
    - `grep -n "indexSubtree" src/renderer/diff.js` returns a match.
    - `grep -n "removeSubtree" src/renderer/diff.js` returns a match.
    - `node --test tests/renderer-diff.test.js tests/node-identity-static.test.js` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wire sidecar -> renderer index | Opaque ids from capture become local Node references. |
| Renderer index -> overlays | Overlay payloads from capture resolve into host-document geometry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | renderer index | mitigate | Tests cover sidecar mismatch, add/remove updates, and stale-miss resync behavior. |
| T-07-05 | Denial of Service | diff apply hot path | mitigate | Static gate prevents per-op identity querySelector from returning. |
| T-07-06 | Information Disclosure | overlay resolution | mitigate | Resolver returns geometry only and does not expose text/html/attrs. |
</threat_model>

<verification>
```bash
node --test tests/renderer-diff.test.js tests/renderer-overlays.test.js tests/renderer-viewer.test.js tests/node-identity-static.test.js
npm test
```
</verification>

<success_criteria>
- Renderer applies diff ops through an internal `Map<nid, Node>`.
- Overlay anchoring uses the same resolver.
- Add/remove ops update the index.
- Static tests reject the old identity querySelector hot path.
</success_criteria>

<output>
After completion, create `.planning/phases/07-weakmap-node-identity-semantic-addressing-api/07-02-SUMMARY.md`.
</output>

