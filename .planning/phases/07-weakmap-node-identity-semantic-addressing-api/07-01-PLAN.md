---
phase: 07-weakmap-node-identity-semantic-addressing-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/protocol/messages.js
  - src/capture/index.js
  - tests/capture-identity.test.js
  - tests/differential/normalize.js
  - tests/differential/divergence-ledger.js
autonomous: true
requirements: [CAPT-07, VIEW-03]
must_haves:
  truths:
    - "D-03: capture internals stop using framework-owned live-page identity attributes"
    - "D-04: diff ops continue to carry opaque string nid, parentNid, and beforeNid fields"
    - "D-05: capture identity is an internal WeakMap-based mirror with reverse lookup"
    - "D-06: fresh start resets the identity mirror; resume continues the same session"
    - "D-07: moved live elements reuse existing nids"
    - "D-08: snapshot and add-op identity travels through structured nodeIds sidecars"
    - "D-09: oracle divergence is limited to identity markup removal"
    - "D-10: page-owned data-fsb-nid attributes are ordinary page data, not PhantomStream identity"
    - "D-25: move-preservation coverage proves a moved live element keeps the same nid"
  artifacts:
    - path: "tests/capture-identity.test.js"
      provides: "Wave 0 coverage for no live-page identity mutation, sidecars, move preservation, and capture getNodeId"
    - path: "src/capture/index.js"
      provides: "WeakMap-backed capture identity mirror and nodeIds sidecars"
    - path: "src/protocol/messages.js"
      provides: "JSDoc wire contract for SnapshotPayload.nodeIds and add-op nodeIds"
---

<objective>
Replace capture-side `data-fsb-nid` identity writes with an internal node mirror and structured `nodeIds` sidecars while preserving existing nid-addressed diff op fields.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/07-weakmap-node-identity-semantic-addressing-api/07-CONTEXT.md
@.planning/phases/07-weakmap-node-identity-semantic-addressing-api/07-RESEARCH.md
@.planning/phases/07-weakmap-node-identity-semantic-addressing-api/07-PATTERNS.md
@.planning/phases/07-weakmap-node-identity-semantic-addressing-api/07-VALIDATION.md
@src/protocol/messages.js
@src/capture/index.js
@tests/differential/normalize.js
@tests/differential/divergence-ledger.js
@tests/security-mask.test.js
@tests/capture-skip.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED capture identity tests</name>
  <files>tests/capture-identity.test.js</files>
  <read_first>
    - `tests/capture-lifecycle.test.js`
    - `tests/capture-skip.test.js`
    - `tests/security-mask.test.js`
    - `src/capture/index.js`
    - `src/protocol/messages.js`
    - `.planning/phases/07-weakmap-node-identity-semantic-addressing-api/07-VALIDATION.md`
  </read_first>
  <action>
    Create `tests/capture-identity.test.js` with `node:test`, `node:assert/strict`, and the existing jsdom setup style from capture tests. Add RED tests requiring these exact behaviors: after `createCapture({ transport }).start()`, `document.querySelectorAll('[data-fsb-nid]').length === 0`; a page `MutationObserver` watching `{ attributes: true, subtree: true }` records no mutation with `attributeName === 'data-fsb-nid'`; a fixture element that already has `data-fsb-nid="page-owned"` keeps that value as ordinary page data and `capture.getNodeId(element)` returns PhantomStream's internal nid instead of `page-owned`; the first `STREAM.SNAPSHOT` payload includes `Array.isArray(payload.nodeIds) === true` and `payload.nodeIds.length` equals the number of serialized elements parsed from `payload.html`; adding `<section id="late"><span>late</span></section>` emits one add op with `Array.isArray(op.nodeIds) === true` and `op.nodeIds.length === 2`; moving an existing element emits add/remove-compatible ops that keep the original nid for the moved element; and `capture.getNodeId(element)` returns that same nid for tracked elements and `null` for untracked nodes.
  </action>
  <verify>
    <automated>set +e; node --test tests/capture-identity.test.js; code=$?; test "$code" -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `tests/capture-identity.test.js` contains the exact string `document.querySelectorAll('[data-fsb-nid]').length`.
    - `tests/capture-identity.test.js` contains the exact string `attributeName === 'data-fsb-nid'`.
    - `tests/capture-identity.test.js` contains the exact string `page-owned`.
    - `tests/capture-identity.test.js` asserts `Array.isArray(payload.nodeIds)`.
    - The RED verify command exits 0 only because `node --test tests/capture-identity.test.js` exits non-zero before implementation.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement capture WeakMap mirror and nodeIds sidecars</name>
  <files>src/protocol/messages.js, src/capture/index.js, tests/capture-identity.test.js</files>
  <read_first>
    - `src/capture/index.js`
    - `src/protocol/messages.js`
    - `tests/capture-identity.test.js`
    - `tests/security-mask.test.js`
    - `tests/capture-skip.test.js`
  </read_first>
  <action>
    In `src/protocol/messages.js`, document `SnapshotPayload.nodeIds` as `string[]` and document add diff ops as `{ op:'add', parentNid, html, beforeNid|null, nodeIds:string[] }`. In `src/capture/index.js`, replace identity stamping with closure-local helpers using `WeakMap<Element, string>` and `Map<string, Element>`: `ensureNodeId(element)`, `getTrackedNodeId(element)`, `clearNodeMirror()`, and `buildNodeIdSidecar(root, cloneToNid)`. `ensureNodeId` must reuse an existing id for moved nodes. `start()` must clear the mirror and reset `nextNodeId = 1` before snapshot serialization; `resume()` must not clear it. Snapshot payloads must include `nodeIds` in final serialized element preorder. Add ops from `processAddedNode` must include `nodeIds` for the added root and descendants in final wire-html preorder. Replace live `NID_ATTR` reads in childList, attr, characterData, truncation, and blocked placeholder paths with mirror helper calls. Return `getNodeId` on the capture handle beside `start`, `stop`, `pause`, and `resume`; it returns `null` when the argument is not an active tracked element.
  </action>
  <verify>
    <automated>node --test tests/capture-identity.test.js tests/capture-lifecycle.test.js tests/capture-skip.test.js tests/security-mask.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "WeakMap" src/capture/index.js` returns at least one match.
    - `grep -n "nodeIds" src/protocol/messages.js src/capture/index.js tests/capture-identity.test.js` returns matches in all three files.
    - `grep -n "getNodeId" src/capture/index.js tests/capture-identity.test.js` returns matches in both files.
    - `node --test tests/capture-identity.test.js tests/capture-lifecycle.test.js tests/capture-skip.test.js tests/security-mask.test.js` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Preserve differential oracle discipline for identity markup removal</name>
  <files>tests/differential/normalize.js, tests/differential/divergence-ledger.js, tests/differential/oracle.test.js, tests/capture-identity.test.js</files>
  <read_first>
    - `tests/differential/normalize.js`
    - `tests/differential/divergence-ledger.js`
    - `tests/differential/oracle.test.js`
    - `.planning/phases/07-weakmap-node-identity-semantic-addressing-api/07-RESEARCH.md`
  </read_first>
  <action>
    Update the differential normalizer so reference snapshot/add HTML with `data-fsb-nid` attributes and extracted snapshot/add HTML without framework identity attributes compare as the same structural DOM when the raw nid sequence in `nodeIds` matches the reference's preorder nid sequence. Add a divergence-ledger entry named `D8-weakmap-node-identity-sidecar` describing exactly this intentional difference: identity markup removed from extracted HTML, existing diff op fields and nid order preserved. Do not broaden normalization for unrelated attributes, text, styles, sanitization, or mutation op ordering.
  </action>
  <verify>
    <automated>node --test tests/differential/oracle.test.js tests/capture-identity.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "D8-weakmap-node-identity-sidecar" tests/differential/divergence-ledger.js` returns one match.
    - `grep -n "data-fsb-nid" tests/differential/normalize.js` returns a match in logic that removes only framework identity attrs for comparison.
    - `node --test tests/differential/oracle.test.js tests/capture-identity.test.js` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Observed page -> capture identity mirror | Page DOM nodes are tracked without mutating page-visible attributes. |
| Capture -> wire payload | Internal identities cross into structured payload sidecars. |
| Reference oracle -> extracted oracle | Intentional identity markup removal must not hide other drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | observed page DOM | mitigate | Tests assert no `data-fsb-nid` attributes and no identity attribute MutationObserver records. |
| T-07-02 | Information Disclosure | sidecar metadata | mitigate | Sidecars contain opaque ids only; no text/html/attrs are added to telemetry. |
| T-07-03 | Repudiation | differential oracle | mitigate | Ledger D8 records the exact intentional divergence and normalizer remains narrow. |
</threat_model>

<verification>
```bash
node --test tests/capture-identity.test.js tests/capture-lifecycle.test.js tests/capture-skip.test.js tests/security-mask.test.js
node --test tests/differential/oracle.test.js tests/capture-identity.test.js
npm test
```
</verification>

<success_criteria>
- Capture no longer writes framework identity attributes to the observed page.
- Snapshot and add ops carry `nodeIds` sidecars.
- Existing nid diff fields remain present.
- Move preservation is covered by tests.
- Differential oracle passes with only the identity markup divergence ledgered.
</success_criteria>

<output>
After completion, create `.planning/phases/07-weakmap-node-identity-semantic-addressing-api/07-01-SUMMARY.md`.
</output>
