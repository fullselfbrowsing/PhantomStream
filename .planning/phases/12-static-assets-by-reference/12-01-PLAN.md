---
phase: 12-static-assets-by-reference
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/differential/fixtures/static-assets.html
  - tests/renderer-snapshot.test.js
  - tests/relay-asset-bytes.test.js
  - tests/renderer-asset-policy.test.js
  - tests/renderer-asset-gate.test.js
  - tests/capture-asset-degrade.test.js
autonomous: true
requirements: [ASST-01, ASST-02, ASST-05]
must_haves:
  truths:
    - "The CSP srcdoc string pins img-src, pins default-src 'none', has NO script-src, and has NO media-src — accidental widening fails loudly"
    - "A relay byte-identity test proves a snapshot containing image URLs is forwarded receive→send byte-for-byte (no image bytes traverse the relay)"
    - "A focused static-assets fixture exercises img/srcset/picture+source/svg-image/background-image/video-poster/blob-img/oversized-data-img and is registered in the oracle matrix"
    - "RED Wave-0 scaffolds exist for the capture-degrade, renderer-asset-policy, and renderer-asset-gate suites so Plans 02 and 03 implement against named, failing tests"
  artifacts:
    - path: "tests/differential/fixtures/static-assets.html"
      provides: "Focused asset-surface fixture for the oracle"
      contains: "srcset"
    - path: "tests/relay-asset-bytes.test.js"
      provides: "Relay byte-identity proof for SC#1 (no image bytes on the wire)"
      min_lines: 30
    - path: "tests/renderer-snapshot.test.js"
      provides: "Extended CSP confirm-only assertion (img-src / no media-src / no script-src)"
      contains: "media-src"
    - path: "tests/renderer-asset-policy.test.js"
      provides: "RED scaffold for the pure origin classifier (filled by Plan 03)"
      contains: "classifyAssetOrigin"
    - path: "tests/renderer-asset-gate.test.js"
      provides: "RED scaffold for the renderer gate / mediaMode / pin (filled by Plan 03)"
      contains: "gateAssetUrl"
    - path: "tests/capture-asset-degrade.test.js"
      provides: "RED scaffold for capture degrade + currentSrc enrich (filled by Plan 02)"
      contains: "data-ps-asset-unavailable"
  key_links:
    - from: "tests/differential/fixtures/static-assets.html"
      to: "tests/differential/oracle.test.js"
      via: "MATRIX row registration (Plan 02 adds the scenario import + row)"
      pattern: "static-assets"
    - from: "tests/relay-asset-bytes.test.js"
      to: "src/relay/relay.js"
      via: "receive→sendToTargets byte-verbatim assertion"
      pattern: "receive"
---

<objective>
Prove the already-shipped by-reference static-asset pipeline (ASST-01, ASST-02) and
lock the viewer CSP at confirm-only (ASST-05), then lay the RED Wave-0 test scaffolds
that Plans 02 (capture) and 03 (renderer) implement against. This is the first vertical
slice: after this plan a developer can run the suite and SEE that (a) image URLs ride the
wire as absolute strings with zero bytes through the relay, and (b) the srcdoc CSP allows
images while still blocking scripts/media — both demoable with one `npm test`.

Purpose: ASST-01/02/05 are ~95% shipped (verified by reading the source at exact lines);
the genuine deliverable is the verification + a focused fixture + the failing scaffolds the
later plans fill, so feedback sampling has a target from the very first commit.
Output: a focused asset fixture, a relay byte-identity test, an extended CSP assertion, and
three RED scaffolds (`capture-asset-degrade`, `renderer-asset-policy`, `renderer-asset-gate`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-static-assets-by-reference/12-CONTEXT.md
@.planning/phases/12-static-assets-by-reference/12-RESEARCH.md
@.planning/phases/12-static-assets-by-reference/12-VALIDATION.md

<interfaces>
<!-- Extracted from the shipped source this plan asserts against. Use directly — no codebase hunt needed. -->

CSP confirm-only target (src/renderer/snapshot.js:57-62, CSP_META):
  default-src 'none'; img-src http: https: data:; style-src http: https: 'unsafe-inline'; font-src http: https: data:
  → NO script-src, NO media-src. media-src is Phase 13; adding it here is forbidden (CONTEXT critical constraint 1, 6).

Existing CSP test pin (tests/renderer-snapshot.test.js:54-66) — the exact-string template to extend:
  const CSP_CONTENT = "default-src 'none'; img-src http: https: data:; style-src http: https: 'unsafe-inline'; font-src http: https: data:";
  test('the exact adopted CSP meta is the FIRST element after <head>...', ...) asserts html.includes('<head><meta http-equiv="Content-Security-Policy" content="' + CSP_CONTENT + '"><meta charset="UTF-8">')
  Import in that file: import { buildSnapshotHtml } from '../src/renderer/snapshot.js'; minimalPayload() helper already defined.

Relay byte-verbatim proof site (src/relay/relay.js): receive(...) → checkRelayFrameLimit (classifies by type only, never parses payload) → sendToTargets → target.send(options.raw). SC#1 = the raw frame in equals the raw frame out.

Oracle matrix (tests/differential/oracle.test.js:48-63, MATRIX array) and scenario imports (:29-39). Existing fixtures with assets: basic.html (<img src> :25), heavy-realistic.html (<img srcset> :26, SVG xlink :29). The NEW fixture proves the enrichment+degrade path, not just absolutify.

jsdom caveats (VERIFIED in research): jsdom 29.1.1 returns img.currentSrc===""  (no resource loading) and does NOT enforce meta-CSP or fetch. So: real CSP enforcement + real blocked-GET + real currentSrc are Playwright UAT only; this plan's CSP test is a STRING assertion.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Focused static-assets oracle fixture + relay byte-identity proof + CSP confirm assertion</name>
  <files>tests/differential/fixtures/static-assets.html, tests/relay-asset-bytes.test.js, tests/renderer-snapshot.test.js</files>
  <read_first>
    - tests/differential/fixtures/heavy-realistic.html (the asset-bearing fixture pattern: `<img srcset>` :26, SVG xlink :29 — model element shapes on it) and tests/differential/fixtures/basic.html (`<img src>` :25)
    - tests/renderer-snapshot.test.js:48-72 (the exact CSP_CONTENT pin + the `buildSnapshotHtml`/`minimalPayload` glue to extend)
    - src/relay/relay.js (the receive→checkRelayFrameLimit→sendToTargets→`target.send(options.raw)` byte-verbatim path; model the test transport on how existing relay tests drive receive)
    - tests/relay-*.test.js if present (the existing relay test harness shape for constructing a relay + capturing forwarded raw frames)
  </read_first>
  <action>
    Create tests/differential/fixtures/static-assets.html as a focused fixture (model file shape on phase8-fidelity.html / cssom-mode.html) containing, each with a stable id: a plain `<img src>` (https absolute), an `<img srcset>` responsive image, a `<picture>` with two `<source srcset>` + fallback `<img>`, an SVG `<image>` using xlink:href, an element with inline `background-image:url(...)`, a `<video>` with a `poster` attribute (NO playback — poster only, per Phase 12 scope), an `<img>` whose src is a `blob:` URL, and an `<img>` whose src is an oversized `data:image/png;base64,...` URI (pad the base64 body so utf8 length exceeds 262144 bytes — this is the ASSET_DATA_URI_MAX_BYTES default Plan 02 introduces). These last two seed the degrade path Plan 02 asserts; this plan only registers the fixture file (Plan 02 adds the scenario + oracle MATRIX row + D26).
    Create tests/relay-asset-bytes.test.js: build a relay (the existing relay test harness shape), feed a `receive` of a raw frame whose decoded payload is a STREAM.SNAPSHOT containing absolute https `<img>` URL strings, capture what `target.send` forwards, and assert the forwarded raw bytes are byte-identical to the received raw bytes (proves SC#1 — no image bytes added/removed; the relay never fetches). Add a second case asserting the relay does NOT parse or rewrite the payload (the forwarded frame still contains the same URL substring it arrived with).
    Extend tests/renderer-snapshot.test.js: add a `test(...)` that builds `buildSnapshotHtml(minimalPayload())` and asserts the assembled srcdoc string (i) contains `img-src http: https: data:`, (ii) contains `default-src 'none'`, (iii) contains NO `script-src` substring, and (iv) contains NO `media-src` substring — proving the Phase-13 media-src deferral is intentional, not forgotten. Reuse the existing CSP_CONTENT pin style; do NOT modify CSP_META in snapshot.js (confirm-only, per CONTEXT constraints 1 and 6).
  </action>
  <acceptance_criteria>
    - `node --test tests/relay-asset-bytes.test.js` exits 0 and contains an assertion that the forwarded raw frame `=== ` the received raw frame.
    - `node --test tests/renderer-snapshot.test.js` exits 0; the new CSP test asserts presence of `img-src` + `default-src 'none'` AND absence of both `script-src` and `media-src` (4 distinct assertions).
    - `tests/differential/fixtures/static-assets.html` exists and a grep finds `srcset`, `xlink`, `background-image`, `poster`, `blob:`, and `data:image/` in it.
    - The oversized-`data:` `<img>` body, measured by utf8 byte length, is > 262144 (so Plan 02's cap fires on it).
    - src/renderer/snapshot.js CSP_META is byte-unchanged (no diff to that file).
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/relay-asset-bytes.test.js tests/renderer-snapshot.test.js</automated>
  </verify>
  <done>The shipped no-bytes-on-relay guarantee and the confirm-only CSP are pinned by automated tests; the focused asset fixture exists for Plan 02 to wire into the oracle.</done>
</task>

<task type="auto">
  <name>Task 2: RED Wave-0 scaffolds for capture-degrade, renderer-asset-policy, and renderer-asset-gate</name>
  <files>tests/capture-asset-degrade.test.js, tests/renderer-asset-policy.test.js, tests/renderer-asset-gate.test.js</files>
  <read_first>
    - tests/renderer-loopback.test.js:250-265 (the `glueMirror(iframe)` recipe: `cd.open(); cd.write(iframe.getAttribute('srcdoc')); cd.close(); iframe.dispatchEvent(new ...Event('load'))` — the jsdom srcdoc write-glue every renderer-gate test reuses)
    - tests/differential/divergence-ledger.js — the empirical jsdom note that `currentSrc===""` (so degrade-enrich tests must inject currentSrc via `Object.defineProperty`)
    - 12-VALIDATION.md "Minimum regression-catching set per criterion (Nyquist)" — the exact case list each scaffold must enumerate
    - tests/renderer-snapshot.test.js (an existing renderer test for the jsdom import + `createViewer`/`buildSnapshotHtml` import style)
  </read_first>
  <action>
    Create three RED test files whose cases are named and present but assert against not-yet-existing exports, so they fail loudly until Plans 02/03 implement. Each file's top comment must state "RED Wave-0 scaffold — filled by Plan 12-0X" and which Req-IDs it covers.
    tests/capture-asset-degrade.test.js (ASST-03, ASST-04 — Plan 02 fills): enumerate cases — `blob:` src `<img>` → wire clone is a dimensioned `<div data-ps-asset-unavailable="blob">`; oversized `data:` (> ASSET_DATA_URI_MAX_BYTES) → `<div data-ps-asset-unavailable="oversized-data">`; small `data:image/png` (≤ cap) → byte-identical, NO placeholder; `currentSrcDiffers('https://x/2x.png','https://x/1x.png')===true` and `currentSrcDiffers('https://x/a.png','https://x/a.png')===false`; an `<img srcset>` with an injected divergent `currentSrc` (`Object.defineProperty`) → wire clone carries `data-ps-currentsrc`; the live page DOM has NO `data-ps-currentsrc` / `data-ps-asset-unavailable` after capture (no-mutation invariant). Import from `../src/capture/index.js`.
    tests/renderer-asset-policy.test.js (MSEC-01 — Plan 03 fills): a table test over `classifyAssetOrigin(url)` covering — one allowed public `https://cdn.example.com/a.png` (allowed:true, reason 'ok'); `http://cdn.example.com/a.png` (bad-scheme); `https://localhost/a.png`, `https://127.0.0.1/a.png`, `https://10.1.2.3/a.png`, `https://172.16.5.5/a.png`, `https://192.168.1.1/a.png`, `https://169.254.169.254/a.png`, `https://[::1]/a.png`, `https://[fc00::1]/a.png`, `https://[fd12::1]/a.png` (each private-host), `https://intranet/a.png` and `https://host.local/a.png` (unqualified-host), and a malformed `"not a url"` (parse-error). Import from `../src/renderer/asset-policy.js`.
    tests/renderer-asset-gate.test.js (ASST-02, ASST-03, ASST-05, MSEC-01, MSEC-02 — Plan 03 fills): jsdom srcdoc-glue cases — `mediaMode:'reference'` writes an allowed `<img>` src; `mediaMode:'off'` writes NO asset src (placeholder instead); a blocked-origin `<img>` → never written, `data-ps-asset-unavailable="blocked-origin"` placeholder present; a snapshot whose `data-ps-currentsrc` differs → rendered element's effective src is the currentSrc value AND `srcset`/`sizes` are neutralized; a throwing `assetOriginPolicy` hook → fails closed (blocked); `allowAssetOrigins:['cdn.example.com']` → that origin passes. Use `gateAssetUrl`/`createViewer` config. Reuse the `glueMirror` recipe for the srcdoc write.
    Each scaffold MUST be syntactically valid and runnable (it errors on the missing import / undefined export, which is the intended RED state).
  </action>
  <acceptance_criteria>
    - All three files exist, parse, and FAIL when run now (`node --test` reports failing tests, not a syntax error) because the imports/exports they target do not exist yet.
    - `grep -c "test(" tests/capture-asset-degrade.test.js` ≥ 6; `tests/renderer-asset-policy.test.js` enumerates ≥ 12 origin rows (one assertion per denylist range + scheme + unqualified + parse-error + allowed); `tests/renderer-asset-gate.test.js` has ≥ 6 `test(` cases covering each mediaMode value and the pin/neutralize and the fail-closed hook.
    - Each file's header comment names its Plan (12-02 or 12-03) and Req-IDs.
    - tests/renderer-asset-gate.test.js references `glueMirror` (or inlines the identical `cd.open();cd.write(...);cd.close();dispatchEvent(load)` recipe).
  </acceptance_criteria>
  <verify>
    <automated>node -e "for (const f of ['tests/capture-asset-degrade.test.js','tests/renderer-asset-policy.test.js','tests/renderer-asset-gate.test.js']) { new Function(require('fs').readFileSync(f,'utf8').replace(/import .*/g,'')); } console.log('scaffolds parse OK')"</automated>
  </verify>
  <done>Three RED scaffolds enumerate every Nyquist minimum case; Plans 02 and 03 now implement against named failing tests rather than inventing the test surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture page → wire | Attacker-influenced asset URLs leave the captured page as strings |
| wire → relay | Raw frames forwarded verbatim; relay must never fetch or rewrite |
| wire → viewer srcdoc | Snapshot HTML parsed in the sandboxed iframe; CSP is the in-iframe fetch policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | relay (`src/relay/relay.js`) | accept (verify) | SC#1: relay forwards `target.send(options.raw)` byte-verbatim and never fetches; Task 1 pins this with a byte-identity test (no image bytes can be introduced/leaked at the relay). |
| T-12-02 | Tampering / Elevation | viewer srcdoc CSP (`snapshot.js` CSP_META) | mitigate | Confirm-only: Task 1 asserts the srcdoc keeps `default-src 'none'`, keeps `img-src`, and has NO `script-src` and NO `media-src`, so an accidental future widening (esp. premature `media-src` or any `script-src`) fails the build loudly. No CSP_META edit in this plan. |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 12 installs NO packages (RESEARCH Package Legitimacy Audit: none added); all code is in-repo plain-JS ESM against platform + existing vetted deps. No install task exists, so the supply-chain checkpoint is N/A by construction. |
</threat_model>

<verification>
- `npm test` is green (the new fixture is registered by Plan 02; this plan's own tests pass and the three scaffolds are intentionally RED until 02/03 land — note this in the SUMMARY so the wave-merge run is interpreted correctly).
- `node --test tests/relay-asset-bytes.test.js tests/renderer-snapshot.test.js` exits 0.
- `git diff --stat src/renderer/snapshot.js` shows no change (CSP confirm-only).
</verification>

<success_criteria>
- SC#1 (no image bytes on the relay) is pinned by an automated byte-identity test.
- SC#4/ASST-05 (CSP img-src present; default-src 'none'; no script-src; no media-src) is pinned by a string assertion.
- The focused static-assets fixture exists with every asset surface incl. the two degrade cases.
- Three RED Wave-0 scaffolds enumerate the Nyquist minimum sets for Plans 02 and 03.
</success_criteria>

<output>
Create `.planning/phases/12-static-assets-by-reference/12-01-SUMMARY.md` when done.
Note explicitly in the SUMMARY that the three scaffolds are intentionally RED pending Plans 02/03.
</output>
