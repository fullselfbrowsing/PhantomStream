---
phase: 06-extension-mv3-bookmarklet-adapters
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/adapters/bookmarklet.js
  - tests/bookmarklet-adapter.test.js
autonomous: true
requirements: [ADPT-03]
must_haves:
  truths:
    - "D-15: bookmarklet is a convenience adapter and must document/handle browser and CSP limits"
    - "D-16: bookmarklet source carries ephemeral local room/connection config"
    - "D-17: bookmarklet injects the same capture-capable browser artifact path where possible"
    - "D-18: blocked injection fails visibly and does not attempt CSP bypass"
    - "D-24: automated tests cover bookmarklet generation"
  artifacts:
    - path: "src/adapters/bookmarklet.js"
      provides: "Bookmarklet source generation, validation, and loader helpers"
    - path: "tests/bookmarklet-adapter.test.js"
      provides: "Unit tests for generated bookmarklet source and visible failure path"
---

<objective>
Implement the bookmarklet adapter core: source generation, URL/config validation, loader source helpers, and tests that pin visible failure instead of CSP bypass behavior.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-extension-mv3-bookmarklet-adapters/06-CONTEXT.md
@.planning/phases/06-extension-mv3-bookmarklet-adapters/06-RESEARCH.md
@.planning/phases/06-extension-mv3-bookmarklet-adapters/06-PATTERNS.md
@src/adapters/bookmarklet.js
@src/adapters/browser-inject.js
@src/transport/websocket.js
@src/capture/index.js
@tests/websocket-transport.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED bookmarklet generator tests</name>
  <files>tests/bookmarklet-adapter.test.js</files>
  <read_first>
    - `tests/websocket-transport.test.js`
    - `tests/adapter-exports.test.js`
    - `.planning/phases/06-extension-mv3-bookmarklet-adapters/06-VALIDATION.md`
  </read_first>
  <action>
    Create `tests/bookmarklet-adapter.test.js`. Import `createBookmarkletSource`, `createBookmarkletLoaderSource`, and `BOOKMARKLET_ERROR_EVENT` from `../src/adapters/bookmarklet.js`. Add tests that:
    generated source starts with `javascript:(()=>{`;
    generated source contains encoded `scriptUrl` and `wsUrl` but no raw newline;
    invalid or non-http(s) `scriptUrl` throws `bookmarklet-script-url-required`;
    invalid `wsUrl` throws `bookmarklet-ws-url-required`;
    loader source includes `window.__phantomStreamBridge`, `createWebSocketTransport`, `getBrowserInjectSource` or equivalent capture injection hook, and dispatches `BOOKMARKLET_ERROR_EVENT` on script load failure;
    generated source does not contain `eval(` or `Function(`.
  </action>
  <verify>
    <automated>set +e; node --test tests/bookmarklet-adapter.test.js; code=$?; test "$code" -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `tests/bookmarklet-adapter.test.js` contains `BOOKMARKLET_ERROR_EVENT`.
    - `tests/bookmarklet-adapter.test.js` contains `bookmarklet-script-url-required`.
    - RED verify exits 0 only because the focused test command exits non-zero before implementation.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement bookmarklet source and loader helpers</name>
  <files>src/adapters/bookmarklet.js, tests/bookmarklet-adapter.test.js</files>
  <read_first>
    - `src/adapters/bookmarklet.js`
    - `src/adapters/browser-inject.js`
    - `src/transport/websocket.js`
    - `src/capture/index.js`
    - `tests/bookmarklet-adapter.test.js`
  </read_first>
  <action>
    Replace the Plan 01 stub with a real implementation. Export:
    `BOOKMARKLET_ERROR_EVENT = 'phantomstream:bookmarklet-error'`,
    `createBookmarkletSource(options)`,
    and `createBookmarkletLoaderSource(options)`.
    `createBookmarkletSource({ scriptUrl, wsUrl, roomKey })` validates `scriptUrl` is `http:` or `https:`, validates `wsUrl` is `ws:` or `wss:`, optionally embeds `roomKey`, and returns a single-line string starting `javascript:(()=>{` that appends a script element with `src` set to `scriptUrl` plus encoded `ws`, `room`, and `ts` query params. The generated code must attach `onerror` to dispatch `new CustomEvent(BOOKMARKLET_ERROR_EVENT, { detail: { reason: 'script-load-failed' } })`.
    `createBookmarkletLoaderSource({ browserInjectSource })` returns browser JavaScript source that installs `window.__phantomStreamBridge`, opens a source-role WebSocket transport using the encoded `ws` URL, injects/executes the browser capture source, and dispatches `BOOKMARKLET_ERROR_EVENT` with content-free reasons on validation/load failure. Do not use `eval(` or `Function(`.
  </action>
  <verify>
    <automated>node --test tests/bookmarklet-adapter.test.js tests/adapter-exports.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function createBookmarkletSource" src/adapters/bookmarklet.js` returns one match.
    - `grep -n "phantomstream:bookmarklet-error" src/adapters/bookmarklet.js tests/bookmarklet-adapter.test.js` returns matches.
    - `! grep -n "eval(\\|Function(" src/adapters/bookmarklet.js`.
    - Focused tests pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bookmarklet URL -> page | User executes generated JavaScript in page context. |
| Page -> loader script | Page CSP/browser policy can block script loading. |
| Loader -> relay | Captured stream leaves page through WebSocket transport. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering | Generated bookmarklet source | mitigate | Validate URL schemes, encode params, forbid eval/Function, test exact source shape. |
| T-06-11 | Information Disclosure | Bookmarklet error events | mitigate | Error event details include reason codes only, no mirrored payload. |
| T-06-12 | Elevation of Privilege | CSP handling | accept/mitigate | Do not bypass CSP; visible failure event documents blocked injection. |
</threat_model>

<verification>
```bash
node --test tests/bookmarklet-adapter.test.js tests/adapter-exports.test.js
npm test
```
</verification>

<success_criteria>
- Bookmarklet source generation is deterministic, single-line, and validates URL schemes.
- Loader helper installs a capture bridge and visible failure path.
- No CSP bypass or eval-like behavior is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/06-extension-mv3-bookmarklet-adapters/06-04-SUMMARY.md`.
</output>
