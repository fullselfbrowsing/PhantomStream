---
phase: 06-extension-mv3-bookmarklet-adapters
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - bin/phantom-stream.js
  - examples/extension-mv3/server.js
  - examples/extension-mv3/source.html
  - examples/extension-mv3/viewer.html
  - examples/extension-mv3/demo.css
  - tests/extension-demo-cli.test.js
  - .planning/phases/06-extension-mv3-bookmarklet-adapters/06-BROWSER-VERIFICATION.md
autonomous: true
requirements: [ADPT-01]
must_haves:
  truths:
    - "D-20: extension demo is explicit and does not overload the existing demo/playwright-demo commands"
    - "D-21: extension demo binds to 127.0.0.1, uses no-store assets, and prints exact URLs/load instructions"
    - "D-22: MV3 demo proves real loaded extension path, watchdog wiring, and restart recovery"
    - "D-24: automated tests cover CLI/demo wiring and browser verification remains required"
  artifacts:
    - path: "examples/extension-mv3/server.js"
      provides: "Local-only extension demo server and generated extension fixture"
    - path: "tests/extension-demo-cli.test.js"
      provides: "CLI/server contract tests for extension-demo"
    - path: ".planning/phases/06-extension-mv3-bookmarklet-adapters/06-BROWSER-VERIFICATION.md"
      provides: "Browser evidence checklist for loaded extension and watchdog recovery"
---

<objective>
Create the local MV3 extension demo and CLI command that prove ADPT-01 through a real loaded extension fixture, deterministic local pages, and watchdog recovery evidence.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-extension-mv3-bookmarklet-adapters/06-CONTEXT.md
@.planning/phases/06-extension-mv3-bookmarklet-adapters/06-RESEARCH.md
@.planning/phases/06-extension-mv3-bookmarklet-adapters/06-PATTERNS.md
@src/adapters/extension.js
@src/adapters/browser-inject.js
@examples/two-tab-demo/server.js
@examples/playwright-demo/server.js
@bin/phantom-stream.js
@tests/demo-cli.test.js
@tests/playwright-demo-cli.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED extension demo CLI tests</name>
  <files>tests/extension-demo-cli.test.js</files>
  <read_first>
    - `tests/demo-cli.test.js`
    - `tests/playwright-demo-cli.test.js`
    - `bin/phantom-stream.js`
  </read_first>
  <action>
    Create `tests/extension-demo-cli.test.js`. Import `startExtensionDemoServer` from `../examples/extension-mv3/server.js`. Assert it rejects non-local hosts with `demo-host-local-only`, binds `127.0.0.1`, returns `sourceUrl`, `viewerUrl`, `extensionDir`, `roomKey`, `roomKeyPrefix`, `sourceWsUrl`, and `viewerWsUrl`, and serves `/extension/source` and `/extension/viewer` with `cache-control: no-store`. Add a CLI spawn test for `node bin/phantom-stream.js extension-demo --port 0 --no-open` that waits for stdout lines matching `PhantomStream extension demo running on 127.0.0.1`, `Extension directory:`, `Source page: http://127.0.0.1:.../extension/source?room=`, `Viewer: http://127.0.0.1:.../extension/viewer?room=`, and `Room: [a-f0-9]{8}...`.
  </action>
  <verify>
    <automated>set +e; node --test tests/extension-demo-cli.test.js; code=$?; test "$code" -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `tests/extension-demo-cli.test.js` contains `extension-demo --port`.
    - `tests/extension-demo-cli.test.js` contains `Extension directory:`.
    - RED verify exits 0 only because the focused test command exits non-zero before implementation.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement extension demo server and CLI command</name>
  <files>bin/phantom-stream.js, examples/extension-mv3/server.js, examples/extension-mv3/source.html, examples/extension-mv3/viewer.html, examples/extension-mv3/demo.css, tests/extension-demo-cli.test.js</files>
  <read_first>
    - `examples/two-tab-demo/server.js`
    - `examples/playwright-demo/server.js`
    - `bin/phantom-stream.js`
    - `src/adapters/extension.js`
    - `src/adapters/browser-inject.js`
  </read_first>
  <action>
    Add `examples/extension-mv3/server.js` exporting `startExtensionDemoServer(options)`. Reuse the local-only server pattern from `examples/two-tab-demo/server.js`: host must equal `127.0.0.1`, requested port defaults to `8645`, try 20 ports, generate a 32-char hex room key, serve no-store static routes, and expose a bundled relay at `/ws`.
    The returned object must include `sourceUrl`, `viewerUrl`, `sourceWsUrl`, `viewerWsUrl`, `extensionDir`, `roomKey`, `roomKeyPrefix`, `server`, `relay`, `backend`, and `close()`.
    Generate or prepare an unpacked extension fixture directory containing `manifest.json`, `service-worker.js`, and `content-script.js`. The manifest must include `"manifest_version": 3`, `"permissions": ["alarms", "storage", "scripting", "activeTab"]`, `"background": { "service_worker": "service-worker.js", "type": "module" }`, and host permissions for `http://127.0.0.1/*`.
    Add `extension-demo` command parsing to `bin/phantom-stream.js` with `--port`, `--no-open`, and `--help`. Print exactly:
    `PhantomStream extension demo running on 127.0.0.1`,
    `Extension directory: {path}`,
    `Source page: {url}`,
    `Viewer: {url}`,
    `Room: {prefix}...`.
  </action>
  <verify>
    <automated>node --test tests/extension-demo-cli.test.js tests/demo-cli.test.js tests/playwright-demo-cli.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "extension-demo" bin/phantom-stream.js` returns matches in usage, command handling, and parser/output code.
    - `grep -n '"alarms"' examples/extension-mv3/server.js` returns a match.
    - `grep -n "cache-control.*no-store" examples/extension-mv3/server.js` returns a match.
    - Focused CLI tests pass.
  </acceptance_criteria>
</task>

<task type="manual">
  <name>Task 3: Record browser verification for real extension and watchdog recovery</name>
  <files>.planning/phases/06-extension-mv3-bookmarklet-adapters/06-BROWSER-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/06-extension-mv3-bookmarklet-adapters/06-VALIDATION.md`
    - `examples/extension-mv3/server.js`
    - `src/adapters/extension.js`
  </read_first>
  <action>
    Create `06-BROWSER-VERIFICATION.md` with an `## Extension MV3` section. Record the exact command run (`node bin/phantom-stream.js extension-demo --port 0 --no-open`), extension directory path, source URL, viewer URL, browser used, and evidence rows for: extension loaded with no service-worker console errors; content script injected; viewer receives initial snapshot; source page mutation appears in viewer; watchdog alarm is registered; simulated or forced SW restart triggers `mv3-watchdog-resnapshot` and viewer returns to live state. Mark any item not actually exercised as `human_needed` with a reason.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-extension-mv3-bookmarklet-adapters/06-BROWSER-VERIFICATION.md && grep -n "mv3-watchdog-resnapshot" .planning/phases/06-extension-mv3-bookmarklet-adapters/06-BROWSER-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `06-BROWSER-VERIFICATION.md` contains `## Extension MV3`.
    - `06-BROWSER-VERIFICATION.md` contains `mv3-watchdog-resnapshot`.
    - `06-BROWSER-VERIFICATION.md` contains either `VERIFIED` rows or `human_needed` rows with reasons for all six evidence points.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local demo server -> browser extension | Fixture files are loaded as privileged extension code. |
| Browser page -> viewer | Mirrored DOM crosses through local relay. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Information Disclosure | Demo stdout | mitigate | Print only URLs, extension dir, and room prefix; never print mirrored payloads. |
| T-06-08 | Elevation of Privilege | Extension fixture | mitigate | Local-only fixture, explicit permissions, no store UX, no public host permissions beyond local demo. |
| T-06-09 | Denial of Service | Demo server | mitigate | Reuse strict path handling and close relay/backend/server on SIGINT. |
</threat_model>

<verification>
```bash
node --test tests/extension-demo-cli.test.js tests/demo-cli.test.js tests/playwright-demo-cli.test.js
npm test
```
</verification>

<success_criteria>
- `phantom-stream extension-demo` starts a local-only demo and prints extension load instructions.
- Demo tests prove deterministic URLs, room key, no-store assets, and local binding.
- Browser verification artifact records extension load, live mirror, and watchdog recovery evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/06-extension-mv3-bookmarklet-adapters/06-03-SUMMARY.md`.
</output>
