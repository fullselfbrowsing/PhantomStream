---
phase: 06-extension-mv3-bookmarklet-adapters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/adapters/browser-inject.js
  - src/adapters/extension.js
  - src/adapters/bookmarklet.js
  - tests/adapter-exports.test.js
autonomous: true
requirements: [ADPT-01, ADPT-03]
must_haves:
  truths:
    - "D-03: package exports include ./adapters/extension and ./adapters/bookmarklet"
    - "D-06: browser-injected code remains a checked-in classic script with no import/export syntax"
    - "D-07: adapter bridge preserves createCapture transport send/flush and lifecycle start/stop/pause/resume hooks"
    - "D-24: automated tests cover package exports and inject artifact invariants"
  artifacts:
    - path: "src/adapters/browser-inject.js"
      provides: "Shared source helper for browser injection contexts"
      exports: ["getBrowserInjectSource"]
    - path: "src/adapters/extension.js"
      provides: "Extension adapter public surface placeholder for Wave 2 implementation"
    - path: "src/adapters/bookmarklet.js"
      provides: "Bookmarklet adapter public surface placeholder for Wave 2 implementation"
    - path: "tests/adapter-exports.test.js"
      provides: "Export and browser inject artifact coverage"
---

<objective>
Create the shared adapter foundation for Phase 6: package exports, public module stubs, and a common browser-inject source helper that reuses the existing no-build inject artifact precedent.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-extension-mv3-bookmarklet-adapters/06-CONTEXT.md
@.planning/phases/06-extension-mv3-bookmarklet-adapters/06-RESEARCH.md
@.planning/phases/06-extension-mv3-bookmarklet-adapters/06-PATTERNS.md
@.planning/phases/06-extension-mv3-bookmarklet-adapters/06-VALIDATION.md
@package.json
@src/adapters/playwright.js
@src/adapters/playwright-inject.js
@tests/playwright-adapter.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED export and inject artifact tests</name>
  <files>tests/adapter-exports.test.js</files>
  <read_first>
    - `tests/playwright-adapter.test.js`
    - `tests/websocket-transport.test.js`
    - `package.json`
    - `src/adapters/playwright.js`
    - `src/adapters/playwright-inject.js`
  </read_first>
  <action>
    Create `tests/adapter-exports.test.js` using `node:test` and `node:assert/strict`. Assert `package.json` has `exports["./adapters/extension"] === "./src/adapters/extension.js"` and `exports["./adapters/bookmarklet"] === "./src/adapters/bookmarklet.js"`. Import `getBrowserInjectSource` from `../src/adapters/browser-inject.js` and assert its returned source includes `createCapture`, `window.__phantomStreamBridge`, `window.__phantomStreamStart`, and `window.__phantomStreamStop`, and does not include `import ` or `export `. Import `createExtensionAdapter` and `createBookmarkletSource` from their future modules and assert both are functions.
  </action>
  <verify>
    <automated>set +e; node --test tests/adapter-exports.test.js; code=$?; test "$code" -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `tests/adapter-exports.test.js` imports `getBrowserInjectSource` from `../src/adapters/browser-inject.js`.
    - `tests/adapter-exports.test.js` contains the exact strings `./adapters/extension` and `./adapters/bookmarklet`.
    - The RED verify command exits 0 only because the focused test command exits non-zero before implementation.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement adapter exports and shared browser inject helper</name>
  <files>package.json, src/adapters/browser-inject.js, src/adapters/extension.js, src/adapters/bookmarklet.js, tests/adapter-exports.test.js</files>
  <read_first>
    - `package.json`
    - `src/adapters/playwright.js`
    - `src/adapters/playwright-inject.js`
    - `tests/adapter-exports.test.js`
  </read_first>
  <action>
    Add package subpath exports for `./adapters/extension` and `./adapters/bookmarklet`. Create `src/adapters/browser-inject.js` exporting `getBrowserInjectSource()`, implemented by reading the checked-in classic script artifact from `src/adapters/playwright-inject.js` or an equivalent shared source file with `readFileSync(fileURLToPath(new URL(...)))`. Create minimal public modules:
    `src/adapters/extension.js` exports `createExtensionAdapter(options)` that validates `options` is an object and returns a handle placeholder `{ install, dispose }` with no side effects yet.
    `src/adapters/bookmarklet.js` exports `createBookmarkletSource(options)` that validates `options` is an object and throws `Error('bookmarklet-script-url-required')` until implemented in Plan 04.
    The stubs exist only to establish exports; Plan 02 and Plan 04 replace placeholder behavior.
  </action>
  <verify>
    <automated>node --test tests/adapter-exports.test.js tests/playwright-adapter.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"./adapters/extension"' package.json` returns one match.
    - `grep -n '"./adapters/bookmarklet"' package.json` returns one match.
    - `grep -n "export function getBrowserInjectSource" src/adapters/browser-inject.js` returns one match.
    - `node --test tests/adapter-exports.test.js tests/playwright-adapter.test.js` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package exports -> consumers | New adapter subpaths become public API surface. |
| Browser inject source -> host pages | Classic script runs in browser contexts and must not gain dependencies or dynamic remote code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Browser inject artifact | mitigate | Static tests assert no `import`/`export` and required bridge hooks; later plans add adapter-specific behavior. |
| T-06-02 | Information Disclosure | Adapter exports | mitigate | Foundation stubs do not log or persist payload contents. |
</threat_model>

<verification>
```bash
node --test tests/adapter-exports.test.js tests/playwright-adapter.test.js
npm test
```
</verification>

<success_criteria>
- Package exports exist for extension and bookmarklet adapters.
- Shared browser inject helper returns a no-build classic script artifact.
- No implementation-specific MV3/bookmarklet behavior is hidden in foundation stubs.
</success_criteria>

<output>
After completion, create `.planning/phases/06-extension-mv3-bookmarklet-adapters/06-01-SUMMARY.md`.
</output>
