---
phase: 09-cssom-capture-mode
plan: 07
type: execute
wave: 4
depends_on:
  - 09-05
  - 09-06
files_modified:
  - src/adapters/playwright.js
  - src/adapters/playwright-inject.js
  - tests/adapter-exports.test.js
  - tests/playwright-adapter.test.js
  - tests/playwright-cssom-mode.test.js
autonomous: true
requirements:
  - CAPT-10
must_haves:
  truths:
    - "The checked-in Playwright/browser inject artifact carries the same CSSOM capture and protocol behavior as `src/capture/index.js`."
    - "Playwright adapter users can opt into CSSOM capture with `captureOptions: { styleMode: 'cssom' }` while default adapter behavior remains computed mode."
    - "Browser and bookmarklet paths continue to consume the synchronized classic script artifact without adding a runtime build step."
  artifacts:
    - path: "src/adapters/playwright.js"
      provides: "Adapter captureOptions wiring into the checked-in inject artifact"
    - path: "src/adapters/playwright-inject.js"
      provides: "Classic script artifact synchronized with CSSOM capture and style-source protocol behavior"
    - path: "tests/playwright-cssom-mode.test.js"
      provides: "Green real-browser CSSOM smoke through checked-in inject artifact"
  key_links:
    - from: "src/adapters/playwright.js"
      to: "src/adapters/playwright-inject.js"
      via: "buildPlaywrightInjectSource({ bridgeToken, captureOptions })"
      pattern: "PHANTOM_STREAM_CAPTURE_OPTIONS"
    - from: "src/adapters/browser-inject.js"
      to: "src/adapters/playwright.js"
      via: "getBrowserInjectSource reads getPlaywrightInjectSource"
      pattern: "getBrowserInjectSource"
---

<objective>
Synchronize browser-injected artifacts and expose CSSOM opt-in through the Playwright adapter.

Purpose: Phase 9 must work in real injected browser contexts, not just ESM unit tests. The checked-in classic script is shared by Playwright, browser-inject, and bookmarklet loader paths.
Output: Green adapter/static tests and Playwright CSSOM smoke using `src/adapters/playwright-inject.js`.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-cssom-capture-mode/09-CONTEXT.md
@.planning/phases/09-cssom-capture-mode/09-RESEARCH.md
@.planning/phases/09-cssom-capture-mode/09-VALIDATION.md
@.planning/phases/09-cssom-capture-mode/09-PATTERNS.md
@.planning/phases/09-cssom-capture-mode/09-05-SUMMARY.md
@.planning/phases/09-cssom-capture-mode/09-06-SUMMARY.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-08-SUMMARY.md
@src/adapters/playwright.js
@src/adapters/playwright-inject.js
@src/adapters/browser-inject.js
@src/adapters/bookmarklet.js
@tests/adapter-exports.test.js
@tests/playwright-adapter.test.js
@tests/playwright-cssom-mode.test.js

<interfaces>
Existing adapter surface:

```js
export function getPlaywrightInjectSource() { return buildPlaywrightInjectSource(); }
function buildPlaywrightInjectSource(options) { /* token replacement */ }
export function createPlaywrightAdapter(options) { /* installs injectSource via addInitScript */ }
```

Planned exact adapter option:

```js
createPlaywrightAdapter({
  page,
  transport,
  captureOptions: { styleMode: 'cssom' }
});
```
</interfaces>
</context>

<decision_coverage>
- D-03: Adapter defaults to computed mode unless `captureOptions.styleMode === 'cssom'`.
- D-08: Adapter does not add hidden cross-origin fetch behavior; explicit `fetchStylesheet` remains a core capture hook for same-runtime hosts.
- D-20: Synchronizes `src/adapters/playwright-inject.js`; browser-inject and bookmarklet paths keep using that artifact.
- D-21: Real-browser tests cover CSS-in-JS classes, cross-origin fallback, constructable/adopted sheets, insertRule/deleteRule, shadow scopes, and frame scopes.
- D-23: Real-browser smoke records payload-size and serialize-latency evidence only.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Playwright adapter captureOptions wiring</name>
  <files>src/adapters/playwright.js, src/adapters/playwright-inject.js, tests/playwright-adapter.test.js, tests/adapter-exports.test.js</files>
  <read_first>
    - `src/adapters/playwright.js` `getPlaywrightInjectSource`, `buildPlaywrightInjectSource`, `createPlaywrightAdapter`, and bridge token replacement.
    - `src/adapters/playwright-inject.js` bootstrap section with `PHANTOM_STREAM_BRIDGE_TOKEN`, `phantomStreamEnsureCapture`, and `createCapture({ transport, logger })`.
    - `src/adapters/browser-inject.js` and `src/adapters/bookmarklet.js` current use of `getBrowserInjectSource`.
    - `tests/playwright-adapter.test.js` install and inject source tests.
    - `tests/adapter-exports.test.js` classic script and browser-inject tests.
    - `.planning/phases/09-cssom-capture-mode/09-CONTEXT.md` decisions D-03, D-08, and D-20.
  </read_first>
  <behavior>
    - `getPlaywrightInjectSource({ captureOptions: { styleMode: 'cssom' } })` returns a classic script containing `var PHANTOM_STREAM_CAPTURE_OPTIONS = {"styleMode":"cssom"};`.
    - `createPlaywrightAdapter({ page, transport, captureOptions: { styleMode: 'cssom' } })` passes those serializable options into `buildPlaywrightInjectSource`.
    - Unsupported capture option keys and function values are not serialized into the classic script.
    - Default `getPlaywrightInjectSource()` and default adapter install preserve computed mode with `var PHANTOM_STREAM_CAPTURE_OPTIONS = {};`.
    - Browser-inject and bookmarklet loader tests still pass because they read the synchronized artifact through `getBrowserInjectSource()`.
  </behavior>
  <action>
    In `src/adapters/playwright.js`, change `getPlaywrightInjectSource()` to accept optional `options` and call `buildPlaywrightInjectSource(options)`.

    Add constant `INJECT_CAPTURE_OPTIONS_DECLARATION = 'var PHANTOM_STREAM_CAPTURE_OPTIONS = {};';`. Add helper `normalizeCaptureOptions(options)` that returns `{ styleMode: 'cssom' }` only when `options.captureOptions && options.captureOptions.styleMode === 'cssom'`; otherwise return `{}`. Do not serialize `fetchStylesheet` functions or arbitrary keys.

    Update `buildPlaywrightInjectSource(options)` to replace both bridge token and capture options declarations. Use `JSON.stringify(normalizeCaptureOptions(opts))`.

    In `createPlaywrightAdapter`, pass `captureOptions: cfg.captureOptions || {}` into `buildPlaywrightInjectSource({ bridgeToken, captureOptions: cfg.captureOptions || {} })`.

    In `src/adapters/playwright-inject.js`, add `var PHANTOM_STREAM_CAPTURE_OPTIONS = {};` near `PHANTOM_STREAM_BRIDGE_TOKEN`. In `phantomStreamEnsureCapture()`, construct:
    `var captureOptions = PHANTOM_STREAM_CAPTURE_OPTIONS && Object(PHANTOM_STREAM_CAPTURE_OPTIONS) === PHANTOM_STREAM_CAPTURE_OPTIONS ? PHANTOM_STREAM_CAPTURE_OPTIONS : {};`
    then call `createCapture(Object.assign({}, captureOptions, { transport: phantomStreamTransport, logger: phantomStreamLogger }))`.
  </action>
  <verify>
    <automated>node --test tests/playwright-adapter.test.js tests/adapter-exports.test.js tests/bookmarklet-adapter.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "PHANTOM_STREAM_CAPTURE_OPTIONS|captureOptions|normalizeCaptureOptions|styleMode" src/adapters/playwright.js src/adapters/playwright-inject.js tests/playwright-adapter.test.js tests/adapter-exports.test.js` returns matches.
    - `node --test tests/playwright-adapter.test.js tests/adapter-exports.test.js tests/bookmarklet-adapter.test.js` passes.
    - `rg -n "fetchStylesheet|=>|import |export |require\\(" src/adapters/playwright-inject.js` returns no ESM/CommonJS matches and no serialized fetch hook matches; ordinary classic-script `function (` declarations are allowed by `tests/adapter-exports.test.js`.
  </acceptance_criteria>
  <done>Playwright adapter can pass serializable CSSOM opt-in capture options to the checked-in classic script while defaults remain computed mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synchronize inject artifact with CSSOM capture behavior and browser smoke</name>
  <files>src/adapters/playwright-inject.js, tests/playwright-cssom-mode.test.js, tests/adapter-exports.test.js</files>
  <read_first>
    - `src/adapters/playwright-inject.js` full classic script artifact.
    - `src/capture/index.js` CSSOM snapshot and dynamic style-op implementation from Plans 09-03 and 09-05.
    - `src/protocol/messages.js` `DIFF_OP.STYLE_SOURCE`.
    - `tests/playwright-cssom-mode.test.js` RED smoke from Plan 09-02.
    - `tests/adapter-exports.test.js` classic script assertions.
    - `.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-08-SUMMARY.md` artifact sync pattern and static-safe dialog dispatch note.
  </read_first>
  <behavior>
    - `src/adapters/playwright-inject.js` contains `STYLE_SOURCE`, `styleMode`, `styleSources`, `styleStrategy`, `queueStyleSourceChange`, `patchCssStyleSheetMethods`, and `adoptedStyleSheets`.
    - The artifact remains a classic script with no `import`, `export`, or `require(`.
    - Static-safe dialog dispatch spelling from Phase 8 remains intact.
    - `node --test tests/playwright-cssom-mode.test.js` passes in real Chromium.
    - Browser-inject and bookmarklet adapter tests pass through the shared artifact path.
  </behavior>
  <action>
    Regenerate or patch `src/adapters/playwright-inject.js` from the current `src/capture/index.js` and `src/protocol/messages.js` behavior using the existing artifact sync method in the repo. Preserve the Phase 8 artifact bootstrap globals:
    - `window.__phantomStreamBridge`
    - `window.__phantomStreamCapture`
    - `window.__phantomStreamHandleControl`
    - `window.__phantomStreamGetNodeId`
    - `window.__phantomStreamStart`
    - `window.__phantomStreamStop`

    Ensure `PHANTOM_STREAM_CAPTURE_OPTIONS` remains token-replaceable by `src/adapters/playwright.js`. Preserve the artifact-only static-safe dialog CustomEvent dispatch spelling used after Phase 8 if regeneration changes it.

    Update `tests/playwright-cssom-mode.test.js` from RED to green assertions using `getPlaywrightInjectSource({ captureOptions: { styleMode: 'cssom' } })` or `createPlaywrightAdapter({ captureOptions: { styleMode: 'cssom' } })` as appropriate for the test case. Keep smoke assertions content-free and numeric for payload bytes and serialize latency.
  </action>
  <verify>
    <automated>node --test tests/playwright-cssom-mode.test.js tests/adapter-exports.test.js tests/playwright-adapter.test.js tests/extension-adapter.test.js tests/bookmarklet-adapter.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "STYLE_SOURCE|style-source|styleMode|styleSources|styleStrategy|queueStyleSourceChange|patchCssStyleSheetMethods|adoptedStyleSheets|PHANTOM_STREAM_CAPTURE_OPTIONS" src/adapters/playwright-inject.js` returns matches.
    - `node --test tests/playwright-cssom-mode.test.js tests/adapter-exports.test.js tests/playwright-adapter.test.js tests/extension-adapter.test.js tests/bookmarklet-adapter.test.js` passes.
    - `rg -n "import |export |require\\(" src/adapters/playwright-inject.js` returns no matches.
    - `rg -n "dispatchEvent\\(" src/adapters/playwright-inject.js` returns no direct static-forbidden matches except the established safe spelling allowed by `tests/adapter-exports.test.js`.
  </acceptance_criteria>
  <done>Checked-in browser inject artifact carries CSSOM mode and browser smoke passes through the same artifact consumed by Playwright/browser/bookmarklet paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host adapter config -> injected page capture | Adapter serializes opt-in config into a page-world classic script. |
| page CSSOM -> bridge transport | Injected capture forwards CSSOM snapshots and style ops through the bridge. |
| bookmarklet/browser loader -> injected artifact | Non-Playwright adapters consume the same checked-in script source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-25 | I | `normalizeCaptureOptions` | mitigate | Serialize only `styleMode: 'cssom'`; do not stringify host functions, fetch hooks, secrets, or arbitrary option keys. |
| T-09-26 | T | inject artifact sync | mitigate | Preserve classic script static gates, bridge token replacement, and Phase 8 dialog dispatch hardening. |
| T-09-27 | I | Playwright CSSOM smoke | mitigate | Smoke diagnostics and payload evidence remain numeric/content-free; no CSS text or page content in adapter events. |
| T-09-28 | E | injected renderer/capture boundary | mitigate | Do not add scripts to the renderer sandbox; changes are capture-side artifact only. |
</threat_model>

<verification>
```bash
node --test tests/playwright-cssom-mode.test.js tests/adapter-exports.test.js tests/playwright-adapter.test.js tests/extension-adapter.test.js tests/bookmarklet-adapter.test.js
```
</verification>

<success_criteria>
- Playwright adapter and checked-in inject artifact support CSSOM opt-in via `captureOptions.styleMode`.
- Browser-inject and bookmarklet paths keep using the synchronized artifact.
- Real-browser CSSOM smoke passes through the checked-in artifact.
</success_criteria>

<output>
After completion, create `.planning/phases/09-cssom-capture-mode/09-07-SUMMARY.md`.
</output>
