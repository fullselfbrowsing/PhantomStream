---
phase: 09-cssom-capture-mode
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/renderer-cssom-mode.test.js
  - tests/playwright-cssom-mode.test.js
  - tests/differential/fixtures/cssom-mode.html
  - tests/differential/scenarios/cssom-capture-mode.js
  - tests/differential/oracle.test.js
  - tests/differential/divergence-ledger.js
autonomous: true
requirements:
  - CAPT-10
must_haves:
  truths:
    - "Renderer CSSOM replay has RED tests for top document, open shadow root, and same-origin frame scoped style sources."
    - "Real Chromium has RED smoke coverage for CSS-in-JS class flips, cross-origin CDN fallback, constructable/adopted sheets, insertRule/deleteRule, shadow/frame scopes, payload size, and serialize latency."
    - "The differential oracle has RED opt-in CSSOM divergence coverage while default computed mode stays reference-compatible."
    - "Decision coverage: D-06: document/shadow/frame scopes; D-11: live style-op path; D-12: STREAM.MUTATIONS style updates; D-19: class flips use cascade; D-21: required fixtures; D-23: payload/latency smoke only; D-24: opt-in oracle divergence."
  artifacts:
    - path: "tests/renderer-cssom-mode.test.js"
      provides: "Renderer RED coverage for scoped CSSOM snapshot install and style-source op application"
    - path: "tests/playwright-cssom-mode.test.js"
      provides: "Real-browser RED smoke for CSSOM platform behavior and payload/latency evidence"
    - path: "tests/differential/fixtures/cssom-mode.html"
      provides: "CSSOM oracle fixture with class flip and authored stylesheet sources"
    - path: "tests/differential/scenarios/cssom-capture-mode.js"
      provides: "Opt-in CSSOM oracle scenario"
  key_links:
    - from: "tests/renderer-cssom-mode.test.js"
      to: "src/renderer/index.js"
      via: "installStyleSources into document, shadow root, and frame document scopes"
      pattern: "installStyleSources"
    - from: "tests/playwright-cssom-mode.test.js"
      to: "src/adapters/playwright-inject.js"
      via: "checked-in inject artifact in real Chromium"
      pattern: "playwright-inject"
    - from: "tests/differential/oracle.test.js"
      to: "tests/differential/divergence-ledger.js"
      via: "scenario-pinned CSSOM opt-in divergence"
      pattern: "cssom-capture-mode"
---

<objective>
Create Wave 0 RED tests for renderer scoped CSSOM replay, real-browser CSSOM smoke, and differential oracle discipline.

Purpose: CSSOM mode must be proven in the viewer and browser-injected path, not just unit-tested at the capture boundary. The oracle must also make CSSOM divergence explicit without weakening default-mode reference comparison.
Output: RED renderer, Playwright, fixture, scenario, and oracle coverage for CAPT-10.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-cssom-capture-mode/09-CONTEXT.md
@.planning/phases/09-cssom-capture-mode/09-RESEARCH.md
@.planning/phases/09-cssom-capture-mode/09-VALIDATION.md
@.planning/phases/09-cssom-capture-mode/09-PATTERNS.md
@src/renderer/snapshot.js
@src/renderer/index.js
@src/renderer/diff.js
@src/renderer/sanitize.js
@src/adapters/playwright-inject.js
@tests/renderer-iframe.test.js
@tests/renderer-shadow-dom.test.js
@tests/playwright-fidelity-phase8.test.js
@tests/differential/oracle.test.js
@tests/differential/divergence-ledger.js

<interfaces>
Existing renderer and browser-test contracts to use directly:

```js
// src/renderer/index.js
export function createViewer(options) {
  // returns { detach, destroy, clearHighlight, getViewportMapping, highlightNode, on, registerOverlay, requestSubtree, resolveNode }
}

// src/renderer/diff.js
export function applyMutations(doc, mutations, counters, hooks) {
  // identity hooks include resolve, indexSubtree, removeSubtree, installShadowRoot, installFrames
}

// tests/playwright-fidelity-phase8.test.js
// Uses chromium, page.exposeFunction('__phantomStreamBridge', handler), addScriptTag({ path: INJECT_PATH }), and waitForMessage().
```
</interfaces>
</context>

<decision_coverage>
- D-06: RED renderer and browser tests require CSSOM sources in top document, open shadow roots keyed by host nid, and same-origin frames keyed by frame nid.
- D-11: RED Playwright tests require a narrow live style-op path instead of snapshot-only CSSOM.
- D-12: RED renderer tests require style updates through `STREAM.MUTATIONS`, not a new top-level stream.
- D-19: RED tests include class flips that only work when stylesheet rules, not stale generated inline styles, control cascade.
- D-21: RED Playwright tests include production-style CSS-in-JS class names, cross-origin CDN fallback, constructable/adopted stylesheets, insertRule/deleteRule, shadow scopes, and frame scopes.
- D-23: RED Playwright tests include payload-size and serialize-latency smoke evidence without Phase 12 baseline tables.
- D-24: RED oracle tests keep CSSOM divergence opt-in and ledgered.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED renderer scoped CSSOM replay tests</name>
  <files>tests/renderer-cssom-mode.test.js</files>
  <read_first>
    - `tests/renderer-cssom-mode.test.js` to confirm the file does not already exist before creating it.
    - `.planning/phases/09-cssom-capture-mode/09-CONTEXT.md` decisions D-06, D-12, D-17, D-18, and D-19.
    - `.planning/phases/09-cssom-capture-mode/09-VALIDATION.md` rows 09-W0-02, 09-W0-04, and 09-W0-05.
    - `.planning/phases/09-cssom-capture-mode/09-PATTERNS.md` section for `tests/renderer-cssom-mode.test.js`.
    - `tests/renderer-iframe.test.js`, `tests/renderer-shadow-dom.test.js`, and `tests/renderer-diff.test.js` for manual transport, srcdoc glue, and identity-hook helpers.
    - `src/renderer/snapshot.js`, `src/renderer/index.js`, `src/renderer/diff.js`, and `src/renderer/sanitize.js`.
  </read_first>
  <behavior>
    - Test 1: top-document `styleSources` with `scope.kind === 'document'` render as safe `<style data-ps-style-source-id>` or `<link rel="stylesheet">` elements in the mirror head.
    - Test 2: shadow-root `styleSources` with `scope.kind === 'shadow'` and `scope.hostNid` install inside the reconstructed `ShadowRoot`, not the top document.
    - Test 3: frame `styleSources` with `scope.kind === 'frame'` and `scope.frameNid` install inside the same-origin frame document, not the top document.
    - Test 4: `STREAM.MUTATIONS` carrying `DIFF_OP.STYLE_SOURCE` with `action: 'replace'`, `'upsert'`, and `'remove'` updates or removes only the matching scoped source.
    - Test 5: CSS text is inserted through `textContent` after `scrubCssText`; hostile CSS tokens do not survive in installed style text.
    - Test 6: missing shadow/frame scope increments stale/apply counters and requests a content-free resync reason `stale-style-scope`.
  </behavior>
  <action>
    Create `tests/renderer-cssom-mode.test.js` with flat `node:test` tests. Use a manual viewer transport helper matching `tests/renderer-iframe.test.js`, a `baseSnapshot()` helper containing `styleStrategy`, `styleSources`, `shadowRoots`, and `frames`, and a `glueIframe()` helper for jsdom `srcdoc`.

    Reference exact renderer attributes and op fields: `data-ps-style-source-id`, `styleSources`, `styleStrategy`, `sourceId`, `scope.kind`, `hostNid`, `frameNid`, `DIFF_OP.STYLE_SOURCE`, `action`, `source`, and `stale-style-scope`.

    These tests are expected to fail until Plans 09-04 and 09-06 implement scoped renderer CSSOM installation and style-source mutation application.
  </action>
  <verify>
    <automated>set +e; node --test tests/renderer-cssom-mode.test.js; code=$?; test "$code" -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/renderer-cssom-mode.test.js` passes.
    - `rg -n "styleSources|data-ps-style-source-id|STYLE_SOURCE|style-source|stale-style-scope|hostNid|frameNid|textContent" tests/renderer-cssom-mode.test.js` returns matches.
    - `set +e; node --test tests/renderer-cssom-mode.test.js; code=$?; test "$code" -ne 0` passes, proving renderer RED coverage exists before implementation.
  </acceptance_criteria>
  <done>Renderer CSSOM RED tests exist and fail for missing scoped install/style-op behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RED Playwright CSSOM smoke tests</name>
  <files>tests/playwright-cssom-mode.test.js</files>
  <read_first>
    - `tests/playwright-cssom-mode.test.js` to confirm the file does not already exist before creating it.
    - `.planning/phases/09-cssom-capture-mode/09-CONTEXT.md` decisions D-11, D-14, D-20, D-21, D-22, and D-23.
    - `.planning/phases/09-cssom-capture-mode/09-VALIDATION.md` rows 09-W0-03, 09-W0-04, 09-W0-06, and 09-W0-07.
    - `.planning/phases/09-cssom-capture-mode/09-PATTERNS.md` section for `tests/playwright-cssom-mode.test.js`.
    - `tests/playwright-fidelity-phase8.test.js` for real-browser harness, checked-in inject artifact path, and message wait helpers.
    - `src/adapters/playwright-inject.js` and `src/adapters/playwright.js`.
  </read_first>
  <behavior>
    - Test 1: real Chromium checked-in inject artifact can start capture with `styleMode: 'cssom'` and emits `styleStrategy.mode === 'cssom'`.
    - Test 2: a production-style CSS-in-JS class name such as `.css-1a2b3c-active` changes appearance after a class flip because generated computed inline styles do not override the rule.
    - Test 3: a blocked cross-origin stylesheet follows fallback without leaking CSS text; safe href relink or computed fallback keeps the mirror from breaking.
    - Test 4: constructable `CSSStyleSheet` and `document.adoptedStyleSheets` sources appear in `styleSources`.
    - Test 5: `insertRule` and `deleteRule` produce `STREAM.MUTATIONS` entries with `op === 'style-source'`.
    - Test 6: open shadow-root and same-origin frame CSSOM scopes appear in the snapshot and are not flattened into the document.
    - Test 7: payload-size and serialize-latency smoke values are recorded in content-free numeric assertions: CSSOM snapshot bytes are less than computed-mode snapshot bytes for the fixture, and snapshot elapsed time is a finite non-negative number.
  </behavior>
  <action>
    Create `tests/playwright-cssom-mode.test.js` using `node:test`, `node:assert/strict`, `playwright` `chromium`, and `fileURLToPath(new URL('../src/adapters/playwright-inject.js', import.meta.url))` exactly like `tests/playwright-fidelity-phase8.test.js`.

    The fixture HTML should include:
    - CSS-in-JS-like classes `.css-1a2b3c-base` and `.css-1a2b3c-active`.
    - A `<link rel="stylesheet" href="https://cdn.example.test/app.css">` or routed local equivalent that makes `cssRules` unavailable in Chromium.
    - A constructable sheet created with `new CSSStyleSheet()` and assigned to `document.adoptedStyleSheets`.
    - An open shadow root with its own constructable/adopted or inline style source.
    - A same-origin `iframe srcdoc` with a local stylesheet.
    - `insertRule` and `deleteRule` calls after capture start.

    Keep smoke output numeric and content-free. Do not add Phase 12 baseline comparisons, tables, dispersion, WebRTC, CDP screencast, or rrweb comparisons.
  </action>
  <verify>
    <automated>set +e; node --test tests/playwright-cssom-mode.test.js; code=$?; test "$code" -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/playwright-cssom-mode.test.js` passes.
    - `rg -n "css-1a2b3c|adoptedStyleSheets|insertRule|deleteRule|playwright-inject|styleMode|style-source|approxCssBytes|latency|payload" tests/playwright-cssom-mode.test.js` returns matches.
    - `set +e; node --test tests/playwright-cssom-mode.test.js; code=$?; test "$code" -ne 0` passes, proving browser RED coverage exists before implementation.
    - `rg -n "WebRTC|rrweb|baseline|dispersion|table" tests/playwright-cssom-mode.test.js` returns no matches.
  </acceptance_criteria>
  <done>Playwright CSSOM RED smoke exists and fails for missing browser/inject CSSOM behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add RED opt-in CSSOM differential oracle coverage</name>
  <files>tests/differential/fixtures/cssom-mode.html, tests/differential/scenarios/cssom-capture-mode.js, tests/differential/oracle.test.js, tests/differential/divergence-ledger.js</files>
  <read_first>
    - `tests/differential/fixtures/cssom-mode.html` and `tests/differential/scenarios/cssom-capture-mode.js` to confirm they do not already exist before creating them.
    - `tests/differential/oracle.test.js` existing MATRIX and empty-ledger guard pattern.
    - `tests/differential/divergence-ledger.js` existing mismatch-kind entries and stale-entry detection contract.
    - `.planning/phases/09-cssom-capture-mode/09-CONTEXT.md` decision D-24.
    - `.planning/phases/09-cssom-capture-mode/09-PATTERNS.md` sections for differential oracle files.
    - `.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-09-SUMMARY.md` D24 oracle allowance pattern.
  </read_first>
  <behavior>
    - Test 1: default oracle MATRIX entries continue to run with `config: {}` and do not opt into CSSOM mode.
    - Test 2: a new CSSOM oracle entry uses `fixture: 'cssom-mode.html'`, `scenario: cssomCaptureMode`, and config that causes extracted capture to run with `styleMode: 'cssom'`.
    - Test 3: CSSOM divergence is scenario-pinned and ledgered with id `D25-cssom-mode-style-sources`.
    - Test 4: an empty-ledger guard for the CSSOM scenario throws `UNDECLARED DIVERGENCE`, proving the new ledger entry is load-bearing.
  </behavior>
  <action>
    Create `tests/differential/fixtures/cssom-mode.html` with a small body, authored stylesheet rules, and a class flip target. Do not include external network dependencies in this oracle fixture.

    Create `tests/differential/scenarios/cssom-capture-mode.js` exporting a named scenario function `cssomCaptureMode`. The scenario should mutate a CSS-controlled class after start and use the existing scenario style from `tests/differential/scenarios/phase8-protocol-extensions.js`.

    Update `tests/differential/oracle.test.js` to import `cssomCaptureMode` and add one MATRIX entry with `config: { styleMode: 'cssom' }`. Add a CSSOM empty-ledger guard named `cssom-capture-mode with an EMPTY ledger throws UNDECLARED DIVERGENCE -- D25 entry is load-bearing`.

    Add a RED mismatch entry in `tests/differential/divergence-ledger.js` with id `D25-cssom-mode-style-sources`, `kind: 'mismatch'`, affected message types `[STREAM.SNAPSHOT, STREAM.MUTATIONS]`, affected scenario `['cssom-capture-mode']`, and a strict `appliesTo` predicate that only matches CSSOM-specific `styleStrategy`, `styleSources`, or `op: 'style-source'` differences. This can fail until production emits the planned fields, but it must not excuse default-mode computed divergences.
  </action>
  <verify>
    <automated>set +e; node --test tests/differential/oracle.test.js; code=$?; test "$code" -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/differential/fixtures/cssom-mode.html` passes.
    - `test -f tests/differential/scenarios/cssom-capture-mode.js` passes.
    - `rg -n "cssomCaptureMode|cssom-mode.html|styleMode: 'cssom'|D25-cssom-mode-style-sources|EMPTY ledger|styleSources|style-source" tests/differential/oracle.test.js tests/differential/divergence-ledger.js tests/differential/scenarios/cssom-capture-mode.js` returns matches.
    - `set +e; node --test tests/differential/oracle.test.js; code=$?; test "$code" -ne 0` passes, proving oracle RED coverage exists before implementation.
  </acceptance_criteria>
  <done>CSSOM oracle RED coverage exists and is opt-in, scenario-pinned, and ledger-load-bearing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture payload -> renderer scoped CSS insertion | Style sources from the page are installed into mirror document, shadow, and frame scopes. |
| browser CSSOM platform -> checked-in inject artifact | Real Chromium executes the classic script artifact with page-controlled CSS and DOM. |
| extracted stream -> differential oracle | Intentional opt-in divergence must not mask default-mode regressions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | E | renderer scoped style tests | mitigate | RED tests require sandbox/CSP posture to remain unchanged and CSS insertion to use sanitized text or safe links only. |
| T-09-06 | I | Playwright cross-origin fixture | mitigate | RED tests require cross-origin stylesheet fallback without content leakage and content-free diagnostics only. |
| T-09-07 | D | payload/latency smoke | mitigate | RED tests record numeric smoke evidence only and avoid Phase 12 benchmark scope. |
| T-09-08 | R/T | differential ledger | mitigate | RED oracle tests require scenario-pinned CSSOM divergence and an empty-ledger guard so default computed mode is not silently excused. |
</threat_model>

<verification>
```bash
set +e; node --test tests/renderer-cssom-mode.test.js; code=$?; test "$code" -ne 0
set +e; node --test tests/playwright-cssom-mode.test.js; code=$?; test "$code" -ne 0
set +e; node --test tests/differential/oracle.test.js; code=$?; test "$code" -ne 0
```
</verification>

<success_criteria>
- Wave 0 renderer, Playwright, and oracle tests exist and are RED for missing implementation.
- No production source file is modified by this plan.
- D-06, D-11, D-12, D-19, D-21, D-23, and D-24 are represented by explicit test names or assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/09-cssom-capture-mode/09-02-SUMMARY.md`.
</output>
