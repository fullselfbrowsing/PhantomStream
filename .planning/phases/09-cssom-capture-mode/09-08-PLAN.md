---
phase: 09-cssom-capture-mode
plan: 08
type: execute
wave: 5
depends_on:
  - 09-07
files_modified:
  - tests/differential/normalize.js
  - tests/differential/divergence-ledger.js
  - tests/differential/oracle.test.js
  - src/capture/README.md
  - src/renderer/README.md
  - docs/ARCHITECTURE.md
  - docs/SECURITY.md
  - docs/DESIGN-HISTORY.md
autonomous: true
requirements:
  - CAPT-10
must_haves:
  truths:
    - "Default computed mode remains reference-compatible, and CSSOM divergence is opt-in, scenario-pinned, and ledger-load-bearing."
    - "Project docs describe CSSOM mode, fallback chain, style-source ops, security posture, and remaining browser boundaries without claiming Phase 12 ablation results."
    - "The Phase 09 quick gate, Playwright CSSOM smoke, and full `npm test` pass."
  artifacts:
    - path: "tests/differential/divergence-ledger.js"
      provides: "Narrow CSSOM divergence entry D25-cssom-mode-style-sources"
    - path: "docs/ARCHITECTURE.md"
      provides: "Architecture documentation for stylesheet-centric capture mode"
    - path: "docs/SECURITY.md"
      provides: "Security documentation for CSSOM fallback and sanitizer boundaries"
    - path: "docs/DESIGN-HISTORY.md"
      provides: "Design history entry for frozen-style drift fix and payload/latency smoke posture"
  key_links:
    - from: "tests/differential/oracle.test.js"
      to: "tests/differential/divergence-ledger.js"
      via: "D25 CSSOM empty-ledger guard"
      pattern: "D25-cssom-mode-style-sources"
    - from: "docs/SECURITY.md"
      to: "src/capture/index.js and src/renderer/sanitize.js"
      via: "sanitizeForWire('css') and scrubCssText documented as CSSOM boundaries"
      pattern: "CSSOM"
---

<objective>
Finalize CSSOM oracle discipline, documentation, and full automated verification.

Purpose: Phase 9 is both a framework feature and the paper's later ablation arm. The final plan keeps research integrity intact by documenting what shipped, what remains out of scope, and proving default-mode compatibility.
Output: Green differential oracle, updated docs, green quick gate, green Playwright CSSOM smoke, and green full suite.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-cssom-capture-mode/09-CONTEXT.md
@.planning/phases/09-cssom-capture-mode/09-RESEARCH.md
@.planning/phases/09-cssom-capture-mode/09-VALIDATION.md
@.planning/phases/09-cssom-capture-mode/09-PATTERNS.md
@.planning/phases/09-cssom-capture-mode/09-07-SUMMARY.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-09-SUMMARY.md
@tests/differential/normalize.js
@tests/differential/divergence-ledger.js
@tests/differential/oracle.test.js
@src/capture/README.md
@src/renderer/README.md
@docs/ARCHITECTURE.md
@docs/SECURITY.md
@docs/DESIGN-HISTORY.md

<interfaces>
Final Phase 09 commands:

```bash
node --test tests/capture-cssom-mode.test.js tests/renderer-cssom-mode.test.js tests/security-cssom-sanitize.test.js tests/protocol.test.js tests/adapter-exports.test.js
node --test tests/playwright-cssom-mode.test.js
npm test
```
</interfaces>
</context>

<decision_coverage>
- D-01 and D-02: Documents that planning used conservative defaults from context and surfaces no research blocker requiring deviation.
- D-03: Oracle and docs state computed mode remains the default.
- D-07 and D-08: Docs state the fallback chain and no CORS/CSP/security bypass.
- D-10, D-17, and D-18: Docs state capture and renderer sanitizer boundaries and sandbox/CSP posture.
- D-20: Final gate includes adapter artifact tests.
- D-23: Docs describe payload-size and serialize-latency smoke only; full ablation tables remain Phase 12.
- D-24: Oracle divergence is opt-in and narrowly ledgered.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finalize CSSOM differential oracle ledger</name>
  <files>tests/differential/normalize.js, tests/differential/divergence-ledger.js, tests/differential/oracle.test.js</files>
  <read_first>
    - `tests/differential/normalize.js` current empty-sidecar normalization and CSSOM field handling, if any.
    - `tests/differential/divergence-ledger.js` D25 entry from Plan 09-02 and existing D24 Phase 8 predicates.
    - `tests/differential/oracle.test.js` CSSOM MATRIX entry and empty-ledger guard from Plan 09-02.
    - `.planning/phases/09-cssom-capture-mode/09-CONTEXT.md` decision D-24.
    - `.planning/phases/09-cssom-capture-mode/09-PATTERNS.md` differential oracle sections.
  </read_first>
  <behavior>
    - Default-mode MATRIX entries still use `config: {}` and pass without CSSOM ledger entries.
    - CSSOM MATRIX entry remains the only entry with `config: { styleMode: 'cssom' }`.
    - `D25-cssom-mode-style-sources` matches only CSSOM scenario differences involving `styleStrategy`, `styleSources`, or `op: 'style-source'`.
    - Empty-ledger CSSOM guard throws `UNDECLARED DIVERGENCE`.
    - Stale-entry detection proves D25 matched at least one real CSSOM divergence.
  </behavior>
  <action>
    Tighten `tests/differential/divergence-ledger.js` D25 predicate so it requires `scenarioName === 'cssom-capture-mode'` and rejects all other scenarios. Match only:
    - snapshot payload differences where extracted has `styleStrategy.mode === 'cssom'` or non-empty `styleSources`;
    - mutation payload differences where extracted has at least one mutation with `op === DIFF_OP.STYLE_SOURCE`.

    Update `tests/differential/normalize.js` only if needed to normalize absent-vs-empty CSSOM arrays for default computed mode. Do not normalize away non-empty `styleSources`, non-empty `styleStrategy`, or `style-source` mutations.

    Ensure `tests/differential/oracle.test.js` includes a CSSOM empty-ledger guard and that default-mode zero-consultation assertions continue to exclude only scenario-pinned mismatch entries.
  </action>
  <verify>
    <automated>node --test tests/differential/oracle.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "D25-cssom-mode-style-sources|cssom-capture-mode|styleStrategy|styleSources|STYLE_SOURCE|EMPTY ledger" tests/differential/divergence-ledger.js tests/differential/oracle.test.js tests/differential/normalize.js` returns matches.
    - `node --test tests/differential/oracle.test.js` passes.
    - `rg -n "styleSources.*return true|return true.*styleSources|scenarioName !== 'cssom-capture-mode'" tests/differential/divergence-ledger.js` returns matches proving the predicate is scenario-guarded and shape-guarded.
  </acceptance_criteria>
  <done>CSSOM oracle divergence is opt-in, narrow, and load-bearing while default computed mode remains reference-compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Document CSSOM mode, fallback chain, security posture, and final gates</name>
  <files>src/capture/README.md, src/renderer/README.md, docs/ARCHITECTURE.md, docs/SECURITY.md, docs/DESIGN-HISTORY.md</files>
  <read_first>
    - `src/capture/README.md` current Phase 8 capture surfaces and CSSOM deferral text.
    - `src/renderer/README.md` current renderer scope and sandbox docs.
    - `docs/ARCHITECTURE.md` known limitations and style pipeline sections.
    - `docs/SECURITY.md` sanitizer, CSP, sandbox, iframe/shadow, and CSSOM warning sections.
    - `docs/DESIGN-HISTORY.md` frozen computed-style drift and performance lessons.
    - `.planning/phases/09-cssom-capture-mode/09-CONTEXT.md` decisions D-03 through D-24.
  </read_first>
  <behavior>
    - Capture README documents `styleMode: 'computed' | 'cssom'`, default computed mode, `fetchStylesheet({ href, scope, ownerKind })`, `styleSources`, `styleStrategy`, and style-source ops.
    - Renderer README documents scoped CSSOM installation in document, shadow root, and same-origin frame scopes and `DIFF_OP.STYLE_SOURCE` upsert/replace/remove handling.
    - Architecture docs describe CSSOM mode as the Phase 9 fix for frozen-style drift and payload reduction, with Phase 8 scopes preserved.
    - Security docs describe cross-origin stylesheet fallback, explicit host fetch capability, sanitizer chokepoints, content-free diagnostics, and unchanged sandbox/CSP posture.
    - Design history records the reason for stylesheet-centric mode and states that full ablation tables remain Phase 12.
  </behavior>
  <action>
    Update the five docs with exact names:
    - `styleMode: 'computed' | 'cssom'`
    - `fetchStylesheet({ href, scope, ownerKind })`
    - `styleSources[]`
    - `styleStrategy`
    - `DIFF_OP.STYLE_SOURCE`
    - `action: 'upsert' | 'replace' | 'remove'`
    - fallback reasons `cssRules-blocked`, `href-relinked`, `adapter-fetch`, `computed-fallback`
    - diagnostics `cssom-hook-unavailable`, `cssom-style-source-stale`, `stale-style-scope`

    Remove or revise any text that says full CSSOM capture is still deferred. Preserve explicit out-of-scope boundaries: closed shadow roots, cross-origin iframe content, media stream mirroring, npm publishing, FSB swap-in, and Phase 12 baseline/ablation tables.
  </action>
  <verify>
    <automated>rg -n "styleMode|styleSources|styleStrategy|STYLE_SOURCE|fetchStylesheet|cssRules-blocked|href-relinked|adapter-fetch|computed-fallback|cssom-hook-unavailable|stale-style-scope|Phase 12" src/capture/README.md src/renderer/README.md docs/ARCHITECTURE.md docs/SECURITY.md docs/DESIGN-HISTORY.md && node --test tests/capture-cssom-mode.test.js tests/renderer-cssom-mode.test.js tests/security-cssom-sanitize.test.js tests/protocol.test.js tests/adapter-exports.test.js && node --test tests/playwright-cssom-mode.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "styleMode|styleSources|styleStrategy|STYLE_SOURCE|fetchStylesheet" src/capture/README.md src/renderer/README.md docs/ARCHITECTURE.md docs/SECURITY.md docs/DESIGN-HISTORY.md` returns matches.
    - `rg -n "closed shadow|cross-origin iframe content|media stream|Phase 12|baseline|ablation" docs/ARCHITECTURE.md docs/SECURITY.md docs/DESIGN-HISTORY.md` returns matches preserving explicit boundaries.
    - `node --test tests/capture-cssom-mode.test.js tests/renderer-cssom-mode.test.js tests/security-cssom-sanitize.test.js tests/protocol.test.js tests/adapter-exports.test.js` passes.
    - `node --test tests/playwright-cssom-mode.test.js` passes.
    - `npm test` passes.
  </acceptance_criteria>
  <done>Docs accurately describe the shipped CSSOM mode and the final Phase 09 automated gates are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSSOM divergence -> oracle ledger | Intentional opt-in divergence must not hide default-mode regressions. |
| docs -> downstream implementers/users | Documentation must not imply browser-boundary bypasses or unrun Phase 12 benchmark claims. |
| final gate -> release readiness | Automated gates must sample unit, security, oracle, adapter, browser, and full-suite behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-29 | R | differential oracle | mitigate | D25 ledger entry is scenario-pinned, shape-pinned, and proven load-bearing by empty-ledger test. |
| T-09-30 | I | documentation | mitigate | Docs explicitly state no cross-origin CSS content bypass, no cross-origin iframe content, and content-free diagnostics. |
| T-09-31 | D | final verification | mitigate | Run focused quick command, Playwright CSSOM smoke, and full `npm test`. |
| T-09-32 | T | security docs | mitigate | Docs name both CSS sanitizer chokepoints and unchanged sandbox/CSP posture. |
</threat_model>

<verification>
```bash
node --test tests/differential/oracle.test.js
node --test tests/capture-cssom-mode.test.js tests/renderer-cssom-mode.test.js tests/security-cssom-sanitize.test.js tests/protocol.test.js tests/adapter-exports.test.js
node --test tests/playwright-cssom-mode.test.js
npm test
```
</verification>

<success_criteria>
- CSSOM oracle divergence is opt-in, narrow, and load-bearing.
- Docs reflect Phase 9 shipped behavior and preserve out-of-scope boundaries.
- Phase 09 quick command, Playwright CSSOM smoke, and full suite pass.
</success_criteria>

<output>
After completion, create `.planning/phases/09-cssom-capture-mode/09-08-SUMMARY.md`.
</output>
