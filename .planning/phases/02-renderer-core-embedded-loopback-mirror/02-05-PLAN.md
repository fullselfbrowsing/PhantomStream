---
phase: 02-renderer-core-embedded-loopback-mirror
plan: 05
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - examples/loopback-transport.js
  - examples/serve.js
  - examples/loopback-mirror.html
  - package.json
autonomous: true
requirements: [ADPT-04, VIEW-01]
must_haves:
  truths:
    - "`npm run example:loopback` starts a zero-dependency localhost server and prints the demo URL"
    - "examples/loopback-mirror.html imports capture + viewer as native ES modules (the embedded-SDK adapter, ADPT-04) and wires them through the loopback transport with the recursion guard"
    - "The demo page implements the approved 02-UI-SPEC contract: two-pane layout, LIVE badge, playground controls, exact copy strings"
    - "Module scripts are served with text/javascript MIME; path traversal is rejected; server binds localhost only"
    - "The renderer is importable as @fullselfbrowsing/phantom-stream/renderer (package exports entry)"
  artifacts:
    - path: "examples/loopback-transport.js"
      provides: "createLoopbackTransport — both transport ends + onControl glue seam"
      exports: ["createLoopbackTransport"]
    - path: "examples/serve.js"
      provides: "Dependency-free node:http static server, repo root, MIME map, traversal guard"
    - path: "examples/loopback-mirror.html"
      provides: "First-light demo page per 02-UI-SPEC"
      min_lines: 150
    - path: "package.json"
      provides: "./renderer export + example:loopback script"
      contains: "./renderer"
  key_links:
    - from: "examples/loopback-mirror.html"
      to: "src/renderer/index.js + src/capture/index.js"
      via: "native ESM imports ../src/... (served over http by serve.js)"
      pattern: "\\.\\./src/(renderer|capture)/index\\.js"
    - from: "examples/loopback-mirror.html"
      to: "capture recursion guard"
      via: "skipElement attribute predicate on data-phantomstream-ui"
      pattern: "data-phantomstream-ui"
    - from: "package.json"
      to: "examples/serve.js"
      via: "scripts entry example:loopback"
      pattern: "example:loopback"
---

<objective>
Ship the project's "first light" demo: one plain HTML page importing capture + viewer directly as native ES modules (the embedded-SDK adapter, ADPT-04), mirrored live through a loopback transport with zero infrastructure — served by a dependency-free Node static server. Layout, colors, copy, and interactions are LOCKED by the approved 02-UI-SPEC; this plan implements that contract, it does not redesign it.

Purpose: the human-visible proof of phase success criteria 1 and 2, and the copy-pasteable embedding example for every future host.
Output: `examples/` (transport, server, demo page) + package.json wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-renderer-core-embedded-loopback-mirror/02-UI-SPEC.md
@.planning/phases/02-renderer-core-embedded-loopback-mirror/02-RESEARCH.md
@src/renderer/index.js
@src/capture/index.js
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Loopback transport module, static server, package wiring</name>
  <files>examples/loopback-transport.js, examples/serve.js, package.json</files>
  <read_first>
    - .planning/phases/02-renderer-core-embedded-loopback-mirror/02-RESEARCH.md Pattern 1 (createLoopbackTransport complete design — copy it) and Pattern 7 (serve.js complete implementation — copy, then apply the security deltas below)
    - package.json (current exports/scripts shape — extend, do not restructure)
    - src/capture/index.js lines 142-160 (capture Transport contract the loopback's captureTransport must satisfy)
  </read_first>
  <action>
    (1) Create examples/loopback-transport.js: ES module exporting createLoopbackTransport() exactly per 02-RESEARCH Pattern 1 — two Sets (toViewer for ext:* handlers, toHost for dash:* handlers), fanOut via queueMicrotask (one async hop breaks resync re-entrancy; FIFO preserved), returning an object with captureTransport (send), viewerTransport (send + onMessage returning unsubscribe), and onControl returning unsubscribe. Style: var + function expressions (capture-core convention), JSDoc on the export, file-top comment noting the design source and that tests deliberately duplicate this locally (parallel-safe convention — never import from tests/ into examples/ or vice versa).
    (2) Create examples/serve.js per 02-RESEARCH Pattern 7: node:http createServer, ROOT = repo root resolved from new URL('..', import.meta.url), MIME map with text/javascript for .js and .mjs (strict-MIME ESM requirement) plus .html/.css/.json/.svg/.png/.ico/.map, path-traversal guard (resolved path must start with ROOT + sep), directory requests resolve only to an explicit index.html (no directory listing — a directory without index.html returns 404), unknown extensions get application/octet-stream, missing files 404, traversal 403. SECURITY deltas beyond the research snippet (required): listen on port 8642 bound to '127.0.0.1' (localhost only); decodeURIComponent the pathname inside try/catch BEFORE resolving (malformed encodings get 400) so percent-encoded dot-dot cannot bypass the prefix guard. On listen, print the full demo URL: http://localhost:8642/examples/loopback-mirror.html
    (3) Edit package.json: add the exports entry "./renderer" mapping to "./src/renderer/index.js" (alongside ./protocol and ./capture) and the scripts entry "example:loopback" running "node examples/serve.js". Leave the test glob untouched (flat tests/ naming needs no change).
  </action>
  <verify>
    <automated>node --check examples/serve.js && node --check examples/loopback-transport.js && node -e "import('./examples/loopback-transport.js').then(m => process.exit(typeof m.createLoopbackTransport === 'function' ? 0 : 1))" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --check examples/serve.js` and `node --check examples/loopback-transport.js` exit 0
    - With the server running in background: `curl -s -o /dev/null -w '%{content_type}' http://127.0.0.1:8642/src/renderer/index.js` starts with `text/javascript`
    - `curl -s -o /dev/null -w '%{http_code}' --path-as-is http://127.0.0.1:8642/../package.json` returns 403 or 404 (never 200), and the percent-encoded variant `/%2e%2e/%2e%2e/etc/passwd` returns 400/403/404 (never 200); kill the background server afterward
    - `node -p "JSON.parse(require('fs').readFileSync('package.json','utf8')).exports['./renderer']"` prints ./src/renderer/index.js and `node -p "JSON.parse(require('fs').readFileSync('package.json','utf8')).scripts['example:loopback']"` prints node examples/serve.js
    - serve.js contains '127.0.0.1' (localhost bind) and contains no require/import of anything outside node: builtins
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Server serves the repo root with correct ESM MIME, localhost-only, traversal-proof; transport module importable; package exports the renderer.</done>
</task>

<task type="auto">
  <name>Task 2: The first-light demo page (loopback-mirror.html)</name>
  <files>examples/loopback-mirror.html</files>
  <read_first>
    - .planning/phases/02-renderer-core-embedded-loopback-mirror/02-UI-SPEC.md sections "Demo Page Contract", "Copywriting Contract", "Color", "Typography", "Spacing Scale", "Motion & Interaction Contract" (the LOCKED design — implement verbatim)
    - .planning/phases/02-renderer-core-embedded-loopback-mirror/02-RESEARCH.md Pattern 4 (wiring order + recursion guard) and Pitfall 3 (alert blocks paint — the log-line affordance)
    - examples/loopback-transport.js (the module this page imports)
    - src/renderer/index.js and src/capture/index.js (factory signatures and handle methods)
  </read_first>
  <action>
    Create examples/loopback-mirror.html — one self-contained file: inline style block plus one inline module script importing ./loopback-transport.js, ../src/renderer/index.js, ../src/capture/index.js, and CONTROL from ../src/protocol/messages.js. Implement the 02-UI-SPEC demo contract EXACTLY.
    LAYOUT: page background #0f1117, text #e0e0e0, padding 24px, max-width 1200px centered; header with title "PhantomStream — Loopback Mirror" (28px/600), subtitle "A page mirroring itself live. No network, no server — one document, two DOMs." (15px/400, #888), and the LIVE badge (8px round #22c55e dot pulsing via a 2000ms ease-in-out infinite keyframes rule wrapped in a prefers-reduced-motion: no-preference media query; 13px/600 label "LIVE"; flips to solid #6b7280 with label "PAUSED" when auto-mutate is off); two-pane CSS grid 1fr 1fr gap 16px, stacking to one column under 900px; each pane #1e1e2e background, 1px solid #333 border, 12px radius, 16px padding; pane headers "SOURCE" / "MIRROR" at 13px/600 uppercase, letter-spacing 1px, #888, margin-bottom 8px; the mirror stage div has aspect-ratio 16/10 and min-height 200px with the empty state behind the viewer ("Waiting for first snapshot…" heading + "The mirror appears as soon as capture sends its first frame." body — 13px #888 centered); footer 13px mono #888: "Loopback transport — zero network, zero servers. Capture and viewer share this document." plus a small mono log area for channel events.
    CONTROLS (left pane, gap 8px): primary "Add row" (#f59e0b background, #0f1117 text, 600 weight, 8px 16px padding, 6px radius, hover brightness 1.1); secondaries "Remove row", "Edit text", "Show dialog", "Auto-mutate" (transparent background, 1px solid #333, #e0e0e0 text, hover rgba(255,255,255,0.06)); 150ms ease hover transitions. The amber accent #f59e0b appears ONLY on the primary CTA — never on links, headers, badges, or borders (UI-SPEC reserved list).
    PLAYGROUND: a list of sample rows in the source pane. Add row appends a row; Remove row removes the last; Edit text rewrites a random row's text; Auto-mutate toggles a ~1000ms interval of random row mutations and flips the LIVE/PAUSED badge; Show dialog calls native alert with the message "PhantomStream mirrored this dialog." and appends the footer log line "dialog mirrored: alert open → closed" after dismissal (Pitfall 3: the mirrored card is typically not paintable during a same-page alert — the log line is the visible proof; include an HTML comment explaining this, and note the card IS visible in two-context deployments).
    WIRING (order is load-bearing — RESEARCH Pattern 4, loopback has no buffering): create the transport; createViewer({ container: mirrorStage, transport: transport.viewerTransport }) so the viewer DOM exists and is skip-marked BEFORE capture; createCapture({ transport: transport.captureTransport, skipElement: attribute predicate returning true when el.getAttribute exists and el.getAttribute('data-phantomstream-ui') is truthy }) — attribute-based, never object identity; transport.onControl mapping CONTROL.START to capture.start() (also map STOP/PAUSE/RESUME to the matching capture handle methods for completeness); finally capture.start() on load — the mirror starts automatically (criterion 1 auto-attach). The page must NOT set or modify any sandbox attribute itself (the viewer owns the sandbox contract).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('examples/loopback-mirror.html','utf8'); const must=['PhantomStream — Loopback Mirror','one document, two DOMs','Waiting for first snapshot…','data-phantomstream-ui','../src/renderer/index.js','../src/capture/index.js','./loopback-transport.js','Add row','Auto-mutate','prefers-reduced-motion','#f59e0b']; const missing=must.filter(m=>!s.includes(m)); if(missing.length){console.error('MISSING:',missing);process.exit(1)} if(/sandbox\s*=/.test(s)){console.error('page must not set sandbox');process.exit(1)} console.log('demo contract strings OK')" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - The contract-string check script above exits 0 (all locked copy + import paths + recursion-guard marker present; no sandbox override in the page)
    - With `npm run example:loopback` running, `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8642/examples/loopback-mirror.html` returns 200 (kill the server afterward)
    - The module script's wiring order in source reads: transport → createViewer → createCapture → onControl → capture.start()
    - The five control buttons exist with the exact UI-SPEC labels: Add row / Remove row / Edit text / Show dialog / Auto-mutate
    - `npm test` exits 0 (suite untouched by example files)
  </acceptance_criteria>
  <done>The first-light demo exists per the approved UI contract and is servable end-to-end; real-browser verification happens in plan 02-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → serve.js | Arbitrary request paths hit the static server |
| demo page → viewer | The page mirrors itself; mirrored HTML renders in the sandboxed iframe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Information disclosure | serve.js path traversal / directory listing | mitigate | resolve + ROOT-prefix guard, decodeURIComponent-before-resolve, no directory listing (explicit index.html only), curl-verified in acceptance criteria |
| T-02-17 | Elevation | demo server reachable from the network | mitigate | bind 127.0.0.1 only; dev-demo scope documented by the printed URL |
| T-02-18 | Denial | mirror-of-mirror recursion in the self-mirroring page | mitigate | data-phantomstream-ui skipElement predicate (attribute-based) — same guard proven by the plan 02-04 e2e tests |
| T-02-19 | Elevation | demo page weakening the sandbox | mitigate | page never touches the sandbox attribute (greppable acceptance criterion); the viewer's creation-time assertion throws on any deviation |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase; serve.js uses node: builtins only |
</threat_model>

<verification>
- Syntax checks + import smoke test for both example modules
- curl checks: ESM MIME correct, traversal rejected, demo page 200
- package.json exports/scripts entries present
- `npm test` green
</verification>

<success_criteria>
- `npm run example:loopback` + one browser tab = live self-mirror (zero external dependencies)
- The demo doubles as the canonical embedded-SDK (ADPT-04) usage example, recursion guard included
- UI-SPEC contract implemented verbatim (copy, colors, layout, motion)
</success_criteria>

<output>
Create `.planning/phases/02-renderer-core-embedded-loopback-mirror/02-05-SUMMARY.md` when done.
</output>
