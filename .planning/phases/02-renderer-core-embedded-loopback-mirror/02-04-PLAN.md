---
phase: 02-renderer-core-embedded-loopback-mirror
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - tests/renderer-loopback.test.js
  - src/renderer/README.md
autonomous: true
requirements: [ADPT-04, VIEW-04, VIEW-06]
must_haves:
  truths:
    - "Capture + viewer run end-to-end in ONE jsdom page over a loopback transport: snapshot renders, a live DOM mutation in the source pane appears in the mirror document (the first end-to-end proof)"
    - "The attribute-based skipElement recursion guard works: the snapshot contains no viewer DOM and no nested srcdoc iframe; mutation echo is absent"
    - "Stale-miss threshold triggers CONTROL.START through the loopback, the glue restarts capture, and a fresh snapshot with new identity arrives (recovery proven end-to-end)"
    - "A custom overlay kind registered via handle.registerOverlay receives its payload end-to-end from a capture-side overlayProvider; dialog open/close mirrors through STREAM.DIALOG"
    - "src/renderer/README.md documents the viewer contract and every intentional divergence from dashboard.js"
  artifacts:
    - path: "tests/renderer-loopback.test.js"
      provides: "End-to-end integration test (the executable form of phase success criterion 2)"
      min_lines: 150
    - path: "src/renderer/README.md"
      provides: "Viewer docs + divergence ledger + Phase-3 known-gaps queue"
      contains: "divergence"
  key_links:
    - from: "tests/renderer-loopback.test.js"
      to: "src/capture/index.js + src/renderer/index.js"
      via: "createCapture + createViewer wired through a local createLoopbackTransport"
      pattern: "createCapture|createViewer"
    - from: "tests/renderer-loopback.test.js"
      to: "iframe.contentDocument"
      via: "manual srcdoc parse glue cd.open()/write(iframe.srcdoc)/close() — jsdom never parses srcdoc"
      pattern: "\\.open\\(\\)"
    - from: "loopback glue"
      to: "capture.start()"
      via: "onControl(CONTROL.START) → capture.start() (resync round-trip)"
      pattern: "CONTROL\\.START"
---

<objective>
Prove phase success criterion 2 in executable form: capture core + viewer running end-to-end in a single page with zero infrastructure, tracking DOM mutations in real time — including the recursion guard, the CONTROL.START recovery round-trip, dialog mirroring, and a custom overlay kind flowing capture→wire→registry. Then write the renderer README with the divergence ledger (Phase 1 discipline: every intentional departure explicit, never accidental).

Purpose: the first end-to-end proof of the framework, in CI forever.
Output: `tests/renderer-loopback.test.js` green; `src/renderer/README.md` rewritten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-renderer-core-embedded-loopback-mirror/02-RESEARCH.md
@.planning/phases/02-renderer-core-embedded-loopback-mirror/02-PATTERNS.md
@src/renderer/index.js
@src/capture/index.js
@src/capture/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Loopback e2e — core mirror path + recursion guard + resync round-trip</name>
  <files>tests/renderer-loopback.test.js</files>
  <read_first>
    - .planning/phases/02-renderer-core-embedded-loopback-mirror/02-RESEARCH.md "Loopback e2e test recipe" + Pattern 1 (createLoopbackTransport full design) + Pattern 4 (recursion guard, wiring order — viewer BEFORE capture, loopback has no buffering)
    - tests/capture-skip.test.js (setupEnv with AUDITED_GLOBALS swap, settle, silentLogger — duplicate ALL helpers locally per the parallel-safe convention stated at its top)
    - tests/differential/harness.js lines 192-229 and 282-286 (globals-swap precedent + settle cadence)
    - src/capture/index.js lines 142-203 (createCapture options: transport, logger, skipElement, overlayProvider)
    - src/renderer/index.js (createViewer signature + handle shape as built in plan 02-03)
  </read_first>
  <behavior>
    - Test: full wiring (transport → createViewer → createCapture with skipElement attribute predicate → onControl glue → capture.start() → settle) produces exactly one STREAM.SNAPSHOT and a non-empty iframe.srcdoc containing the source pane's nid-stamped content
    - Test: the srcdoc string contains NO 'data-phantomstream-ui' and NO nested iframe-with-srcdoc (recursion guard — snapshot path clean)
    - Test: after manual srcdoc glue (cd.open(); cd.write(iframe.srcdoc); cd.close();), appending an element to the source pane + settle yields an applied 'add' in the mirror document (cd.querySelector finds the new nid-stamped node) — the live-mutation proof
    - Test: a text edit in the source pane + settle updates the mirrored node's textContent
    - Test: setting iframe.srcdoc does NOT echo back as capture mutations (no STREAM.MUTATIONS containing attr 'srcdoc' — diff-path recursion guard)
    - Test: injecting 3 MUTATIONS batches with bogus parentNids (current identity) through the capture end drives the viewer to send CONTROL.START; the onControl glue calls capture.start(); a SECOND snapshot with a DIFFERENT streamSessionId arrives and resets the viewer (srcdoc replaced; subsequent valid mutation applies after re-glue)
  </behavior>
  <action>
    Create tests/renderer-loopback.test.js. Duplicate locally (parallel-safe convention): setupEnv building a JSDOM page (url https://fixture.test/page, pretendToBeVisual: true, VirtualConsole) whose body contains a source pane div (a few nid-target child rows) and an empty mirror-container div; the audited-globals swap + presence-aware restore + teardown that destroys viewer/stops capture FIRST; settle(win) (the 0ms / rAF / 20ms cadence); silent + recording loggers. Implement a local createLoopbackTransport per 02-RESEARCH Pattern 1 VERBATIM: Set-based fan-out, queueMicrotask hop (breaks resync re-entrancy), returning { captureTransport: {send}, viewerTransport: {send, onMessage}, onControl } — note settle's 20ms tail covers the microtask hop. Wiring ORDER inside each test (Pattern 4 — no buffering, viewer must exist before the first snapshot): transport = createLoopbackTransport(); viewer = createViewer({ container: mirrorContainer, transport: transport.viewerTransport, logger }); capture = createCapture({ transport: transport.captureTransport, logger, skipElement: function (el) { return !!(el.getAttribute && el.getAttribute('data-phantomstream-ui')); } }) — attribute-based predicate, NEVER object identity (runs against detached clones during serialization, live elements during diffing); transport.onControl(function (type) { if (type === CONTROL.START) capture.start(); }); capture.start(); await settle. Record all viewer-bound messages by also subscribing a recorder via viewerTransport.onMessage, and all control sends via onControl. Implement the behavior list. For the resync test, craft MUTATIONS payloads stamped with the CURRENT identity (read it from the recorded snapshot payload) so they pass isCurrentStream but miss their parentNid. Assert CONTROL.START count is exactly 1 before the new snapshot lands (latch). After the second snapshot, redo the srcdoc glue before asserting recovery.
  </action>
  <verify>
    <automated>node --test tests/renderer-loopback.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/renderer-loopback.test.js` exits 0 with at least 6 passing tests
    - The file contains the manual srcdoc glue sequence (open/write/close on contentDocument) and a comment citing the jsdom srcdoc limitation
    - The skipElement predicate in the file is attribute-based (`getAttribute`), with zero identity comparisons against viewer elements
    - The resync assertion counts CONTROL.START messages === 1 for the first generation
    - `npm test` exits 0 (full suite green with the new e2e in the glob)
  </acceptance_criteria>
  <done>Phase success criterion 2 is an executable, CI-enforced test: one page, capture + viewer, live mirror, self-healing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Loopback e2e — dialog + custom overlay channels</name>
  <files>tests/renderer-loopback.test.js</files>
  <read_first>
    - tests/renderer-loopback.test.js (as written in Task 1 — extend the same file, reuse its local helpers)
    - .planning/phases/02-renderer-core-embedded-loopback-mirror/02-RESEARCH.md recipe step 9 (send STREAM.DIALOG through the transport — never run the dialog interceptor, it needs runScripts:'dangerously') and Pattern 5 (overlay kinds)
    - src/renderer/overlays.js (built-in class names ps-overlay-* for DOM assertions)
  </read_first>
  <behavior>
    - Test: sending STREAM.DIALOG {dialog:{type:'alert', state:'open', message:'saved!'}} through the capture end + settle → the viewer's ps-overlay-dialog backdrop has display 'flex', type label 'Alert', message 'saved!'; sending state:'closed' → display 'none'
    - Test: capture created with overlayProvider returning {glow:null, progress:null, badge:{x:5,y:6,w:10,h:8,text:'agent'}} and viewer with handle.registerOverlay('badge', recordingRenderFn) → after a broadcast trigger (start/mutation + settle), recordingRenderFn was called with payload.text 'agent', a mapped anchorRect object, and the layer element — custom DOM-anchored overlays proven END-TO-END (capture → wire → registry)
    - Test: an unregistered kind from the provider (e.g. sparkles) → viewer logs an 'unknown overlay kind' warn and nothing throws (recording logger assertion)
    - Test: a new snapshot resets overlays (dialog hidden after resync even if previously open)
  </behavior>
  <action>
    Extend tests/renderer-loopback.test.js with the side-channel tests per the behavior list, reusing the Task 1 helpers and wiring order. Dialog messages are injected via transport.captureTransport.send(STREAM.DIALOG, {dialog: {...}}) — do NOT run capture's dialog interceptor (requires runScripts:'dangerously'; RESEARCH recipe step 9); this still exercises the full viewer-side path (dispatch → overlays.handleDialogMessage → card DOM). For the custom-overlay test, rely on the plan-02-02 capture edit: configure overlayProvider at createCapture time, register the custom kind on the viewer handle BEFORE capture.start(), then trigger a broadcast (start force-broadcasts; if needed, mutate the source pane and settle past the overlay throttle — read src/capture/index.js broadcast call sites to pick the deterministic trigger). Assert the renderFn argument shapes: (payloadValue, anchorRect, layer) where anchorRect carries numeric top/left/width/height mapped through the viewer's scaleState.
  </action>
  <verify>
    <automated>node --test tests/renderer-loopback.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/renderer-loopback.test.js` exits 0 with at least 10 passing tests total (Task 1 + Task 2)
    - The custom-overlay test asserts the renderFn was invoked with the provider's payload value (deep-includes text 'agent')
    - The dialog tests assert display values 'flex' and 'none' on the ps-overlay-dialog element
    - `npm test` exits 0
  </acceptance_criteria>
  <done>VIEW-04 ("hosts can send custom DOM-anchored overlays through the documented message type") and the dialog half of VIEW-06 are proven end-to-end in CI.</done>
</task>

<task type="auto">
  <name>Task 3: Renderer README + divergence ledger</name>
  <files>src/renderer/README.md</files>
  <read_first>
    - src/capture/README.md (the section template to mirror: identity/provenance paragraph, factory usage, options table, contract sections, divergence ledger entries, "Behavioral changes queued", environment note)
    - src/renderer/README.md (current stub — replaced)
    - .planning/phases/02-renderer-core-embedded-loopback-mirror/02-UI-SPEC.md "Reference Parity Ledger" section (the pre-compiled divergence list to encode)
    - src/renderer/index.js + src/renderer/overlays.js (final API surface to document)
  </read_first>
  <action>
    Rewrite src/renderer/README.md mirroring src/capture/README.md's structure. Sections: (1) identity + provenance (extracted from reference/dashboard/dashboard.js stream-relevant ranges; parity bar; Phase 2). (2) Usage: createViewer({ container, transport }) example with the loopback transport, the ViewerTransport typedef ({send, onMessage → unsubscribe}), handle { detach, destroy, registerOverlay } semantics (detach = unsubscribe + remove DOM; destroy = detach + disconnect observers + clear registry, idempotent). (3) Options table (container / transport / logger). (4) Overlay channel contract: payload-key-as-kind dispatch, registerOverlay(kind, renderFn(payload, anchorRect, layer)), anchor-rect priority (nid → x/y/w/h → null), built-ins glow/progress/dialog, custom renderFns own their escaping, unknown kinds logged + ignored. (5) Recursion guard for same-page hosts: the data-phantomstream-ui marker + the copy-pasteable attribute-based skipElement predicate, with the warning that predicates MUST be attribute/id-based (clone-vs-live). (6) DIVERGENCE LEDGER — one explicit entry each: layout modes dropped (hosts own layout, D-03); dash:request-status dropped; tabId identity checks dropped; transport-event ring buffers dropped; FSB 9-state preview machine → minimal waiting/streaming gate (VIEW-02 lands Phase 4); Font Awesome → inline SVG icons; iframe title renamed "PhantomStream live mirror"; aria-live on dialog card + prefers-reduced-motion guards (additive); glow 2px/4px/shadow + progress pill visuals are documented reconstructions (reference CSS not vendored); dialog identity-nesting quirk ported as parity (top-level check always accepts dialogs); resync message is CONTROL.START — dash:request-snapshot does not exist in the protocol. (7) "Behavioral changes queued for Phase 3+": raw inlineStyles </style> breakout (Pitfall 9 — render-side sanitizer chokepoint), on* attributes surviving capture (sandbox is the backstop; SEC-01/SEC-02), pre-onload mutation drop self-heals via resync (Pitfall 4), per-op querySelector hot path (Phase 7 Map<nid,Node>). (8) Environment: jsdom srcdoc limitation + the write-glue test pattern; sandbox contract statement (allow-same-origin only, asserted, full embed security contract documented in Phase 3).
  </action>
  <verify>
    <automated>grep -ciE "divergence" src/renderer/README.md && grep -c "registerOverlay" src/renderer/README.md && grep -c "data-phantomstream-ui" src/renderer/README.md</automated>
  </verify>
  <acceptance_criteria>
    - README contains a divergence section with >= 10 distinct entries (layout modes, dash:request-status, tabId, ring buffers, state machine, FA→SVG, iframe title, aria/reduced-motion, RECON visuals, dialog quirk, CONTROL.START naming)
    - README documents the skipElement recursion-guard predicate verbatim (attribute-based)
    - README states the sandbox contract: allow-same-origin only, no allow-scripts, asserted at creation
    - A "Phase 3" or "queued" section lists the raw-inline-styles and on*-attribute gaps
    - `npm test` still exits 0 (docs change — suite untouched)
  </acceptance_criteria>
  <done>Every intentional departure from the reference is explicit; the viewer is documented well enough for an external host to embed it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture payload → viewer (loopback) | Same-page loopback still renders attacker-influenced page HTML into the sandboxed mirror |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Denial | mirror-of-mirror feedback amplification (srcdoc echo) | mitigate | attribute-based skipElement recursion guard — proven by the no-echo and snapshot-cleanliness e2e tests in Task 1 |
| T-02-14 | Elevation | dialog/overlay message content rendering as HTML | mitigate | e2e tests pin textContent rendering (literal-text dialog message) end-to-end |
| T-02-15 | Information disclosure | README under-documenting accepted risks | mitigate | Task 3 explicitly ledgers the Phase-3 gaps (raw inline styles, on* survival) so the accepted risk is visible, owned, and queued |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase |
</threat_model>

<verification>
- `node --test tests/renderer-loopback.test.js` green (>= 10 tests)
- `npm test` green
- README divergence ledger complete per acceptance criteria
</verification>

<success_criteria>
- Phase success criterion 2 (first end-to-end proof) and criterion 5 (custom overlays through the documented message type) hold as automated tests
- Recovery (criterion-2 resilience) proven: stale misses → CONTROL.START → fresh snapshot, in one page
- Divergence discipline maintained (Phase 1 ledger standard)
</success_criteria>

<output>
Create `.planning/phases/02-renderer-core-embedded-loopback-mirror/02-04-SUMMARY.md` when done.
</output>
