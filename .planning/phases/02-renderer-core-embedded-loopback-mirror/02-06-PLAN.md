---
phase: 02-renderer-core-embedded-loopback-mirror
plan: 06
type: execute
wave: 4
depends_on: ["02-04", "02-05"]
files_modified: []
autonomous: false
requirements: [ADPT-04, VIEW-01, VIEW-04, VIEW-06]
must_haves:
  truths:
    - "The full test suite (protocol + capture + oracle + renderer + loopback e2e) is green before human verification"
    - "A human has verified the loopback demo in a real Chromium browser: live mutation tracking, scaling, scroll follow, dialog log-line, no mirror-of-mirror"
  artifacts: []
  key_links: []
---

<objective>
Phase gate: run the full automated suite, then hand the loopback demo to the user for real-browser verification — the one thing jsdom cannot prove (layout, paint, real srcdoc parsing, visual parity). This is the phase's only human checkpoint, planned per 02-RESEARCH "Environment Availability" (real-browser rigor formally arrives in Phases 4/5/12; this demo check is the Phase-2 stand-in).

Purpose: confirm phase success criteria 1, 2, 4, 5 hold visually before `/gsd-verify-work`.
Output: human approval (or issue list feeding a fix cycle).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-renderer-core-embedded-loopback-mirror/02-UI-SPEC.md
@.planning/phases/02-renderer-core-embedded-loopback-mirror/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite gate + demo boot smoke check</name>
  <files>(none — verification only)</files>
  <read_first>
    - package.json (test + example:loopback scripts as wired by plan 02-05)
    - .planning/phases/02-renderer-core-embedded-loopback-mirror/02-VALIDATION.md (sampling contract — phase gate requires full suite green)
  </read_first>
  <action>
    Run the complete automated gate: npm test (all of tests/*.test.js + tests/differential/*.test.js — protocol, capture, differential oracle, all six renderer files must pass). Then boot the demo server in background (node examples/serve.js), curl http://127.0.0.1:8642/examples/loopback-mirror.html for a 200 and curl a src/ module for text/javascript content-type, then kill the server. Report the test count and any failures. Do NOT proceed to the checkpoint with anything red — fix-forward within this phase's plans first.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with zero failing tests (expected: 50 pre-phase tests + all new renderer/capture-overlay tests)
    - Demo page responds 200 over the local server; module MIME is text/javascript
    - Server process is terminated after the smoke check (no orphaned listener on 8642)
  </acceptance_criteria>
  <done>Everything automatable is green; the only remaining unknown is real-browser visual behavior.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real-browser verification of the loopback mirror</name>
  <files>(none — human verification)</files>
  <action>Present the verification steps below to the user and pause. Everything automatable was already automated in Task 1; this checkpoint covers only what jsdom/CI cannot prove (real rendering, paint, srcdoc parsing, visual parity).</action>
  <what-built>
    Phase 2 complete in code: an embeddable viewer (`createViewer({ container, transport })` → sandboxed, scale-to-fit live mirror with overlay/dialog/scroll channels and self-healing resync) plus the first end-to-end proof — a single page that mirrors itself through a loopback transport with zero infrastructure. All automated tests are green; this checkpoint verifies what jsdom cannot: real rendering.
  </what-built>
  <how-to-verify>
    1. From the repo root run: npm run example:loopback
    2. Open http://localhost:8642/examples/loopback-mirror.html in Chrome/Chromium.
    3. FIRST LIGHT — within ~1s the MIRROR pane (right) should show a scaled live copy of the page. Confirm the mirror pane shows the demo page content itself and there is NO infinite mirror-inside-mirror tunnel in the mirrored mirror pane (the viewer region should appear excluded/empty in the mirror — recursion guard working).
    4. LIVE TRACKING — click "Add row" several times, then "Remove row" and "Edit text": every change must appear in the mirror within a beat (~paint cadence). Toggle "Auto-mutate" on: continuous random mutations should track; badge shows pulsing green LIVE. Toggle off: badge flips to gray PAUSED.
    5. SCALING — resize the browser window (and narrow below 900px so panes stack): the mirror should rescale to fit its container, keeping aspect ratio with letterboxing, never overflowing.
    6. SCROLL — make the page tall enough to scroll (add many rows), then scroll the page: the mirror should follow the captured scroll position (smooth). Scrolling inside the mirror itself should not feed anything back.
    7. DIALOG — click "Show dialog": a native alert appears. After dismissing it, the footer log should read "dialog mirrored: alert open → closed". (Seeing the mirrored dialog CARD during the alert is NOT expected on this same-page demo — the browser does not paint while alert blocks; the log line is the proof. The card renders normally in two-context deployments.)
    8. SANDBOX — in DevTools, inspect the mirror iframe: its sandbox attribute must be exactly "allow-same-origin" and the console must be free of viewer errors.
    9. VISUAL PARITY — dark two-pane layout per spec: #0f1117 page, #1e1e2e panes, amber "Add row" button only, uppercase pane headers, mono footer line.
  </how-to-verify>
  <resume-signal>Type "approved" if all nine checks pass, or describe exactly which step failed and what you saw (screenshot/console output welcome).</resume-signal>
  <verify>
    <automated>covered by Task 1 (npm test + demo boot smoke); this step is human-only by design — real-browser rendering cannot be automated this phase (02-RESEARCH Environment Availability)</automated>
    <human-check>All nine steps in how-to-verify confirmed in a real Chromium browser</human-check>
  </verify>
  <done>User replied "approved" (or raised issues that were fixed and re-verified).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human verification → phase gate | Visual confirmation of the sandbox + recursion-guard controls landed in earlier plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Elevation | sandbox silently weakened by any late change | mitigate | Checkpoint step 8 has the human inspect the live sandbox attribute in DevTools, on top of the automated token-pinning test from plan 02-03 |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase |
</threat_model>

<verification>
- npm test green (full suite, including the differential oracle)
- Human approval of all nine real-browser checks
</verification>

<success_criteria>
- All five phase success criteria confirmed: criterion 1 (createViewer live scaled mirror), 2 (one-page end-to-end proof), 3 (sandbox inspected), 4 (scroll + dialog parity), 5 (overlay channel — proven in CI by 02-04; glow/progress visuals ride the FSB-parity CSS)
- Phase ready for /gsd-verify-work
</success_criteria>

<output>
Create `.planning/phases/02-renderer-core-embedded-loopback-mirror/02-06-SUMMARY.md` when done (record the human verification outcome and any issues raised).
</output>
