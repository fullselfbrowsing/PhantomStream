---
phase: 02-renderer-core-embedded-loopback-mirror
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/snapshot.js
  - src/renderer/diff.js
  - tests/renderer-snapshot.test.js
  - tests/renderer-diff.test.js
autonomous: true
requirements: [VIEW-01]
must_haves:
  truths:
    - "buildSnapshotHtml(payload) returns the exact reference srcdoc document string (doctype, charset meta, viewport meta, stylesheet links, raw inline styles, parity reset CSS, shell attrs, raw body html)"
    - "applyMutations applies add/rm/attr/text ops against ANY injected Document, counts misses/failures, and fires the resync callback at parity thresholds (>=3 stale misses, >=2 apply failures, batch failure immediate)"
    - "One bad op never aborts the rest of the batch"
  artifacts:
    - path: "src/renderer/snapshot.js"
      provides: "Pure snapshot HTML builder: escapeAttribute, buildShellAttributeString, buildSnapshotHtml"
      exports: ["escapeAttribute", "buildShellAttributeString", "buildSnapshotHtml"]
      min_lines: 60
    - path: "src/renderer/diff.js"
      provides: "Document-parameterized diff applier with miss accounting"
      exports: ["applyMutations"]
      min_lines: 60
    - path: "tests/renderer-snapshot.test.js"
      provides: "Unit tests pinning the builder output"
    - path: "tests/renderer-diff.test.js"
      provides: "Unit tests for ops, counters, thresholds"
  key_links:
    - from: "src/renderer/diff.js"
      to: "src/protocol/messages.js"
      via: "NID_ATTR import (never the literal data-fsb-nid)"
      pattern: "import \\{[^}]*NID_ATTR[^}]*\\} from '\\.\\./protocol/messages\\.js'"
    - from: "tests/renderer-diff.test.js"
      to: "document.implementation.createHTMLDocument"
      via: "jsdom-safe Document target (srcdoc never parses in jsdom 29 — verified in 02-RESEARCH.md)"
      pattern: "createHTMLDocument"
---

<objective>
Extract the two pure transforms of the viewer from `reference/dashboard/dashboard.js` into `src/renderer/`: the snapshot-document builder (payload → srcdoc HTML string) and the Document-parameterized diff applier (ops → DOM writes + miss accounting). These are the contracts `createViewer` (plan 02-03) wires together; factoring them as pure/Document-parameterized functions is mandatory because jsdom 29 does not parse `srcdoc` (02-RESEARCH.md Pattern 3, verified empirically).

Purpose: parity-faithful reconstruction primitives, each provable in jsdom without an iframe.
Output: `src/renderer/snapshot.js`, `src/renderer/diff.js`, and their unit test files — all green under `node --test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-renderer-core-embedded-loopback-mirror/02-CONTEXT.md
@.planning/phases/02-renderer-core-embedded-loopback-mirror/02-RESEARCH.md
@.planning/phases/02-renderer-core-embedded-loopback-mirror/02-PATTERNS.md
@.planning/phases/02-renderer-core-embedded-loopback-mirror/02-UI-SPEC.md
@src/protocol/messages.js
</context>

<interfaces>
From src/protocol/messages.js (import, never redefine):

```javascript
export const NID_ATTR = 'data-fsb-nid';
export const DIFF_OP = { ADD: 'add', REMOVE: 'rm', ATTR: 'attr', TEXT: 'text' };
// SnapshotPayload typedef: { html, stylesheets, inlineStyles, htmlAttrs, bodyAttrs,
//   htmlStyle, bodyStyle, scrollX, scrollY, viewportWidth, viewportHeight, ... }
```

Contracts this plan DEFINES (consumed by plan 02-03):

```javascript
// snapshot.js
export function escapeAttribute(value): string
export function buildShellAttributeString(attrs, styleText): string
export function buildSnapshotHtml(payload): string   // pure, no DOM access

// diff.js
// counters = { staleMisses: number, applyFailures: number } — mutated in place
// hooks = { logger, requestResync(reason, details) }
export function applyMutations(doc, mutations, counters, hooks): void
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port the pure snapshot HTML builder (snapshot.js)</name>
  <files>src/renderer/snapshot.js, tests/renderer-snapshot.test.js</files>
  <read_first>
    - reference/dashboard/dashboard.js lines 2671-2694 (escapePreviewAttribute + buildShellAttributeString — port VERBATIM) and lines 2783-2800 (document assembly)
    - .planning/phases/02-renderer-core-embedded-loopback-mirror/02-UI-SPEC.md section "Mirror document (srcdoc) reset CSS" (exact parity wrapper)
    - src/protocol/messages.js (SnapshotPayload typedef — JSDoc reference for the param type)
    - src/capture/index.js lines 1-60 (file-top provenance comment shape + import style to replicate)
    - tests/capture-lifecycle.test.js lines 1-40 (flat node:test style for the pure test file)
  </read_first>
  <behavior>
    - Test: builder output starts with '&lt;!DOCTYPE html&gt;&lt;html' and contains '&lt;meta charset="UTF-8"&gt;'
    - Test: viewport meta is '&lt;meta name="viewport" content="width=1920"&gt;' when payload.viewportWidth is absent; uses the provided value when present
    - Test: output contains the exact parity reset CSS string 'body { margin: 0; overflow: hidden; } *::selection { background: transparent; } ::-webkit-scrollbar { display: none; }' inside a style tag
    - Test: stylesheet URLs render as link tags with only double-quotes escaped to &amp;quot;
    - Test: inlineStyles entries are wrapped in style tags RAW — a '&lt;/style&gt;' inside CSS passes through unmodified (Pitfall 9 parity pin; Phase 3 owns sanitization)
    - Test: payload.html is inserted raw between body tags (no escaping/normalizing — parity)
    - Test: buildShellAttributeString drops 'style' key, drops any key starting with 'on' (onclick), drops names failing /^[a-z][a-z0-9_:.~-]*$/ after lowercasing, drops null/undefined values, appends style="..." from trimmed styleText, escapes values via escapeAttribute, and returns leading-space-prefixed string or ''
    - Test: escapeAttribute maps null/undefined to '' and escapes &amp;, ", &lt;, &gt; in that order
  </behavior>
  <action>
    Create src/renderer/snapshot.js as a pure ESM module (no DOM access, no module-level side effects — style analog: src/protocol/messages.js). Port three functions behaviorally verbatim from dashboard.js: (1) escapeAttribute — rename of escapePreviewAttribute (dashboard.js:2671-2677); (2) buildShellAttributeString(attrs, styleText) — verbatim port of dashboard.js:2679-2694 including the name regex /^[a-z][a-z0-9_:.~-]*$/, the style/on*-prefix drop, and the trailing styleText append; (3) buildSnapshotHtml(payload) — assembly per dashboard.js:2785-2800: doctype + html shell attrs (from payload.htmlAttrs/payload.htmlStyle) + head with charset meta, viewport meta width=(payload.viewportWidth || 1920), stylesheetLinks (each '&lt;link rel="stylesheet" href="..."&gt;' with only " replaced by &amp;quot;), inlineStyleTags raw, then the exact reset-CSS style tag from 02-UI-SPEC, then body shell attrs (payload.bodyAttrs/payload.bodyStyle) + payload.html raw + closing tags. Use var + || defaulting and function declarations per the cross-runtime style convention (Shared Pattern 3 in 02-PATTERNS.md). Add a file-top provenance comment naming the ported dashboard.js line ranges and the Pitfall-9 known gap (raw inline styles — Phase 3 chokepoint). JSDoc on all three exports; @param {SnapshotPayload} on buildSnapshotHtml. Write tests/renderer-snapshot.test.js FIRST (behavior list above) in flat node:test style with node:assert/strict — pure string assertions, no JSDOM needed. Implements D-05 (srcdoc parity wrapper).
  </action>
  <verify>
    <automated>node --test tests/renderer-snapshot.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/renderer-snapshot.test.js` exits 0 with at least 7 passing tests
    - `grep -c "export function" src/renderer/snapshot.js` returns 3
    - `grep -F 'body { margin: 0; overflow: hidden; }' src/renderer/snapshot.js` matches (exact parity reset CSS present)
    - snapshot.js contains zero occurrences of `document.` or `window.` (pure module): `grep -v '^//' src/renderer/snapshot.js | grep -cE '\bdocument\.|\bwindow\.'` returns 0
    - The name-filter regex `^[a-z][a-z0-9_:.~-]*$` appears verbatim in snapshot.js
  </acceptance_criteria>
  <done>Pure builder produces reference-parity srcdoc documents, pinned by unit tests; no DOM dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port the Document-parameterized diff applier (diff.js)</name>
  <files>src/renderer/diff.js, tests/renderer-diff.test.js</files>
  <read_first>
    - reference/dashboard/dashboard.js lines 3209-3356 (handleDOMMutations — the full op loop, miss counting, thresholds, whole-batch catch)
    - .planning/phases/02-renderer-core-embedded-loopback-mirror/02-PATTERNS.md section "src/renderer/diff.js" (the exact port skeleton, what to DROP)
    - src/protocol/messages.js (NID_ATTR, DIFF_OP)
    - tests/capture-skip.test.js lines 30-124 (setupEnv/teardown jsdom recipe — duplicate helpers locally per parallel-safe convention)
  </read_first>
  <behavior>
    - Test: 'add' op with valid parentNid inserts m.html's firstElementChild via temp-div innerHTML; with m.beforeNid present uses insertBefore (null lookup result behaves as appendChild)
    - Test: 'rm' op removes the nid-addressed element; 'attr' op with val null calls removeAttribute, otherwise setAttribute; 'text' op sets textContent on the nid-addressed element
    - Test: a miss (querySelector returns null) increments counters.staleMisses, logs a warn through hooks.logger, and does NOT throw
    - Test: when counters.staleMisses reaches 3, hooks.requestResync is called with reason 'stale-mutation-parent'
    - Test: an op whose apply throws increments counters.applyFailures and the NEXT op in the same batch still applies (per-op containment)
    - Test: when counters.applyFailures reaches 2, hooks.requestResync is called with reason 'dom-mutation-apply-failed'
    - Test: a whole-batch failure (e.g. mutations is not iterable inside the guarded region) increments counters.applyFailures and calls hooks.requestResync with reason 'dom-mutation-batch-failed' immediately
    - Test: all ops address nodes via the NID_ATTR constant (assert a node stamped data-fsb-nid is found)
  </behavior>
  <action>
    Create src/renderer/diff.js exporting applyMutations(doc, mutations, counters, hooks) — Document-parameterized per 02-RESEARCH.md Pattern 3. Port the op loop from dashboard.js:3209-3356 with this division of responsibility: diff.js owns the per-op switch (add/rm/attr/text exactly per the PATTERNS skeleton), per-op try/catch containment, counter increments, parity thresholds (counters.staleMisses >= 3 → hooks.requestResync('stale-mutation-parent', {op, nid}); counters.applyFailures >= 2 → hooks.requestResync('dom-mutation-apply-failed', {op, nid})), and the whole-batch try/catch (catch → counters.applyFailures += 1 + hooks.requestResync('dom-mutation-batch-failed', {error: String})). It does NOT own the resync latch (index.js owns it — requestResync is an injected callback), does NOT touch scroll (post-batch scroll re-apply needs contentWindow and lives in index.js), and does NOT read iframe state. DROP every recordDashboardTransportEvent/recordDashboardTransportError call — replace with hooks.logger.warn lines prefixed '[Renderer]'. Build nid selectors with NID_ATTR imported from ../protocol/messages.js — never the string literal. Missing/empty mutations array → return without error. Add file-top provenance comment citing dashboard.js:3209-3356 and the dropped FSB calls. Write tests/renderer-diff.test.js FIRST: jsdom env (duplicate setupEnv/teardown helpers locally per the parallel-safe convention from tests/capture-skip.test.js), target documents created via dom.window.document.implementation.createHTMLDocument() seeded through body.innerHTML with data-fsb-nid-stamped elements (verified working in jsdom 29 — never use iframe/srcdoc here), recording logger + recording requestResync stub to assert reasons and call counts. Implements D-06 (miss accounting as health signal).
  </action>
  <verify>
    <automated>node --test tests/renderer-diff.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/renderer-diff.test.js` exits 0 with at least 8 passing tests
    - `grep -c "createHTMLDocument" tests/renderer-diff.test.js` returns >= 1 and `grep -c "srcdoc" tests/renderer-diff.test.js` returns 0
    - `grep -v '^//' src/renderer/diff.js | grep -c "data-fsb-nid"` returns 0 (NID_ATTR import used instead)
    - The three resync reason strings 'stale-mutation-parent', 'dom-mutation-apply-failed', 'dom-mutation-batch-failed' each appear exactly once in src/renderer/diff.js
    - `npm test` exits 0 (existing 50-test suite + differential oracle still green)
  </acceptance_criteria>
  <done>Diff applier applies all four op types against any injected Document with parity miss accounting and thresholds, proven by unit tests; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture payload → renderer | Snapshot html / op subtrees / attr values originate from an attacker-influenced observed page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation | buildSnapshotHtml raw payload.html / raw inlineStyles | accept (this phase) | Parity-locked raw insertion; script execution blocked downstream by the `allow-same-origin`-only sandbox (plan 02-03, criterion 3). Full sanitization chokepoints are Phase 3 (SEC-01/SEC-02) — documented in the renderer README ledger (plan 02-04) |
| T-02-02 | Tampering | buildShellAttributeString | mitigate | Verbatim port keeps the on*-prefix and style drops + attribute-value escaping; pinned by unit tests in this plan |
| T-02-03 | Elevation | applyMutations 'add' op temp-div innerHTML | accept (this phase) | Raw per parity; executes inside the sandboxed mirror document only (no allow-scripts). Phase 3 adds the render-side DOM-fragment sanitizer at this exact chokepoint |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase (02-RESEARCH.md Package Legitimacy Audit: nothing to audit) |
</threat_model>

<verification>
- `node --test tests/renderer-snapshot.test.js tests/renderer-diff.test.js` green
- `npm test` green (no regression of the 50 existing tests or the differential oracle)
- Both modules import protocol constants rather than redefining wire strings
</verification>

<success_criteria>
- buildSnapshotHtml output is byte-identical in structure to the reference assembly for the same payload (reset CSS, metas, escaping rules pinned by tests)
- applyMutations works against document.implementation.createHTMLDocument() targets — the jsdom-mandated seam exists
- Parity thresholds (3 stale misses / 2 apply failures / batch fail immediate) verified by tests
</success_criteria>

<output>
Create `.planning/phases/02-renderer-core-embedded-loopback-mirror/02-01-SUMMARY.md` when done.
</output>
