---
phase: 02-renderer-core-embedded-loopback-mirror
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/overlays.js
  - tests/renderer-overlays.test.js
  - src/capture/index.js
  - src/capture/README.md
  - tests/capture-overlay-forward.test.js
autonomous: true
requirements: [VIEW-04, VIEW-06]
must_haves:
  truths:
    - "Glow and progress render as pre-registered built-ins through the SAME registry hosts use (extension seam proven by construction)"
    - "Unknown overlay kinds are logged and ignored — never thrown (forward-compatible)"
    - "Dialog card mirrors alert/confirm/prompt with parity visuals: capitalized type label, textContent-only message, icon by type, flex/none show-hide"
    - "Capture forwards EVERY overlayProvider key on the wire (custom kinds reach the viewer); with no provider the wire shape is unchanged — differential oracle stays green"
  artifacts:
    - path: "src/renderer/overlays.js"
      provides: "Overlay layer + kind-keyed registry + glow/progress/dialog built-ins + coordinate mapping + injected CSS"
      exports: ["createOverlays", "mapRectToHost", "OVERLAY_CSS"]
      min_lines: 150
    - path: "tests/renderer-overlays.test.js"
      provides: "Registry dispatch, built-in behavior, containment tests"
    - path: "tests/capture-overlay-forward.test.js"
      provides: "Wire-shape tests for the capture overlay forwarding edit"
  key_links:
    - from: "src/capture/index.js"
      to: "STREAM.OVERLAY wire payload"
      via: "broadcastOverlayState forwards all provider keys; glow/progress default null; identity keys never overwritten"
      pattern: "STREAM\\.OVERLAY"
    - from: "src/renderer/overlays.js"
      to: "overlay payload kinds"
      via: "every non-identity payload key dispatched through the registry"
      pattern: "streamSessionId|snapshotId"
---

<objective>
Build the extensible overlay channel (VIEW-04) and the dialog card (the dialog half of VIEW-06): a host-document overlay layer positioned ABOVE the sandboxed iframe, a registry keyed by overlay kind with `registerOverlay(kind, renderFn(payload, anchorRect, layer))`, glow + progress ported as pre-registered built-ins with parity visuals per 02-UI-SPEC, and the reference dialog card. Also make the small, oracle-safe capture edit (02-RESEARCH.md Open Question 2, recommended): `broadcastOverlayState` currently drops every overlayProvider key except glow/progress — without the edit, custom DOM-anchored overlays cannot flow end-to-end and VIEW-04's success criterion is unmet.

Purpose: the documented, extensible overlay message type with built-ins proving the seam.
Output: `src/renderer/overlays.js` + tests; modified `src/capture/index.js` with the differential oracle re-proven green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-renderer-core-embedded-loopback-mirror/02-CONTEXT.md
@.planning/phases/02-renderer-core-embedded-loopback-mirror/02-RESEARCH.md
@.planning/phases/02-renderer-core-embedded-loopback-mirror/02-PATTERNS.md
@.planning/phases/02-renderer-core-embedded-loopback-mirror/02-UI-SPEC.md
@src/protocol/messages.js
</context>

<interfaces>
Contracts this plan DEFINES (consumed by plan 02-03's createViewer):

```javascript
// overlays.js
export var OVERLAY_CSS; // single CSS string, .ps-overlay-* classes per 02-UI-SPEC

// pure: rect {x,y,w,h} in capture-page viewport coords + scale {s, offsetX, offsetY}
// → {top, left, width, height} px numbers (dashboard.js:3381-3384 formula)
export function mapRectToHost(rect, scale): { top, left, width, height }

// Factory. opts = { document, logger }
// Returns:
//   layer            — the overlay layer element (caller appends into viewer root)
//   register(kind, renderFn)         — registry write; renderFn(payload, anchorRect, layer)
//   handleOverlayMessage(payload, ctx) — ctx = { scale, resolveNidRect(nid) → hostRect|null }
//   handleDialogMessage(payload)     — STREAM.DIALOG handler (dialog card show/hide)
//   resetOverlays()                  — hide all kinds (new-snapshot reset)
export function createOverlays(opts)
```

Wire shape (from src/capture/index.js broadcastOverlayState, post-edit):
`{ glow: ...|null, progress: ...|null, ...customKinds, streamSessionId, snapshotId }`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Overlay layer, registry, and built-ins (overlays.js)</name>
  <files>src/renderer/overlays.js, tests/renderer-overlays.test.js</files>
  <read_first>
    - .planning/phases/02-renderer-core-embedded-loopback-mirror/02-UI-SPEC.md sections "Overlay layer", "Action-glow rect", "Progress pill", "Dialog card" (exact CSS values, class names, sub-layer z-indexes — the locked contract)
    - reference/dashboard/dashboard.js lines 3374-3443 (glow/progress/dialog handlers — behavior port source)
    - .planning/phases/02-renderer-core-embedded-loopback-mirror/02-RESEARCH.md Pattern 5 (registry design, payload-key-as-kind dispatch, anchor-rect priority) and Pitfall 8 (nested dialog identity quirk)
    - src/capture/index.js lines 277-320 (safeSkipElement/safeSend containment shape to replicate as safeRenderOverlay)
    - tests/capture-skip.test.js lines 30-124 and 214-219 (jsdom setupEnv + recording-logger patterns — duplicate locally)
  </read_first>
  <behavior>
    - Test: createOverlays returns a layer div with position:absolute, inset 0, pointer-events:none, and three built-in elements using classes ps-overlay-glow / ps-overlay-progress / ps-overlay-dialog (with ps-overlay-dialog-card, -icon, -type, -message descendants)
    - Test: handleOverlayMessage({glow:{state:'active',x:100,y:50,w:200,h:40}, progress:null, streamSessionId:'s', snapshotId:1}, ctx with scale {s:0.5, offsetX:10, offsetY:20}) positions glow at top 45px / left 60px / width 100px / height 20px and sets display non-none
    - Test: glow.state !== 'active' or glow null → glow display 'none'
    - Test: progress {mode:'determinate', percent:62.4, phase:'Navigating'} → textContent '62% - Navigating'; indeterminate {label:'Working'} → 'Working - Working'; payload without progress key leaves pill hidden
    - Test: dialog open via handleDialogMessage({dialog:{type:'confirm', state:'open', message:'&lt;b&gt;hi&lt;/b&gt;'}}) → backdrop display 'flex', type label 'Confirm', message rendered as literal text (textContent, no element children); state 'closed' → display 'none'
    - Test: unknown kind ('mystery' key in payload) → logger.warn called once, no throw, message loop continues to next kind
    - Test: a registered custom renderFn that throws is contained (logger.error, no propagation)
    - Test: register('badge', fn) then a payload with badge:{x:1,y:2,w:3,h:4} → fn called with (payloadValue, mappedAnchorRect, layer)
    - Test: resetOverlays hides glow, progress, and dialog
    - Test: mapRectToHost pure-math cases including s=1/offsets 0 identity
  </behavior>
  <action>
    Create src/renderer/overlays.js. OVERLAY_CSS: single exported string containing the exact .ps-overlay-* rules from 02-UI-SPEC (glow: 2px solid #f59e0b border, 4px radius, box-shadow 0 0 12px rgba(245,158,11,0.6), 100ms ease transitions wrapped in @media (prefers-reduced-motion: no-preference), display none default; progress pill: bottom/left 8px, rgba(0,0,0,0.75) scrim, 600 13px/1.2 system-ui, 4px 12px padding, 6px radius, ellipsis; dialog: 0.5 scrim backdrop centered flex, card #1e1e2e / 1px #333 / 12px radius / 24px padding / max-width 320px / width 80% / centered / shadow 0 8px 32px rgba(0,0,0,0.4), icon 28px #f59e0b margin-bottom 12px, type 13px 600 uppercase 1px letter-spacing #888 margin-bottom 8px, message 14px #e0e0e0 1.5 line-height word-break max-height 200px overflow-y auto). mapRectToHost(rect, scale): the dashboard.js:3381-3384 formula (top = scale.offsetY + rect.y*scale.s, left = scale.offsetX + rect.x*scale.s, width = rect.w*scale.s, height = rect.h*scale.s). createOverlays({document, logger}): builds the layer (absolute, inset 0, overflow hidden, pointer-events none, z-index 2) containing glow div (z-index 10), progress div (z-index 20), dialog backdrop (z-index 30) with card/icon/type/message structure and three inline SVG icons (warning-triangle for alert, question-circle for confirm, keyboard for prompt — FA replacement, UI-SPEC-locked divergence). Accessibility per UI-SPEC: aria-hidden="true" on the glow and progress elements (NOT on the layer itself — an aria-hidden ancestor would suppress the dialog), role="status" aria-live="polite" on the dialog card. Registry: a Map of kind → renderFn; register(kind, renderFn) overwrites silently; glow and progress are pre-registered through this same Map (zero special-casing — D-09/D-10). handleOverlayMessage(payload, ctx): kinds = own keys of payload minus streamSessionId/snapshotId; for each kind, null value on a built-in hides it; registered kind → safeRenderOverlay wrapper (try/catch → logger.error, copy the safeSkipElement containment shape); unregistered → logger.warn('[Renderer] unknown overlay kind ignored', kind) and continue (D-12). Anchor-rect priority per RESEARCH Pattern 5: payload value with nid → ctx.resolveNidRect(nid); value with numeric x/y/w/h → mapRectToHost(value, ctx.scale); neither → null. handleDialogMessage(payload): dialog = payload.dialog || payload (parity fallback, dashboard.js:3417); open → type label capitalized via charAt(0).toUpperCase()+slice(1) defaulting 'alert'→'Alert', message via textContent NEVER innerHTML, icon swap by type, backdrop display 'flex'; closed → 'none'. Document in JSDoc that custom renderFns receive the raw payload value and own their escaping, and that renderFns receive null payload on reset so they can clear. resetOverlays(): hide all three built-ins and invoke each custom-registered renderFn with (null, null, layer) inside the containment wrapper (D-13 reset parity, dashboard.js:2762-2764). File-top provenance comment citing dashboard.js:3374-3443 + divergences (FA→SVG, ps-overlay-* names, aria additions). Write tests/renderer-overlays.test.js FIRST per the behavior list — jsdom env with locally-duplicated setupEnv/recording-logger helpers; stub ctx with fixed scale and a resolveNidRect returning canned rects.
  </action>
  <verify>
    <automated>node --test tests/renderer-overlays.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/renderer-overlays.test.js` exits 0 with at least 10 passing tests
    - `grep -c "ps-overlay-" src/renderer/overlays.js` returns >= 7 and `grep -c "dash-preview" src/renderer/overlays.js` returns 0
    - `grep -c "fa-solid" src/renderer/overlays.js` returns 0; `grep -c "<svg" src/renderer/overlays.js` returns >= 3 (inline SVG icons)
    - `grep -c "innerHTML" src/renderer/overlays.js` counts only the SVG icon construction if any — the dialog message path uses textContent: `grep -n "textContent" src/renderer/overlays.js` shows the message assignment
    - The exact colors #f59e0b, #1e1e2e, #333, #888, #e0e0e0 all appear in OVERLAY_CSS
    - Unknown-kind warning string contains 'unknown overlay kind'
  </acceptance_criteria>
  <done>Overlay layer + registry + parity built-ins exist; glow/progress/dialog behavior pinned by tests; extension seam proven by a custom-kind test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture-side overlay key forwarding (oracle-safe edit)</name>
  <files>src/capture/index.js, tests/capture-overlay-forward.test.js, src/capture/README.md</files>
  <read_first>
    - src/capture/index.js lines 1130-1170 (broadcastOverlayState — the exact edit site) and lines 142-203 (Options typedef where overlayProvider is documented)
    - .planning/phases/02-renderer-core-embedded-loopback-mirror/02-RESEARCH.md Open Question 2 + Pattern 5 "Capture-side gap" (constraints: glow/progress still default null; identity keys never overwritten; oracle-safe because no differential fixture configures an overlayProvider)
    - src/capture/README.md (divergence-entry format to extend)
    - tests/capture-skip.test.js (setupEnv + loopback transport + settle recipe to duplicate locally)
  </read_first>
  <behavior>
    - Test: with NO overlayProvider configured, every STREAM.OVERLAY message has exactly the keys {glow, progress, streamSessionId, snapshotId} with glow === null and progress === null (reference wire shape unchanged — oracle protection)
    - Test: with overlayProvider returning {badge: {nid: 7, text: 'hi'}}, the OVERLAY message contains badge deep-equal to that value AND glow === null AND progress === null (absent built-ins still default null)
    - Test: with overlayProvider returning {glow: {...}, custom1: 1, streamSessionId: 'attacker'}, the message's streamSessionId equals the capture session's id, NOT 'attacker' (identity keys set last, never overwritten)
    - Test: an overlayProvider that throws yields the default {glow: null, progress: null} message (error swallowed exactly like the reference)
  </behavior>
  <action>
    Edit broadcastOverlayState in src/capture/index.js (currently ~lines 1139-1167): build the outgoing payload by copying every own enumerable key from the provider's returned state object, then ensure payload.glow = state.glow || null and payload.progress = state.progress || null (defaults preserved even when the provider omits them), then assign streamSessionId and snapshotId LAST so provider keys can never overwrite identity. With no provider or a throwing provider, the payload must remain exactly { glow: null, progress: null, streamSessionId, snapshotId } — byte-compatible with the reference wire shape (this is what keeps the differential oracle green; no fixture configures an overlayProvider, verified in 02-RESEARCH.md). Keep the existing throttle (OVERLAY_THROTTLE_MS) and try/catch-ignore around the provider call untouched. Update the broadcastOverlayState JSDoc and the overlayProvider entry in the Options typedef to document the new pass-through contract ("all provider keys forwarded as overlay kinds; glow/progress default null; identity keys reserved"). Add a divergence/extension entry to src/capture/README.md following its existing ledger-entry format: additive, default-off, Phase-2 motivation (VIEW-04 custom overlay kinds), wire-compatible when no provider present. Write tests/capture-overlay-forward.test.js FIRST per the behavior list — locally-duplicated setupEnv + recording loopback transport + settle helpers; trigger broadcasts via capture.start() and (if start alone does not emit OVERLAY) a mutation followed by settle — read the broadcast call sites in src/capture/index.js to confirm the trigger before writing assertions. Flat tests/ naming keeps the existing npm test glob unchanged.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/capture-overlay-forward.test.js` exits 0 with at least 4 passing tests
    - `npm test` exits 0 — ALL existing tests including tests/differential/*.test.js (the oracle) stay green with the edit in place
    - With no provider, the OVERLAY payload key set is exactly ['glow','progress','streamSessionId','snapshotId'] (asserted in test via Object.keys length 4)
    - src/capture/README.md contains a new entry mentioning overlay key forwarding (grep -i "overlay" src/capture/README.md matches in the divergence/extension section)
  </acceptance_criteria>
  <done>Custom overlay kinds flow onto the wire; reference wire shape with no provider is provably unchanged; oracle green; edit documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| overlay payload → host-document DOM | Overlay payloads originate from the capture side (attacker-influenced page context) but render in the HOST document, above the sandbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Elevation | dialog card message / progress text rendering | mitigate | textContent ONLY for all message/label text (parity, dashboard.js:3414-3417) — pinned by the literal-text test in Task 1 |
| T-02-05 | Elevation | custom renderFn receives raw payload in host document | mitigate (documented) | JSDoc contract states custom renderFns own their escaping; built-ins prove the safe pattern; containment wrapper stops a malicious/buggy renderFn from breaking the loop |
| T-02-06 | Spoofing | provider keys overwriting stream identity on the wire | mitigate | Identity keys assigned last in broadcastOverlayState; pinned by the 'attacker' identity test in Task 2 |
| T-02-07 | Denial | throwing renderFn / unknown kinds crashing the viewer message loop | mitigate | safeRenderOverlay try/catch → logger; unknown kinds logged + ignored (D-12); both pinned by tests |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase |
</threat_model>

<verification>
- `node --test tests/renderer-overlays.test.js tests/capture-overlay-forward.test.js` green
- `npm test` green — differential oracle proves the capture edit is wire-safe
- Glow/progress registered through the same Map hosts use (read the code: no special-case dispatch path)
</verification>

<success_criteria>
- registerOverlay seam works: a test-registered custom kind receives (payload, anchorRect, layer)
- Parity visuals locked: CSS values match 02-UI-SPEC verbatim
- Dialog card behavior matches reference (capitalized labels, textContent, flex/none)
- Capture forwards custom kinds; default wire shape untouched
</success_criteria>

<output>
Create `.planning/phases/02-renderer-core-embedded-loopback-mirror/02-02-SUMMARY.md` when done.
</output>
