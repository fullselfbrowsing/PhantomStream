---
phase: 05-playwright-cdp-adapter-remote-control-agent-demo
plan: 06
type: execute
wave: 4
depends_on:
  - 05-05
files_modified:
  - .planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-BROWSER-VERIFICATION.md
autonomous: false
requirements:
  - ADPT-02
  - PKG-02
  - VIEW-05
  - SEC-04
must_haves:
  truths:
    - "Full automated Phase 05 and project tests pass before browser verification"
    - "The Playwright demo runs with the local relay and a real driven browser page"
    - "Default-deny control emits denial and dispatches no driver input"
    - "After approval, mirror click/type/scroll changes the real driven page and the mirror follows"
    - "Fixture navigation or reload triggers a fresh snapshot and viewer returns to live"
  artifacts:
    - path: ".planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-BROWSER-VERIFICATION.md"
      provides: "Recorded browser verification evidence for denied control, approved remote actions, and navigation re-snapshot"
      contains: "Denied control inert"
  key_links:
    - from: "node bin/phantom-stream.js playwright-demo --drive"
      to: "examples/playwright-demo/server.js"
      via: "local Playwright demo launch"
      pattern: "Viewer:"
    - from: "browser checkpoint"
      to: "05-BROWSER-VERIFICATION.md"
      via: "recorded observed states and counters"
      pattern: "Navigation re-snapshot"
---

<objective>
Run the final automated and browser verification gate for Phase 05.

Purpose: The phase is not complete until the demo proves default-deny inertness, approved click/type/scroll remote control, and navigation re-snapshot in a real browser surface.
Output: full-suite pass, Playwright browser setup, human/browser checkpoint, and verification evidence artifact.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-CONTEXT.md
@.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-VALIDATION.md
@.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-UI-SPEC.md
@.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-01-SUMMARY.md
@.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-02-SUMMARY.md
@.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-03-SUMMARY.md
@.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-04-SUMMARY.md
@.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-05-SUMMARY.md
@bin/phantom-stream.js
@examples/playwright-demo/server.js
@examples/playwright-demo/viewer.js
@examples/playwright-demo/fixture.js
@tests/remote-control-protocol.test.js
@tests/playwright-adapter.test.js
@tests/playwright-adapter-cdp.test.js
@tests/renderer-remote-control.test.js
@tests/playwright-demo-cli.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated Phase 05 gate and browser dependency setup</name>
  <files>.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-BROWSER-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-VALIDATION.md`
    - `package.json`
    - `bin/phantom-stream.js`
    - `examples/playwright-demo/server.js`
  </read_first>
  <action>
    Run the Phase 05 quick command exactly:
    `node --test tests/remote-control-protocol.test.js tests/playwright-adapter.test.js tests/playwright-adapter-cdp.test.js tests/renderer-remote-control.test.js tests/playwright-demo-cli.test.js`.
    Then run `npm test`. Then run `npx playwright install chromium` so the browser verification has a managed Chromium available. Create `.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-BROWSER-VERIFICATION.md` with a heading `# Phase 05 Browser Verification`, the exact commands run, and placeholder sections for `Denied control inert`, `Approved click/type/scroll`, and `Navigation re-snapshot`; fill command results immediately with PASS/FAIL and timestamps.
  </action>
  <verify>
    <automated>node --test tests/remote-control-protocol.test.js tests/playwright-adapter.test.js tests/playwright-adapter-cdp.test.js tests/renderer-remote-control.test.js tests/playwright-demo-cli.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passes before browser verification starts.
    - `npx playwright install chromium` completes or reports Chromium already installed.
    - `grep -n "Denied control inert\\|Approved click/type/scroll\\|Navigation re-snapshot" .planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-BROWSER-VERIFICATION.md` returns all three section headings.
  </acceptance_criteria>
  <done>Automated checks are green and the verification artifact is ready for browser evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser verification of denied and approved remote control</name>
  <files>.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-BROWSER-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-VALIDATION.md`
    - `.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-UI-SPEC.md`
    - `examples/playwright-demo/viewer.js`
    - `examples/playwright-demo/fixture.js`
  </read_first>
  <action>
    Start the demo with `node bin/phantom-stream.js playwright-demo --drive --headed --port 0`. Capture the printed `Viewer:` and `Driven page:` URLs. Open the viewer URL in the browser tool. Verify the default state first: lifecycle reaches `Live`, authorization mode is `Deny`, control state is `locked`, and health counters advance without mirrored content appearing in logs. Click `Request control`; expected result is control state `denied`, action log includes `Remote control denied` or equivalent denial event, denied counter increments, active border is absent, and the driven page click count/text/scroll values do not change.

    Switch the authorization segmented control to `Approve`, click `Request control`, and verify state becomes `active` with the accent stage outline. Click the mirrored `Click target`; expected result is the real driven page click count changes and the mirror updates. Focus/click the mirrored `Remote text` input, type printable characters, and verify the fixture echo changes while the demo log shows `Type sent: {n} chars` and not the typed string. Wheel over the mirrored scroll region; expected result is the real page scroll marker changes and the mirror follows. Activate `Navigate fixture` or reload the fixture; expected result is navigation count increments, the viewer transitions through a resnapshot state if visible, and returns to `Live` with a fresh rendered fixture.
  </action>
  <verify>
    <automated>npm test</automated>
    <manual>Human/browser checkpoint: execute the steps in `<action>` and type `approved` only after denied inertness, approved click/type/scroll, and navigation re-snapshot all pass.</manual>
  </verify>
  <acceptance_criteria>
    - Terminal output contains `PhantomStream Playwright demo running on 127.0.0.1`, `Viewer:`, `Driven page:`, `Room:`, and `Control: default-deny`.
    - Denied control dispatches zero driver actions and changes no fixture counters/text/scroll.
    - Approved click/type/scroll visibly changes the driven fixture and the mirror follows.
    - Typed printable text is absent from action log and health/status UI; only character count is shown.
    - Navigation or reload produces a fresh live mirror after resnapshot.
  </acceptance_criteria>
  <done>Human/browser verifier approves the real-browser demo behavior required by ADPT-02, PKG-02, VIEW-05, and SEC-04.</done>
  <resume-signal>Type `approved` or describe the observed issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record browser verification evidence</name>
  <files>.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-BROWSER-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-BROWSER-VERIFICATION.md`
    - `.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-VALIDATION.md`
  </read_first>
  <action>
    After the checkpoint is approved, update `05-BROWSER-VERIFICATION.md` with the actual viewer URL, driven page URL, room prefix, browser used, timestamp, and PASS evidence under the three required sections. Include the observed lifecycle/control states (`locked`, `denied`, `active`, `stopped` if used), control counters (`control requests`, `denied`, `dispatched`), and navigation count. Record typed input evidence as character count only; do not write raw typed text, mirrored HTML, fixture text, attributes, or non-local URLs.
  </action>
  <verify>
    <automated>grep -n "Denied control inert: PASS\\|Approved click/type/scroll: PASS\\|Navigation re-snapshot: PASS" .planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-BROWSER-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `05-BROWSER-VERIFICATION.md` contains `Denied control inert: PASS`.
    - `05-BROWSER-VERIFICATION.md` contains `Approved click/type/scroll: PASS`.
    - `05-BROWSER-VERIFICATION.md` contains `Navigation re-snapshot: PASS`.
    - `grep -n "secret\\|password\\|<html\\|<div\\|typed words" .planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-BROWSER-VERIFICATION.md` returns no matches unless those words appear in section labels from this plan; do not record raw page content.
  </acceptance_criteria>
  <done>Verification evidence is recorded without content leakage and Phase 05 is ready for final `/gsd-verify-work` if requested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local demo -> browser automation | Final verification launches a browser and exercises native input. |
| human/browser observation -> verification artifact | Observed behavior is recorded into planning artifacts without page content leakage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | browser checkpoint actions | mitigate | Browser steps exercise malformed-proofed action paths already validated by 05-01 and 05-02. |
| T-05-02 | Information Disclosure | `05-BROWSER-VERIFICATION.md` | mitigate | Artifact records states, counts, coordinates, local URLs, and character counts only. |
| T-05-03 | Elevation of Privilege | default-deny path | mitigate | Checkpoint must prove Deny mode emits denied state before any active control. |
| T-05-04 | Elevation of Privilege | denied action inertness | mitigate | Checkpoint must prove denied actions change no driven-page counters/text/scroll. |
| T-05-05 | Tampering | replay path | mitigate | Checkpoint exercises Playwright-driven click/type/scroll; static tests from 05-02 remain green. |
| T-05-06 | Information Disclosure | navigation survival | mitigate | Checkpoint proves navigation re-snapshot via main-frame injection/restart without child-frame corruption. |
| T-05-07 | Tampering | coordinate replay | mitigate | Checkpoint exercises click and scroll through mapped mirror coordinates. |
| T-05-08 | Spoofing | local-only safety | mitigate | Checkpoint starts only `127.0.0.1` demo URLs and records no LAN/public URLs. |
| T-05-09 | Tampering | relay non-execution | mitigate | Full `npm test` keeps raw relay tests green; browser checkpoint verifies driver logic lives in adapter/demo process. |
</threat_model>

<verification>
Overall phase gate:

```bash
node --test tests/remote-control-protocol.test.js tests/playwright-adapter.test.js tests/playwright-adapter-cdp.test.js tests/renderer-remote-control.test.js tests/playwright-demo-cli.test.js
npm test
npx playwright install chromium
node bin/phantom-stream.js playwright-demo --drive --headed --port 0
```

The last command is used for the browser checkpoint and should be stopped cleanly after verification.
</verification>

<success_criteria>
- Automated quick and full test suites pass.
- Browser verification proves denied inertness, approved click/type/scroll, and navigation re-snapshot.
- Verification evidence is recorded in `05-BROWSER-VERIFICATION.md` without content leakage.
- All Phase 05 requirements are demonstrably covered.
</success_criteria>

<output>
After completion, create `.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-06-SUMMARY.md`.
</output>
