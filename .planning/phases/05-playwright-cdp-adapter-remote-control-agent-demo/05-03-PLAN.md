---
phase: 05-playwright-cdp-adapter-remote-control-agent-demo
plan: 03
type: execute
wave: 1
depends_on:
  - 05-01
files_modified:
  - src/renderer/overlays.js
  - src/renderer/index.js
  - tests/renderer-remote-control.test.js
  - tests/renderer-viewer.test.js
autonomous: true
requirements:
  - VIEW-05
  - SEC-04
must_haves:
  truths:
    - "A host overlay can map mirror-stage points back to captured-page viewport CSS pixels"
    - "Clicks in letterboxed areas are rejected instead of clamped into the real page"
    - "The viewer library exposes mapping state but renders no authorization UI or control chrome"
    - "Existing viewer lifecycle, health, overlay, and sandbox behavior remains unchanged"
  artifacts:
    - path: "src/renderer/overlays.js"
      provides: "Pure inverse coordinate mapper"
      exports: ["mapHostPointToViewport"]
    - path: "src/renderer/index.js"
      provides: "Viewer handle mapping getter"
      exports: ["createViewer", "computeScale"]
    - path: "tests/renderer-remote-control.test.js"
      provides: "Nyquist coverage for inverse mapping and host-owned control overlay boundaries"
    - path: "tests/renderer-viewer.test.js"
      provides: "Updated handle-shape contract including getViewportMapping"
  key_links:
    - from: "examples/playwright-demo/viewer.js"
      to: "src/renderer/index.js"
      via: "viewer.getViewportMapping() in later plan"
      pattern: "getViewportMapping"
    - from: "src/renderer/index.js"
      to: "src/renderer/overlays.js"
      via: "barrel export of mapHostPointToViewport"
      pattern: "mapHostPointToViewport"
---

<objective>
Expose renderer coordinate state needed by host-owned remote-control overlays.

Purpose: VIEW-05 requires click/scroll coordinates to be reverse-mapped from the mirror into the real page, while the UI contract requires all control chrome to remain demo/host-owned.
Output: a pure inverse mapping helper, a `viewer.getViewportMapping()` handle method, and renderer tests covering letterbox rejection.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-CONTEXT.md
@.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-RESEARCH.md
@.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-PATTERNS.md
@.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-VALIDATION.md
@.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-UI-SPEC.md
@.planning/phases/02-renderer-core-embedded-loopback-mirror/02-03-SUMMARY.md
@.planning/phases/04-relay-ws-transport-two-tab-demo/04-03-SUMMARY.md
@src/renderer/index.js
@src/renderer/overlays.js
@tests/renderer-viewer.test.js
@tests/renderer-overlays.test.js

<interfaces>
Existing renderer scale API:
```js
export function computeScale(pageW, pageH, containerW, containerH) -> {
  s, offsetX, offsetY, pageW, pageH
}
createViewer({ container, transport }) -> {
  detach, destroy, on, registerOverlay
}
```

Existing forward mapping:
```js
export function mapRectToHost(rect, scale) -> {
  top, left, width, height
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 renderer inverse-mapping tests</name>
  <files>tests/renderer-remote-control.test.js, tests/renderer-viewer.test.js</files>
  <read_first>
    - `.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-UI-SPEC.md`
    - `.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-PATTERNS.md`
    - `tests/renderer-viewer.test.js`
    - `tests/renderer-overlays.test.js`
  </read_first>
  <behavior>
    - `mapHostPointToViewport({ x: 60, y: 45 }, { s: 0.5, offsetX: 10, offsetY: 20, pageW: 400, pageH: 200 })` returns `{ inside: true, x: 100, y: 50 }`.
    - Points left/top/right/bottom of the scaled page return `inside:false` and do not report clamped in-page coordinates as dispatchable.
    - Edge points inside the page clamp to `0..pageW-1` and `0..pageH-1` after inside classification.
    - `createViewer()` handle includes `getViewportMapping()` alongside `detach`, `destroy`, `on`, and `registerOverlay`.
    - `getViewportMapping()` returns a cloned object with `scale`, `viewport`, and `container` fields; mutating the returned object does not mutate viewer state.
    - Renderer source contains no `Request control`, `Authorization hook`, `Control active`, or `REMOTE_CONTROL` UI strings; control overlay remains host/demo-owned.
  </behavior>
  <action>
    Create `tests/renderer-remote-control.test.js` using the existing jsdom setup style from `tests/renderer-viewer.test.js`. Add focused tests for `mapHostPointToViewport` and `viewer.getViewportMapping()`. Update the existing handle-shape test in `tests/renderer-viewer.test.js` to expect `getViewportMapping` as an intentional public method. This is the RED Wave 0 task, so tests are expected to fail before Task 2 because the helper and handle method do not exist yet.
  </action>
  <verify>
    <automated>set +e; node --test tests/renderer-remote-control.test.js tests/renderer-viewer.test.js; code=$?; test "$code" -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `tests/renderer-remote-control.test.js` imports `mapHostPointToViewport` from `../src/renderer/index.js`.
    - `tests/renderer-remote-control.test.js` contains assertions for `inside:false`.
    - `tests/renderer-viewer.test.js` contains `getViewportMapping` in the expected handle keys.
    - The RED verify command exits 0 only because the focused test command exits non-zero before implementation.
  </acceptance_criteria>
  <done>Wave 0 renderer tests exist and cover T-05-07 and the UI boundary from `05-UI-SPEC.md`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement inverse mapping and viewer mapping getter</name>
  <files>src/renderer/overlays.js, src/renderer/index.js, tests/renderer-remote-control.test.js, tests/renderer-viewer.test.js</files>
  <read_first>
    - `src/renderer/overlays.js`
    - `src/renderer/index.js`
    - `tests/renderer-viewer.test.js`
    - `.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-UI-SPEC.md`
  </read_first>
  <action>
    Add `export function mapHostPointToViewport(point, scale)` to `src/renderer/overlays.js`. Use `scale.s` when it is finite and positive, otherwise `1`; compute raw viewport coordinates as `(point.x - offsetX) / s` and `(point.y - offsetY) / s`; classify `inside` before clamping using `pageW`/`pageH`; return `{ inside: false, x: null, y: null }` for letterbox/out-of-bounds points; return rounded, clamped `{ inside: true, x, y }` for in-bounds points. Treat missing `pageW`/`pageH` as `0`, which makes the point non-dispatchable.

    In `src/renderer/index.js`, import/export the helper through the existing renderer barrel, extend the `ViewerHandle` typedef with `getViewportMapping`, and add a local function that returns:
    `{ scale: { s, offsetX, offsetY, pageW, pageH }, viewport: { width: pageW, height: pageH }, container: { width: container.clientWidth || 0, height: container.clientHeight || 0 } }`.
    Return a fresh object on every call. Do not add DOM event listeners, buttons, badges, authorization copy, transparent overlays, or `REMOTE_CONTROL` protocol handling to the renderer library.
  </action>
  <verify>
    <automated>node --test tests/renderer-remote-control.test.js tests/renderer-viewer.test.js tests/renderer-overlays.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function mapHostPointToViewport" src/renderer/overlays.js` returns one match.
    - `grep -n "getViewportMapping" src/renderer/index.js tests/renderer-viewer.test.js` returns matches in both files.
    - `grep -n "Request control\\|Authorization hook\\|Control active\\|REMOTE_CONTROL" src/renderer/index.js src/renderer/overlays.js` returns no matches.
    - Focused tests pass with the command in `<verify>`.
  </acceptance_criteria>
  <done>The renderer exposes safe mapping state for host-owned control overlays while preserving the viewer library/UI boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host control overlay -> real browser viewport | Pointer and wheel coordinates from host DOM are translated into driver-native viewport coordinates. |
| viewer library -> demo UI | Library exposes mapping data but must not become the authorization or UI boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | protocol validators | mitigate | 05-01 rejects malformed control payloads before adapter dispatch. |
| T-05-02 | Information Disclosure | renderer mapping getter | mitigate | Getter returns dimensions/scale only; no mirrored HTML/text/attrs are exposed. |
| T-05-03 | Elevation of Privilege | authorization | mitigate | 05-02 adapter remains the authorization boundary; renderer does not authorize. |
| T-05-04 | Elevation of Privilege | inactive controls | mitigate | 05-05 host overlay stays inert unless control state is active; renderer exposes no dispatch path. |
| T-05-05 | Tampering | replay | mitigate | 05-02 adapter uses driver-native input; renderer only computes coordinates. |
| T-05-06 | Information Disclosure | frame selection | mitigate | 05-02 main-frame bridge filtering remains adapter responsibility. |
| T-05-07 | Tampering | `mapHostPointToViewport` | mitigate | Reject letterbox/out-of-bounds points with `inside:false`; clamp only after inside classification. |
| T-05-08 | Spoofing | demo safety | mitigate | 05-04/05-05 demo uses local-only server and default-deny UI. |
| T-05-09 | Tampering | relay boundary | mitigate | Renderer sends no relay frames directly in this plan; 05-01/05-04 preserve raw relay behavior. |
</threat_model>

<verification>
Run after this plan:

```bash
node --test tests/renderer-remote-control.test.js tests/renderer-viewer.test.js tests/renderer-overlays.test.js
npm test
```
</verification>

<success_criteria>
- Host code can call `viewer.getViewportMapping()` and pass the returned scale into `mapHostPointToViewport()`.
- Letterbox clicks are non-dispatchable.
- Renderer tests and existing viewer tests remain green.
- No authorization or product UI text is added to `src/renderer`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-playwright-cdp-adapter-remote-control-agent-demo/05-03-SUMMARY.md`.
</output>
