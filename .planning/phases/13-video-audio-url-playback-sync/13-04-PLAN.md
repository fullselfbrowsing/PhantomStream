---
phase: 13-video-audio-url-playback-sync
plan: 04
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - tests/differential/fixtures/media-playback-sync.html
  - tests/differential/scenarios/media-playback-sync.js
  - tests/differential/divergence-ledger.js
autonomous: true
requirements: [MEDIA-02, MWIRE-01]
must_haves:
  truths:
    - "the differential oracle stays green: the media-playback-sync scenario drives a real <video>/<audio> so the extracted side emits a media[] baseline + STREAM.MEDIA messages the FSB reference does not, and the new D27 ledger entry declares exactly that divergence"
    - "the D27 entry and a fixture that DETERMINISTICALLY fires it land together — no stale entry (the oracle's stale-entry detector would fail an entry that never matches), following the D26 single-predicate discipline"
    - "the normalizer/comparator includes the new top-level payload.media field so the SNAPSHOT divergence is the one D27 claims, not an unrelated comparator gap"
  artifacts:
    - path: "tests/differential/fixtures/media-playback-sync.html"
      provides: "A <video> (and <audio>) present at snapshot time so media[] is non-empty"
      contains: "<video"
    - path: "tests/differential/scenarios/media-playback-sync.js"
      provides: "beforeStart property stubs on both sides + run dispatching play + (post-throttle) timeupdate"
      contains: "media-playback-sync"
    - path: "tests/differential/divergence-ledger.js"
      provides: "D27-media-playback-sync mismatch entry pinned to the media-playback-sync scenario"
      contains: "D27"
  key_links:
    - from: "tests/differential/scenarios/media-playback-sync.js"
      to: "tests/differential/fixtures/media-playback-sync.html"
      via: "scenario references the fixture; both register in the differential runner"
      pattern: "media-playback-sync"
    - from: "tests/differential/divergence-ledger.js D27.appliesTo"
      to: "STREAM.MEDIA + STREAM.SNAPSHOT media[]"
      via: "predicate matches the extracted-only media divergence for scenarioName 'media-playback-sync'"
      pattern: "STREAM\\.MEDIA"
---

## Phase Goal

**As a** host embedding the PhantomStream viewer, **I want to** mirror a `<video>`/`<audio>` element's playback (play/pause/seek/rate) from the captured tab over a new throttled side channel with drift-corrected sync, **so that** the viewer plays the source media in lockstep without the bytes ever crossing the relay and without wedging on autoplay policy.

<objective>
The oracle-discipline slice: keep the differential oracle green now that the extracted capture emits a `media[]` baseline and `STREAM.MEDIA` messages the FSB reference does not. Add a `media-playback-sync` fixture + scenario that deterministically triggers the divergence and a single D27 ledger entry that declares its exact shape. This slice exists only because the oracle would otherwise hard-fail — it is the standing project discipline (the D26 precedent in Phase 12).

Purpose: The differential oracle is PhantomStream's correctness anchor (reference-vs-extracted op-stream equivalence). Every intentional extracted-only divergence must be a scenario-pinned ledger entry; an entry that never fires is flagged stale and fails the suite — so the entry and a firing fixture must land together with a single, exact predicate.
Output: `media-playback-sync.html`, `media-playback-sync.js`, and the D27 ledger entry; the full differential suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-video-audio-url-playback-sync/13-CONTEXT.md
@.planning/phases/13-video-audio-url-playback-sync/13-RESEARCH.md
@.planning/phases/13-video-audio-url-playback-sync/13-02-SUMMARY.md

<interfaces>
<!-- Differential-oracle anchors extracted this session. -->

tests/differential/divergence-ledger.js:
- D26-currentsrc-variant-pin entry — line 622-661 (the EXACT model to copy: id/kind/description/rationale/affectedMessages/affectedScenarios/appliesTo(refMsg, extMsg, scenarioName))
- ledgerCovers(ledger, refMsg, extMsg, scenarioName) — line 758 (the consumer; an entry whose appliesTo never returns true is flagged stale)

Existing scenario/fixture models to follow:
- tests/differential/scenarios/static-assets.js  (the beforeStart Object.defineProperty injection trick — harmless on the reference, load-bearing on the extracted side)
- tests/differential/scenarios/scroll.js          (a side-channel scenario that dispatches events and asserts the extracted-only message)
- tests/differential/fixtures/static-assets.html  (fixture-shape precedent)
- tests/differential/oracle.test.js               (the runner; compareStreams returns the FIRST ledger match — so two same-index SNAPSHOT divergences cannot both fire: keep ONE predicate)

From src/protocol/messages.js (Plan 01): STREAM.MEDIA, STREAM.SNAPSHOT.
From src/capture/index.js (Plan 02): the extracted core now emits payload.media on snapshot and STREAM.MEDIA on media events; the FSB reference (reference/extension/dom-stream.js) emits NEITHER.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: media-playback-sync fixture + scenario that deterministically fires the divergence</name>
  <files>tests/differential/fixtures/media-playback-sync.html, tests/differential/scenarios/media-playback-sync.js</files>
  <read_first>
    - tests/differential/scenarios/static-assets.js (the beforeStart defineProperty injection pattern to copy)
    - tests/differential/scenarios/scroll.js (side-channel event-dispatch scenario shape)
    - tests/differential/fixtures/static-assets.html (fixture shape)
    - tests/differential/oracle.test.js (lines 40-129 — how scenarios/fixtures are registered and compared; compareStreams returns the first ledger match)
    - .planning/phases/13-video-audio-url-playback-sync/13-RESEARCH.md (Differential Oracle Impact ~624-668: the fixture/scenario shape, Assumption A4 ~534 "entry + firing fixture must land together", the normalize.js caveat ~664)
    - .planning/phases/13-video-audio-url-playback-sync/13-02-SUMMARY.md (what Plan 02 actually emits — media[] keys + STREAM.MEDIA fields)
  </read_first>
  <behavior>
    - tests/differential/fixtures/media-playback-sync.html contains a `<video id="media-vid" src="https://cdn.fixture.test/clip.mp4">` (and optionally an `<audio>`) present at snapshot time, so the extracted serializeDOM emits a non-empty payload.media.
    - tests/differential/scenarios/media-playback-sync.js exports a beforeStart that Object.defineProperty-stubs the element's currentTime/paused/duration to deterministic values on BOTH the reference and extracted runs (harmless on the reference, load-bearing on the extracted side), and a run that dispatches `new Event('play')` then (after advancing past MEDIA_SYNC_THROTTLE_MS) `new Event('timeupdate')` — exercising a discrete + a heartbeat emission.
    - Registered in the differential runner so `node --test tests/differential/oracle.test.js` executes it; without a ledger entry the oracle WOULD hard-fail (proving the fixture truly fires the divergence — confirm by running before Task 2 lands the entry, or land them together and confirm green).
  </behavior>
  <action>
    Create tests/differential/fixtures/media-playback-sync.html modeled on static-assets.html with a snapshot-time `<video id="media-vid" src="https://cdn.fixture.test/clip.mp4"></video>` plus an `<audio id="media-aud" src="https://cdn.fixture.test/clip.mp3"></audio>`. Create tests/differential/scenarios/media-playback-sync.js modeled on static-assets.js + scroll.js: a beforeStart(doc) that defineProperty-stubs currentTime (e.g. 5), paused (false), duration (e.g. 30) on both media elements (deterministic on both sides — the static-assets currentSrc-injection trick); a run that dispatches Event('play') then waits past MEDIA_SYNC_THROTTLE_MS (250 ms — use the scenario's existing clock/wait helper) and dispatches Event('timeupdate'). Register the scenario+fixture in the differential runner exactly as static-assets/scroll are registered. Verify the normalizer/comparator includes the new top-level payload.media field (RESEARCH normalize.js caveat) so the SNAPSHOT comparison diverges on media[], not silently strips it. Reference: produces the deterministic extracted-only media divergence MEDIA-02/MWIRE-01 require the oracle to ledger.
  </action>
  <verify>
    <automated>node --test tests/differential/oracle.test.js</automated>
  </verify>
  <done>media-playback-sync fixture + scenario exist, are registered, and deterministically drive a real <video>/<audio> so the extracted side emits media[] + STREAM.MEDIA; the comparator includes payload.media.</done>
</task>

<task type="auto">
  <name>Task 2: D27 ledger entry declaring the exact media divergence (single predicate, D26 discipline)</name>
  <files>tests/differential/divergence-ledger.js</files>
  <read_first>
    - tests/differential/divergence-ledger.js (the file being modified; D26 entry 622-661 as the exact model; ledgerCovers 758 — stale-entry semantics)
    - tests/differential/oracle.test.js (the stale-entry detector + compareStreams first-match behavior)
    - .planning/phases/13-video-audio-url-playback-sync/13-RESEARCH.md (Differential Oracle Impact: the recommended D27 entry shape ~630-661, the single-predicate caveat ~664; STATE.md [Phase 12-02] D26-only rationale)
  </read_first>
  <behavior>
    - A D27-media-playback-sync mismatch entry exists with affectedScenarios: ['media-playback-sync'] and affectedMessages: [STREAM.SNAPSHOT, STREAM.MEDIA].
    - appliesTo(refMsg, extMsg, scenarioName) returns false for any scenarioName !== 'media-playback-sync'; returns true for (a) an extracted-only trailing STREAM.MEDIA message (refMsg undefined, extMsg.type === STREAM.MEDIA) AND (b) a same-index SNAPSHOT where only the extracted payload carries a non-empty media[] — a SINGLE predicate covering both shapes (compareStreams returns the first match, so no second entry).
    - With both this entry and Task 1's fixture present, `node --test tests/differential/oracle.test.js` is green AND the stale-entry detector does not flag D27 (it fires on the media-playback-sync scenario).
  </behavior>
  <action>
    Add the D27-media-playback-sync entry to the ledger array in tests/differential/divergence-ledger.js, copying the D26 entry structure: id 'D27-media-playback-sync', kind 'mismatch', a description naming the media[] baseline fields + the STREAM.MEDIA side channel and that the FSB reference emits neither, a rationale citing MEDIA-02/MEDIA-03/MWIRE-01 (CONTEXT-locked; side-channel-by-nid like DIFF_OP.VALUE, never baked into the clone), affectedMessages [STREAM.SNAPSHOT, STREAM.MEDIA], affectedScenarios ['media-playback-sync'], and a single appliesTo(refMsg, extMsg, scenarioName) that: returns false unless scenarioName === 'media-playback-sync'; returns true for Shape A (refMsg === undefined && extMsg && extMsg.type === STREAM.MEDIA); returns true for Shape B (both SNAPSHOT, extMsg.payload.media non-empty array && ref has none); else false. Keep it ONE predicate (D26 discipline — compareStreams returns the first match; a second same-index entry would never fire and fail stale detection). Reference: closes the differential-oracle gap MEDIA-02/MWIRE-01 open.
  </action>
  <verify>
    <automated>node --test tests/differential/oracle.test.js</automated>
  </verify>
  <done>D27 entry present, scenario-pinned, single-predicate covering both the trailing-STREAM.MEDIA and the media[]-only-SNAPSHOT divergence shapes; oracle green; D27 not flagged stale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures/scenarios -> differential oracle | Test-only code; no production trust boundary crossed. The oracle is a correctness gate, not a runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-14 | Tampering (correctness regression) | differential oracle blind to media divergence | mitigate | A scenario-pinned D27 entry + a deterministically-firing media fixture make every extracted-only media divergence an explicit, reviewed ledger declaration; an undeclared media divergence still hard-fails the oracle. |
| T-13-15 | Repudiation (silent stale entry) | a ledger entry that never matches | mitigate | Entry + firing fixture land together; single predicate per the D26 discipline; the stale-entry detector confirms D27 fires on the media-playback-sync scenario. |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed (RESEARCH audit: none). No install task; no blocking checkpoint required. hls.js is Phase 14 and absent here. |
</threat_model>

<verification>
- `node --test tests/differential/oracle.test.js` green WITH the media-playback-sync scenario active and the D27 entry firing (not stale).
- Full suite `node --test tests/*.test.js tests/differential/*.test.js` green (the baseline 449/449 plus the new media tests across Plans 01-04).
- The D26 entry and all prior ledger entries remain non-stale (no regression to the existing oracle discipline).
</verification>

<success_criteria>
- The differential oracle stays green now that capture emits `media[]` + `STREAM.MEDIA`; the extracted-only divergence is declared by a single scenario-pinned D27 entry (MEDIA-02, MWIRE-01).
- Entry + firing fixture land together with one predicate (D26 discipline); no stale-entry failure.
</success_criteria>

<output>
Create `.planning/phases/13-video-audio-url-playback-sync/13-04-SUMMARY.md` when done.
</output>
