---
phase: 10-npm-packaging-0-x-publish
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/package-publish.test.js
autonomous: true
requirements:
  - PKG-03
  - PKG-04
must_haves:
  truths:
    - "Package validation tests fail RED until types, exports, files whitelist, package scripts, docs, and release workflow exist."
    - "Tests inspect the installable tarball shape, not only source files."
  artifacts:
    - path: "tests/package-publish.test.js"
      provides: "RED package export/type/tarball/docs/release validation coverage"
---

<objective>
Add RED tests that define the npm package acceptance surface before package metadata and tooling changes.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-npm-packaging-0-x-publish/10-CONTEXT.md
@.planning/phases/10-npm-packaging-0-x-publish/10-RESEARCH.md
@.planning/phases/10-npm-packaging-0-x-publish/10-VALIDATION.md
@.planning/phases/10-npm-packaging-0-x-publish/10-PATTERNS.md
@package.json
@README.md
@.github/workflows/ci.yml
</context>

<threat_model>
| Threat | Mitigation in this plan |
|---|---|
| Publishing private/planning/reference files | RED tests assert tarball exclusions for `.planning/`, `reference/`, `.context/`, and `tests/`. |
| Broken package exports/types | RED tests assert every public subpath has a `types` condition and generated declaration target. |
| Ungated publish | RED tests assert release docs/workflow keep real publish behind a gated workflow/manual approval path. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED package publish validation tests</name>
  <files>tests/package-publish.test.js</files>
  <read_first>
    - `package.json`
    - `README.md`
    - `.github/workflows/ci.yml`
    - `.planning/phases/10-npm-packaging-0-x-publish/10-RESEARCH.md`
    - `.planning/phases/10-npm-packaging-0-x-publish/10-PATTERNS.md`
  </read_first>
  <action>
    Create `tests/package-publish.test.js` using `node:test` and `node:assert/strict`.

    The tests must assert all of these exact future-state conditions:
    - `package.json` has scripts `types`, `lint:package`, `attw`, `package:pack`, `package:smoke`, and `package:check`.
    - `package.json` has a `files` whitelist that includes `src`, `dist/types`, `bin`, `examples`, `README.md`, and `LICENSE`, and does not include `.planning`, `reference`, or `tests`.
    - Every existing public export key (`"."`, `"./protocol"`, `"./capture"`, `"./renderer"`, `"./relay"`, `"./transport/websocket"`, `"./adapters/playwright"`, `"./adapters/extension"`, `"./adapters/bookmarklet"`) is an object with `"types"` first and `"default"` last.
    - Every export `"types"` value points under `./dist/types/`, and every `"default"` value points at the existing runtime JS source.
    - `tsconfig.types.json` exists and contains `allowJs`, `checkJs`, `declaration`, `emitDeclarationOnly`, `declarationMap`, and `outDir`.
    - `.github/workflows/ci.yml` runs `npm run package:check`.
    - `.github/workflows/publish.yml` exists, has `id-token: write`, uses `registry-url: 'https://registry.npmjs.org'`, runs `npm run package:check` before publish, and runs only on release/tag style events.
    - `docs/QUICKSTARTS.md` and `docs/RELEASE.md` exist.
    - `README.md` links to `docs/QUICKSTARTS.md`, names CSSOM mode as shipped, and no longer says v1 capture enhancements are planned for CSSOM.

    Keep the tests RED. Do not modify package metadata or docs in this task.
  </action>
  <verify>
    <automated>set +e; node --test tests/package-publish.test.js; code=$?; test "$code" -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/package-publish.test.js` passes.
    - `rg -n "package:check|tsconfig.types.json|docs/QUICKSTARTS.md|publish.yml|id-token: write|dist/types|reference" tests/package-publish.test.js` returns matches.
    - `set +e; node --test tests/package-publish.test.js; code=$?; test "$code" -ne 0` passes, proving the tests are RED.
  </acceptance_criteria>
  <done>Package acceptance tests exist and fail only because Phase 10 implementation is missing.</done>
</task>

</tasks>

