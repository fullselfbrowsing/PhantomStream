---
phase: 08-shadow-dom-iframes-fidelity-completion
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/capture-subtree-fetch.test.js
  - tests/renderer-subtree-fetch.test.js
  - tests/playwright-fidelity-phase8.test.js
autonomous: true
requirements:
  - CAPT-11
  - CAPT-08
  - CAPT-09
  - CAPT-05
must_haves:
  truths:
    - "On-demand subtree recovery has failing capture and renderer tests for request latching, staleness, sanitized payloads, and current-snapshot application."
    - "A browser-backed Phase 8 smoke test exists for real shadow slot behavior, iframe origin behavior, and actual input/change events."
  artifacts:
    - path: "tests/capture-subtree-fetch.test.js"
      provides: "Capture RED coverage for CONTROL.SUBTREE_REQUEST handling and STREAM.SUBTREE_RESPONSE payloads"
    - path: "tests/renderer-subtree-fetch.test.js"
      provides: "Renderer RED coverage for requestSubtree latching and response installation"
    - path: "tests/playwright-fidelity-phase8.test.js"
      provides: "Real Chromium RED smoke for CAPT-08, CAPT-09, and CAPT-05"
  key_links:
    - from: "tests/renderer-subtree-fetch.test.js"
      to: "src/renderer/index.js"
      via: "viewer.requestSubtree sends CONTROL.SUBTREE_REQUEST and handles STREAM.SUBTREE_RESPONSE"
      pattern: "requestSubtree"
    - from: "tests/capture-subtree-fetch.test.js"
      to: "src/capture/index.js"
      via: "capture.handleControl serializes requested live subtree"
      pattern: "SUBTREE_RESPONSE"
    - from: "tests/playwright-fidelity-phase8.test.js"
      to: "src/adapters/playwright-inject.js"
      via: "real browser validates checked-in inject artifact behavior"
      pattern: "chromium"
---

<objective>
Create the Wave 0 RED tests for targeted subtree recovery and the browser-backed Phase 8 fidelity smoke.

Purpose: CAPT-11 must be proven as a bounded request/response path, and jsdom cannot fully validate real browser shadow slot, frame, and input behavior.
Output: Three focused test files that fail against the current implementation for missing Phase 8 behavior.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-CONTEXT.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-RESEARCH.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-VALIDATION.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-PATTERNS.md
@tests/renderer-loopback.test.js
@tests/playwright-adapter.test.js
@src/capture/index.js
@src/renderer/index.js
@src/protocol/messages.js

<interfaces>
Existing viewer recovery pattern:

```js
function requestResync(reason, details) {
  if (resyncPending) return;
  resyncPending = true;
  safeSend(CONTROL.START, { trigger: 'preview-resync', reason: reason || 'unknown' });
}
```

Planned subtree request/response wire shape:

```js
CONTROL.SUBTREE_REQUEST = 'dash:ps-subtree-request';
STREAM.SUBTREE_RESPONSE = 'ext:ps-subtree-response';
// request payload: { requestId, nid, streamSessionId, snapshotId, reason }
// response payload: { requestId, nid, status, html, nodeIds, shadowRoots, frames, streamSessionId, snapshotId }
```
</interfaces>
</context>

<decision_coverage>
- D-19: RED subtree tests require on-demand fetch for missing/truncated nids with sanitized subtree payloads and nodeIds.
- D-20: RED subtree tests require streamSessionId/snapshotId staleness and soft ignore behavior for gone/skipped/blocked nodes.
- D-21: RED subtree tests require fetch responses to reuse snapshot/add serialization policy for sanitization, masking, URL absolutification, style, and identity sidecars.
- D-22: RED subtree tests require bounded/latching behavior with no cascading fetch storms.
- D-23: Wave 0 fixtures cover truncated fetch plus browser-backed shadow, iframe, and input fidelity.
- D-25: Browser smoke establishes final capture/renderer/adapter test coverage expected after implementation.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED subtree fetch capture and renderer tests</name>
  <files>tests/capture-subtree-fetch.test.js, tests/renderer-subtree-fetch.test.js</files>
  <read_first>
    - `.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-CONTEXT.md` decisions D-19 through D-22 and D-23.
    - `.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-VALIDATION.md` rows 08-W0-05.
    - `tests/renderer-loopback.test.js` for loopback transport and resync latch patterns.
    - `tests/security-sanitize-capture.test.js` and `tests/security-sanitize-render.test.js` for sanitizer leak assertions.
  </read_first>
  <behavior>
    - Test 1: `capture.handleControl(CONTROL.SUBTREE_REQUEST, { requestId, nid, streamSessionId, snapshotId })` emits `STREAM.SUBTREE_RESPONSE` with `status: 'ok'`, sanitized `html`, `nodeIds`, and current identity.
    - Test 2: stale session/snapshot, gone, skipped, blocked, and untracked nids produce content-free miss responses with `status` set to `stale`, `gone`, `skipped`, or `blocked`.
    - Test 3: `viewer.requestSubtree(nid)` sends one request per nid while a request is in flight and includes `requestId`, `nid`, `streamSessionId`, and `snapshotId`.
    - Test 4: current `STREAM.SUBTREE_RESPONSE` payloads replace a `data-phantomstream-truncated="true"` placeholder, sanitize before import, index `nodeIds`, and clear the latch; stale responses are ignored and also clear their matching latch.
  </behavior>
  <action>
    Create `tests/capture-subtree-fetch.test.js` with local jsdom helpers and a recording transport. Start capture, get a live nid with `capture.getNodeId(element)`, call the planned public `capture.handleControl(CONTROL.SUBTREE_REQUEST, payload)`, and assert response shape. Include hostile HTML in the requested subtree and assert no `onclick`, `javascript:`, raw password, or `data:text/html` reaches the wire.

    Create `tests/renderer-subtree-fetch.test.js` with direct `createViewer` tests. Use a snapshot containing a mirror-only truncated marker element such as `<div data-phantomstream-truncated="true"></div>` with a sidecar nid. Call the planned `viewer.requestSubtree(nid)`, assert the sent request is latched by nid, emit `STREAM.SUBTREE_RESPONSE`, glue the iframe document where jsdom needs it, and assert the placeholder is replaced by sanitized content with `viewer.resolveNode(newNid)` working.

    Reference the planned constants and fields exactly: `CONTROL.SUBTREE_REQUEST`, `STREAM.SUBTREE_RESPONSE`, `requestId`, `status`, `nid`, `html`, `nodeIds`, `shadowRoots`, and `frames`. These tests are expected to fail until Plans 08-04 and 08-07 land protocol and implementation.
  </action>
  <verify>
    <automated>set +e; node --test tests/capture-subtree-fetch.test.js tests/renderer-subtree-fetch.test.js; code=$?; test "$code" -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/capture-subtree-fetch.test.js` passes.
    - `test -f tests/renderer-subtree-fetch.test.js` passes.
    - `rg -n "SUBTREE_REQUEST|SUBTREE_RESPONSE|requestSubtree|requestId|data-phantomstream-truncated|status" tests/capture-subtree-fetch.test.js tests/renderer-subtree-fetch.test.js` returns matches.
    - The RED verification command exits 0 by proving the focused tests fail before implementation.
  </acceptance_criteria>
  <done>Subtree fetch RED tests exist, encode D-19 through D-22 and CAPT-11, and fail against the current implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RED Playwright fidelity smoke for Phase 8</name>
  <files>tests/playwright-fidelity-phase8.test.js</files>
  <read_first>
    - `.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-CONTEXT.md` decisions D-03 through D-15, D-23, and D-25.
    - `.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-VALIDATION.md` row 08-W0-06.
    - `tests/playwright-adapter.test.js` and `tests/playwright-adapter-cdp.test.js` for Playwright import/setup style.
    - `src/adapters/playwright-inject.js` because the final browser smoke must exercise the checked-in artifact after Plan 08-08.
  </read_first>
  <behavior>
    - Test 1: in Chromium, a page with open shadow roots, default/named slots, and slot reassignment mirrors without duplicating slotted light-DOM text.
    - Test 2: a same-origin iframe mirrors inert content while a cross-origin or inaccessible frame renders as a content-free label.
    - Test 3: real `input` and `change` events from Chromium update mirrored input, textarea, select, checkbox, and radio state.
  </behavior>
  <action>
    Create `tests/playwright-fidelity-phase8.test.js` using `node:test`, `node:assert/strict`, and `import { chromium } from 'playwright'`. Keep helpers local to the file. Build an in-page fixture with a source side and viewer side connected by the existing loopback transport pattern, or use the Playwright adapter if that path is simpler after implementation.

    The test must be autonomous and must not require a visible browser. Use Playwright-managed Chromium headless mode. Keep external navigation out of the test except for a content-free cross-origin/inaccessible iframe simulation; if a real cross-origin navigation is unreliable, make the iframe `contentDocument` inaccessible from capture in the fixture and assert content-free behavior.

    These tests are expected to fail until the Phase 8 implementation and the checked-in inject artifact update are complete.
  </action>
  <verify>
    <automated>set +e; node --test tests/playwright-fidelity-phase8.test.js; code=$?; test "$code" -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/playwright-fidelity-phase8.test.js` passes.
    - `rg -n "chromium|attachShadow|iframe|input|change|select|checkbox|radio" tests/playwright-fidelity-phase8.test.js` returns matches.
    - The RED verification command exits 0 by proving the browser smoke fails before implementation or by failing on missing Phase 8 behavior rather than syntax/import errors.
  </acceptance_criteria>
  <done>Playwright RED smoke exists for real browser Phase 8 behavior and fails against the current implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viewer -> capture control | Viewer sends targeted subtree requests to capture through the same transport/host boundary as lifecycle controls. |
| live subtree -> wire response | Capture serializes an attacker-controlled live subtree on demand. |
| browser fixture -> test process | Playwright launches Chromium and executes fixture code under test control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | D | subtree request tests | mitigate | Tests must require request latching by nid/requestId and no repeated request storm before response. |
| T-08-10 | I | capture subtree response tests | mitigate | Tests must prove stale/gone/skipped/blocked responses are content-free and raw masked values never appear in wire JSON. |
| T-08-11 | E | renderer subtree install tests | mitigate | Tests must require `sanitizeFragment` before import/index and sandbox contract remains unchanged. |
| T-08-12 | T | browser smoke | mitigate | Playwright fixture must exercise public capture/viewer paths and checked-in artifact behavior, not private test-only shortcuts. |
</threat_model>

<verification>
Run both RED-focused commands:

```bash
set +e; node --test tests/capture-subtree-fetch.test.js tests/renderer-subtree-fetch.test.js; code=$?; test "$code" -ne 0
set +e; node --test tests/playwright-fidelity-phase8.test.js; code=$?; test "$code" -ne 0
```
</verification>

<success_criteria>
- The three test files exist and are executable with `node --test`.
- Tests fail against the current implementation for missing subtree/browser fidelity behavior, not for syntax/import errors.
- No production source file is modified in this Wave 0 plan.
- D-19 through D-23 and browser evidence for D-25 are represented by explicit test names or assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-03-SUMMARY.md`.
</output>
