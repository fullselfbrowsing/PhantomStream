---
phase: 08-shadow-dom-iframes-fidelity-completion
plan: 09
type: execute
wave: 6
depends_on:
  - 08-08
files_modified:
  - src/capture/README.md
  - src/renderer/README.md
  - docs/ARCHITECTURE.md
  - docs/SECURITY.md
  - docs/DESIGN-HISTORY.md
  - tests/differential/normalize.js
  - tests/differential/divergence-ledger.js
  - tests/differential/oracle.test.js
  - package.json
autonomous: true
requirements:
  - CAPT-05
  - CAPT-06
  - CAPT-08
  - CAPT-09
  - CAPT-11
must_haves:
  truths:
    - "Docs describe completed Phase 8 behavior and explicitly keep full CSSOM capture out of Phase 8 per D-18."
    - "Differential oracle entries for D-24 are narrow and scenario-pinned for shadow/frame/value/subtree protocol extensions."
    - "D-23: Focused fixtures exist for each fidelity gap: open shadow root + slots, same-origin iframe, cross-origin iframe placeholder, input value drift, late-added styled node, and truncated-region fetch."
    - "Final automated gate for D-25 runs focused capture, renderer, security, adapter, Playwright, differential, and full-suite tests."
  artifacts:
    - path: "src/capture/README.md"
      provides: "Capture documentation for shadow roots, frames, value diffs, add-op styles, and subtree controls"
    - path: "src/renderer/README.md"
      provides: "Renderer documentation for shadow/frame reconstruction, value diffs, and requestSubtree"
    - path: "docs/ARCHITECTURE.md"
      provides: "Updated fidelity behavior and remaining limitations"
    - path: "docs/SECURITY.md"
      provides: "Updated shadow/frame/value/subtree threat mitigations"
    - path: "docs/DESIGN-HISTORY.md"
      provides: "Phase 8 style and truncation recovery design record"
    - path: "tests/differential/divergence-ledger.js"
      provides: "Narrow ledger entries for intentional Phase 8 protocol divergence"
  key_links:
    - from: "tests/differential/oracle.test.js"
      to: "tests/differential/divergence-ledger.js"
      via: "scenario-pinned divergence coverage"
      pattern: "phase8"
    - from: "docs/SECURITY.md"
      to: "src/capture/index.js and src/renderer/sanitize.js"
      via: "documented sanitizer and sandbox policy for new Phase 8 payloads"
      pattern: "sanitizeForWire|sanitizeFragment"
---

<objective>
Close Phase 8 documentation, differential oracle discipline, and final automated verification.

Purpose: Phase 8 changes user-visible fidelity behavior and intentional divergence from the FSB reference. The final plan documents the shipped behavior, keeps oracle allowances narrow, and proves the whole phase after the adapter artifact is synchronized.
Output: Updated docs/oracle files, green Phase 8 focused command, and green `npm test`.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-CONTEXT.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-RESEARCH.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-VALIDATION.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-PATTERNS.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-04-SUMMARY.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-05-SUMMARY.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-06-SUMMARY.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-07-SUMMARY.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-08-SUMMARY.md
@src/capture/README.md
@src/renderer/README.md
@docs/ARCHITECTURE.md
@docs/SECURITY.md
@docs/DESIGN-HISTORY.md
@tests/differential/normalize.js
@tests/differential/divergence-ledger.js
@tests/differential/oracle.test.js
@package.json
</context>

<decision_coverage>
- D-04: Docs/oracle must describe structured shadow metadata tied to host nid and real mirror shadow roots.
- D-05: Docs/oracle must describe slot semantics without slotted light-DOM duplication.
- D-06: Docs/oracle must describe shadow-root addressing as Phase 7 nid extension with no selector fallback.
- D-09: Docs/oracle must preserve cross-origin iframe placeholders and no origin bypass.
- D-10: Docs/oracle must preserve inert no-scripts iframe mirroring, not live remote documents.
- D-12: Docs/oracle must describe explicit input/change capture for value property drift.
- D-13: Docs/oracle must cover input, textarea, select, checkbox/radio, and selected option state.
- D-14: Docs/oracle must document value-bearing updates through masking/privacy chokepoints.
- D-15: Docs/oracle must document narrow value diff behavior and no typed telemetry.
- D-16: Docs/oracle must document curated computed styles/default elision for add ops.
- D-17: Docs/oracle must document batched style/layout reads and no forced reflow loop.
- D-18: Docs must explicitly state full CSSOM capture remains Phase 9 only.
- D-19: Docs/oracle must describe on-demand subtree fetch for missing/truncated nids.
- D-20: Docs/oracle must describe streamSessionId/snapshotId staleness and soft miss handling.
- D-21: Docs/oracle must describe subtree response reuse of snapshot/add serialization policy.
- D-22: Docs/oracle must describe bounded/latching behavior with no fetch storms.
- D-23: Final verification must cover focused fixtures for shadow+slots, iframes, input drift, late-added styles, and truncated fetch.
- D-24: Differential ledger updates must be narrow and scenario-pinned for Phase 8 protocol divergence.
- D-25: Final automated gate must run capture, renderer, security, adapter, Playwright, differential, and full-suite tests after the phase.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Update docs and differential oracle discipline</name>
  <files>src/capture/README.md, src/renderer/README.md, docs/ARCHITECTURE.md, docs/SECURITY.md, docs/DESIGN-HISTORY.md, tests/differential/normalize.js, tests/differential/divergence-ledger.js, tests/differential/oracle.test.js</files>
  <read_first>
    - `08-CONTEXT.md` decisions D-03 through D-25 and deferred ideas.
    - `08-RESEARCH.md` Architecture Patterns, Common Pitfalls, Security Domain, and Open Questions (RESOLVED).
    - `tests/differential/normalize.js`, `tests/differential/divergence-ledger.js`, and `tests/differential/oracle.test.js` existing ledger patterns.
    - `docs/ARCHITECTURE.md`, `docs/SECURITY.md`, and `docs/DESIGN-HISTORY.md` current limitations/performance sections.
  </read_first>
  <action>
    Update docs to describe the completed Phase 8 behavior:
    - `src/capture/README.md`: open shadow root traversal/observation, same-origin frame serialization, value diffs, add-op styles, subtree request handling, and `handleControl`.
    - `src/renderer/README.md`: shadow root reconstruction, inert frame rendering, `DIFF_OP.VALUE`, `requestSubtree`, `STREAM.SUBTREE_RESPONSE`, and identity-index behavior across shadow/frame scopes.
    - `docs/ARCHITECTURE.md`: remove or revise limitations fixed by Phase 8; keep closed shadow roots, cross-origin iframe content, media, and CSSOM mode as explicit remaining limitations.
    - `docs/SECURITY.md`: document that shadow/frame/subtree HTML reuses `sanitizeForWire`, `sanitizeFragment`, CSP meta, and sandbox without `allow-scripts`; document value-diff masking.
    - `docs/DESIGN-HISTORY.md`: record that late-added styles use curated computed properties and batched reads, not all-property enumeration.

    Update the differential oracle only where Phase 8 intentionally diverges from the FSB reference. Add scenario-pinned entries for shadow/frame/value/subtree protocol extensions if the oracle sees mismatch kinds. Keep predicates narrow: match the exact scenario name and affected `STREAM.SNAPSHOT` or `STREAM.MUTATIONS` message shape. Do not create broad rules that excuse unrelated mismatches. Update `tests/differential/oracle.test.js` matrix only with Phase 8 fixtures/scenarios needed to keep D-24 honest.
  </action>
  <verify>
    <automated>node --test tests/differential/oracle.test.js tests/node-identity-static.test.js tests/security-chokepoint-purity.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "shadow root|iframe|value diff|requestSubtree|subtree" src/capture/README.md src/renderer/README.md docs/ARCHITECTURE.md docs/SECURITY.md docs/DESIGN-HISTORY.md` returns matches.
    - `rg -n "closed shadow|cross-origin iframe content|CSSOM" docs/ARCHITECTURE.md docs/SECURITY.md` returns matches documenting remaining/non-Phase-8 limits.
    - `node --test tests/differential/oracle.test.js` passes.
    - Any new ledger entry is scenario-pinned and references Phase 8 behavior by id or description.
  </acceptance_criteria>
  <done>Docs and differential oracle reflect Phase 8 behavior without broad divergence allowances or undocumented security boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Run the final Phase 8 automated gate</name>
  <files>package.json</files>
  <read_first>
    - `.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-VALIDATION.md` full quick command and full suite command.
    - All Phase 8 plan summaries 08-04 through 08-08.
    - `package.json` scripts.
  </read_first>
  <action>
    Run the Phase 8 quick command exactly:

    `node --test tests/capture-shadow-dom.test.js tests/renderer-shadow-dom.test.js tests/capture-iframe.test.js tests/renderer-iframe.test.js tests/capture-input-values.test.js tests/renderer-value-diff.test.js tests/capture-added-styles.test.js tests/capture-subtree-fetch.test.js tests/renderer-subtree-fetch.test.js tests/playwright-fidelity-phase8.test.js`

    Then run `npm test`. If failures occur, fix only regressions in files modified by Phase 8 plans or tests that were made obsolete by the completed Phase 8 behavior. Do not add dependencies unless a failing test proves the existing stack cannot exercise required behavior. Per the resolved research question, do not bump Playwright from locked `1.60.0` unless an actual Phase 8 browser test proves a Playwright bug.
  </action>
  <verify>
    <automated>node --test tests/capture-shadow-dom.test.js tests/renderer-shadow-dom.test.js tests/capture-iframe.test.js tests/renderer-iframe.test.js tests/capture-input-values.test.js tests/renderer-value-diff.test.js tests/capture-added-styles.test.js tests/capture-subtree-fetch.test.js tests/renderer-subtree-fetch.test.js tests/playwright-fidelity-phase8.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Phase 8 quick command passes.
    - `npm test` passes.
    - `git diff -- package.json package-lock.json` shows no production dependency bump unless the summary records a test-proven reason.
    - `rg -n "allow-scripts|querySelector\\('\\[data-fsb-nid|data-fsb-nid" src/capture/index.js src/renderer/index.js src/renderer/diff.js src/adapters/playwright-inject.js` is compatible with the existing static identity and sandbox tests.
  </acceptance_criteria>
  <done>All Phase 8 focused tests, browser smoke, differential oracle, security gates, adapter tests, and full suite are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> users/hosts | Documentation must not imply cross-origin/closed-root content is captured or scripts run in mirror frames. |
| oracle ledger -> regression gate | Divergence predicates can hide real regressions if too broad. |
| full automated gate -> package state | Final verification may expose dependency pressure, but package bumps must be test-proven. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-32 | I | docs | mitigate | Document cross-origin frames and closed shadow roots as content-free/non-captured limitations. |
| T-08-33 | R | differential ledger | mitigate | Add only scenario-pinned Phase 8 divergence entries; full oracle must pass. |
| T-08-34 | D | final automated gate | mitigate | Run focused quick command and `npm test`; no dependency bump without test-proven need. |
</threat_model>

<verification>
```bash
node --test tests/differential/oracle.test.js tests/node-identity-static.test.js tests/security-chokepoint-purity.test.js
node --test tests/capture-shadow-dom.test.js tests/renderer-shadow-dom.test.js tests/capture-iframe.test.js tests/renderer-iframe.test.js tests/capture-input-values.test.js tests/renderer-value-diff.test.js tests/capture-added-styles.test.js tests/capture-subtree-fetch.test.js tests/renderer-subtree-fetch.test.js tests/playwright-fidelity-phase8.test.js
npm test
```
</verification>

<success_criteria>
- Docs cover all completed Phase 8 fidelity behavior and clearly defer full CSSOM capture to Phase 9.
- Differential oracle allowances for D-24 are narrow, scenario-pinned, and verified.
- D-23 and D-25 are complete: focused fixtures exist and full capture/renderer/security/adapter tests run.
- CAPT-05, CAPT-06, CAPT-08, CAPT-09, and CAPT-11 are covered by final verification.
</success_criteria>

<output>
After completion, create `.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-09-SUMMARY.md`.
</output>
