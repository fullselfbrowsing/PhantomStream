---
phase: 08-shadow-dom-iframes-fidelity-completion
plan: 08
type: execute
wave: 5
depends_on:
  - 08-07
files_modified:
  - src/adapters/playwright.js
  - src/adapters/playwright-inject.js
  - tests/playwright-adapter.test.js
  - tests/playwright-adapter-cdp.test.js
  - tests/playwright-fidelity-phase8.test.js
autonomous: true
requirements:
  - CAPT-05
  - CAPT-06
  - CAPT-08
  - CAPT-09
  - CAPT-11
must_haves:
  truths:
    - "The checked-in classic Playwright/CDP inject artifact carries Phase 8 shadow, frame, value, style, and subtree capture behavior without ESM/CommonJS imports."
    - "The public Playwright adapter routes CONTROL.SUBTREE_REQUEST frames into the injected capture handle and forwards STREAM.SUBTREE_RESPONSE back through the transport."
    - "The Playwright fidelity smoke exercises the public adapter/inject path for D-04, D-05, D-06, D-09, D-10, D-12, D-13, D-14, D-15, D-16, D-17, D-19, D-20, D-21, D-22, and D-25."
  artifacts:
    - path: "src/adapters/playwright.js"
      provides: "Adapter routing from viewer subtree control frames into the injected page capture handle"
    - path: "src/adapters/playwright-inject.js"
      provides: "Classic script artifact synchronized with Phase 8 capture behavior"
    - path: "tests/playwright-adapter.test.js"
      provides: "Adapter unit coverage for subtree request forwarding and response propagation"
    - path: "tests/playwright-fidelity-phase8.test.js"
      provides: "Browser-backed adapter/inject verification for Phase 8 fidelity"
  key_links:
    - from: "src/adapters/playwright-inject.js"
      to: "src/capture/index.js"
      via: "classic script mirrors capture core changes without ESM imports"
      pattern: "shadowRoots"
    - from: "tests/playwright-fidelity-phase8.test.js"
      to: "src/adapters/playwright-inject.js"
      via: "real browser smoke exercises checked-in inject artifact behavior"
      pattern: "chromium"
    - from: "src/adapters/playwright.js"
      to: "src/adapters/playwright-inject.js"
      via: "transport CONTROL.SUBTREE_REQUEST invokes page-side handleControl and returns STREAM.SUBTREE_RESPONSE"
      pattern: "SUBTREE_REQUEST"
---

<objective>
Synchronize the checked-in browser inject artifact with Phase 8 capture behavior and prove it through adapter/browser verification.

Purpose: Phase 8 changes capture protocol behavior that must work in the Playwright/CDP injection path, not only the ESM capture core.
Output: Updated `src/adapters/playwright-inject.js` and green adapter-focused Playwright verification.
</objective>

<execution_context>
@/Users/lakshmanturlapati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshmanturlapati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-CONTEXT.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-RESEARCH.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-VALIDATION.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-PATTERNS.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-04-SUMMARY.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-05-SUMMARY.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-06-SUMMARY.md
@.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-07-SUMMARY.md
@src/adapters/playwright.js
@src/adapters/playwright-inject.js
@src/capture/index.js
@tests/adapter-exports.test.js
@tests/playwright-adapter.test.js
@tests/playwright-adapter-cdp.test.js
@tests/playwright-fidelity-phase8.test.js
</context>

<decision_coverage>
- D-04: Inject artifact preserves host-nid-tied structured shadow metadata and mirror shadow-root behavior from capture core.
- D-05: Browser smoke covers default/named/reassigned slot behavior without slotted light-DOM duplication.
- D-06: Inject artifact preserves Phase 7 nid addressing for shadow descendants with no selector fallback.
- D-09: Browser smoke keeps cross-origin iframe output content-free and does not bypass origin policy.
- D-10: Inject artifact preserves inert, no-scripts iframe mirror behavior rather than live remote documents.
- D-12: Inject artifact includes explicit input/change capture for value property drift.
- D-13: Browser smoke covers input, textarea, select, checkbox/radio, and selected option state.
- D-14: Inject artifact preserves masking/privacy chokepoints for value-bearing updates.
- D-15: Inject artifact preserves narrow value diffs and typed-value telemetry exclusion.
- D-16: Inject artifact preserves curated computed styles/default elision on add ops.
- D-17: Inject artifact preserves batched style/layout reads without a broad forced reflow loop.
- D-19: Inject artifact carries on-demand subtree request/response support for missing/truncated nids.
- D-20: Inject artifact preserves streamSessionId/snapshotId staleness and soft miss behavior.
- D-21: Inject artifact reuses snapshot/add serialization policy for subtree responses.
- D-22: Inject artifact preserves bounded/latching behavior with no fetch storms.
- D-25: Adapter and Playwright tests are part of the required full post-phase coverage.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Synchronize the checked-in browser inject artifact and adapter subtree routing</name>
  <files>src/adapters/playwright.js, src/adapters/playwright-inject.js, tests/playwright-adapter.test.js, tests/playwright-adapter-cdp.test.js, tests/playwright-fidelity-phase8.test.js</files>
  <read_first>
    - `src/adapters/playwright.js` current transport subscription, `CONTROL.START` routing, `handleControlMessage`, and injected page evaluate patterns.
    - `src/capture/index.js` final Phase 8 implementation from Plans 08-04 through 08-07.
    - `src/adapters/playwright-inject.js` current classic script artifact.
    - `07-04-SUMMARY.md` artifact synchronization pattern.
    - `tests/adapter-exports.test.js`, `tests/playwright-adapter.test.js`, `tests/playwright-adapter-cdp.test.js`, and `tests/playwright-fidelity-phase8.test.js`.
  </read_first>
  <action>
    Update `src/adapters/playwright-inject.js` so the classic script includes the same Phase 8 capture behavior as `src/capture/index.js`: shadowRoots sidecars, frame payloads, value diffs, late-added computed styles, subtree request handling, and the expanded protocol constants. Preserve the no-build injection contract: no `import `, no `export `, no CommonJS `require`, and bridge globals remain compatible with the Playwright/CDP adapter.

    Keep any generated/manual transform consistent with Phase 7 conventions: inlined protocol constants, plain function declarations/`var` where the artifact already uses them, and no new runtime dependency. If a helper is too large to safely hand-copy, use the repo's existing artifact-generation pattern if present; otherwise update carefully and verify with static gates.

    Update `src/adapters/playwright.js` so transport messages with `type === CONTROL.SUBTREE_REQUEST` are forwarded into the page-side injected capture handle, not ignored. Add a contained async helper such as `forwardSubtreeRequest(payload)` that calls `page.evaluate(({ type, payload: request }) => window.__phantomStreamHandleControl && window.__phantomStreamHandleControl(type, request), { type: CONTROL.SUBTREE_REQUEST, payload })` or the final bridge name exposed by `src/adapters/playwright-inject.js`. The helper must:
    - preserve the request payload fields `{ requestId, nid, streamSessionId, snapshotId, reason }`;
    - log only content-free failure reasons on evaluation errors;
    - not route subtree requests through remote-control authorization;
    - allow the injected capture's normal transport/binding path to emit `STREAM.SUBTREE_RESPONSE` back to the adapter transport.

    Update `src/adapters/playwright-inject.js` to expose the page-side control bridge used above, for example `window.__phantomStreamHandleControl = function(type, payload) { return phantomStreamCapture.handleControl(type, payload); }`, while keeping existing `__phantomStreamStart`, `__phantomStreamStop`, `__phantomStreamCapture`, and `__phantomStreamGetNodeId` bridges compatible.

    Update `tests/playwright-adapter.test.js` and/or `tests/playwright-adapter-cdp.test.js` to prove a `CONTROL.SUBTREE_REQUEST` received by the adapter is evaluated in the page and that a synthetic injected `STREAM.SUBTREE_RESPONSE` reaches the transport/binding path. Update `tests/playwright-fidelity-phase8.test.js` only as needed so subtree recovery exercises the public adapter/inject path rather than a test-only shortcut.
  </action>
  <verify>
    <automated>node --test tests/adapter-exports.test.js tests/playwright-adapter.test.js tests/playwright-adapter-cdp.test.js tests/playwright-fidelity-phase8.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "shadowRoots|frames|SUBTREE_REQUEST|SUBTREE_RESPONSE|DIFF_OP|value|selectedValues" src/adapters/playwright-inject.js` returns matches.
    - `rg -n "SUBTREE_REQUEST|forwardSubtreeRequest|__phantomStreamHandleControl|handleControl" src/adapters/playwright.js src/adapters/playwright-inject.js` returns matches.
    - `rg -n "import |export |require\\(" src/adapters/playwright-inject.js` returns no matches.
    - `node --test tests/playwright-adapter.test.js tests/playwright-adapter-cdp.test.js` passes and includes a subtree request forwarding assertion.
    - `node --test tests/playwright-fidelity-phase8.test.js` passes.
    - Adapter export and Playwright adapter tests pass.
  </acceptance_criteria>
  <done>Browser-injected Playwright/CDP artifact carries the same Phase 8 capture behavior as the ESM capture core, and the public adapter forwards subtree requests to injected capture control handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ESM capture -> classic inject artifact | Browser injection path must preserve security and fidelity behavior without module imports. |
| viewer transport -> Playwright adapter -> injected capture | Subtree fetch requests cross from viewer control transport into page evaluate and back through the injected binding. |
| checked-in artifact -> browser page | The injected classic script runs in the captured page context and must preserve masking, sanitization, and bounded control behavior. |
| browser fixture -> test process | Playwright launches Chromium and executes fixture code under test control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-29 | T | `src/adapters/playwright-inject.js` | mitigate | Static tests forbid ESM imports/exports and browser smoke exercises the checked-in artifact. |
| T-08-30 | I | value/frame/shadow payloads in inject artifact | mitigate | Preserve capture-side masking, cross-origin placeholder policy, and sanitizer reuse from the ESM implementation. |
| T-08-31 | D | subtree request handling in inject artifact | mitigate | Preserve nid/requestId latching and stale/gone/skipped/blocked soft responses from the ESM implementation. |
| T-08-32 | R | `src/adapters/playwright.js` subtree forwarding | mitigate | Log only content-free evaluate failure reasons and keep subtree requests outside remote-control authorization paths. |
</threat_model>

<verification>
```bash
node --test tests/adapter-exports.test.js tests/playwright-adapter.test.js tests/playwright-adapter-cdp.test.js tests/playwright-fidelity-phase8.test.js
```
</verification>

<success_criteria>
- `src/adapters/playwright-inject.js` is synchronized with Phase 8 capture behavior.
- The inject artifact remains classic-script compatible: no `import`, `export`, or `require`.
- Adapter and Playwright fidelity tests pass through the public adapter/inject path.
- D-04, D-05, D-06, D-09, D-10, D-12, D-13, D-14, D-15, D-16, D-17, D-19, D-20, D-21, D-22, and D-25 are preserved in the injected artifact path.
</success_criteria>

<output>
After completion, create `.planning/phases/08-shadow-dom-iframes-fidelity-completion/08-08-SUMMARY.md`.
</output>
