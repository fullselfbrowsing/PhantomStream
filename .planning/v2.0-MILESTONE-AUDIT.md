---
milestone: v2.0
milestone_name: Asset & Media Streaming
audited: 2026-06-21
status: tech_debt
scores:
  requirements: 20/20
  phases: 4/4
  integration: sound
  nyquist: 4/4 compliant
gaps:
  requirements: []   # none — all 20 satisfied
  integration: []    # none — every cross-phase seam wired
  flows: []          # none — E2E media pipeline traced end-to-end
tech_debt:
  - phase: 12-static-assets-by-reference
    items:
      - "Deferred (environment): 5 real-Chrome static-asset rendering UAT items (img/srcset/picture/background-image/poster render from source) — jsdom does not parse srcdoc / fetch subresources. Tracked in 12-HUMAN-UAT.md."
  - phase: 13-video-audio-url-playback-sync
    items:
      - "Deferred (environment): 4 real-Chrome media-playback UAT items (muted-autoplay-in-sandbox, blocked-play affordance click, live seekable.end no-throw, real-timeline drift convergence) — jsdom has no media timeline; FSB automation runs tabs hidden (media suspended). Tracked in 13-HUMAN-UAT.md (FSB validation attempted; structural invariants confirmed live)."
  - phase: 14-adaptive-streaming-adapter-discovery-fallback
    items:
      - "Deferred (environment): 4 real-Chrome adaptive UAT items (cross-Document MSE attach, live-edge HLS sync, DRM degrade observed, real CSP blob: enforcement) — jsdom has no MSE; hidden-tab automation suspends media. Tracked in 14-HUMAN-UAT.md."
  - phase: 15-media-security-masking-threat-model-docs
    items:
      - "Deferred (environment): 2 real-Chrome security UAT items (no-referrer header suppression, live CSP enforcement) — jsdom issues no real requests / enforces no CSP. String-layer contracts unit-asserted. Tracked in 15-HUMAN-UAT.md."
---

# Milestone v2.0 — Asset & Media Streaming — Audit

**Audited:** 2026-06-21
**Status:** tech_debt (no blockers; documented environment-forced real-browser UAT is the only outstanding item)

## Summary

Milestone v2.0 extended PhantomStream beyond DOM/text to mirror media **by reference**:
static assets, progressive `<video>`/`<audio>` with drift-corrected sync, best-effort
adaptive HLS/DASH with graceful fallback, and a completed media security contract — all
preserving the low-bandwidth core value (only URLs + small state messages cross the relay;
media bytes load from the source in the viewer's browser).

All 20 milestone requirements are **satisfied**, all 4 phases **verified**, cross-phase
integration is **sound** (every seam traced through code), and all 4 phases are **Nyquist
compliant**. The full test suite is **704/704** green and the differential oracle is **48/48**
(capture-wire byte-identity preserved). The published module stays **zero hard runtime
dependencies** (hls.js is an optional peerDependency, dynamic-import only).

The only outstanding item is **documented, environment-forced real-browser UAT** (15 items
across Phases 12–15): the test environment (jsdom + the FSB automation browser that runs tabs
hidden, suspending media) cannot exercise real subresource fetches, MSE/adaptive playback, CSP
enforcement, or referrer-header suppression. The backing logic of every one is unit-tested; the
never-break poster fallback is the safety net. These need a foregrounded/non-headless browser to
observe and are tracked in the per-phase HUMAN-UAT.md files.

## Requirements Coverage (20/20 satisfied)

| Phase | Requirements | Status | Verification |
|-------|--------------|--------|--------------|
| 12 — Static Assets by Reference | ASST-01..05, MSEC-01, MSEC-02 | satisfied | 12-VERIFICATION human_needed (5/5 must-haves) |
| 13 — Video/Audio URL + Playback Sync | MEDIA-01..05, MWIRE-01, MWIRE-02 | satisfied | 13-VERIFICATION human_needed (7/7 must-haves) |
| 14 — Adaptive Streaming + Adapter Discovery + Fallback | MADPT-01..04 | satisfied | 14-VERIFICATION human_needed (4/4 SC + 7/7 invariants) |
| 15 — Media Security, Masking, Threat Model & Docs | MSEC-03, MSEC-04 | satisfied | 15-VERIFICATION human_needed (13/13 must-haves) |

All 20 REQ-IDs are marked `Complete` in the REQUIREMENTS.md traceability table, listed in the
corresponding SUMMARY frontmatter, and verified in the phase VERIFICATION.md files. No orphaned
or unsatisfied requirement. (`human_needed` here means automated must-haves passed with deferred
real-browser observation — not a gap.)

## Cross-Phase Integration (sound)

The gsd-integration-checker traced every seam through the actual code (not summaries) and found
no wiring gap, payload-shape mismatch, contradictory security gate, or broken flow:

- **Capture → Protocol → Renderer media path:** `sendMediaState`/`buildMediaBaselineEntry` →
  `STREAM.MEDIA` → `handleMedia` → pure `reconcileMediaDrift` → `applyMediaAction`; `MediaSyncPayload`
  shape identical producer↔consumer.
- **Adapter discovery → renderer player:** `emitMediaHint` (`MediaHintPayload`) → `STREAM.MEDIA_HINT`
  → `handleMediaHint` (re-gates the manifest URL through the SAME Phase-12 `gateAssetUrl`) →
  `media-player.js` attach tree → single `degrade` sink.
- **Security gates end-to-end:** CSP layers cleanly (`default-src 'none'` + `img-src` [P12] +
  `media-src ... blob:` [P13/P14] + `referrer` no-referrer [P15], no `script-src`/`connect-src`);
  sandbox asserted exactly `allow-same-origin`; string-layer gate runs pre-srcdoc; masked media
  emits no `STREAM.MEDIA`.
- **Shared invariants:** envelope/relay byte-unchanged (verbatim raw forwarding); oracle 48/48;
  no new hard dependency.
- **Never-break chain:** every failure path (no-manifest/drm/mse-opaque/no-player/blocked-origin/
  masked) converges to poster/placeholder; re-snapshot teardown revokes object URLs (no leak).

## Nyquist Compliance (4/4)

| Phase | VALIDATION.md | nyquist_compliant | Status |
|-------|---------------|-------------------|--------|
| 12 | exists | true | COMPLIANT |
| 13 | exists | true | COMPLIANT |
| 14 | exists | true | COMPLIANT |
| 15 | exists | true | COMPLIANT |

## Tech Debt — Deferred Real-Browser UAT (environment-forced)

No code debt (no stubs, TODOs, or missing features). The remaining items are real-browser
*observation* of behaviors that are fully unit-tested.

**Autonomous FSB validation (2026-06-21):** an autonomous real-Chrome pass against the loopback
mirror validated the static-asset + security-posture slice live (images load even in the hidden
automation tab, unlike media):
- **Phase 12 — PASS live:** `<img>` renders from the source CDN URL with real pixels
  (`naturalWidth 550`); a private-host img degrades to a `data-ps-asset-unavailable="blocked-origin"`
  placeholder with the URL absent from the wire (MSEC-01); an injected `<script>` is dropped at
  capture and does not execute, CSP `default-src 'none'`/no-`script-src` present (ASST-05).
- **Phase 15 — PASS live:** `<meta name="referrer" content="no-referrer">` present + correctly
  placed; sandbox exactly `allow-same-origin`; `on*` handlers stripped; script-blocking enforced.

| Phase | Status after FSB pass | Why remaining items deferred |
|-------|-----------------------|------------------------------|
| 12 | 2 PASS + 1 corroborated; 2 deferred (srcset DPR, mixed-content/CORS) | DPR/https-viewer setup needed |
| 13 | 4 deferred (muted-autoplay, affordance, live seek, drift) | jsdom has no media timeline; FSB tab hidden (media suspended) |
| 14 | 4 deferred (cross-Document MSE, live HLS, DRM, CSP blob:) | jsdom has no MSE; hidden-tab suspension |
| 15 | 1 PASS (script-blocking) + 1 corroborated (no-referrer meta; literal header needs devtools) | media-blob: play suspended in hidden tab |

**Net:** ~5 of the original 15 UAT items are now validated/corroborated live; the remaining ~10 are
genuinely blocked by the hidden-tab media suspension (real `<video>`/MSE playback) or need DPR/https
setup. The never-break poster fallback bounds the residual risk.

**Recommendation:** acknowledge and complete the milestone — the work is done and tested, the
asset + security posture is now confirmed in real Chrome, and the residual media-playback UAT is
best run in a foregrounded browser alongside the v2.1 evaluation harness.
